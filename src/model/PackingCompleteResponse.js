/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PackingCompleteResponse model module.
 * @module model/PackingCompleteResponse
 * @version 0.1.0
 */
class PackingCompleteResponse {
    /**
     * Constructs a new <code>PackingCompleteResponse</code>.
     * @alias module:model/PackingCompleteResponse
     * @param message {String} 
     * @param newState {String} 
     * @param orderNumber {String} 
     * @param success {Boolean} 
     */
    constructor(message, newState, orderNumber, success) { 
        
        PackingCompleteResponse.initialize(this, message, newState, orderNumber, success);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, message, newState, orderNumber, success) { 
        obj['message'] = message;
        obj['new_state'] = newState;
        obj['order_number'] = orderNumber;
        obj['success'] = success;
    }

    /**
     * Constructs a <code>PackingCompleteResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PackingCompleteResponse} obj Optional instance to populate.
     * @return {module:model/PackingCompleteResponse} The populated <code>PackingCompleteResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PackingCompleteResponse();

            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('new_state')) {
                obj['new_state'] = ApiClient.convertToType(data['new_state'], 'String');
            }
            if (data.hasOwnProperty('order_number')) {
                obj['order_number'] = ApiClient.convertToType(data['order_number'], 'String');
            }
            if (data.hasOwnProperty('success')) {
                obj['success'] = ApiClient.convertToType(data['success'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PackingCompleteResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PackingCompleteResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PackingCompleteResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['new_state'] && !(typeof data['new_state'] === 'string' || data['new_state'] instanceof String)) {
            throw new Error("Expected the field `new_state` to be a primitive type in the JSON string but got " + data['new_state']);
        }
        // ensure the json data is a string
        if (data['order_number'] && !(typeof data['order_number'] === 'string' || data['order_number'] instanceof String)) {
            throw new Error("Expected the field `order_number` to be a primitive type in the JSON string but got " + data['order_number']);
        }

        return true;
    }


}

PackingCompleteResponse.RequiredProperties = ["message", "new_state", "order_number", "success"];

/**
 * @member {String} message
 */
PackingCompleteResponse.prototype['message'] = undefined;

/**
 * @member {String} new_state
 */
PackingCompleteResponse.prototype['new_state'] = undefined;

/**
 * @member {String} order_number
 */
PackingCompleteResponse.prototype['order_number'] = undefined;

/**
 * @member {Boolean} success
 */
PackingCompleteResponse.prototype['success'] = undefined;






export default PackingCompleteResponse;


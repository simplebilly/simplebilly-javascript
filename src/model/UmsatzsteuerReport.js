/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import VatDetail from './VatDetail';

/**
 * The UmsatzsteuerReport model module.
 * @module model/UmsatzsteuerReport
 * @version 0.1.0
 */
class UmsatzsteuerReport {
    /**
     * Constructs a new <code>UmsatzsteuerReport</code>.
     * @alias module:model/UmsatzsteuerReport
     * @param generatedAt {String} 
     * @param inputTax {Array.<module:model/VatDetail>} 
     * @param outputTax {Array.<module:model/VatDetail>} 
     * @param period {String} 
     * @param totalInputTax {String} 
     * @param totalOutputTax {String} 
     * @param vatPayable {String} 
     * @param vatRefund {String} 
     */
    constructor(generatedAt, inputTax, outputTax, period, totalInputTax, totalOutputTax, vatPayable, vatRefund) { 
        
        UmsatzsteuerReport.initialize(this, generatedAt, inputTax, outputTax, period, totalInputTax, totalOutputTax, vatPayable, vatRefund);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, generatedAt, inputTax, outputTax, period, totalInputTax, totalOutputTax, vatPayable, vatRefund) { 
        obj['generated_at'] = generatedAt;
        obj['input_tax'] = inputTax;
        obj['output_tax'] = outputTax;
        obj['period'] = period;
        obj['total_input_tax'] = totalInputTax;
        obj['total_output_tax'] = totalOutputTax;
        obj['vat_payable'] = vatPayable;
        obj['vat_refund'] = vatRefund;
    }

    /**
     * Constructs a <code>UmsatzsteuerReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UmsatzsteuerReport} obj Optional instance to populate.
     * @return {module:model/UmsatzsteuerReport} The populated <code>UmsatzsteuerReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UmsatzsteuerReport();

            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('input_tax')) {
                obj['input_tax'] = ApiClient.convertToType(data['input_tax'], [VatDetail]);
            }
            if (data.hasOwnProperty('output_tax')) {
                obj['output_tax'] = ApiClient.convertToType(data['output_tax'], [VatDetail]);
            }
            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
            if (data.hasOwnProperty('total_input_tax')) {
                obj['total_input_tax'] = ApiClient.convertToType(data['total_input_tax'], 'String');
            }
            if (data.hasOwnProperty('total_output_tax')) {
                obj['total_output_tax'] = ApiClient.convertToType(data['total_output_tax'], 'String');
            }
            if (data.hasOwnProperty('vat_payable')) {
                obj['vat_payable'] = ApiClient.convertToType(data['vat_payable'], 'String');
            }
            if (data.hasOwnProperty('vat_refund')) {
                obj['vat_refund'] = ApiClient.convertToType(data['vat_refund'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UmsatzsteuerReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UmsatzsteuerReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UmsatzsteuerReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        if (data['input_tax']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['input_tax'])) {
                throw new Error("Expected the field `input_tax` to be an array in the JSON data but got " + data['input_tax']);
            }
            // validate the optional field `input_tax` (array)
            for (const item of data['input_tax']) {
                VatDetail.validateJSON(item);
            };
        }
        if (data['output_tax']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['output_tax'])) {
                throw new Error("Expected the field `output_tax` to be an array in the JSON data but got " + data['output_tax']);
            }
            // validate the optional field `output_tax` (array)
            for (const item of data['output_tax']) {
                VatDetail.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }
        // ensure the json data is a string
        if (data['total_input_tax'] && !(typeof data['total_input_tax'] === 'string' || data['total_input_tax'] instanceof String)) {
            throw new Error("Expected the field `total_input_tax` to be a primitive type in the JSON string but got " + data['total_input_tax']);
        }
        // ensure the json data is a string
        if (data['total_output_tax'] && !(typeof data['total_output_tax'] === 'string' || data['total_output_tax'] instanceof String)) {
            throw new Error("Expected the field `total_output_tax` to be a primitive type in the JSON string but got " + data['total_output_tax']);
        }
        // ensure the json data is a string
        if (data['vat_payable'] && !(typeof data['vat_payable'] === 'string' || data['vat_payable'] instanceof String)) {
            throw new Error("Expected the field `vat_payable` to be a primitive type in the JSON string but got " + data['vat_payable']);
        }
        // ensure the json data is a string
        if (data['vat_refund'] && !(typeof data['vat_refund'] === 'string' || data['vat_refund'] instanceof String)) {
            throw new Error("Expected the field `vat_refund` to be a primitive type in the JSON string but got " + data['vat_refund']);
        }

        return true;
    }


}

UmsatzsteuerReport.RequiredProperties = ["generated_at", "input_tax", "output_tax", "period", "total_input_tax", "total_output_tax", "vat_payable", "vat_refund"];

/**
 * @member {String} generated_at
 */
UmsatzsteuerReport.prototype['generated_at'] = undefined;

/**
 * @member {Array.<module:model/VatDetail>} input_tax
 */
UmsatzsteuerReport.prototype['input_tax'] = undefined;

/**
 * @member {Array.<module:model/VatDetail>} output_tax
 */
UmsatzsteuerReport.prototype['output_tax'] = undefined;

/**
 * @member {String} period
 */
UmsatzsteuerReport.prototype['period'] = undefined;

/**
 * @member {String} total_input_tax
 */
UmsatzsteuerReport.prototype['total_input_tax'] = undefined;

/**
 * @member {String} total_output_tax
 */
UmsatzsteuerReport.prototype['total_output_tax'] = undefined;

/**
 * @member {String} vat_payable
 */
UmsatzsteuerReport.prototype['vat_payable'] = undefined;

/**
 * @member {String} vat_refund
 */
UmsatzsteuerReport.prototype['vat_refund'] = undefined;






export default UmsatzsteuerReport;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Section model module.
 * @module model/Section
 * @version 0.1.0
 */
class Section {
    /**
     * Constructs a new <code>Section</code>.
     * @alias module:model/Section
     * @param bodyHtml {String} 
     * @param bodyHtmlEn {String} 
     * @param id {String} 
     * @param title {String} 
     * @param titleEn {String} 
     */
    constructor(bodyHtml, bodyHtmlEn, id, title, titleEn) { 
        
        Section.initialize(this, bodyHtml, bodyHtmlEn, id, title, titleEn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bodyHtml, bodyHtmlEn, id, title, titleEn) { 
        obj['bodyHtml'] = bodyHtml;
        obj['bodyHtmlEn'] = bodyHtmlEn;
        obj['id'] = id;
        obj['title'] = title;
        obj['titleEn'] = titleEn;
    }

    /**
     * Constructs a <code>Section</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Section} obj Optional instance to populate.
     * @return {module:model/Section} The populated <code>Section</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Section();

            if (data.hasOwnProperty('bodyHtml')) {
                obj['bodyHtml'] = ApiClient.convertToType(data['bodyHtml'], 'String');
            }
            if (data.hasOwnProperty('bodyHtmlEn')) {
                obj['bodyHtmlEn'] = ApiClient.convertToType(data['bodyHtmlEn'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('titleEn')) {
                obj['titleEn'] = ApiClient.convertToType(data['titleEn'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Section</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Section</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Section.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bodyHtml'] && !(typeof data['bodyHtml'] === 'string' || data['bodyHtml'] instanceof String)) {
            throw new Error("Expected the field `bodyHtml` to be a primitive type in the JSON string but got " + data['bodyHtml']);
        }
        // ensure the json data is a string
        if (data['bodyHtmlEn'] && !(typeof data['bodyHtmlEn'] === 'string' || data['bodyHtmlEn'] instanceof String)) {
            throw new Error("Expected the field `bodyHtmlEn` to be a primitive type in the JSON string but got " + data['bodyHtmlEn']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['titleEn'] && !(typeof data['titleEn'] === 'string' || data['titleEn'] instanceof String)) {
            throw new Error("Expected the field `titleEn` to be a primitive type in the JSON string but got " + data['titleEn']);
        }

        return true;
    }


}

Section.RequiredProperties = ["bodyHtml", "bodyHtmlEn", "id", "title", "titleEn"];

/**
 * @member {String} bodyHtml
 */
Section.prototype['bodyHtml'] = undefined;

/**
 * @member {String} bodyHtmlEn
 */
Section.prototype['bodyHtmlEn'] = undefined;

/**
 * @member {String} id
 */
Section.prototype['id'] = undefined;

/**
 * @member {String} title
 */
Section.prototype['title'] = undefined;

/**
 * @member {String} titleEn
 */
Section.prototype['titleEn'] = undefined;






export default Section;


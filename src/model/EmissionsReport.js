/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CategoryTotal from './CategoryTotal';
import DataQuality from './DataQuality';
import ScopeTotal from './ScopeTotal';
import TargetProgress from './TargetProgress';
import YearTotal from './YearTotal';

/**
 * The EmissionsReport model module.
 * @module model/EmissionsReport
 * @version 0.1.0
 */
class EmissionsReport {
    /**
     * Constructs a new <code>EmissionsReport</code>.
     * @alias module:model/EmissionsReport
     * @param byCategory {Array.<module:model/CategoryTotal>} 
     * @param byScope {Array.<module:model/ScopeTotal>} 
     * @param byYear {Array.<module:model/YearTotal>} 
     * @param dataQuality {module:model/DataQuality} 
     * @param targets {Array.<module:model/TargetProgress>} 
     * @param totalTco2e {String} 
     */
    constructor(byCategory, byScope, byYear, dataQuality, targets, totalTco2e) { 
        
        EmissionsReport.initialize(this, byCategory, byScope, byYear, dataQuality, targets, totalTco2e);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, byCategory, byScope, byYear, dataQuality, targets, totalTco2e) { 
        obj['by_category'] = byCategory;
        obj['by_scope'] = byScope;
        obj['by_year'] = byYear;
        obj['data_quality'] = dataQuality;
        obj['targets'] = targets;
        obj['total_tco2e'] = totalTco2e;
    }

    /**
     * Constructs a <code>EmissionsReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmissionsReport} obj Optional instance to populate.
     * @return {module:model/EmissionsReport} The populated <code>EmissionsReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmissionsReport();

            if (data.hasOwnProperty('by_category')) {
                obj['by_category'] = ApiClient.convertToType(data['by_category'], [CategoryTotal]);
            }
            if (data.hasOwnProperty('by_scope')) {
                obj['by_scope'] = ApiClient.convertToType(data['by_scope'], [ScopeTotal]);
            }
            if (data.hasOwnProperty('by_year')) {
                obj['by_year'] = ApiClient.convertToType(data['by_year'], [YearTotal]);
            }
            if (data.hasOwnProperty('data_quality')) {
                obj['data_quality'] = DataQuality.constructFromObject(data['data_quality']);
            }
            if (data.hasOwnProperty('intensity_per_employee')) {
                obj['intensity_per_employee'] = ApiClient.convertToType(data['intensity_per_employee'], 'Number');
            }
            if (data.hasOwnProperty('intensity_per_revenue_mio')) {
                obj['intensity_per_revenue_mio'] = ApiClient.convertToType(data['intensity_per_revenue_mio'], 'Number');
            }
            if (data.hasOwnProperty('net_revenue')) {
                obj['net_revenue'] = ApiClient.convertToType(data['net_revenue'], 'Number');
            }
            if (data.hasOwnProperty('spend_based_estimate_tco2e')) {
                obj['spend_based_estimate_tco2e'] = ApiClient.convertToType(data['spend_based_estimate_tco2e'], 'Number');
            }
            if (data.hasOwnProperty('targets')) {
                obj['targets'] = ApiClient.convertToType(data['targets'], [TargetProgress]);
            }
            if (data.hasOwnProperty('total_tco2e')) {
                obj['total_tco2e'] = ApiClient.convertToType(data['total_tco2e'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmissionsReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmissionsReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmissionsReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['by_category']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['by_category'])) {
                throw new Error("Expected the field `by_category` to be an array in the JSON data but got " + data['by_category']);
            }
            // validate the optional field `by_category` (array)
            for (const item of data['by_category']) {
                CategoryTotal.validateJSON(item);
            };
        }
        if (data['by_scope']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['by_scope'])) {
                throw new Error("Expected the field `by_scope` to be an array in the JSON data but got " + data['by_scope']);
            }
            // validate the optional field `by_scope` (array)
            for (const item of data['by_scope']) {
                ScopeTotal.validateJSON(item);
            };
        }
        if (data['by_year']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['by_year'])) {
                throw new Error("Expected the field `by_year` to be an array in the JSON data but got " + data['by_year']);
            }
            // validate the optional field `by_year` (array)
            for (const item of data['by_year']) {
                YearTotal.validateJSON(item);
            };
        }
        // validate the optional field `data_quality`
        if (data['data_quality']) { // data not null
          DataQuality.validateJSON(data['data_quality']);
        }
        if (data['targets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['targets'])) {
                throw new Error("Expected the field `targets` to be an array in the JSON data but got " + data['targets']);
            }
            // validate the optional field `targets` (array)
            for (const item of data['targets']) {
                TargetProgress.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_tco2e'] && !(typeof data['total_tco2e'] === 'string' || data['total_tco2e'] instanceof String)) {
            throw new Error("Expected the field `total_tco2e` to be a primitive type in the JSON string but got " + data['total_tco2e']);
        }

        return true;
    }


}

EmissionsReport.RequiredProperties = ["by_category", "by_scope", "by_year", "data_quality", "targets", "total_tco2e"];

/**
 * @member {Array.<module:model/CategoryTotal>} by_category
 */
EmissionsReport.prototype['by_category'] = undefined;

/**
 * @member {Array.<module:model/ScopeTotal>} by_scope
 */
EmissionsReport.prototype['by_scope'] = undefined;

/**
 * @member {Array.<module:model/YearTotal>} by_year
 */
EmissionsReport.prototype['by_year'] = undefined;

/**
 * @member {module:model/DataQuality} data_quality
 */
EmissionsReport.prototype['data_quality'] = undefined;

/**
 * @member {Number} intensity_per_employee
 */
EmissionsReport.prototype['intensity_per_employee'] = undefined;

/**
 * tCO2e per million EUR net revenue.
 * @member {Number} intensity_per_revenue_mio
 */
EmissionsReport.prototype['intensity_per_revenue_mio'] = undefined;

/**
 * Sum of paid/sent/partially-paid invoices (EUR net) in the year.
 * @member {Number} net_revenue
 */
EmissionsReport.prototype['net_revenue'] = undefined;

/**
 * Spend-based estimate from bookkeeping payments (EXIOBASE factor).
 * @member {Number} spend_based_estimate_tco2e
 */
EmissionsReport.prototype['spend_based_estimate_tco2e'] = undefined;

/**
 * @member {Array.<module:model/TargetProgress>} targets
 */
EmissionsReport.prototype['targets'] = undefined;

/**
 * @member {String} total_tco2e
 */
EmissionsReport.prototype['total_tco2e'] = undefined;






export default EmissionsReport;


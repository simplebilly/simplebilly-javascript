/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmailTemplateStatus from './EmailTemplateStatus';

/**
 * The EmailTemplate model module.
 * @module model/EmailTemplate
 * @version 0.1.0
 */
class EmailTemplate {
    /**
     * Constructs a new <code>EmailTemplate</code>.
     * @alias module:model/EmailTemplate
     * @param body {String} E-mail body with optional placeholders.
     * @param name {String} Human-readable template name, e.g. \"Follow-up after quote\".
     * @param status {module:model/EmailTemplateStatus} One of: active | inactive
     * @param subject {String} E-mail subject line with optional placeholders.
     */
    constructor(body, name, status, subject) { 
        
        EmailTemplate.initialize(this, body, name, status, subject);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, body, name, status, subject) { 
        obj['body'] = body;
        obj['name'] = name;
        obj['status'] = status;
        obj['subject'] = subject;
    }

    /**
     * Constructs a <code>EmailTemplate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmailTemplate} obj Optional instance to populate.
     * @return {module:model/EmailTemplate} The populated <code>EmailTemplate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmailTemplate();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], EmailTemplateStatus);
            }
            if (data.hasOwnProperty('subject')) {
                obj['subject'] = ApiClient.convertToType(data['subject'], 'String');
            }
            if (data.hasOwnProperty('variables')) {
                obj['variables'] = ApiClient.convertToType(data['variables'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmailTemplate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmailTemplate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmailTemplate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['subject'] && !(typeof data['subject'] === 'string' || data['subject'] instanceof String)) {
            throw new Error("Expected the field `subject` to be a primitive type in the JSON string but got " + data['subject']);
        }

        return true;
    }


}

EmailTemplate.RequiredProperties = ["body", "name", "status", "subject"];

/**
 * E-mail body with optional placeholders.
 * @member {String} body
 */
EmailTemplate.prototype['body'] = undefined;

/**
 * Human-readable template name, e.g. \"Follow-up after quote\".
 * @member {String} name
 */
EmailTemplate.prototype['name'] = undefined;

/**
 * One of: active | inactive
 * @member {module:model/EmailTemplateStatus} status
 */
EmailTemplate.prototype['status'] = undefined;

/**
 * E-mail subject line with optional placeholders.
 * @member {String} subject
 */
EmailTemplate.prototype['subject'] = undefined;

/**
 * Placeholders used by this template, e.g. `[\"contact.first_name\"]`.
 * @member {Object} variables
 */
EmailTemplate.prototype['variables'] = undefined;






export default EmailTemplate;


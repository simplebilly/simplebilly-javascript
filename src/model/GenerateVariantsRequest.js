/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GenerateVariantsRequest model module.
 * @module model/GenerateVariantsRequest
 * @version 0.1.0
 */
class GenerateVariantsRequest {
    /**
     * Constructs a new <code>GenerateVariantsRequest</code>.
     * Request body for generating the full variant set from option values.
     * @alias module:model/GenerateVariantsRequest
     * @param productId {String} 
     */
    constructor(productId) { 
        
        GenerateVariantsRequest.initialize(this, productId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, productId) { 
        obj['productId'] = productId;
    }

    /**
     * Constructs a <code>GenerateVariantsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GenerateVariantsRequest} obj Optional instance to populate.
     * @return {module:model/GenerateVariantsRequest} The populated <code>GenerateVariantsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GenerateVariantsRequest();

            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], {'String': ['String']});
            }
            if (data.hasOwnProperty('priceDelta')) {
                obj['priceDelta'] = ApiClient.convertToType(data['priceDelta'], 'String');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('skuPrefix')) {
                obj['skuPrefix'] = ApiClient.convertToType(data['skuPrefix'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GenerateVariantsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GenerateVariantsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GenerateVariantsRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['priceDelta'] && !(typeof data['priceDelta'] === 'string' || data['priceDelta'] instanceof String)) {
            throw new Error("Expected the field `priceDelta` to be a primitive type in the JSON string but got " + data['priceDelta']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['skuPrefix'] && !(typeof data['skuPrefix'] === 'string' || data['skuPrefix'] instanceof String)) {
            throw new Error("Expected the field `skuPrefix` to be a primitive type in the JSON string but got " + data['skuPrefix']);
        }

        return true;
    }


}

GenerateVariantsRequest.RequiredProperties = ["productId"];

/**
 * Option name → list of values, e.g. `{\"Color\": [\"Red\", \"Blue\"], \"Size\": [\"S\", \"M\"]}`. The cartesian product of these lists is generated.
 * @member {Object.<String, Array.<String>>} options
 */
GenerateVariantsRequest.prototype['options'] = undefined;

/**
 * Optional per-variant price delta applied to every generated variant.
 * @member {String} priceDelta
 */
GenerateVariantsRequest.prototype['priceDelta'] = undefined;

/**
 * @member {String} productId
 */
GenerateVariantsRequest.prototype['productId'] = undefined;

/**
 * Optional prefix for the generated SKUs (suffix is the option values joined by `-`). Falls back to the parent product's SKU.
 * @member {String} skuPrefix
 */
GenerateVariantsRequest.prototype['skuPrefix'] = undefined;






export default GenerateVariantsRequest;


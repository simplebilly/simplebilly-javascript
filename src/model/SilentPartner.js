/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InstrumentType from './InstrumentType';

/**
 * The SilentPartner model module.
 * @module model/SilentPartner
 * @version 0.1.0
 */
class SilentPartner {
    /**
     * Constructs a new <code>SilentPartner</code>.
     * @alias module:model/SilentPartner
     * @param instrumentType {module:model/InstrumentType} Instrument: \"typisch\" | \"atypisch\" | \"partiarisches_darlehen\" | \"genussrecht\".
     */
    constructor(instrumentType) { 
        
        SilentPartner.initialize(this, instrumentType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, instrumentType) { 
        obj['instrumentType'] = instrumentType;
    }

    /**
     * Constructs a <code>SilentPartner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SilentPartner} obj Optional instance to populate.
     * @return {module:model/SilentPartner} The populated <code>SilentPartner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SilentPartner();

            if (data.hasOwnProperty('contractDate')) {
                obj['contractDate'] = ApiClient.convertToType(data['contractDate'], 'Date');
            }
            if (data.hasOwnProperty('einlage')) {
                obj['einlage'] = ApiClient.convertToType(data['einlage'], 'String');
            }
            if (data.hasOwnProperty('gewinnquotePct')) {
                obj['gewinnquotePct'] = ApiClient.convertToType(data['gewinnquotePct'], 'String');
            }
            if (data.hasOwnProperty('gewinnvortrag')) {
                obj['gewinnvortrag'] = ApiClient.convertToType(data['gewinnvortrag'], 'String');
            }
            if (data.hasOwnProperty('instrumentType')) {
                obj['instrumentType'] = ApiClient.convertToType(data['instrumentType'], InstrumentType);
            }
            if (data.hasOwnProperty('kestPflichtig')) {
                obj['kestPflichtig'] = ApiClient.convertToType(data['kestPflichtig'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('verlustVerrechnungskonto')) {
                obj['verlustVerrechnungskonto'] = ApiClient.convertToType(data['verlustVerrechnungskonto'], 'String');
            }
            if (data.hasOwnProperty('verlustbeteiligung')) {
                obj['verlustbeteiligung'] = ApiClient.convertToType(data['verlustbeteiligung'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SilentPartner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SilentPartner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SilentPartner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['einlage'] && !(typeof data['einlage'] === 'string' || data['einlage'] instanceof String)) {
            throw new Error("Expected the field `einlage` to be a primitive type in the JSON string but got " + data['einlage']);
        }
        // ensure the json data is a string
        if (data['gewinnquotePct'] && !(typeof data['gewinnquotePct'] === 'string' || data['gewinnquotePct'] instanceof String)) {
            throw new Error("Expected the field `gewinnquotePct` to be a primitive type in the JSON string but got " + data['gewinnquotePct']);
        }
        // ensure the json data is a string
        if (data['gewinnvortrag'] && !(typeof data['gewinnvortrag'] === 'string' || data['gewinnvortrag'] instanceof String)) {
            throw new Error("Expected the field `gewinnvortrag` to be a primitive type in the JSON string but got " + data['gewinnvortrag']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['verlustVerrechnungskonto'] && !(typeof data['verlustVerrechnungskonto'] === 'string' || data['verlustVerrechnungskonto'] instanceof String)) {
            throw new Error("Expected the field `verlustVerrechnungskonto` to be a primitive type in the JSON string but got " + data['verlustVerrechnungskonto']);
        }

        return true;
    }


}

SilentPartner.RequiredProperties = ["instrumentType"];

/**
 * Datum des Vertragsabschlusses.
 * @member {Date} contractDate
 */
SilentPartner.prototype['contractDate'] = undefined;

/**
 * Einlage (§ 230 HGB).
 * @member {String} einlage
 */
SilentPartner.prototype['einlage'] = undefined;

/**
 * Gewinnbeteiligungsquote in Prozent (§ 231 HGB).
 * @member {String} gewinnquotePct
 */
SilentPartner.prototype['gewinnquotePct'] = undefined;

/**
 * Nicht erhobene Gewinne (§ 232 Abs. 3 HGB).
 * @member {String} gewinnvortrag
 */
SilentPartner.prototype['gewinnvortrag'] = undefined;

/**
 * Instrument: \"typisch\" | \"atypisch\" | \"partiarisches_darlehen\" | \"genussrecht\".
 * @member {module:model/InstrumentType} instrumentType
 */
SilentPartner.prototype['instrumentType'] = undefined;

/**
 * 25 % Kapitalertragsteuer einbehalten (§ 43 Abs. 1 Nr. 3 EStG; typisch + partiarisches Darlehen).
 * @member {Boolean} kestPflichtig
 */
SilentPartner.prototype['kestPflichtig'] = undefined;

/**
 * Name des stillen Gesellschafters.
 * @member {String} name
 */
SilentPartner.prototype['name'] = undefined;

/**
 * Freitext-Notizen.
 * @member {String} notes
 */
SilentPartner.prototype['notes'] = undefined;

/**
 * Kumulierte Verluste gegen die Einlage (§ 232 Abs. 2 HGB, ≤ Einlage).
 * @member {String} verlustVerrechnungskonto
 */
SilentPartner.prototype['verlustVerrechnungskonto'] = undefined;

/**
 * Verlustbeteiligung (§ 231 Abs. 2 HGB; kann ausgeschlossen werden).
 * @member {Boolean} verlustbeteiligung
 */
SilentPartner.prototype['verlustbeteiligung'] = undefined;






export default SilentPartner;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TimeEntryDto model module.
 * @module model/TimeEntryDto
 * @version 0.1.0
 */
class TimeEntryDto {
    /**
     * Constructs a new <code>TimeEntryDto</code>.
     * API shape returned to the frontend (matches &#x60;frontend/src/types/time-entry.ts&#x60;).
     * @alias module:model/TimeEntryDto
     * @param createdAt {Date} 
     * @param date {Date} 
     * @param employeeId {String} 
     * @param timeEntryId {String} 
     */
    constructor(createdAt, date, employeeId, timeEntryId) { 
        
        TimeEntryDto.initialize(this, createdAt, date, employeeId, timeEntryId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, createdAt, date, employeeId, timeEntryId) { 
        obj['created_at'] = createdAt;
        obj['date'] = date;
        obj['employee_id'] = employeeId;
        obj['time_entry_id'] = timeEntryId;
    }

    /**
     * Constructs a <code>TimeEntryDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TimeEntryDto} obj Optional instance to populate.
     * @return {module:model/TimeEntryDto} The populated <code>TimeEntryDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TimeEntryDto();

            if (data.hasOwnProperty('clock_in')) {
                obj['clock_in'] = ApiClient.convertToType(data['clock_in'], 'Date');
            }
            if (data.hasOwnProperty('clock_out')) {
                obj['clock_out'] = ApiClient.convertToType(data['clock_out'], 'Date');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('date')) {
                obj['date'] = ApiClient.convertToType(data['date'], 'Date');
            }
            if (data.hasOwnProperty('employee_id')) {
                obj['employee_id'] = ApiClient.convertToType(data['employee_id'], 'String');
            }
            if (data.hasOwnProperty('hours')) {
                obj['hours'] = ApiClient.convertToType(data['hours'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('time_entry_id')) {
                obj['time_entry_id'] = ApiClient.convertToType(data['time_entry_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TimeEntryDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TimeEntryDto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TimeEntryDto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['employee_id'] && !(typeof data['employee_id'] === 'string' || data['employee_id'] instanceof String)) {
            throw new Error("Expected the field `employee_id` to be a primitive type in the JSON string but got " + data['employee_id']);
        }
        // ensure the json data is a string
        if (data['hours'] && !(typeof data['hours'] === 'string' || data['hours'] instanceof String)) {
            throw new Error("Expected the field `hours` to be a primitive type in the JSON string but got " + data['hours']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['time_entry_id'] && !(typeof data['time_entry_id'] === 'string' || data['time_entry_id'] instanceof String)) {
            throw new Error("Expected the field `time_entry_id` to be a primitive type in the JSON string but got " + data['time_entry_id']);
        }

        return true;
    }


}

TimeEntryDto.RequiredProperties = ["created_at", "date", "employee_id", "time_entry_id"];

/**
 * @member {Date} clock_in
 */
TimeEntryDto.prototype['clock_in'] = undefined;

/**
 * @member {Date} clock_out
 */
TimeEntryDto.prototype['clock_out'] = undefined;

/**
 * @member {Date} created_at
 */
TimeEntryDto.prototype['created_at'] = undefined;

/**
 * @member {Date} date
 */
TimeEntryDto.prototype['date'] = undefined;

/**
 * @member {String} employee_id
 */
TimeEntryDto.prototype['employee_id'] = undefined;

/**
 * @member {String} hours
 */
TimeEntryDto.prototype['hours'] = undefined;

/**
 * @member {String} notes
 */
TimeEntryDto.prototype['notes'] = undefined;

/**
 * @member {String} time_entry_id
 */
TimeEntryDto.prototype['time_entry_id'] = undefined;






export default TimeEntryDto;


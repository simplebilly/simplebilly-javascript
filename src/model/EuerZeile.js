/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EuerZeile model module.
 * @module model/EuerZeile
 * @version 0.1.0
 */
class EuerZeile {
    /**
     * Constructs a new <code>EuerZeile</code>.
     * @alias module:model/EuerZeile
     * @param abschnitt {String} 
     * @param betrag {String} 
     * @param bezeichnung {String} 
     * @param zeile {Number} 
     */
    constructor(abschnitt, betrag, bezeichnung, zeile) { 
        
        EuerZeile.initialize(this, abschnitt, betrag, bezeichnung, zeile);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, abschnitt, betrag, bezeichnung, zeile) { 
        obj['abschnitt'] = abschnitt;
        obj['betrag'] = betrag;
        obj['bezeichnung'] = bezeichnung;
        obj['zeile'] = zeile;
    }

    /**
     * Constructs a <code>EuerZeile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EuerZeile} obj Optional instance to populate.
     * @return {module:model/EuerZeile} The populated <code>EuerZeile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EuerZeile();

            if (data.hasOwnProperty('abschnitt')) {
                obj['abschnitt'] = ApiClient.convertToType(data['abschnitt'], 'String');
            }
            if (data.hasOwnProperty('betrag')) {
                obj['betrag'] = ApiClient.convertToType(data['betrag'], 'String');
            }
            if (data.hasOwnProperty('bezeichnung')) {
                obj['bezeichnung'] = ApiClient.convertToType(data['bezeichnung'], 'String');
            }
            if (data.hasOwnProperty('zeile')) {
                obj['zeile'] = ApiClient.convertToType(data['zeile'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EuerZeile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EuerZeile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EuerZeile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['abschnitt'] && !(typeof data['abschnitt'] === 'string' || data['abschnitt'] instanceof String)) {
            throw new Error("Expected the field `abschnitt` to be a primitive type in the JSON string but got " + data['abschnitt']);
        }
        // ensure the json data is a string
        if (data['betrag'] && !(typeof data['betrag'] === 'string' || data['betrag'] instanceof String)) {
            throw new Error("Expected the field `betrag` to be a primitive type in the JSON string but got " + data['betrag']);
        }
        // ensure the json data is a string
        if (data['bezeichnung'] && !(typeof data['bezeichnung'] === 'string' || data['bezeichnung'] instanceof String)) {
            throw new Error("Expected the field `bezeichnung` to be a primitive type in the JSON string but got " + data['bezeichnung']);
        }

        return true;
    }


}

EuerZeile.RequiredProperties = ["abschnitt", "betrag", "bezeichnung", "zeile"];

/**
 * @member {String} abschnitt
 */
EuerZeile.prototype['abschnitt'] = undefined;

/**
 * @member {String} betrag
 */
EuerZeile.prototype['betrag'] = undefined;

/**
 * @member {String} bezeichnung
 */
EuerZeile.prototype['bezeichnung'] = undefined;

/**
 * @member {Number} zeile
 */
EuerZeile.prototype['zeile'] = undefined;






export default EuerZeile;


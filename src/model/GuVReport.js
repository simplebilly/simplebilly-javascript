/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GuVItem from './GuVItem';

/**
 * The GuVReport model module.
 * @module model/GuVReport
 * @version 0.1.0
 */
class GuVReport {
    /**
     * Constructs a new <code>GuVReport</code>.
     * @alias module:model/GuVReport
     * @param expenses {Array.<module:model/GuVItem>} 
     * @param generatedAt {String} 
     * @param netIncome {String} 
     * @param period {String} 
     * @param revenue {Array.<module:model/GuVItem>} 
     * @param totalExpenses {String} 
     * @param totalRevenue {String} 
     */
    constructor(expenses, generatedAt, netIncome, period, revenue, totalExpenses, totalRevenue) { 
        
        GuVReport.initialize(this, expenses, generatedAt, netIncome, period, revenue, totalExpenses, totalRevenue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, expenses, generatedAt, netIncome, period, revenue, totalExpenses, totalRevenue) { 
        obj['expenses'] = expenses;
        obj['generated_at'] = generatedAt;
        obj['net_income'] = netIncome;
        obj['period'] = period;
        obj['revenue'] = revenue;
        obj['total_expenses'] = totalExpenses;
        obj['total_revenue'] = totalRevenue;
    }

    /**
     * Constructs a <code>GuVReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GuVReport} obj Optional instance to populate.
     * @return {module:model/GuVReport} The populated <code>GuVReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GuVReport();

            if (data.hasOwnProperty('expenses')) {
                obj['expenses'] = ApiClient.convertToType(data['expenses'], [GuVItem]);
            }
            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('net_income')) {
                obj['net_income'] = ApiClient.convertToType(data['net_income'], 'String');
            }
            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
            if (data.hasOwnProperty('revenue')) {
                obj['revenue'] = ApiClient.convertToType(data['revenue'], [GuVItem]);
            }
            if (data.hasOwnProperty('total_expenses')) {
                obj['total_expenses'] = ApiClient.convertToType(data['total_expenses'], 'String');
            }
            if (data.hasOwnProperty('total_revenue')) {
                obj['total_revenue'] = ApiClient.convertToType(data['total_revenue'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GuVReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GuVReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GuVReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['expenses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['expenses'])) {
                throw new Error("Expected the field `expenses` to be an array in the JSON data but got " + data['expenses']);
            }
            // validate the optional field `expenses` (array)
            for (const item of data['expenses']) {
                GuVItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        // ensure the json data is a string
        if (data['net_income'] && !(typeof data['net_income'] === 'string' || data['net_income'] instanceof String)) {
            throw new Error("Expected the field `net_income` to be a primitive type in the JSON string but got " + data['net_income']);
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }
        if (data['revenue']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['revenue'])) {
                throw new Error("Expected the field `revenue` to be an array in the JSON data but got " + data['revenue']);
            }
            // validate the optional field `revenue` (array)
            for (const item of data['revenue']) {
                GuVItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_expenses'] && !(typeof data['total_expenses'] === 'string' || data['total_expenses'] instanceof String)) {
            throw new Error("Expected the field `total_expenses` to be a primitive type in the JSON string but got " + data['total_expenses']);
        }
        // ensure the json data is a string
        if (data['total_revenue'] && !(typeof data['total_revenue'] === 'string' || data['total_revenue'] instanceof String)) {
            throw new Error("Expected the field `total_revenue` to be a primitive type in the JSON string but got " + data['total_revenue']);
        }

        return true;
    }


}

GuVReport.RequiredProperties = ["expenses", "generated_at", "net_income", "period", "revenue", "total_expenses", "total_revenue"];

/**
 * @member {Array.<module:model/GuVItem>} expenses
 */
GuVReport.prototype['expenses'] = undefined;

/**
 * @member {String} generated_at
 */
GuVReport.prototype['generated_at'] = undefined;

/**
 * @member {String} net_income
 */
GuVReport.prototype['net_income'] = undefined;

/**
 * @member {String} period
 */
GuVReport.prototype['period'] = undefined;

/**
 * @member {Array.<module:model/GuVItem>} revenue
 */
GuVReport.prototype['revenue'] = undefined;

/**
 * @member {String} total_expenses
 */
GuVReport.prototype['total_expenses'] = undefined;

/**
 * @member {String} total_revenue
 */
GuVReport.prototype['total_revenue'] = undefined;






export default GuVReport;


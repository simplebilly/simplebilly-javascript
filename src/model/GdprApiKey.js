/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GdprApiKey model module.
 * @module model/GdprApiKey
 * @version 0.1.0
 */
class GdprApiKey {
    /**
     * Constructs a new <code>GdprApiKey</code>.
     * @alias module:model/GdprApiKey
     * @param createdAt {Date} 
     * @param id {String} 
     * @param keyId {String} 
     * @param name {String} 
     * @param revoked {Boolean} 
     */
    constructor(createdAt, id, keyId, name, revoked) { 
        
        GdprApiKey.initialize(this, createdAt, id, keyId, name, revoked);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, createdAt, id, keyId, name, revoked) { 
        obj['createdAt'] = createdAt;
        obj['id'] = id;
        obj['keyId'] = keyId;
        obj['name'] = name;
        obj['revoked'] = revoked;
    }

    /**
     * Constructs a <code>GdprApiKey</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GdprApiKey} obj Optional instance to populate.
     * @return {module:model/GdprApiKey} The populated <code>GdprApiKey</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GdprApiKey();

            if (data.hasOwnProperty('createdAt')) {
                obj['createdAt'] = ApiClient.convertToType(data['createdAt'], 'Date');
            }
            if (data.hasOwnProperty('expiresAt')) {
                obj['expiresAt'] = ApiClient.convertToType(data['expiresAt'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('keyId')) {
                obj['keyId'] = ApiClient.convertToType(data['keyId'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('revoked')) {
                obj['revoked'] = ApiClient.convertToType(data['revoked'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GdprApiKey</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GdprApiKey</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GdprApiKey.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['keyId'] && !(typeof data['keyId'] === 'string' || data['keyId'] instanceof String)) {
            throw new Error("Expected the field `keyId` to be a primitive type in the JSON string but got " + data['keyId']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

GdprApiKey.RequiredProperties = ["createdAt", "id", "keyId", "name", "revoked"];

/**
 * @member {Date} createdAt
 */
GdprApiKey.prototype['createdAt'] = undefined;

/**
 * @member {Date} expiresAt
 */
GdprApiKey.prototype['expiresAt'] = undefined;

/**
 * @member {String} id
 */
GdprApiKey.prototype['id'] = undefined;

/**
 * @member {String} keyId
 */
GdprApiKey.prototype['keyId'] = undefined;

/**
 * @member {String} name
 */
GdprApiKey.prototype['name'] = undefined;

/**
 * @member {Boolean} revoked
 */
GdprApiKey.prototype['revoked'] = undefined;






export default GdprApiKey;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ParticipationCreate model module.
 * @module model/ParticipationCreate
 * @version 0.1.0
 */
class ParticipationCreate {
    /**
     * Constructs a new <code>ParticipationCreate</code>.
     * @alias module:model/ParticipationCreate
     */
    constructor() { 
        
        ParticipationCreate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ParticipationCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ParticipationCreate} obj Optional instance to populate.
     * @return {module:model/ParticipationCreate} The populated <code>ParticipationCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ParticipationCreate();

            if (data.hasOwnProperty('acquiredAt')) {
                obj['acquiredAt'] = ApiClient.convertToType(data['acquiredAt'], 'Date');
            }
            if (data.hasOwnProperty('boardAppointment')) {
                obj['boardAppointment'] = ApiClient.convertToType(data['boardAppointment'], 'Boolean');
            }
            if (data.hasOwnProperty('companyName')) {
                obj['companyName'] = ApiClient.convertToType(data['companyName'], 'String');
            }
            if (data.hasOwnProperty('controlAgreement')) {
                obj['controlAgreement'] = ApiClient.convertToType(data['controlAgreement'], 'Boolean');
            }
            if (data.hasOwnProperty('legalForm')) {
                obj['legalForm'] = ApiClient.convertToType(data['legalForm'], 'String');
            }
            if (data.hasOwnProperty('ownershipPct')) {
                obj['ownershipPct'] = ApiClient.convertToType(data['ownershipPct'], 'String');
            }
            if (data.hasOwnProperty('purposeVehicle')) {
                obj['purposeVehicle'] = ApiClient.convertToType(data['purposeVehicle'], 'Boolean');
            }
            if (data.hasOwnProperty('votingMajority')) {
                obj['votingMajority'] = ApiClient.convertToType(data['votingMajority'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ParticipationCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ParticipationCreate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['companyName'] && !(typeof data['companyName'] === 'string' || data['companyName'] instanceof String)) {
            throw new Error("Expected the field `companyName` to be a primitive type in the JSON string but got " + data['companyName']);
        }
        // ensure the json data is a string
        if (data['legalForm'] && !(typeof data['legalForm'] === 'string' || data['legalForm'] instanceof String)) {
            throw new Error("Expected the field `legalForm` to be a primitive type in the JSON string but got " + data['legalForm']);
        }
        // ensure the json data is a string
        if (data['ownershipPct'] && !(typeof data['ownershipPct'] === 'string' || data['ownershipPct'] instanceof String)) {
            throw new Error("Expected the field `ownershipPct` to be a primitive type in the JSON string but got " + data['ownershipPct']);
        }

        return true;
    }


}



/**
 * Datum des Erwerbs der Beteiligung.
 * @member {Date} acquiredAt
 */
ParticipationCreate.prototype['acquiredAt'] = undefined;

/**
 * Bestellungsrecht für Geschäftsführung/Aufsichtsrat (§ 290 Abs. 2 Nr. 2 HGB).
 * @member {Boolean} boardAppointment
 */
ParticipationCreate.prototype['boardAppointment'] = undefined;

/**
 * Name des Beteiligungsunternehmens (§ 271 HGB).
 * @member {String} companyName
 */
ParticipationCreate.prototype['companyName'] = undefined;

/**
 * Beherrschungsvertrag (§ 290 Abs. 2 Nr. 3 HGB).
 * @member {Boolean} controlAgreement
 */
ParticipationCreate.prototype['controlAgreement'] = undefined;

/**
 * Rechtsform, z. B. \"GmbH\".
 * @member {String} legalForm
 */
ParticipationCreate.prototype['legalForm'] = undefined;

/**
 * Anteilsquote in Prozent (§ 271 HGB; > 20 % widerlegbare Vermutung).
 * @member {String} ownershipPct
 */
ParticipationCreate.prototype['ownershipPct'] = undefined;

/**
 * Zweckgesellschaft (§ 290 Abs. 2 Nr. 4 HGB).
 * @member {Boolean} purposeVehicle
 */
ParticipationCreate.prototype['purposeVehicle'] = undefined;

/**
 * Stimmrechtsmehrheit (§ 290 Abs. 2 Nr. 1 HGB).
 * @member {Boolean} votingMajority
 */
ParticipationCreate.prototype['votingMajority'] = undefined;






export default ParticipationCreate;


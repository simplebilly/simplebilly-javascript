/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AccountOverview from './AccountOverview';
import BalanceSheet from './BalanceSheet';
import IncomeStatement from './IncomeStatement';
import VatSummary from './VatSummary';

/**
 * The EBilanzReport model module.
 * @module model/EBilanzReport
 * @version 0.1.0
 */
class EBilanzReport {
    /**
     * Constructs a new <code>EBilanzReport</code>.
     * @alias module:model/EBilanzReport
     * @param accountOverview {Array.<module:model/AccountOverview>} 
     * @param balanceSheet {module:model/BalanceSheet} 
     * @param generatedAt {String} 
     * @param incomeStatement {module:model/IncomeStatement} 
     * @param period {String} 
     * @param vatSummary {module:model/VatSummary} 
     */
    constructor(accountOverview, balanceSheet, generatedAt, incomeStatement, period, vatSummary) { 
        
        EBilanzReport.initialize(this, accountOverview, balanceSheet, generatedAt, incomeStatement, period, vatSummary);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountOverview, balanceSheet, generatedAt, incomeStatement, period, vatSummary) { 
        obj['account_overview'] = accountOverview;
        obj['balance_sheet'] = balanceSheet;
        obj['generated_at'] = generatedAt;
        obj['income_statement'] = incomeStatement;
        obj['period'] = period;
        obj['vat_summary'] = vatSummary;
    }

    /**
     * Constructs a <code>EBilanzReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EBilanzReport} obj Optional instance to populate.
     * @return {module:model/EBilanzReport} The populated <code>EBilanzReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EBilanzReport();

            if (data.hasOwnProperty('account_overview')) {
                obj['account_overview'] = ApiClient.convertToType(data['account_overview'], [AccountOverview]);
            }
            if (data.hasOwnProperty('balance_sheet')) {
                obj['balance_sheet'] = BalanceSheet.constructFromObject(data['balance_sheet']);
            }
            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('income_statement')) {
                obj['income_statement'] = IncomeStatement.constructFromObject(data['income_statement']);
            }
            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
            if (data.hasOwnProperty('vat_summary')) {
                obj['vat_summary'] = VatSummary.constructFromObject(data['vat_summary']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EBilanzReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EBilanzReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EBilanzReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['account_overview']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['account_overview'])) {
                throw new Error("Expected the field `account_overview` to be an array in the JSON data but got " + data['account_overview']);
            }
            // validate the optional field `account_overview` (array)
            for (const item of data['account_overview']) {
                AccountOverview.validateJSON(item);
            };
        }
        // validate the optional field `balance_sheet`
        if (data['balance_sheet']) { // data not null
          BalanceSheet.validateJSON(data['balance_sheet']);
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        // validate the optional field `income_statement`
        if (data['income_statement']) { // data not null
          IncomeStatement.validateJSON(data['income_statement']);
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }
        // validate the optional field `vat_summary`
        if (data['vat_summary']) { // data not null
          VatSummary.validateJSON(data['vat_summary']);
        }

        return true;
    }


}

EBilanzReport.RequiredProperties = ["account_overview", "balance_sheet", "generated_at", "income_statement", "period", "vat_summary"];

/**
 * @member {Array.<module:model/AccountOverview>} account_overview
 */
EBilanzReport.prototype['account_overview'] = undefined;

/**
 * @member {module:model/BalanceSheet} balance_sheet
 */
EBilanzReport.prototype['balance_sheet'] = undefined;

/**
 * @member {String} generated_at
 */
EBilanzReport.prototype['generated_at'] = undefined;

/**
 * @member {module:model/IncomeStatement} income_statement
 */
EBilanzReport.prototype['income_statement'] = undefined;

/**
 * @member {String} period
 */
EBilanzReport.prototype['period'] = undefined;

/**
 * @member {module:model/VatSummary} vat_summary
 */
EBilanzReport.prototype['vat_summary'] = undefined;






export default EBilanzReport;


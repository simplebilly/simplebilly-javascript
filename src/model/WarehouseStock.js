/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The WarehouseStock model module.
 * @module model/WarehouseStock
 * @version 0.1.0
 */
class WarehouseStock {
    /**
     * Constructs a new <code>WarehouseStock</code>.
     * @alias module:model/WarehouseStock
     * @param productId {String} 
     * @param quantity {Number} 
     * @param warehouseId {String} 
     */
    constructor(productId, quantity, warehouseId) { 
        
        WarehouseStock.initialize(this, productId, quantity, warehouseId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, productId, quantity, warehouseId) { 
        obj['productId'] = productId;
        obj['quantity'] = quantity;
        obj['warehouseId'] = warehouseId;
    }

    /**
     * Constructs a <code>WarehouseStock</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WarehouseStock} obj Optional instance to populate.
     * @return {module:model/WarehouseStock} The populated <code>WarehouseStock</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WarehouseStock();

            if (data.hasOwnProperty('batchNumber')) {
                obj['batchNumber'] = ApiClient.convertToType(data['batchNumber'], 'String');
            }
            if (data.hasOwnProperty('binLocation')) {
                obj['binLocation'] = ApiClient.convertToType(data['binLocation'], 'String');
            }
            if (data.hasOwnProperty('expiryDate')) {
                obj['expiryDate'] = ApiClient.convertToType(data['expiryDate'], 'Date');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('serialNumbers')) {
                obj['serialNumbers'] = ApiClient.convertToType(data['serialNumbers'], Object);
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WarehouseStock</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WarehouseStock</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of WarehouseStock.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['batchNumber'] && !(typeof data['batchNumber'] === 'string' || data['batchNumber'] instanceof String)) {
            throw new Error("Expected the field `batchNumber` to be a primitive type in the JSON string but got " + data['batchNumber']);
        }
        // ensure the json data is a string
        if (data['binLocation'] && !(typeof data['binLocation'] === 'string' || data['binLocation'] instanceof String)) {
            throw new Error("Expected the field `binLocation` to be a primitive type in the JSON string but got " + data['binLocation']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}

WarehouseStock.RequiredProperties = ["productId", "quantity", "warehouseId"];

/**
 * Batch/lot number (Chargennummer) — `None` for non-batched goods.
 * @member {String} batchNumber
 */
WarehouseStock.prototype['batchNumber'] = undefined;

/**
 * @member {String} binLocation
 */
WarehouseStock.prototype['binLocation'] = undefined;

/**
 * Expiry date for batch-tracked goods.
 * @member {Date} expiryDate
 */
WarehouseStock.prototype['expiryDate'] = undefined;

/**
 * @member {String} productId
 */
WarehouseStock.prototype['productId'] = undefined;

/**
 * @member {Number} quantity
 */
WarehouseStock.prototype['quantity'] = undefined;

/**
 * JSON array of serial numbers (Seriennummern) in this stock row.
 * @member {Object} serialNumbers
 */
WarehouseStock.prototype['serialNumbers'] = undefined;

/**
 * @member {String} warehouseId
 */
WarehouseStock.prototype['warehouseId'] = undefined;






export default WarehouseStock;


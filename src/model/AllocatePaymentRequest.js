/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AllocatePaymentRequest model module.
 * @module model/AllocatePaymentRequest
 * @version 0.1.0
 */
class AllocatePaymentRequest {
    /**
     * Constructs a new <code>AllocatePaymentRequest</code>.
     * @alias module:model/AllocatePaymentRequest
     * @param amount {Number} 
     * @param invoiceId {String} 
     * @param paymentId {String} 
     */
    constructor(amount, invoiceId, paymentId) { 
        
        AllocatePaymentRequest.initialize(this, amount, invoiceId, paymentId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, invoiceId, paymentId) { 
        obj['amount'] = amount;
        obj['invoice_id'] = invoiceId;
        obj['payment_id'] = paymentId;
    }

    /**
     * Constructs a <code>AllocatePaymentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AllocatePaymentRequest} obj Optional instance to populate.
     * @return {module:model/AllocatePaymentRequest} The populated <code>AllocatePaymentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AllocatePaymentRequest();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('invoice_id')) {
                obj['invoice_id'] = ApiClient.convertToType(data['invoice_id'], 'String');
            }
            if (data.hasOwnProperty('payment_id')) {
                obj['payment_id'] = ApiClient.convertToType(data['payment_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AllocatePaymentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AllocatePaymentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AllocatePaymentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['invoice_id'] && !(typeof data['invoice_id'] === 'string' || data['invoice_id'] instanceof String)) {
            throw new Error("Expected the field `invoice_id` to be a primitive type in the JSON string but got " + data['invoice_id']);
        }
        // ensure the json data is a string
        if (data['payment_id'] && !(typeof data['payment_id'] === 'string' || data['payment_id'] instanceof String)) {
            throw new Error("Expected the field `payment_id` to be a primitive type in the JSON string but got " + data['payment_id']);
        }

        return true;
    }


}

AllocatePaymentRequest.RequiredProperties = ["amount", "invoice_id", "payment_id"];

/**
 * @member {Number} amount
 */
AllocatePaymentRequest.prototype['amount'] = undefined;

/**
 * @member {String} invoice_id
 */
AllocatePaymentRequest.prototype['invoice_id'] = undefined;

/**
 * @member {String} payment_id
 */
AllocatePaymentRequest.prototype['payment_id'] = undefined;






export default AllocatePaymentRequest;


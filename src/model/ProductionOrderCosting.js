/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CostingLine from './CostingLine';

/**
 * The ProductionOrderCosting model module.
 * @module model/ProductionOrderCosting
 * @version 0.1.0
 */
class ProductionOrderCosting {
    /**
     * Constructs a new <code>ProductionOrderCosting</code>.
     * Actual-costing (Nachkalkulation) report for a production order.
     * @alias module:model/ProductionOrderCosting
     * @param costPerUnit {String} material_cost_total ÷ quantity.
     * @param costSource {String} \"actual\" when costed from stock-movement consumption, else \"planned\".
     * @param lines {Array.<module:model/CostingLine>} 
     * @param materialCostTotal {String} Total material cost for the whole order.
     * @param orderNumber {String} 
     * @param productionOrderId {String} 
     * @param quantity {Number} 
     * @param status {String} 
     */
    constructor(costPerUnit, costSource, lines, materialCostTotal, orderNumber, productionOrderId, quantity, status) { 
        
        ProductionOrderCosting.initialize(this, costPerUnit, costSource, lines, materialCostTotal, orderNumber, productionOrderId, quantity, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, costPerUnit, costSource, lines, materialCostTotal, orderNumber, productionOrderId, quantity, status) { 
        obj['costPerUnit'] = costPerUnit;
        obj['costSource'] = costSource;
        obj['lines'] = lines;
        obj['materialCostTotal'] = materialCostTotal;
        obj['orderNumber'] = orderNumber;
        obj['productionOrderId'] = productionOrderId;
        obj['quantity'] = quantity;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>ProductionOrderCosting</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductionOrderCosting} obj Optional instance to populate.
     * @return {module:model/ProductionOrderCosting} The populated <code>ProductionOrderCosting</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductionOrderCosting();

            if (data.hasOwnProperty('costPerUnit')) {
                obj['costPerUnit'] = ApiClient.convertToType(data['costPerUnit'], 'String');
            }
            if (data.hasOwnProperty('costSource')) {
                obj['costSource'] = ApiClient.convertToType(data['costSource'], 'String');
            }
            if (data.hasOwnProperty('lines')) {
                obj['lines'] = ApiClient.convertToType(data['lines'], [CostingLine]);
            }
            if (data.hasOwnProperty('marginPerUnit')) {
                obj['marginPerUnit'] = ApiClient.convertToType(data['marginPerUnit'], 'String');
            }
            if (data.hasOwnProperty('marginPercent')) {
                obj['marginPercent'] = ApiClient.convertToType(data['marginPercent'], 'String');
            }
            if (data.hasOwnProperty('materialCostTotal')) {
                obj['materialCostTotal'] = ApiClient.convertToType(data['materialCostTotal'], 'String');
            }
            if (data.hasOwnProperty('orderNumber')) {
                obj['orderNumber'] = ApiClient.convertToType(data['orderNumber'], 'String');
            }
            if (data.hasOwnProperty('productionOrderId')) {
                obj['productionOrderId'] = ApiClient.convertToType(data['productionOrderId'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('salePrice')) {
                obj['salePrice'] = ApiClient.convertToType(data['salePrice'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductionOrderCosting</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductionOrderCosting</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductionOrderCosting.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['costPerUnit'] && !(typeof data['costPerUnit'] === 'string' || data['costPerUnit'] instanceof String)) {
            throw new Error("Expected the field `costPerUnit` to be a primitive type in the JSON string but got " + data['costPerUnit']);
        }
        // ensure the json data is a string
        if (data['costSource'] && !(typeof data['costSource'] === 'string' || data['costSource'] instanceof String)) {
            throw new Error("Expected the field `costSource` to be a primitive type in the JSON string but got " + data['costSource']);
        }
        if (data['lines']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['lines'])) {
                throw new Error("Expected the field `lines` to be an array in the JSON data but got " + data['lines']);
            }
            // validate the optional field `lines` (array)
            for (const item of data['lines']) {
                CostingLine.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['marginPerUnit'] && !(typeof data['marginPerUnit'] === 'string' || data['marginPerUnit'] instanceof String)) {
            throw new Error("Expected the field `marginPerUnit` to be a primitive type in the JSON string but got " + data['marginPerUnit']);
        }
        // ensure the json data is a string
        if (data['marginPercent'] && !(typeof data['marginPercent'] === 'string' || data['marginPercent'] instanceof String)) {
            throw new Error("Expected the field `marginPercent` to be a primitive type in the JSON string but got " + data['marginPercent']);
        }
        // ensure the json data is a string
        if (data['materialCostTotal'] && !(typeof data['materialCostTotal'] === 'string' || data['materialCostTotal'] instanceof String)) {
            throw new Error("Expected the field `materialCostTotal` to be a primitive type in the JSON string but got " + data['materialCostTotal']);
        }
        // ensure the json data is a string
        if (data['orderNumber'] && !(typeof data['orderNumber'] === 'string' || data['orderNumber'] instanceof String)) {
            throw new Error("Expected the field `orderNumber` to be a primitive type in the JSON string but got " + data['orderNumber']);
        }
        // ensure the json data is a string
        if (data['productionOrderId'] && !(typeof data['productionOrderId'] === 'string' || data['productionOrderId'] instanceof String)) {
            throw new Error("Expected the field `productionOrderId` to be a primitive type in the JSON string but got " + data['productionOrderId']);
        }
        // ensure the json data is a string
        if (data['salePrice'] && !(typeof data['salePrice'] === 'string' || data['salePrice'] instanceof String)) {
            throw new Error("Expected the field `salePrice` to be a primitive type in the JSON string but got " + data['salePrice']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

ProductionOrderCosting.RequiredProperties = ["costPerUnit", "costSource", "lines", "materialCostTotal", "orderNumber", "productionOrderId", "quantity", "status"];

/**
 * material_cost_total ÷ quantity.
 * @member {String} costPerUnit
 */
ProductionOrderCosting.prototype['costPerUnit'] = undefined;

/**
 * \"actual\" when costed from stock-movement consumption, else \"planned\".
 * @member {String} costSource
 */
ProductionOrderCosting.prototype['costSource'] = undefined;

/**
 * @member {Array.<module:model/CostingLine>} lines
 */
ProductionOrderCosting.prototype['lines'] = undefined;

/**
 * sale_price − cost_per_unit.
 * @member {String} marginPerUnit
 */
ProductionOrderCosting.prototype['marginPerUnit'] = undefined;

/**
 * margin_per_unit ÷ cost_per_unit as a percentage.
 * @member {String} marginPercent
 */
ProductionOrderCosting.prototype['marginPercent'] = undefined;

/**
 * Total material cost for the whole order.
 * @member {String} materialCostTotal
 */
ProductionOrderCosting.prototype['materialCostTotal'] = undefined;

/**
 * @member {String} orderNumber
 */
ProductionOrderCosting.prototype['orderNumber'] = undefined;

/**
 * @member {String} productionOrderId
 */
ProductionOrderCosting.prototype['productionOrderId'] = undefined;

/**
 * @member {Number} quantity
 */
ProductionOrderCosting.prototype['quantity'] = undefined;

/**
 * Finished product's sale price per unit (used to compute margin).
 * @member {String} salePrice
 */
ProductionOrderCosting.prototype['salePrice'] = undefined;

/**
 * @member {String} status
 */
ProductionOrderCosting.prototype['status'] = undefined;






export default ProductionOrderCosting;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KontoItem from './KontoItem';

/**
 * The KontoReport model module.
 * @module model/KontoReport
 * @version 0.1.0
 */
class KontoReport {
    /**
     * Constructs a new <code>KontoReport</code>.
     * @alias module:model/KontoReport
     * @param generatedAt {String} 
     * @param konten {Array.<module:model/KontoItem>} 
     * @param period {String} 
     */
    constructor(generatedAt, konten, period) { 
        
        KontoReport.initialize(this, generatedAt, konten, period);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, generatedAt, konten, period) { 
        obj['generated_at'] = generatedAt;
        obj['konten'] = konten;
        obj['period'] = period;
    }

    /**
     * Constructs a <code>KontoReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KontoReport} obj Optional instance to populate.
     * @return {module:model/KontoReport} The populated <code>KontoReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KontoReport();

            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('konten')) {
                obj['konten'] = ApiClient.convertToType(data['konten'], [KontoItem]);
            }
            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KontoReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KontoReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KontoReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        if (data['konten']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['konten'])) {
                throw new Error("Expected the field `konten` to be an array in the JSON data but got " + data['konten']);
            }
            // validate the optional field `konten` (array)
            for (const item of data['konten']) {
                KontoItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }

        return true;
    }


}

KontoReport.RequiredProperties = ["generated_at", "konten", "period"];

/**
 * @member {String} generated_at
 */
KontoReport.prototype['generated_at'] = undefined;

/**
 * @member {Array.<module:model/KontoItem>} konten
 */
KontoReport.prototype['konten'] = undefined;

/**
 * @member {String} period
 */
KontoReport.prototype['period'] = undefined;






export default KontoReport;


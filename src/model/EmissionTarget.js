/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmissionTargetScope from './EmissionTargetScope';

/**
 * The EmissionTarget model module.
 * @module model/EmissionTarget
 * @version 0.1.0
 */
class EmissionTarget {
    /**
     * Constructs a new <code>EmissionTarget</code>.
     * @alias module:model/EmissionTarget
     * @param baseValue {String} 
     * @param baseYear {Number} tCO2e in the base year (actuals).
     * @param description {String} Transition-plan narrative (ESRS E1-1 light), may be empty.
     * @param scope {module:model/EmissionTargetScope} \"total\" | \"1\" | \"2\" | \"3\".
     * @param targetValue {String} 
     * @param targetYear {Number} tCO2e target for the target year.
     */
    constructor(baseValue, baseYear, description, scope, targetValue, targetYear) { 
        
        EmissionTarget.initialize(this, baseValue, baseYear, description, scope, targetValue, targetYear);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, baseValue, baseYear, description, scope, targetValue, targetYear) { 
        obj['baseValue'] = baseValue;
        obj['baseYear'] = baseYear;
        obj['description'] = description;
        obj['scope'] = scope;
        obj['targetValue'] = targetValue;
        obj['targetYear'] = targetYear;
    }

    /**
     * Constructs a <code>EmissionTarget</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmissionTarget} obj Optional instance to populate.
     * @return {module:model/EmissionTarget} The populated <code>EmissionTarget</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmissionTarget();

            if (data.hasOwnProperty('baseValue')) {
                obj['baseValue'] = ApiClient.convertToType(data['baseValue'], 'String');
            }
            if (data.hasOwnProperty('baseYear')) {
                obj['baseYear'] = ApiClient.convertToType(data['baseYear'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], EmissionTargetScope);
            }
            if (data.hasOwnProperty('targetValue')) {
                obj['targetValue'] = ApiClient.convertToType(data['targetValue'], 'String');
            }
            if (data.hasOwnProperty('targetYear')) {
                obj['targetYear'] = ApiClient.convertToType(data['targetYear'], 'Number');
            }
            if (data.hasOwnProperty('updatedAt')) {
                obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmissionTarget</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmissionTarget</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmissionTarget.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['baseValue'] && !(typeof data['baseValue'] === 'string' || data['baseValue'] instanceof String)) {
            throw new Error("Expected the field `baseValue` to be a primitive type in the JSON string but got " + data['baseValue']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['targetValue'] && !(typeof data['targetValue'] === 'string' || data['targetValue'] instanceof String)) {
            throw new Error("Expected the field `targetValue` to be a primitive type in the JSON string but got " + data['targetValue']);
        }

        return true;
    }


}

EmissionTarget.RequiredProperties = ["baseValue", "baseYear", "description", "scope", "targetValue", "targetYear"];

/**
 * @member {String} baseValue
 */
EmissionTarget.prototype['baseValue'] = undefined;

/**
 * tCO2e in the base year (actuals).
 * @member {Number} baseYear
 */
EmissionTarget.prototype['baseYear'] = undefined;

/**
 * Transition-plan narrative (ESRS E1-1 light), may be empty.
 * @member {String} description
 */
EmissionTarget.prototype['description'] = undefined;

/**
 * \"total\" | \"1\" | \"2\" | \"3\".
 * @member {module:model/EmissionTargetScope} scope
 */
EmissionTarget.prototype['scope'] = undefined;

/**
 * @member {String} targetValue
 */
EmissionTarget.prototype['targetValue'] = undefined;

/**
 * tCO2e target for the target year.
 * @member {Number} targetYear
 */
EmissionTarget.prototype['targetYear'] = undefined;

/**
 * @member {Date} updatedAt
 */
EmissionTarget.prototype['updatedAt'] = undefined;






export default EmissionTarget;


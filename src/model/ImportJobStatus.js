/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ImportJobStatus model module.
 * @module model/ImportJobStatus
 * @version 0.1.0
 */
class ImportJobStatus {
    /**
     * Constructs a new <code>ImportJobStatus</code>.
     * @alias module:model/ImportJobStatus
     * @param jobId {String} 
     * @param processed {Number} 
     * @param progress {Number} 0–100
     * @param stage {String} queued | fetching | downloading | importing | done
     * @param status {String} pending | running | done | failed
     * @param total {Number} 
     */
    constructor(jobId, processed, progress, stage, status, total) { 
        
        ImportJobStatus.initialize(this, jobId, processed, progress, stage, status, total);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jobId, processed, progress, stage, status, total) { 
        obj['job_id'] = jobId;
        obj['processed'] = processed;
        obj['progress'] = progress;
        obj['stage'] = stage;
        obj['status'] = status;
        obj['total'] = total;
    }

    /**
     * Constructs a <code>ImportJobStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImportJobStatus} obj Optional instance to populate.
     * @return {module:model/ImportJobStatus} The populated <code>ImportJobStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImportJobStatus();

            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('job_id')) {
                obj['job_id'] = ApiClient.convertToType(data['job_id'], 'String');
            }
            if (data.hasOwnProperty('processed')) {
                obj['processed'] = ApiClient.convertToType(data['processed'], 'Number');
            }
            if (data.hasOwnProperty('progress')) {
                obj['progress'] = ApiClient.convertToType(data['progress'], 'Number');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('stage')) {
                obj['stage'] = ApiClient.convertToType(data['stage'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImportJobStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImportJobStatus</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ImportJobStatus.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        // ensure the json data is a string
        if (data['job_id'] && !(typeof data['job_id'] === 'string' || data['job_id'] instanceof String)) {
            throw new Error("Expected the field `job_id` to be a primitive type in the JSON string but got " + data['job_id']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['stage'] && !(typeof data['stage'] === 'string' || data['stage'] instanceof String)) {
            throw new Error("Expected the field `stage` to be a primitive type in the JSON string but got " + data['stage']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

ImportJobStatus.RequiredProperties = ["job_id", "processed", "progress", "stage", "status", "total"];

/**
 * Set only when the job failed.
 * @member {String} error
 */
ImportJobStatus.prototype['error'] = undefined;

/**
 * @member {String} job_id
 */
ImportJobStatus.prototype['job_id'] = undefined;

/**
 * @member {Number} processed
 */
ImportJobStatus.prototype['processed'] = undefined;

/**
 * 0–100
 * @member {Number} progress
 */
ImportJobStatus.prototype['progress'] = undefined;

/**
 * Which competitor the import came from (lexoffice | billbee); the frontend uses it to label the job. Absent for legacy jobs.
 * @member {String} provider
 */
ImportJobStatus.prototype['provider'] = undefined;

/**
 * queued | fetching | downloading | importing | done
 * @member {String} stage
 */
ImportJobStatus.prototype['stage'] = undefined;

/**
 * pending | running | done | failed
 * @member {String} status
 */
ImportJobStatus.prototype['status'] = undefined;

/**
 * @member {Number} total
 */
ImportJobStatus.prototype['total'] = undefined;






export default ImportJobStatus;


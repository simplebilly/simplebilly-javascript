/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RfqStatus from './RfqStatus';

/**
 * The RfqCreate model module.
 * @module model/RfqCreate
 * @version 0.1.0
 */
class RfqCreate {
    /**
     * Constructs a new <code>RfqCreate</code>.
     * @alias module:model/RfqCreate
     * @param lineItems {Object} JSON array of `{product_id, name, sku, quantity, requested_unit_price?, quoted_unit_price?}`.
     * @param requestedDate {Date} 
     * @param rfqNumber {String} 
     * @param status {module:model/RfqStatus} One of: draft | sent | offer_received | rejected | converted
     */
    constructor(lineItems, requestedDate, rfqNumber, status) { 
        
        RfqCreate.initialize(this, lineItems, requestedDate, rfqNumber, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, lineItems, requestedDate, rfqNumber, status) { 
        obj['lineItems'] = lineItems;
        obj['requestedDate'] = requestedDate;
        obj['rfqNumber'] = rfqNumber;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>RfqCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RfqCreate} obj Optional instance to populate.
     * @return {module:model/RfqCreate} The populated <code>RfqCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RfqCreate();

            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('lineItems')) {
                obj['lineItems'] = ApiClient.convertToType(data['lineItems'], Object);
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('requestedDate')) {
                obj['requestedDate'] = ApiClient.convertToType(data['requestedDate'], 'Date');
            }
            if (data.hasOwnProperty('responseDate')) {
                obj['responseDate'] = ApiClient.convertToType(data['responseDate'], 'Date');
            }
            if (data.hasOwnProperty('rfqNumber')) {
                obj['rfqNumber'] = ApiClient.convertToType(data['rfqNumber'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], RfqStatus);
            }
            if (data.hasOwnProperty('supplierContactId')) {
                obj['supplierContactId'] = ApiClient.convertToType(data['supplierContactId'], 'String');
            }
            if (data.hasOwnProperty('supplierName')) {
                obj['supplierName'] = ApiClient.convertToType(data['supplierName'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RfqCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RfqCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RfqCreate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['rfqNumber'] && !(typeof data['rfqNumber'] === 'string' || data['rfqNumber'] instanceof String)) {
            throw new Error("Expected the field `rfqNumber` to be a primitive type in the JSON string but got " + data['rfqNumber']);
        }
        // ensure the json data is a string
        if (data['supplierContactId'] && !(typeof data['supplierContactId'] === 'string' || data['supplierContactId'] instanceof String)) {
            throw new Error("Expected the field `supplierContactId` to be a primitive type in the JSON string but got " + data['supplierContactId']);
        }
        // ensure the json data is a string
        if (data['supplierName'] && !(typeof data['supplierName'] === 'string' || data['supplierName'] instanceof String)) {
            throw new Error("Expected the field `supplierName` to be a primitive type in the JSON string but got " + data['supplierName']);
        }

        return true;
    }


}

RfqCreate.RequiredProperties = ["lineItems", "requestedDate", "rfqNumber", "status"];

/**
 * @member {String} currency
 */
RfqCreate.prototype['currency'] = undefined;

/**
 * JSON array of `{product_id, name, sku, quantity, requested_unit_price?, quoted_unit_price?}`.
 * @member {Object} lineItems
 */
RfqCreate.prototype['lineItems'] = undefined;

/**
 * @member {String} notes
 */
RfqCreate.prototype['notes'] = undefined;

/**
 * @member {Date} requestedDate
 */
RfqCreate.prototype['requestedDate'] = undefined;

/**
 * @member {Date} responseDate
 */
RfqCreate.prototype['responseDate'] = undefined;

/**
 * @member {String} rfqNumber
 */
RfqCreate.prototype['rfqNumber'] = undefined;

/**
 * One of: draft | sent | offer_received | rejected | converted
 * @member {module:model/RfqStatus} status
 */
RfqCreate.prototype['status'] = undefined;

/**
 * References the supplier entity.
 * @member {String} supplierContactId
 */
RfqCreate.prototype['supplierContactId'] = undefined;

/**
 * @member {String} supplierName
 */
RfqCreate.prototype['supplierName'] = undefined;






export default RfqCreate;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ImportStartRequest model module.
 * @module model/ImportStartRequest
 * @version 0.1.0
 */
class ImportStartRequest {
    /**
     * Constructs a new <code>ImportStartRequest</code>.
     * @alias module:model/ImportStartRequest
     * @param apiKey {String} 
     * @param provider {String} 
     * @param years {Array.<Number>} 
     */
    constructor(apiKey, provider, years) { 
        
        ImportStartRequest.initialize(this, apiKey, provider, years);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, apiKey, provider, years) { 
        obj['apiKey'] = apiKey;
        obj['provider'] = provider;
        obj['years'] = years;
    }

    /**
     * Constructs a <code>ImportStartRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ImportStartRequest} obj Optional instance to populate.
     * @return {module:model/ImportStartRequest} The populated <code>ImportStartRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ImportStartRequest();

            if (data.hasOwnProperty('apiKey')) {
                obj['apiKey'] = ApiClient.convertToType(data['apiKey'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('years')) {
                obj['years'] = ApiClient.convertToType(data['years'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ImportStartRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ImportStartRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ImportStartRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['apiKey'] && !(typeof data['apiKey'] === 'string' || data['apiKey'] instanceof String)) {
            throw new Error("Expected the field `apiKey` to be a primitive type in the JSON string but got " + data['apiKey']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['years'])) {
            throw new Error("Expected the field `years` to be an array in the JSON data but got " + data['years']);
        }

        return true;
    }


}

ImportStartRequest.RequiredProperties = ["apiKey", "provider", "years"];

/**
 * @member {String} apiKey
 */
ImportStartRequest.prototype['apiKey'] = undefined;

/**
 * @member {String} provider
 */
ImportStartRequest.prototype['provider'] = undefined;

/**
 * @member {Array.<Number>} years
 */
ImportStartRequest.prototype['years'] = undefined;






export default ImportStartRequest;


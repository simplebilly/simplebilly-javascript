/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateEmissionEntry model module.
 * @module model/CreateEmissionEntry
 * @version 0.1.0
 */
class CreateEmissionEntry {
    /**
     * Constructs a new <code>CreateEmissionEntry</code>.
     * @alias module:model/CreateEmissionEntry
     * @param activityValue {String} 
     * @param categoryId {String} 
     * @param description {String} 
     * @param method {String} 
     * @param scope {String} 
     * @param unit {String} 
     * @param year {Number} 
     */
    constructor(activityValue, categoryId, description, method, scope, unit, year) { 
        
        CreateEmissionEntry.initialize(this, activityValue, categoryId, description, method, scope, unit, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, activityValue, categoryId, description, method, scope, unit, year) { 
        obj['activity_value'] = activityValue;
        obj['category_id'] = categoryId;
        obj['description'] = description;
        obj['method'] = method;
        obj['scope'] = scope;
        obj['unit'] = unit;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>CreateEmissionEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateEmissionEntry} obj Optional instance to populate.
     * @return {module:model/CreateEmissionEntry} The populated <code>CreateEmissionEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateEmissionEntry();

            if (data.hasOwnProperty('activity_value')) {
                obj['activity_value'] = ApiClient.convertToType(data['activity_value'], 'String');
            }
            if (data.hasOwnProperty('category_id')) {
                obj['category_id'] = ApiClient.convertToType(data['category_id'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('method')) {
                obj['method'] = ApiClient.convertToType(data['method'], 'String');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = ApiClient.convertToType(data['unit'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateEmissionEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateEmissionEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateEmissionEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['activity_value'] && !(typeof data['activity_value'] === 'string' || data['activity_value'] instanceof String)) {
            throw new Error("Expected the field `activity_value` to be a primitive type in the JSON string but got " + data['activity_value']);
        }
        // ensure the json data is a string
        if (data['category_id'] && !(typeof data['category_id'] === 'string' || data['category_id'] instanceof String)) {
            throw new Error("Expected the field `category_id` to be a primitive type in the JSON string but got " + data['category_id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['method'] && !(typeof data['method'] === 'string' || data['method'] instanceof String)) {
            throw new Error("Expected the field `method` to be a primitive type in the JSON string but got " + data['method']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }
        // ensure the json data is a string
        if (data['unit'] && !(typeof data['unit'] === 'string' || data['unit'] instanceof String)) {
            throw new Error("Expected the field `unit` to be a primitive type in the JSON string but got " + data['unit']);
        }

        return true;
    }


}

CreateEmissionEntry.RequiredProperties = ["activity_value", "category_id", "description", "method", "scope", "unit", "year"];

/**
 * @member {String} activity_value
 */
CreateEmissionEntry.prototype['activity_value'] = undefined;

/**
 * @member {String} category_id
 */
CreateEmissionEntry.prototype['category_id'] = undefined;

/**
 * @member {String} description
 */
CreateEmissionEntry.prototype['description'] = undefined;

/**
 * @member {String} method
 */
CreateEmissionEntry.prototype['method'] = undefined;

/**
 * @member {String} scope
 */
CreateEmissionEntry.prototype['scope'] = undefined;

/**
 * @member {String} unit
 */
CreateEmissionEntry.prototype['unit'] = undefined;

/**
 * @member {Number} year
 */
CreateEmissionEntry.prototype['year'] = undefined;






export default CreateEmissionEntry;


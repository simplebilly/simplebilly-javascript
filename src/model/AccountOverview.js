/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AccountOverview model module.
 * @module model/AccountOverview
 * @version 0.1.0
 */
class AccountOverview {
    /**
     * Constructs a new <code>AccountOverview</code>.
     * @alias module:model/AccountOverview
     * @param account {String} 
     * @param accountName {String} 
     * @param balance {String} 
     * @param creditTotal {String} 
     * @param debitTotal {String} 
     */
    constructor(account, accountName, balance, creditTotal, debitTotal) { 
        
        AccountOverview.initialize(this, account, accountName, balance, creditTotal, debitTotal);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, account, accountName, balance, creditTotal, debitTotal) { 
        obj['account'] = account;
        obj['account_name'] = accountName;
        obj['balance'] = balance;
        obj['credit_total'] = creditTotal;
        obj['debit_total'] = debitTotal;
    }

    /**
     * Constructs a <code>AccountOverview</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AccountOverview} obj Optional instance to populate.
     * @return {module:model/AccountOverview} The populated <code>AccountOverview</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AccountOverview();

            if (data.hasOwnProperty('account')) {
                obj['account'] = ApiClient.convertToType(data['account'], 'String');
            }
            if (data.hasOwnProperty('account_name')) {
                obj['account_name'] = ApiClient.convertToType(data['account_name'], 'String');
            }
            if (data.hasOwnProperty('balance')) {
                obj['balance'] = ApiClient.convertToType(data['balance'], 'String');
            }
            if (data.hasOwnProperty('credit_total')) {
                obj['credit_total'] = ApiClient.convertToType(data['credit_total'], 'String');
            }
            if (data.hasOwnProperty('debit_total')) {
                obj['debit_total'] = ApiClient.convertToType(data['debit_total'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AccountOverview</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AccountOverview</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AccountOverview.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account'] && !(typeof data['account'] === 'string' || data['account'] instanceof String)) {
            throw new Error("Expected the field `account` to be a primitive type in the JSON string but got " + data['account']);
        }
        // ensure the json data is a string
        if (data['account_name'] && !(typeof data['account_name'] === 'string' || data['account_name'] instanceof String)) {
            throw new Error("Expected the field `account_name` to be a primitive type in the JSON string but got " + data['account_name']);
        }
        // ensure the json data is a string
        if (data['balance'] && !(typeof data['balance'] === 'string' || data['balance'] instanceof String)) {
            throw new Error("Expected the field `balance` to be a primitive type in the JSON string but got " + data['balance']);
        }
        // ensure the json data is a string
        if (data['credit_total'] && !(typeof data['credit_total'] === 'string' || data['credit_total'] instanceof String)) {
            throw new Error("Expected the field `credit_total` to be a primitive type in the JSON string but got " + data['credit_total']);
        }
        // ensure the json data is a string
        if (data['debit_total'] && !(typeof data['debit_total'] === 'string' || data['debit_total'] instanceof String)) {
            throw new Error("Expected the field `debit_total` to be a primitive type in the JSON string but got " + data['debit_total']);
        }

        return true;
    }


}

AccountOverview.RequiredProperties = ["account", "account_name", "balance", "credit_total", "debit_total"];

/**
 * @member {String} account
 */
AccountOverview.prototype['account'] = undefined;

/**
 * @member {String} account_name
 */
AccountOverview.prototype['account_name'] = undefined;

/**
 * @member {String} balance
 */
AccountOverview.prototype['balance'] = undefined;

/**
 * @member {String} credit_total
 */
AccountOverview.prototype['credit_total'] = undefined;

/**
 * @member {String} debit_total
 */
AccountOverview.prototype['debit_total'] = undefined;






export default AccountOverview;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GdprActivity from './GdprActivity';
import GdprApiKey from './GdprApiKey';
import GdprBillingInfo from './GdprBillingInfo';
import GdprNotification from './GdprNotification';
import GdprRefreshToken from './GdprRefreshToken';
import GdprTenant from './GdprTenant';
import GdprUsageEvent from './GdprUsageEvent';
import GdprUser from './GdprUser';

/**
 * The GdprExport model module.
 * @module model/GdprExport
 * @version 0.1.0
 */
class GdprExport {
    /**
     * Constructs a new <code>GdprExport</code>.
     * @alias module:model/GdprExport
     * @param activityLog {Array.<module:model/GdprActivity>} 
     * @param apiKeys {Array.<module:model/GdprApiKey>} Key identifiers and names only — never a usable credential.
     * @param billing {Array.<module:model/GdprBillingInfo>} 
     * @param exportedAt {Date} 
     * @param generatedByAi {Boolean} Honesty field: this document is a plain data dump, never AI-generated.
     * @param notifications {Array.<module:model/GdprNotification>} 
     * @param refreshTokens {Array.<module:model/GdprRefreshToken>} Session records: metadata only, never the token hash.
     * @param tenants {Array.<module:model/GdprTenant>} 
     * @param usageEvents {Array.<module:model/GdprUsageEvent>} 
     * @param user {module:model/GdprUser} 
     */
    constructor(activityLog, apiKeys, billing, exportedAt, generatedByAi, notifications, refreshTokens, tenants, usageEvents, user) { 
        
        GdprExport.initialize(this, activityLog, apiKeys, billing, exportedAt, generatedByAi, notifications, refreshTokens, tenants, usageEvents, user);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, activityLog, apiKeys, billing, exportedAt, generatedByAi, notifications, refreshTokens, tenants, usageEvents, user) { 
        obj['activityLog'] = activityLog;
        obj['apiKeys'] = apiKeys;
        obj['billing'] = billing;
        obj['exportedAt'] = exportedAt;
        obj['generatedByAi'] = generatedByAi;
        obj['notifications'] = notifications;
        obj['refreshTokens'] = refreshTokens;
        obj['tenants'] = tenants;
        obj['usageEvents'] = usageEvents;
        obj['user'] = user;
    }

    /**
     * Constructs a <code>GdprExport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GdprExport} obj Optional instance to populate.
     * @return {module:model/GdprExport} The populated <code>GdprExport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GdprExport();

            if (data.hasOwnProperty('activityLog')) {
                obj['activityLog'] = ApiClient.convertToType(data['activityLog'], [GdprActivity]);
            }
            if (data.hasOwnProperty('apiKeys')) {
                obj['apiKeys'] = ApiClient.convertToType(data['apiKeys'], [GdprApiKey]);
            }
            if (data.hasOwnProperty('billing')) {
                obj['billing'] = ApiClient.convertToType(data['billing'], [GdprBillingInfo]);
            }
            if (data.hasOwnProperty('exportedAt')) {
                obj['exportedAt'] = ApiClient.convertToType(data['exportedAt'], 'Date');
            }
            if (data.hasOwnProperty('generatedByAi')) {
                obj['generatedByAi'] = ApiClient.convertToType(data['generatedByAi'], 'Boolean');
            }
            if (data.hasOwnProperty('notifications')) {
                obj['notifications'] = ApiClient.convertToType(data['notifications'], [GdprNotification]);
            }
            if (data.hasOwnProperty('refreshTokens')) {
                obj['refreshTokens'] = ApiClient.convertToType(data['refreshTokens'], [GdprRefreshToken]);
            }
            if (data.hasOwnProperty('tenants')) {
                obj['tenants'] = ApiClient.convertToType(data['tenants'], [GdprTenant]);
            }
            if (data.hasOwnProperty('usageEvents')) {
                obj['usageEvents'] = ApiClient.convertToType(data['usageEvents'], [GdprUsageEvent]);
            }
            if (data.hasOwnProperty('user')) {
                obj['user'] = GdprUser.constructFromObject(data['user']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GdprExport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GdprExport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GdprExport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['activityLog']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['activityLog'])) {
                throw new Error("Expected the field `activityLog` to be an array in the JSON data but got " + data['activityLog']);
            }
            // validate the optional field `activityLog` (array)
            for (const item of data['activityLog']) {
                GdprActivity.validateJSON(item);
            };
        }
        if (data['apiKeys']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['apiKeys'])) {
                throw new Error("Expected the field `apiKeys` to be an array in the JSON data but got " + data['apiKeys']);
            }
            // validate the optional field `apiKeys` (array)
            for (const item of data['apiKeys']) {
                GdprApiKey.validateJSON(item);
            };
        }
        if (data['billing']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['billing'])) {
                throw new Error("Expected the field `billing` to be an array in the JSON data but got " + data['billing']);
            }
            // validate the optional field `billing` (array)
            for (const item of data['billing']) {
                GdprBillingInfo.validateJSON(item);
            };
        }
        if (data['notifications']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['notifications'])) {
                throw new Error("Expected the field `notifications` to be an array in the JSON data but got " + data['notifications']);
            }
            // validate the optional field `notifications` (array)
            for (const item of data['notifications']) {
                GdprNotification.validateJSON(item);
            };
        }
        if (data['refreshTokens']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['refreshTokens'])) {
                throw new Error("Expected the field `refreshTokens` to be an array in the JSON data but got " + data['refreshTokens']);
            }
            // validate the optional field `refreshTokens` (array)
            for (const item of data['refreshTokens']) {
                GdprRefreshToken.validateJSON(item);
            };
        }
        if (data['tenants']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['tenants'])) {
                throw new Error("Expected the field `tenants` to be an array in the JSON data but got " + data['tenants']);
            }
            // validate the optional field `tenants` (array)
            for (const item of data['tenants']) {
                GdprTenant.validateJSON(item);
            };
        }
        if (data['usageEvents']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['usageEvents'])) {
                throw new Error("Expected the field `usageEvents` to be an array in the JSON data but got " + data['usageEvents']);
            }
            // validate the optional field `usageEvents` (array)
            for (const item of data['usageEvents']) {
                GdprUsageEvent.validateJSON(item);
            };
        }
        // validate the optional field `user`
        if (data['user']) { // data not null
          GdprUser.validateJSON(data['user']);
        }

        return true;
    }


}

GdprExport.RequiredProperties = ["activityLog", "apiKeys", "billing", "exportedAt", "generatedByAi", "notifications", "refreshTokens", "tenants", "usageEvents", "user"];

/**
 * @member {Array.<module:model/GdprActivity>} activityLog
 */
GdprExport.prototype['activityLog'] = undefined;

/**
 * Key identifiers and names only — never a usable credential.
 * @member {Array.<module:model/GdprApiKey>} apiKeys
 */
GdprExport.prototype['apiKeys'] = undefined;

/**
 * @member {Array.<module:model/GdprBillingInfo>} billing
 */
GdprExport.prototype['billing'] = undefined;

/**
 * @member {Date} exportedAt
 */
GdprExport.prototype['exportedAt'] = undefined;

/**
 * Honesty field: this document is a plain data dump, never AI-generated.
 * @member {Boolean} generatedByAi
 */
GdprExport.prototype['generatedByAi'] = undefined;

/**
 * @member {Array.<module:model/GdprNotification>} notifications
 */
GdprExport.prototype['notifications'] = undefined;

/**
 * Session records: metadata only, never the token hash.
 * @member {Array.<module:model/GdprRefreshToken>} refreshTokens
 */
GdprExport.prototype['refreshTokens'] = undefined;

/**
 * @member {Array.<module:model/GdprTenant>} tenants
 */
GdprExport.prototype['tenants'] = undefined;

/**
 * @member {Array.<module:model/GdprUsageEvent>} usageEvents
 */
GdprExport.prototype['usageEvents'] = undefined;

/**
 * @member {module:model/GdprUser} user
 */
GdprExport.prototype['user'] = undefined;






export default GdprExport;


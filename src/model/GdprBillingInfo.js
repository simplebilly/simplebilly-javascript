/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GdprBillingInfo model module.
 * @module model/GdprBillingInfo
 * @version 0.1.0
 */
class GdprBillingInfo {
    /**
     * Constructs a new <code>GdprBillingInfo</code>.
     * @alias module:model/GdprBillingInfo
     * @param tenantId {String} 
     */
    constructor(tenantId) { 
        
        GdprBillingInfo.initialize(this, tenantId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tenantId) { 
        obj['tenantId'] = tenantId;
    }

    /**
     * Constructs a <code>GdprBillingInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GdprBillingInfo} obj Optional instance to populate.
     * @return {module:model/GdprBillingInfo} The populated <code>GdprBillingInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GdprBillingInfo();

            if (data.hasOwnProperty('currentPeriodEnd')) {
                obj['currentPeriodEnd'] = ApiClient.convertToType(data['currentPeriodEnd'], 'Date');
            }
            if (data.hasOwnProperty('currentPeriodStart')) {
                obj['currentPeriodStart'] = ApiClient.convertToType(data['currentPeriodStart'], 'Date');
            }
            if (data.hasOwnProperty('plan')) {
                obj['plan'] = ApiClient.convertToType(data['plan'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('tenantId')) {
                obj['tenantId'] = ApiClient.convertToType(data['tenantId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GdprBillingInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GdprBillingInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GdprBillingInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['plan'] && !(typeof data['plan'] === 'string' || data['plan'] instanceof String)) {
            throw new Error("Expected the field `plan` to be a primitive type in the JSON string but got " + data['plan']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['tenantId'] && !(typeof data['tenantId'] === 'string' || data['tenantId'] instanceof String)) {
            throw new Error("Expected the field `tenantId` to be a primitive type in the JSON string but got " + data['tenantId']);
        }

        return true;
    }


}

GdprBillingInfo.RequiredProperties = ["tenantId"];

/**
 * @member {Date} currentPeriodEnd
 */
GdprBillingInfo.prototype['currentPeriodEnd'] = undefined;

/**
 * @member {Date} currentPeriodStart
 */
GdprBillingInfo.prototype['currentPeriodStart'] = undefined;

/**
 * @member {String} plan
 */
GdprBillingInfo.prototype['plan'] = undefined;

/**
 * @member {String} status
 */
GdprBillingInfo.prototype['status'] = undefined;

/**
 * @member {String} tenantId
 */
GdprBillingInfo.prototype['tenantId'] = undefined;






export default GdprBillingInfo;


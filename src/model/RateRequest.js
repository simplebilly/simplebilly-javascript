/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Address from './Address';
import CustomerInfo from './CustomerInfo';
import Package from './Package';

/**
 * The RateRequest model module.
 * @module model/RateRequest
 * @version 0.1.0
 */
class RateRequest {
    /**
     * Constructs a new <code>RateRequest</code>.
     * @alias module:model/RateRequest
     * @param packages {Array.<module:model/Package>} 
     * @param recipient {module:model/Address} 
     * @param sender {module:model/Address} 
     */
    constructor(packages, recipient, sender) { 
        
        RateRequest.initialize(this, packages, recipient, sender);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, packages, recipient, sender) { 
        obj['packages'] = packages;
        obj['recipient'] = recipient;
        obj['sender'] = sender;
    }

    /**
     * Constructs a <code>RateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RateRequest} obj Optional instance to populate.
     * @return {module:model/RateRequest} The populated <code>RateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RateRequest();

            if (data.hasOwnProperty('customer')) {
                obj['customer'] = ApiClient.convertToType(data['customer'], CustomerInfo);
            }
            if (data.hasOwnProperty('packages')) {
                obj['packages'] = ApiClient.convertToType(data['packages'], [Package]);
            }
            if (data.hasOwnProperty('recipient')) {
                obj['recipient'] = Address.constructFromObject(data['recipient']);
            }
            if (data.hasOwnProperty('sender')) {
                obj['sender'] = Address.constructFromObject(data['sender']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RateRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `customer`
        if (data['customer']) { // data not null
          CustomerInfo.validateJSON(data['customer']);
        }
        if (data['packages']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['packages'])) {
                throw new Error("Expected the field `packages` to be an array in the JSON data but got " + data['packages']);
            }
            // validate the optional field `packages` (array)
            for (const item of data['packages']) {
                Package.validateJSON(item);
            };
        }
        // validate the optional field `recipient`
        if (data['recipient']) { // data not null
          Address.validateJSON(data['recipient']);
        }
        // validate the optional field `sender`
        if (data['sender']) { // data not null
          Address.validateJSON(data['sender']);
        }

        return true;
    }


}

RateRequest.RequiredProperties = ["packages", "recipient", "sender"];

/**
 * @member {module:model/CustomerInfo} customer
 */
RateRequest.prototype['customer'] = undefined;

/**
 * @member {Array.<module:model/Package>} packages
 */
RateRequest.prototype['packages'] = undefined;

/**
 * @member {module:model/Address} recipient
 */
RateRequest.prototype['recipient'] = undefined;

/**
 * @member {module:model/Address} sender
 */
RateRequest.prototype['sender'] = undefined;






export default RateRequest;


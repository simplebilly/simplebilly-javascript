/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SepaDirectDebitResponse model module.
 * @module model/SepaDirectDebitResponse
 * @version 0.1.0
 */
class SepaDirectDebitResponse {
    /**
     * Constructs a new <code>SepaDirectDebitResponse</code>.
     * @alias module:model/SepaDirectDebitResponse
     * @param contentType {String} 
     * @param filename {String} 
     * @param xmlContent {String} 
     */
    constructor(contentType, filename, xmlContent) { 
        
        SepaDirectDebitResponse.initialize(this, contentType, filename, xmlContent);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contentType, filename, xmlContent) { 
        obj['content_type'] = contentType;
        obj['filename'] = filename;
        obj['xml_content'] = xmlContent;
    }

    /**
     * Constructs a <code>SepaDirectDebitResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SepaDirectDebitResponse} obj Optional instance to populate.
     * @return {module:model/SepaDirectDebitResponse} The populated <code>SepaDirectDebitResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SepaDirectDebitResponse();

            if (data.hasOwnProperty('content_type')) {
                obj['content_type'] = ApiClient.convertToType(data['content_type'], 'String');
            }
            if (data.hasOwnProperty('filename')) {
                obj['filename'] = ApiClient.convertToType(data['filename'], 'String');
            }
            if (data.hasOwnProperty('xml_content')) {
                obj['xml_content'] = ApiClient.convertToType(data['xml_content'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SepaDirectDebitResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SepaDirectDebitResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SepaDirectDebitResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['content_type'] && !(typeof data['content_type'] === 'string' || data['content_type'] instanceof String)) {
            throw new Error("Expected the field `content_type` to be a primitive type in the JSON string but got " + data['content_type']);
        }
        // ensure the json data is a string
        if (data['filename'] && !(typeof data['filename'] === 'string' || data['filename'] instanceof String)) {
            throw new Error("Expected the field `filename` to be a primitive type in the JSON string but got " + data['filename']);
        }
        // ensure the json data is a string
        if (data['xml_content'] && !(typeof data['xml_content'] === 'string' || data['xml_content'] instanceof String)) {
            throw new Error("Expected the field `xml_content` to be a primitive type in the JSON string but got " + data['xml_content']);
        }

        return true;
    }


}

SepaDirectDebitResponse.RequiredProperties = ["content_type", "filename", "xml_content"];

/**
 * @member {String} content_type
 */
SepaDirectDebitResponse.prototype['content_type'] = undefined;

/**
 * @member {String} filename
 */
SepaDirectDebitResponse.prototype['filename'] = undefined;

/**
 * @member {String} xml_content
 */
SepaDirectDebitResponse.prototype['xml_content'] = undefined;






export default SepaDirectDebitResponse;


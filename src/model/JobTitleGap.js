/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The JobTitleGap model module.
 * @module model/JobTitleGap
 * @version 0.1.0
 */
class JobTitleGap {
    /**
     * Constructs a new <code>JobTitleGap</code>.
     * @alias module:model/JobTitleGap
     * @param employeeCount {Number} 
     * @param femaleMeanHourly {String} 
     * @param jobTitle {String} 
     * @param maleMeanHourly {String} 
     * @param meanGapPct {Number} 
     * @param medianGapPct {Number} 
     */
    constructor(employeeCount, femaleMeanHourly, jobTitle, maleMeanHourly, meanGapPct, medianGapPct) { 
        
        JobTitleGap.initialize(this, employeeCount, femaleMeanHourly, jobTitle, maleMeanHourly, meanGapPct, medianGapPct);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, employeeCount, femaleMeanHourly, jobTitle, maleMeanHourly, meanGapPct, medianGapPct) { 
        obj['employee_count'] = employeeCount;
        obj['female_mean_hourly'] = femaleMeanHourly;
        obj['job_title'] = jobTitle;
        obj['male_mean_hourly'] = maleMeanHourly;
        obj['mean_gap_pct'] = meanGapPct;
        obj['median_gap_pct'] = medianGapPct;
    }

    /**
     * Constructs a <code>JobTitleGap</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobTitleGap} obj Optional instance to populate.
     * @return {module:model/JobTitleGap} The populated <code>JobTitleGap</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobTitleGap();

            if (data.hasOwnProperty('employee_count')) {
                obj['employee_count'] = ApiClient.convertToType(data['employee_count'], 'Number');
            }
            if (data.hasOwnProperty('female_mean_hourly')) {
                obj['female_mean_hourly'] = ApiClient.convertToType(data['female_mean_hourly'], 'String');
            }
            if (data.hasOwnProperty('job_title')) {
                obj['job_title'] = ApiClient.convertToType(data['job_title'], 'String');
            }
            if (data.hasOwnProperty('male_mean_hourly')) {
                obj['male_mean_hourly'] = ApiClient.convertToType(data['male_mean_hourly'], 'String');
            }
            if (data.hasOwnProperty('mean_gap_pct')) {
                obj['mean_gap_pct'] = ApiClient.convertToType(data['mean_gap_pct'], 'Number');
            }
            if (data.hasOwnProperty('median_gap_pct')) {
                obj['median_gap_pct'] = ApiClient.convertToType(data['median_gap_pct'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobTitleGap</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobTitleGap</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobTitleGap.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['female_mean_hourly'] && !(typeof data['female_mean_hourly'] === 'string' || data['female_mean_hourly'] instanceof String)) {
            throw new Error("Expected the field `female_mean_hourly` to be a primitive type in the JSON string but got " + data['female_mean_hourly']);
        }
        // ensure the json data is a string
        if (data['job_title'] && !(typeof data['job_title'] === 'string' || data['job_title'] instanceof String)) {
            throw new Error("Expected the field `job_title` to be a primitive type in the JSON string but got " + data['job_title']);
        }
        // ensure the json data is a string
        if (data['male_mean_hourly'] && !(typeof data['male_mean_hourly'] === 'string' || data['male_mean_hourly'] instanceof String)) {
            throw new Error("Expected the field `male_mean_hourly` to be a primitive type in the JSON string but got " + data['male_mean_hourly']);
        }

        return true;
    }


}

JobTitleGap.RequiredProperties = ["employee_count", "female_mean_hourly", "job_title", "male_mean_hourly", "mean_gap_pct", "median_gap_pct"];

/**
 * @member {Number} employee_count
 */
JobTitleGap.prototype['employee_count'] = undefined;

/**
 * @member {String} female_mean_hourly
 */
JobTitleGap.prototype['female_mean_hourly'] = undefined;

/**
 * @member {String} job_title
 */
JobTitleGap.prototype['job_title'] = undefined;

/**
 * @member {String} male_mean_hourly
 */
JobTitleGap.prototype['male_mean_hourly'] = undefined;

/**
 * @member {Number} mean_gap_pct
 */
JobTitleGap.prototype['mean_gap_pct'] = undefined;

/**
 * @member {Number} median_gap_pct
 */
JobTitleGap.prototype['median_gap_pct'] = undefined;






export default JobTitleGap;


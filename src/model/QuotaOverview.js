/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MeteredUsage from './MeteredUsage';
import PlanFeatures from './PlanFeatures';
import PlanLimits from './PlanLimits';
import UsageSnapshot from './UsageSnapshot';

/**
 * The QuotaOverview model module.
 * @module model/QuotaOverview
 * @version 0.1.0
 */
class QuotaOverview {
    /**
     * Constructs a new <code>QuotaOverview</code>.
     * @alias module:model/QuotaOverview
     * @param features {module:model/PlanFeatures} 
     * @param isTrialing {Boolean} 
     * @param limits {module:model/PlanLimits} 
     * @param metered {Array.<module:model/MeteredUsage>} 
     * @param plan {String} 
     * @param planName {String} 
     * @param usage {module:model/UsageSnapshot} 
     */
    constructor(features, isTrialing, limits, metered, plan, planName, usage) { 
        
        QuotaOverview.initialize(this, features, isTrialing, limits, metered, plan, planName, usage);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, features, isTrialing, limits, metered, plan, planName, usage) { 
        obj['features'] = features;
        obj['isTrialing'] = isTrialing;
        obj['limits'] = limits;
        obj['metered'] = metered;
        obj['plan'] = plan;
        obj['planName'] = planName;
        obj['usage'] = usage;
    }

    /**
     * Constructs a <code>QuotaOverview</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QuotaOverview} obj Optional instance to populate.
     * @return {module:model/QuotaOverview} The populated <code>QuotaOverview</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QuotaOverview();

            if (data.hasOwnProperty('features')) {
                obj['features'] = PlanFeatures.constructFromObject(data['features']);
            }
            if (data.hasOwnProperty('isTrialing')) {
                obj['isTrialing'] = ApiClient.convertToType(data['isTrialing'], 'Boolean');
            }
            if (data.hasOwnProperty('limits')) {
                obj['limits'] = PlanLimits.constructFromObject(data['limits']);
            }
            if (data.hasOwnProperty('metered')) {
                obj['metered'] = ApiClient.convertToType(data['metered'], [MeteredUsage]);
            }
            if (data.hasOwnProperty('plan')) {
                obj['plan'] = ApiClient.convertToType(data['plan'], 'String');
            }
            if (data.hasOwnProperty('planName')) {
                obj['planName'] = ApiClient.convertToType(data['planName'], 'String');
            }
            if (data.hasOwnProperty('trialEndsAt')) {
                obj['trialEndsAt'] = ApiClient.convertToType(data['trialEndsAt'], 'Date');
            }
            if (data.hasOwnProperty('usage')) {
                obj['usage'] = UsageSnapshot.constructFromObject(data['usage']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QuotaOverview</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QuotaOverview</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of QuotaOverview.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `features`
        if (data['features']) { // data not null
          PlanFeatures.validateJSON(data['features']);
        }
        // validate the optional field `limits`
        if (data['limits']) { // data not null
          PlanLimits.validateJSON(data['limits']);
        }
        if (data['metered']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['metered'])) {
                throw new Error("Expected the field `metered` to be an array in the JSON data but got " + data['metered']);
            }
            // validate the optional field `metered` (array)
            for (const item of data['metered']) {
                MeteredUsage.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['plan'] && !(typeof data['plan'] === 'string' || data['plan'] instanceof String)) {
            throw new Error("Expected the field `plan` to be a primitive type in the JSON string but got " + data['plan']);
        }
        // ensure the json data is a string
        if (data['planName'] && !(typeof data['planName'] === 'string' || data['planName'] instanceof String)) {
            throw new Error("Expected the field `planName` to be a primitive type in the JSON string but got " + data['planName']);
        }
        // validate the optional field `usage`
        if (data['usage']) { // data not null
          UsageSnapshot.validateJSON(data['usage']);
        }

        return true;
    }


}

QuotaOverview.RequiredProperties = ["features", "isTrialing", "limits", "metered", "plan", "planName", "usage"];

/**
 * @member {module:model/PlanFeatures} features
 */
QuotaOverview.prototype['features'] = undefined;

/**
 * @member {Boolean} isTrialing
 */
QuotaOverview.prototype['isTrialing'] = undefined;

/**
 * @member {module:model/PlanLimits} limits
 */
QuotaOverview.prototype['limits'] = undefined;

/**
 * @member {Array.<module:model/MeteredUsage>} metered
 */
QuotaOverview.prototype['metered'] = undefined;

/**
 * @member {String} plan
 */
QuotaOverview.prototype['plan'] = undefined;

/**
 * @member {String} planName
 */
QuotaOverview.prototype['planName'] = undefined;

/**
 * @member {Date} trialEndsAt
 */
QuotaOverview.prototype['trialEndsAt'] = undefined;

/**
 * @member {module:model/UsageSnapshot} usage
 */
QuotaOverview.prototype['usage'] = undefined;






export default QuotaOverview;


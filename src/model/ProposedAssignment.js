/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProposedAssignment model module.
 * @module model/ProposedAssignment
 * @version 0.1.0
 */
class ProposedAssignment {
    /**
     * Constructs a new <code>ProposedAssignment</code>.
     * @alias module:model/ProposedAssignment
     * @param amountPaid {String} 
     * @param confidence {Number} 
     * @param invoiceId {String} 
     * @param invoiceNumber {String} 
     * @param openAmount {String} 
     * @param paymentDate {String} 
     * @param paymentId {String} 
     * @param reason {String} 
     */
    constructor(amountPaid, confidence, invoiceId, invoiceNumber, openAmount, paymentDate, paymentId, reason) { 
        
        ProposedAssignment.initialize(this, amountPaid, confidence, invoiceId, invoiceNumber, openAmount, paymentDate, paymentId, reason);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amountPaid, confidence, invoiceId, invoiceNumber, openAmount, paymentDate, paymentId, reason) { 
        obj['amount_paid'] = amountPaid;
        obj['confidence'] = confidence;
        obj['invoice_id'] = invoiceId;
        obj['invoice_number'] = invoiceNumber;
        obj['open_amount'] = openAmount;
        obj['payment_date'] = paymentDate;
        obj['payment_id'] = paymentId;
        obj['reason'] = reason;
    }

    /**
     * Constructs a <code>ProposedAssignment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProposedAssignment} obj Optional instance to populate.
     * @return {module:model/ProposedAssignment} The populated <code>ProposedAssignment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProposedAssignment();

            if (data.hasOwnProperty('amount_paid')) {
                obj['amount_paid'] = ApiClient.convertToType(data['amount_paid'], 'String');
            }
            if (data.hasOwnProperty('confidence')) {
                obj['confidence'] = ApiClient.convertToType(data['confidence'], 'Number');
            }
            if (data.hasOwnProperty('customer_id')) {
                obj['customer_id'] = ApiClient.convertToType(data['customer_id'], 'String');
            }
            if (data.hasOwnProperty('invoice_id')) {
                obj['invoice_id'] = ApiClient.convertToType(data['invoice_id'], 'String');
            }
            if (data.hasOwnProperty('invoice_number')) {
                obj['invoice_number'] = ApiClient.convertToType(data['invoice_number'], 'String');
            }
            if (data.hasOwnProperty('open_amount')) {
                obj['open_amount'] = ApiClient.convertToType(data['open_amount'], 'String');
            }
            if (data.hasOwnProperty('payment_date')) {
                obj['payment_date'] = ApiClient.convertToType(data['payment_date'], 'String');
            }
            if (data.hasOwnProperty('payment_id')) {
                obj['payment_id'] = ApiClient.convertToType(data['payment_id'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('reference')) {
                obj['reference'] = ApiClient.convertToType(data['reference'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProposedAssignment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProposedAssignment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProposedAssignment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount_paid'] && !(typeof data['amount_paid'] === 'string' || data['amount_paid'] instanceof String)) {
            throw new Error("Expected the field `amount_paid` to be a primitive type in the JSON string but got " + data['amount_paid']);
        }
        // ensure the json data is a string
        if (data['customer_id'] && !(typeof data['customer_id'] === 'string' || data['customer_id'] instanceof String)) {
            throw new Error("Expected the field `customer_id` to be a primitive type in the JSON string but got " + data['customer_id']);
        }
        // ensure the json data is a string
        if (data['invoice_id'] && !(typeof data['invoice_id'] === 'string' || data['invoice_id'] instanceof String)) {
            throw new Error("Expected the field `invoice_id` to be a primitive type in the JSON string but got " + data['invoice_id']);
        }
        // ensure the json data is a string
        if (data['invoice_number'] && !(typeof data['invoice_number'] === 'string' || data['invoice_number'] instanceof String)) {
            throw new Error("Expected the field `invoice_number` to be a primitive type in the JSON string but got " + data['invoice_number']);
        }
        // ensure the json data is a string
        if (data['open_amount'] && !(typeof data['open_amount'] === 'string' || data['open_amount'] instanceof String)) {
            throw new Error("Expected the field `open_amount` to be a primitive type in the JSON string but got " + data['open_amount']);
        }
        // ensure the json data is a string
        if (data['payment_date'] && !(typeof data['payment_date'] === 'string' || data['payment_date'] instanceof String)) {
            throw new Error("Expected the field `payment_date` to be a primitive type in the JSON string but got " + data['payment_date']);
        }
        // ensure the json data is a string
        if (data['payment_id'] && !(typeof data['payment_id'] === 'string' || data['payment_id'] instanceof String)) {
            throw new Error("Expected the field `payment_id` to be a primitive type in the JSON string but got " + data['payment_id']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['reference'] && !(typeof data['reference'] === 'string' || data['reference'] instanceof String)) {
            throw new Error("Expected the field `reference` to be a primitive type in the JSON string but got " + data['reference']);
        }

        return true;
    }


}

ProposedAssignment.RequiredProperties = ["amount_paid", "confidence", "invoice_id", "invoice_number", "open_amount", "payment_date", "payment_id", "reason"];

/**
 * @member {String} amount_paid
 */
ProposedAssignment.prototype['amount_paid'] = undefined;

/**
 * @member {Number} confidence
 */
ProposedAssignment.prototype['confidence'] = undefined;

/**
 * @member {String} customer_id
 */
ProposedAssignment.prototype['customer_id'] = undefined;

/**
 * @member {String} invoice_id
 */
ProposedAssignment.prototype['invoice_id'] = undefined;

/**
 * @member {String} invoice_number
 */
ProposedAssignment.prototype['invoice_number'] = undefined;

/**
 * @member {String} open_amount
 */
ProposedAssignment.prototype['open_amount'] = undefined;

/**
 * @member {String} payment_date
 */
ProposedAssignment.prototype['payment_date'] = undefined;

/**
 * @member {String} payment_id
 */
ProposedAssignment.prototype['payment_id'] = undefined;

/**
 * @member {String} reason
 */
ProposedAssignment.prototype['reason'] = undefined;

/**
 * @member {String} reference
 */
ProposedAssignment.prototype['reference'] = undefined;






export default ProposedAssignment;


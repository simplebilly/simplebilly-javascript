/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OssDependency from './OssDependency';

/**
 * The OssReport model module.
 * @module model/OssReport
 * @version 0.1.0
 */
class OssReport {
    /**
     * Constructs a new <code>OssReport</code>.
     * @alias module:model/OssReport
     * @param dependencies {Array.<module:model/OssDependency>} 
     * @param totalCount {Number} 
     */
    constructor(dependencies, totalCount) { 
        
        OssReport.initialize(this, dependencies, totalCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, dependencies, totalCount) { 
        obj['dependencies'] = dependencies;
        obj['total_count'] = totalCount;
    }

    /**
     * Constructs a <code>OssReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OssReport} obj Optional instance to populate.
     * @return {module:model/OssReport} The populated <code>OssReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OssReport();

            if (data.hasOwnProperty('dependencies')) {
                obj['dependencies'] = ApiClient.convertToType(data['dependencies'], [OssDependency]);
            }
            if (data.hasOwnProperty('total_count')) {
                obj['total_count'] = ApiClient.convertToType(data['total_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OssReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OssReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OssReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['dependencies']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dependencies'])) {
                throw new Error("Expected the field `dependencies` to be an array in the JSON data but got " + data['dependencies']);
            }
            // validate the optional field `dependencies` (array)
            for (const item of data['dependencies']) {
                OssDependency.validateJSON(item);
            };
        }

        return true;
    }


}

OssReport.RequiredProperties = ["dependencies", "total_count"];

/**
 * @member {Array.<module:model/OssDependency>} dependencies
 */
OssReport.prototype['dependencies'] = undefined;

/**
 * @member {Number} total_count
 */
OssReport.prototype['total_count'] = undefined;






export default OssReport;


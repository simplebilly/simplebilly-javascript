/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BalanceItem from './BalanceItem';

/**
 * The BalanceSheet model module.
 * @module model/BalanceSheet
 * @version 0.1.0
 */
class BalanceSheet {
    /**
     * Constructs a new <code>BalanceSheet</code>.
     * @alias module:model/BalanceSheet
     * @param assets {Array.<module:model/BalanceItem>} 
     * @param balanced {Boolean} 
     * @param equityLiabilities {Array.<module:model/BalanceItem>} 
     * @param totalAssets {String} 
     * @param totalEquityLiabilities {String} 
     */
    constructor(assets, balanced, equityLiabilities, totalAssets, totalEquityLiabilities) { 
        
        BalanceSheet.initialize(this, assets, balanced, equityLiabilities, totalAssets, totalEquityLiabilities);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, assets, balanced, equityLiabilities, totalAssets, totalEquityLiabilities) { 
        obj['assets'] = assets;
        obj['balanced'] = balanced;
        obj['equity_liabilities'] = equityLiabilities;
        obj['total_assets'] = totalAssets;
        obj['total_equity_liabilities'] = totalEquityLiabilities;
    }

    /**
     * Constructs a <code>BalanceSheet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BalanceSheet} obj Optional instance to populate.
     * @return {module:model/BalanceSheet} The populated <code>BalanceSheet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BalanceSheet();

            if (data.hasOwnProperty('assets')) {
                obj['assets'] = ApiClient.convertToType(data['assets'], [BalanceItem]);
            }
            if (data.hasOwnProperty('balanced')) {
                obj['balanced'] = ApiClient.convertToType(data['balanced'], 'Boolean');
            }
            if (data.hasOwnProperty('equity_liabilities')) {
                obj['equity_liabilities'] = ApiClient.convertToType(data['equity_liabilities'], [BalanceItem]);
            }
            if (data.hasOwnProperty('total_assets')) {
                obj['total_assets'] = ApiClient.convertToType(data['total_assets'], 'String');
            }
            if (data.hasOwnProperty('total_equity_liabilities')) {
                obj['total_equity_liabilities'] = ApiClient.convertToType(data['total_equity_liabilities'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BalanceSheet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BalanceSheet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BalanceSheet.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['assets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assets'])) {
                throw new Error("Expected the field `assets` to be an array in the JSON data but got " + data['assets']);
            }
            // validate the optional field `assets` (array)
            for (const item of data['assets']) {
                BalanceItem.validateJSON(item);
            };
        }
        if (data['equity_liabilities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['equity_liabilities'])) {
                throw new Error("Expected the field `equity_liabilities` to be an array in the JSON data but got " + data['equity_liabilities']);
            }
            // validate the optional field `equity_liabilities` (array)
            for (const item of data['equity_liabilities']) {
                BalanceItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_assets'] && !(typeof data['total_assets'] === 'string' || data['total_assets'] instanceof String)) {
            throw new Error("Expected the field `total_assets` to be a primitive type in the JSON string but got " + data['total_assets']);
        }
        // ensure the json data is a string
        if (data['total_equity_liabilities'] && !(typeof data['total_equity_liabilities'] === 'string' || data['total_equity_liabilities'] instanceof String)) {
            throw new Error("Expected the field `total_equity_liabilities` to be a primitive type in the JSON string but got " + data['total_equity_liabilities']);
        }

        return true;
    }


}

BalanceSheet.RequiredProperties = ["assets", "balanced", "equity_liabilities", "total_assets", "total_equity_liabilities"];

/**
 * @member {Array.<module:model/BalanceItem>} assets
 */
BalanceSheet.prototype['assets'] = undefined;

/**
 * @member {Boolean} balanced
 */
BalanceSheet.prototype['balanced'] = undefined;

/**
 * @member {Array.<module:model/BalanceItem>} equity_liabilities
 */
BalanceSheet.prototype['equity_liabilities'] = undefined;

/**
 * @member {String} total_assets
 */
BalanceSheet.prototype['total_assets'] = undefined;

/**
 * @member {String} total_equity_liabilities
 */
BalanceSheet.prototype['total_equity_liabilities'] = undefined;






export default BalanceSheet;


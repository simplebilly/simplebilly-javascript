/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PublicDeliveryAppointmentRequest model module.
 * @module model/PublicDeliveryAppointmentRequest
 * @version 0.1.0
 */
class PublicDeliveryAppointmentRequest {
    /**
     * Constructs a new <code>PublicDeliveryAppointmentRequest</code>.
     * @alias module:model/PublicDeliveryAppointmentRequest
     * @param email {String} 
     * @param requestedDate {Date} 
     * @param supplierName {String} 
     * @param warehouseCode {String} Warehouse `code` — the supplier does not know the warehouse uuid.
     */
    constructor(email, requestedDate, supplierName, warehouseCode) { 
        
        PublicDeliveryAppointmentRequest.initialize(this, email, requestedDate, supplierName, warehouseCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, requestedDate, supplierName, warehouseCode) { 
        obj['email'] = email;
        obj['requestedDate'] = requestedDate;
        obj['supplierName'] = supplierName;
        obj['warehouseCode'] = warehouseCode;
    }

    /**
     * Constructs a <code>PublicDeliveryAppointmentRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PublicDeliveryAppointmentRequest} obj Optional instance to populate.
     * @return {module:model/PublicDeliveryAppointmentRequest} The populated <code>PublicDeliveryAppointmentRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PublicDeliveryAppointmentRequest();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('requestedDate')) {
                obj['requestedDate'] = ApiClient.convertToType(data['requestedDate'], 'Date');
            }
            if (data.hasOwnProperty('supplierName')) {
                obj['supplierName'] = ApiClient.convertToType(data['supplierName'], 'String');
            }
            if (data.hasOwnProperty('timeSlot')) {
                obj['timeSlot'] = ApiClient.convertToType(data['timeSlot'], 'String');
            }
            if (data.hasOwnProperty('warehouseCode')) {
                obj['warehouseCode'] = ApiClient.convertToType(data['warehouseCode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PublicDeliveryAppointmentRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PublicDeliveryAppointmentRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PublicDeliveryAppointmentRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['supplierName'] && !(typeof data['supplierName'] === 'string' || data['supplierName'] instanceof String)) {
            throw new Error("Expected the field `supplierName` to be a primitive type in the JSON string but got " + data['supplierName']);
        }
        // ensure the json data is a string
        if (data['timeSlot'] && !(typeof data['timeSlot'] === 'string' || data['timeSlot'] instanceof String)) {
            throw new Error("Expected the field `timeSlot` to be a primitive type in the JSON string but got " + data['timeSlot']);
        }
        // ensure the json data is a string
        if (data['warehouseCode'] && !(typeof data['warehouseCode'] === 'string' || data['warehouseCode'] instanceof String)) {
            throw new Error("Expected the field `warehouseCode` to be a primitive type in the JSON string but got " + data['warehouseCode']);
        }

        return true;
    }


}

PublicDeliveryAppointmentRequest.RequiredProperties = ["email", "requestedDate", "supplierName", "warehouseCode"];

/**
 * @member {String} email
 */
PublicDeliveryAppointmentRequest.prototype['email'] = undefined;

/**
 * @member {String} notes
 */
PublicDeliveryAppointmentRequest.prototype['notes'] = undefined;

/**
 * @member {Date} requestedDate
 */
PublicDeliveryAppointmentRequest.prototype['requestedDate'] = undefined;

/**
 * @member {String} supplierName
 */
PublicDeliveryAppointmentRequest.prototype['supplierName'] = undefined;

/**
 * @member {String} timeSlot
 */
PublicDeliveryAppointmentRequest.prototype['timeSlot'] = undefined;

/**
 * Warehouse `code` — the supplier does not know the warehouse uuid.
 * @member {String} warehouseCode
 */
PublicDeliveryAppointmentRequest.prototype['warehouseCode'] = undefined;






export default PublicDeliveryAppointmentRequest;


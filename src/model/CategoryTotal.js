/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CategoryTotal model module.
 * @module model/CategoryTotal
 * @version 0.1.0
 */
class CategoryTotal {
    /**
     * Constructs a new <code>CategoryTotal</code>.
     * @alias module:model/CategoryTotal
     * @param categoryId {String} 
     * @param sharePct {Number} 
     * @param tco2e {String} 
     */
    constructor(categoryId, sharePct, tco2e) { 
        
        CategoryTotal.initialize(this, categoryId, sharePct, tco2e);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryId, sharePct, tco2e) { 
        obj['category_id'] = categoryId;
        obj['share_pct'] = sharePct;
        obj['tco2e'] = tco2e;
    }

    /**
     * Constructs a <code>CategoryTotal</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CategoryTotal} obj Optional instance to populate.
     * @return {module:model/CategoryTotal} The populated <code>CategoryTotal</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CategoryTotal();

            if (data.hasOwnProperty('category_id')) {
                obj['category_id'] = ApiClient.convertToType(data['category_id'], 'String');
            }
            if (data.hasOwnProperty('share_pct')) {
                obj['share_pct'] = ApiClient.convertToType(data['share_pct'], 'Number');
            }
            if (data.hasOwnProperty('tco2e')) {
                obj['tco2e'] = ApiClient.convertToType(data['tco2e'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CategoryTotal</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CategoryTotal</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CategoryTotal.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category_id'] && !(typeof data['category_id'] === 'string' || data['category_id'] instanceof String)) {
            throw new Error("Expected the field `category_id` to be a primitive type in the JSON string but got " + data['category_id']);
        }
        // ensure the json data is a string
        if (data['tco2e'] && !(typeof data['tco2e'] === 'string' || data['tco2e'] instanceof String)) {
            throw new Error("Expected the field `tco2e` to be a primitive type in the JSON string but got " + data['tco2e']);
        }

        return true;
    }


}

CategoryTotal.RequiredProperties = ["category_id", "share_pct", "tco2e"];

/**
 * @member {String} category_id
 */
CategoryTotal.prototype['category_id'] = undefined;

/**
 * @member {Number} share_pct
 */
CategoryTotal.prototype['share_pct'] = undefined;

/**
 * @member {String} tco2e
 */
CategoryTotal.prototype['tco2e'] = undefined;






export default CategoryTotal;


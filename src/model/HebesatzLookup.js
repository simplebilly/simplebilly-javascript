/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The HebesatzLookup model module.
 * @module model/HebesatzLookup
 * @version 0.1.0
 */
class HebesatzLookup {
    /**
     * Constructs a new <code>HebesatzLookup</code>.
     * @alias module:model/HebesatzLookup
     * @param bundesland {String} 
     * @param countryCode {String} 
     * @param gemeindeName {String} 
     * @param gemeindeschluessel {String} 
     * @param hebesatzGewerbesteuer {Number} 
     * @param jahr {Number} 
     * @param validFrom {String} 
     */
    constructor(bundesland, countryCode, gemeindeName, gemeindeschluessel, hebesatzGewerbesteuer, jahr, validFrom) { 
        
        HebesatzLookup.initialize(this, bundesland, countryCode, gemeindeName, gemeindeschluessel, hebesatzGewerbesteuer, jahr, validFrom);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bundesland, countryCode, gemeindeName, gemeindeschluessel, hebesatzGewerbesteuer, jahr, validFrom) { 
        obj['bundesland'] = bundesland;
        obj['country_code'] = countryCode;
        obj['gemeinde_name'] = gemeindeName;
        obj['gemeindeschluessel'] = gemeindeschluessel;
        obj['hebesatz_gewerbesteuer'] = hebesatzGewerbesteuer;
        obj['jahr'] = jahr;
        obj['valid_from'] = validFrom;
    }

    /**
     * Constructs a <code>HebesatzLookup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HebesatzLookup} obj Optional instance to populate.
     * @return {module:model/HebesatzLookup} The populated <code>HebesatzLookup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HebesatzLookup();

            if (data.hasOwnProperty('bundesland')) {
                obj['bundesland'] = ApiClient.convertToType(data['bundesland'], 'String');
            }
            if (data.hasOwnProperty('country_code')) {
                obj['country_code'] = ApiClient.convertToType(data['country_code'], 'String');
            }
            if (data.hasOwnProperty('gemeinde_name')) {
                obj['gemeinde_name'] = ApiClient.convertToType(data['gemeinde_name'], 'String');
            }
            if (data.hasOwnProperty('gemeindeschluessel')) {
                obj['gemeindeschluessel'] = ApiClient.convertToType(data['gemeindeschluessel'], 'String');
            }
            if (data.hasOwnProperty('hebesatz_gewerbesteuer')) {
                obj['hebesatz_gewerbesteuer'] = ApiClient.convertToType(data['hebesatz_gewerbesteuer'], 'Number');
            }
            if (data.hasOwnProperty('hebesatz_grundsteuer_b')) {
                obj['hebesatz_grundsteuer_b'] = ApiClient.convertToType(data['hebesatz_grundsteuer_b'], 'Number');
            }
            if (data.hasOwnProperty('jahr')) {
                obj['jahr'] = ApiClient.convertToType(data['jahr'], 'Number');
            }
            if (data.hasOwnProperty('landkreis')) {
                obj['landkreis'] = ApiClient.convertToType(data['landkreis'], 'String');
            }
            if (data.hasOwnProperty('valid_from')) {
                obj['valid_from'] = ApiClient.convertToType(data['valid_from'], 'String');
            }
            if (data.hasOwnProperty('valid_to')) {
                obj['valid_to'] = ApiClient.convertToType(data['valid_to'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HebesatzLookup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HebesatzLookup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HebesatzLookup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bundesland'] && !(typeof data['bundesland'] === 'string' || data['bundesland'] instanceof String)) {
            throw new Error("Expected the field `bundesland` to be a primitive type in the JSON string but got " + data['bundesland']);
        }
        // ensure the json data is a string
        if (data['country_code'] && !(typeof data['country_code'] === 'string' || data['country_code'] instanceof String)) {
            throw new Error("Expected the field `country_code` to be a primitive type in the JSON string but got " + data['country_code']);
        }
        // ensure the json data is a string
        if (data['gemeinde_name'] && !(typeof data['gemeinde_name'] === 'string' || data['gemeinde_name'] instanceof String)) {
            throw new Error("Expected the field `gemeinde_name` to be a primitive type in the JSON string but got " + data['gemeinde_name']);
        }
        // ensure the json data is a string
        if (data['gemeindeschluessel'] && !(typeof data['gemeindeschluessel'] === 'string' || data['gemeindeschluessel'] instanceof String)) {
            throw new Error("Expected the field `gemeindeschluessel` to be a primitive type in the JSON string but got " + data['gemeindeschluessel']);
        }
        // ensure the json data is a string
        if (data['landkreis'] && !(typeof data['landkreis'] === 'string' || data['landkreis'] instanceof String)) {
            throw new Error("Expected the field `landkreis` to be a primitive type in the JSON string but got " + data['landkreis']);
        }
        // ensure the json data is a string
        if (data['valid_from'] && !(typeof data['valid_from'] === 'string' || data['valid_from'] instanceof String)) {
            throw new Error("Expected the field `valid_from` to be a primitive type in the JSON string but got " + data['valid_from']);
        }
        // ensure the json data is a string
        if (data['valid_to'] && !(typeof data['valid_to'] === 'string' || data['valid_to'] instanceof String)) {
            throw new Error("Expected the field `valid_to` to be a primitive type in the JSON string but got " + data['valid_to']);
        }

        return true;
    }


}

HebesatzLookup.RequiredProperties = ["bundesland", "country_code", "gemeinde_name", "gemeindeschluessel", "hebesatz_gewerbesteuer", "jahr", "valid_from"];

/**
 * @member {String} bundesland
 */
HebesatzLookup.prototype['bundesland'] = undefined;

/**
 * @member {String} country_code
 */
HebesatzLookup.prototype['country_code'] = undefined;

/**
 * @member {String} gemeinde_name
 */
HebesatzLookup.prototype['gemeinde_name'] = undefined;

/**
 * @member {String} gemeindeschluessel
 */
HebesatzLookup.prototype['gemeindeschluessel'] = undefined;

/**
 * @member {Number} hebesatz_gewerbesteuer
 */
HebesatzLookup.prototype['hebesatz_gewerbesteuer'] = undefined;

/**
 * @member {Number} hebesatz_grundsteuer_b
 */
HebesatzLookup.prototype['hebesatz_grundsteuer_b'] = undefined;

/**
 * @member {Number} jahr
 */
HebesatzLookup.prototype['jahr'] = undefined;

/**
 * @member {String} landkreis
 */
HebesatzLookup.prototype['landkreis'] = undefined;

/**
 * @member {String} valid_from
 */
HebesatzLookup.prototype['valid_from'] = undefined;

/**
 * @member {String} valid_to
 */
HebesatzLookup.prototype['valid_to'] = undefined;






export default HebesatzLookup;


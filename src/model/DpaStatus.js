/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DpaStatus model module.
 * @module model/DpaStatus
 * @version 0.1.0
 */
class DpaStatus {
    /**
     * Constructs a new <code>DpaStatus</code>.
     * DPA acceptance status of the tenant.
     * @alias module:model/DpaStatus
     * @param accepted {Boolean} 
     */
    constructor(accepted) { 
        
        DpaStatus.initialize(this, accepted);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accepted) { 
        obj['accepted'] = accepted;
    }

    /**
     * Constructs a <code>DpaStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DpaStatus} obj Optional instance to populate.
     * @return {module:model/DpaStatus} The populated <code>DpaStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DpaStatus();

            if (data.hasOwnProperty('accepted')) {
                obj['accepted'] = ApiClient.convertToType(data['accepted'], 'Boolean');
            }
            if (data.hasOwnProperty('acceptedAt')) {
                obj['acceptedAt'] = ApiClient.convertToType(data['acceptedAt'], 'String');
            }
            if (data.hasOwnProperty('acceptedBy')) {
                obj['acceptedBy'] = ApiClient.convertToType(data['acceptedBy'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DpaStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DpaStatus</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DpaStatus.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['acceptedAt'] && !(typeof data['acceptedAt'] === 'string' || data['acceptedAt'] instanceof String)) {
            throw new Error("Expected the field `acceptedAt` to be a primitive type in the JSON string but got " + data['acceptedAt']);
        }
        // ensure the json data is a string
        if (data['acceptedBy'] && !(typeof data['acceptedBy'] === 'string' || data['acceptedBy'] instanceof String)) {
            throw new Error("Expected the field `acceptedBy` to be a primitive type in the JSON string but got " + data['acceptedBy']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }

        return true;
    }


}

DpaStatus.RequiredProperties = ["accepted"];

/**
 * @member {Boolean} accepted
 */
DpaStatus.prototype['accepted'] = undefined;

/**
 * @member {String} acceptedAt
 */
DpaStatus.prototype['acceptedAt'] = undefined;

/**
 * @member {String} acceptedBy
 */
DpaStatus.prototype['acceptedBy'] = undefined;

/**
 * @member {String} version
 */
DpaStatus.prototype['version'] = undefined;






export default DpaStatus;


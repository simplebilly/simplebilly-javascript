/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BoxFit from './BoxFit';
import MethodSuitability from './MethodSuitability';

/**
 * The SuitabilityResult model module.
 * @module model/SuitabilityResult
 * @version 0.1.0
 */
class SuitabilityResult {
    /**
     * Constructs a new <code>SuitabilityResult</code>.
     * @alias module:model/SuitabilityResult
     * @param methods {Array.<module:model/MethodSuitability>} 
     * @param requiresInsurance {Boolean} 
     * @param totalValue {String} 
     * @param totalWeightKg {Number} 
     */
    constructor(methods, requiresInsurance, totalValue, totalWeightKg) { 
        
        SuitabilityResult.initialize(this, methods, requiresInsurance, totalValue, totalWeightKg);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, methods, requiresInsurance, totalValue, totalWeightKg) { 
        obj['methods'] = methods;
        obj['requires_insurance'] = requiresInsurance;
        obj['total_value'] = totalValue;
        obj['total_weight_kg'] = totalWeightKg;
    }

    /**
     * Constructs a <code>SuitabilityResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SuitabilityResult} obj Optional instance to populate.
     * @return {module:model/SuitabilityResult} The populated <code>SuitabilityResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SuitabilityResult();

            if (data.hasOwnProperty('methods')) {
                obj['methods'] = ApiClient.convertToType(data['methods'], [MethodSuitability]);
            }
            if (data.hasOwnProperty('recommended_box')) {
                obj['recommended_box'] = ApiClient.convertToType(data['recommended_box'], BoxFit);
            }
            if (data.hasOwnProperty('requires_insurance')) {
                obj['requires_insurance'] = ApiClient.convertToType(data['requires_insurance'], 'Boolean');
            }
            if (data.hasOwnProperty('total_value')) {
                obj['total_value'] = ApiClient.convertToType(data['total_value'], 'String');
            }
            if (data.hasOwnProperty('total_weight_kg')) {
                obj['total_weight_kg'] = ApiClient.convertToType(data['total_weight_kg'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SuitabilityResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SuitabilityResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SuitabilityResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['methods']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['methods'])) {
                throw new Error("Expected the field `methods` to be an array in the JSON data but got " + data['methods']);
            }
            // validate the optional field `methods` (array)
            for (const item of data['methods']) {
                MethodSuitability.validateJSON(item);
            };
        }
        // validate the optional field `recommended_box`
        if (data['recommended_box']) { // data not null
          BoxFit.validateJSON(data['recommended_box']);
        }
        // ensure the json data is a string
        if (data['total_value'] && !(typeof data['total_value'] === 'string' || data['total_value'] instanceof String)) {
            throw new Error("Expected the field `total_value` to be a primitive type in the JSON string but got " + data['total_value']);
        }

        return true;
    }


}

SuitabilityResult.RequiredProperties = ["methods", "requires_insurance", "total_value", "total_weight_kg"];

/**
 * @member {Array.<module:model/MethodSuitability>} methods
 */
SuitabilityResult.prototype['methods'] = undefined;

/**
 * @member {module:model/BoxFit} recommended_box
 */
SuitabilityResult.prototype['recommended_box'] = undefined;

/**
 * @member {Boolean} requires_insurance
 */
SuitabilityResult.prototype['requires_insurance'] = undefined;

/**
 * @member {String} total_value
 */
SuitabilityResult.prototype['total_value'] = undefined;

/**
 * @member {Number} total_weight_kg
 */
SuitabilityResult.prototype['total_weight_kg'] = undefined;






export default SuitabilityResult;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobTitleGap from './JobTitleGap';
import QuartileBand from './QuartileBand';

/**
 * The PayGapReport model module.
 * @module model/PayGapReport
 * @version 0.1.0
 */
class PayGapReport {
    /**
     * Constructs a new <code>PayGapReport</code>.
     * @alias module:model/PayGapReport
     * @param byJobTitle {Array.<module:model/JobTitleGap>} 
     * @param diverseCount {Number} 
     * @param employeeCount {Number} 
     * @param femaleCount {Number} 
     * @param maleCount {Number} 
     * @param meanGapPct {Number} 
     * @param medianGapPct {Number} 
     * @param quartiles {Array.<module:model/QuartileBand>} 
     */
    constructor(byJobTitle, diverseCount, employeeCount, femaleCount, maleCount, meanGapPct, medianGapPct, quartiles) { 
        
        PayGapReport.initialize(this, byJobTitle, diverseCount, employeeCount, femaleCount, maleCount, meanGapPct, medianGapPct, quartiles);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, byJobTitle, diverseCount, employeeCount, femaleCount, maleCount, meanGapPct, medianGapPct, quartiles) { 
        obj['by_job_title'] = byJobTitle;
        obj['diverse_count'] = diverseCount;
        obj['employee_count'] = employeeCount;
        obj['female_count'] = femaleCount;
        obj['male_count'] = maleCount;
        obj['mean_gap_pct'] = meanGapPct;
        obj['median_gap_pct'] = medianGapPct;
        obj['quartiles'] = quartiles;
    }

    /**
     * Constructs a <code>PayGapReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayGapReport} obj Optional instance to populate.
     * @return {module:model/PayGapReport} The populated <code>PayGapReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayGapReport();

            if (data.hasOwnProperty('by_job_title')) {
                obj['by_job_title'] = ApiClient.convertToType(data['by_job_title'], [JobTitleGap]);
            }
            if (data.hasOwnProperty('diverse_count')) {
                obj['diverse_count'] = ApiClient.convertToType(data['diverse_count'], 'Number');
            }
            if (data.hasOwnProperty('employee_count')) {
                obj['employee_count'] = ApiClient.convertToType(data['employee_count'], 'Number');
            }
            if (data.hasOwnProperty('female_count')) {
                obj['female_count'] = ApiClient.convertToType(data['female_count'], 'Number');
            }
            if (data.hasOwnProperty('male_count')) {
                obj['male_count'] = ApiClient.convertToType(data['male_count'], 'Number');
            }
            if (data.hasOwnProperty('mean_gap_pct')) {
                obj['mean_gap_pct'] = ApiClient.convertToType(data['mean_gap_pct'], 'Number');
            }
            if (data.hasOwnProperty('median_gap_pct')) {
                obj['median_gap_pct'] = ApiClient.convertToType(data['median_gap_pct'], 'Number');
            }
            if (data.hasOwnProperty('quartiles')) {
                obj['quartiles'] = ApiClient.convertToType(data['quartiles'], [QuartileBand]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayGapReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayGapReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayGapReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['by_job_title']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['by_job_title'])) {
                throw new Error("Expected the field `by_job_title` to be an array in the JSON data but got " + data['by_job_title']);
            }
            // validate the optional field `by_job_title` (array)
            for (const item of data['by_job_title']) {
                JobTitleGap.validateJSON(item);
            };
        }
        if (data['quartiles']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['quartiles'])) {
                throw new Error("Expected the field `quartiles` to be an array in the JSON data but got " + data['quartiles']);
            }
            // validate the optional field `quartiles` (array)
            for (const item of data['quartiles']) {
                QuartileBand.validateJSON(item);
            };
        }

        return true;
    }


}

PayGapReport.RequiredProperties = ["by_job_title", "diverse_count", "employee_count", "female_count", "male_count", "mean_gap_pct", "median_gap_pct", "quartiles"];

/**
 * @member {Array.<module:model/JobTitleGap>} by_job_title
 */
PayGapReport.prototype['by_job_title'] = undefined;

/**
 * @member {Number} diverse_count
 */
PayGapReport.prototype['diverse_count'] = undefined;

/**
 * @member {Number} employee_count
 */
PayGapReport.prototype['employee_count'] = undefined;

/**
 * @member {Number} female_count
 */
PayGapReport.prototype['female_count'] = undefined;

/**
 * @member {Number} male_count
 */
PayGapReport.prototype['male_count'] = undefined;

/**
 * @member {Number} mean_gap_pct
 */
PayGapReport.prototype['mean_gap_pct'] = undefined;

/**
 * @member {Number} median_gap_pct
 */
PayGapReport.prototype['median_gap_pct'] = undefined;

/**
 * @member {Array.<module:model/QuartileBand>} quartiles
 */
PayGapReport.prototype['quartiles'] = undefined;






export default PayGapReport;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TimelineEvent from './TimelineEvent';

/**
 * The ContactTimelineResponse model module.
 * @module model/ContactTimelineResponse
 * @version 0.1.0
 */
class ContactTimelineResponse {
    /**
     * Constructs a new <code>ContactTimelineResponse</code>.
     * Aggregated per-contact timeline.
     * @alias module:model/ContactTimelineResponse
     * @param contactId {String} 
     * @param events {Array.<module:model/TimelineEvent>} 
     */
    constructor(contactId, events) { 
        
        ContactTimelineResponse.initialize(this, contactId, events);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contactId, events) { 
        obj['contactId'] = contactId;
        obj['events'] = events;
    }

    /**
     * Constructs a <code>ContactTimelineResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ContactTimelineResponse} obj Optional instance to populate.
     * @return {module:model/ContactTimelineResponse} The populated <code>ContactTimelineResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ContactTimelineResponse();

            if (data.hasOwnProperty('contactId')) {
                obj['contactId'] = ApiClient.convertToType(data['contactId'], 'String');
            }
            if (data.hasOwnProperty('events')) {
                obj['events'] = ApiClient.convertToType(data['events'], [TimelineEvent]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ContactTimelineResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ContactTimelineResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ContactTimelineResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['contactId'] && !(typeof data['contactId'] === 'string' || data['contactId'] instanceof String)) {
            throw new Error("Expected the field `contactId` to be a primitive type in the JSON string but got " + data['contactId']);
        }
        if (data['events']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['events'])) {
                throw new Error("Expected the field `events` to be an array in the JSON data but got " + data['events']);
            }
            // validate the optional field `events` (array)
            for (const item of data['events']) {
                TimelineEvent.validateJSON(item);
            };
        }

        return true;
    }


}

ContactTimelineResponse.RequiredProperties = ["contactId", "events"];

/**
 * @member {String} contactId
 */
ContactTimelineResponse.prototype['contactId'] = undefined;

/**
 * @member {Array.<module:model/TimelineEvent>} events
 */
ContactTimelineResponse.prototype['events'] = undefined;






export default ContactTimelineResponse;


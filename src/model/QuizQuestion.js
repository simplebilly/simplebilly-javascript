/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The QuizQuestion model module.
 * @module model/QuizQuestion
 * @version 0.1.0
 */
class QuizQuestion {
    /**
     * Constructs a new <code>QuizQuestion</code>.
     * @alias module:model/QuizQuestion
     * @param id {String} 
     * @param options {Array.<String>} 
     * @param optionsEn {Array.<String>} 
     * @param question {String} 
     * @param questionEn {String} 
     */
    constructor(id, options, optionsEn, question, questionEn) { 
        
        QuizQuestion.initialize(this, id, options, optionsEn, question, questionEn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, options, optionsEn, question, questionEn) { 
        obj['id'] = id;
        obj['options'] = options;
        obj['optionsEn'] = optionsEn;
        obj['question'] = question;
        obj['questionEn'] = questionEn;
    }

    /**
     * Constructs a <code>QuizQuestion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QuizQuestion} obj Optional instance to populate.
     * @return {module:model/QuizQuestion} The populated <code>QuizQuestion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QuizQuestion();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = ApiClient.convertToType(data['options'], ['String']);
            }
            if (data.hasOwnProperty('optionsEn')) {
                obj['optionsEn'] = ApiClient.convertToType(data['optionsEn'], ['String']);
            }
            if (data.hasOwnProperty('question')) {
                obj['question'] = ApiClient.convertToType(data['question'], 'String');
            }
            if (data.hasOwnProperty('questionEn')) {
                obj['questionEn'] = ApiClient.convertToType(data['questionEn'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QuizQuestion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QuizQuestion</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of QuizQuestion.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['options'])) {
            throw new Error("Expected the field `options` to be an array in the JSON data but got " + data['options']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['optionsEn'])) {
            throw new Error("Expected the field `optionsEn` to be an array in the JSON data but got " + data['optionsEn']);
        }
        // ensure the json data is a string
        if (data['question'] && !(typeof data['question'] === 'string' || data['question'] instanceof String)) {
            throw new Error("Expected the field `question` to be a primitive type in the JSON string but got " + data['question']);
        }
        // ensure the json data is a string
        if (data['questionEn'] && !(typeof data['questionEn'] === 'string' || data['questionEn'] instanceof String)) {
            throw new Error("Expected the field `questionEn` to be a primitive type in the JSON string but got " + data['questionEn']);
        }

        return true;
    }


}

QuizQuestion.RequiredProperties = ["id", "options", "optionsEn", "question", "questionEn"];

/**
 * @member {String} id
 */
QuizQuestion.prototype['id'] = undefined;

/**
 * @member {Array.<String>} options
 */
QuizQuestion.prototype['options'] = undefined;

/**
 * @member {Array.<String>} optionsEn
 */
QuizQuestion.prototype['optionsEn'] = undefined;

/**
 * @member {String} question
 */
QuizQuestion.prototype['question'] = undefined;

/**
 * @member {String} questionEn
 */
QuizQuestion.prototype['questionEn'] = undefined;






export default QuizQuestion;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MarketplaceWebhookEvent model module.
 * @module model/MarketplaceWebhookEvent
 * @version 0.1.0
 */
class MarketplaceWebhookEvent {
    /**
     * Constructs a new <code>MarketplaceWebhookEvent</code>.
     * @alias module:model/MarketplaceWebhookEvent
     * @param connectionId {String} References the marketplace connection entity.
     * @param eventType {String} 
     * @param platform {String} 
     */
    constructor(connectionId, eventType, platform) { 
        
        MarketplaceWebhookEvent.initialize(this, connectionId, eventType, platform);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, connectionId, eventType, platform) { 
        obj['connectionId'] = connectionId;
        obj['eventType'] = eventType;
        obj['platform'] = platform;
    }

    /**
     * Constructs a <code>MarketplaceWebhookEvent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MarketplaceWebhookEvent} obj Optional instance to populate.
     * @return {module:model/MarketplaceWebhookEvent} The populated <code>MarketplaceWebhookEvent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MarketplaceWebhookEvent();

            if (data.hasOwnProperty('connectionId')) {
                obj['connectionId'] = ApiClient.convertToType(data['connectionId'], 'String');
            }
            if (data.hasOwnProperty('eventBody')) {
                obj['eventBody'] = ApiClient.convertToType(data['eventBody'], Object);
            }
            if (data.hasOwnProperty('eventType')) {
                obj['eventType'] = ApiClient.convertToType(data['eventType'], 'String');
            }
            if (data.hasOwnProperty('headers')) {
                obj['headers'] = ApiClient.convertToType(data['headers'], Object);
            }
            if (data.hasOwnProperty('platform')) {
                obj['platform'] = ApiClient.convertToType(data['platform'], 'String');
            }
            if (data.hasOwnProperty('processed')) {
                obj['processed'] = ApiClient.convertToType(data['processed'], 'Boolean');
            }
            if (data.hasOwnProperty('processingError')) {
                obj['processingError'] = ApiClient.convertToType(data['processingError'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MarketplaceWebhookEvent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MarketplaceWebhookEvent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MarketplaceWebhookEvent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['connectionId'] && !(typeof data['connectionId'] === 'string' || data['connectionId'] instanceof String)) {
            throw new Error("Expected the field `connectionId` to be a primitive type in the JSON string but got " + data['connectionId']);
        }
        // ensure the json data is a string
        if (data['eventType'] && !(typeof data['eventType'] === 'string' || data['eventType'] instanceof String)) {
            throw new Error("Expected the field `eventType` to be a primitive type in the JSON string but got " + data['eventType']);
        }
        // ensure the json data is a string
        if (data['platform'] && !(typeof data['platform'] === 'string' || data['platform'] instanceof String)) {
            throw new Error("Expected the field `platform` to be a primitive type in the JSON string but got " + data['platform']);
        }
        // ensure the json data is a string
        if (data['processingError'] && !(typeof data['processingError'] === 'string' || data['processingError'] instanceof String)) {
            throw new Error("Expected the field `processingError` to be a primitive type in the JSON string but got " + data['processingError']);
        }

        return true;
    }


}

MarketplaceWebhookEvent.RequiredProperties = ["connectionId", "eventType", "platform"];

/**
 * References the marketplace connection entity.
 * @member {String} connectionId
 */
MarketplaceWebhookEvent.prototype['connectionId'] = undefined;

/**
 * @member {Object} eventBody
 */
MarketplaceWebhookEvent.prototype['eventBody'] = undefined;

/**
 * @member {String} eventType
 */
MarketplaceWebhookEvent.prototype['eventType'] = undefined;

/**
 * @member {Object} headers
 */
MarketplaceWebhookEvent.prototype['headers'] = undefined;

/**
 * @member {String} platform
 */
MarketplaceWebhookEvent.prototype['platform'] = undefined;

/**
 * @member {Boolean} processed
 */
MarketplaceWebhookEvent.prototype['processed'] = undefined;

/**
 * @member {String} processingError
 */
MarketplaceWebhookEvent.prototype['processingError'] = undefined;






export default MarketplaceWebhookEvent;


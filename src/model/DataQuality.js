/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DataQuality model module.
 * @module model/DataQuality
 * @version 0.1.0
 */
class DataQuality {
    /**
     * Constructs a new <code>DataQuality</code>.
     * @alias module:model/DataQuality
     * @param activityLines {Number} 
     * @param activitySharePct {Number} 
     * @param spendLines {Number} 
     */
    constructor(activityLines, activitySharePct, spendLines) { 
        
        DataQuality.initialize(this, activityLines, activitySharePct, spendLines);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, activityLines, activitySharePct, spendLines) { 
        obj['activity_lines'] = activityLines;
        obj['activity_share_pct'] = activitySharePct;
        obj['spend_lines'] = spendLines;
    }

    /**
     * Constructs a <code>DataQuality</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DataQuality} obj Optional instance to populate.
     * @return {module:model/DataQuality} The populated <code>DataQuality</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DataQuality();

            if (data.hasOwnProperty('activity_lines')) {
                obj['activity_lines'] = ApiClient.convertToType(data['activity_lines'], 'Number');
            }
            if (data.hasOwnProperty('activity_share_pct')) {
                obj['activity_share_pct'] = ApiClient.convertToType(data['activity_share_pct'], 'Number');
            }
            if (data.hasOwnProperty('spend_lines')) {
                obj['spend_lines'] = ApiClient.convertToType(data['spend_lines'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DataQuality</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DataQuality</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DataQuality.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

DataQuality.RequiredProperties = ["activity_lines", "activity_share_pct", "spend_lines"];

/**
 * @member {Number} activity_lines
 */
DataQuality.prototype['activity_lines'] = undefined;

/**
 * @member {Number} activity_share_pct
 */
DataQuality.prototype['activity_share_pct'] = undefined;

/**
 * @member {Number} spend_lines
 */
DataQuality.prototype['spend_lines'] = undefined;






export default DataQuality;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CheckStatus from './CheckStatus';
import Severity from './Severity';

/**
 * The PlausibilityCheck model module.
 * @module model/PlausibilityCheck
 * @version 0.1.0
 */
class PlausibilityCheck {
    /**
     * Constructs a new <code>PlausibilityCheck</code>.
     * @alias module:model/PlausibilityCheck
     * @param detail {String} 
     * @param id {String} 
     * @param name {String} 
     * @param severity {module:model/Severity} 
     * @param status {module:model/CheckStatus} 
     */
    constructor(detail, id, name, severity, status) { 
        
        PlausibilityCheck.initialize(this, detail, id, name, severity, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, detail, id, name, severity, status) { 
        obj['detail'] = detail;
        obj['id'] = id;
        obj['name'] = name;
        obj['severity'] = severity;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>PlausibilityCheck</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PlausibilityCheck} obj Optional instance to populate.
     * @return {module:model/PlausibilityCheck} The populated <code>PlausibilityCheck</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PlausibilityCheck();

            if (data.hasOwnProperty('detail')) {
                obj['detail'] = ApiClient.convertToType(data['detail'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('severity')) {
                obj['severity'] = Severity.constructFromObject(data['severity']);
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = CheckStatus.constructFromObject(data['status']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PlausibilityCheck</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PlausibilityCheck</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PlausibilityCheck.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['detail'] && !(typeof data['detail'] === 'string' || data['detail'] instanceof String)) {
            throw new Error("Expected the field `detail` to be a primitive type in the JSON string but got " + data['detail']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

PlausibilityCheck.RequiredProperties = ["detail", "id", "name", "severity", "status"];

/**
 * @member {String} detail
 */
PlausibilityCheck.prototype['detail'] = undefined;

/**
 * @member {String} id
 */
PlausibilityCheck.prototype['id'] = undefined;

/**
 * @member {String} name
 */
PlausibilityCheck.prototype['name'] = undefined;

/**
 * @member {module:model/Severity} severity
 */
PlausibilityCheck.prototype['severity'] = undefined;

/**
 * @member {module:model/CheckStatus} status
 */
PlausibilityCheck.prototype['status'] = undefined;






export default PlausibilityCheck;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The YearTotal model module.
 * @module model/YearTotal
 * @version 0.1.0
 */
class YearTotal {
    /**
     * Constructs a new <code>YearTotal</code>.
     * @alias module:model/YearTotal
     * @param tco2e {String} 
     * @param year {Number} 
     */
    constructor(tco2e, year) { 
        
        YearTotal.initialize(this, tco2e, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tco2e, year) { 
        obj['tco2e'] = tco2e;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>YearTotal</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/YearTotal} obj Optional instance to populate.
     * @return {module:model/YearTotal} The populated <code>YearTotal</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new YearTotal();

            if (data.hasOwnProperty('tco2e')) {
                obj['tco2e'] = ApiClient.convertToType(data['tco2e'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>YearTotal</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>YearTotal</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of YearTotal.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['tco2e'] && !(typeof data['tco2e'] === 'string' || data['tco2e'] instanceof String)) {
            throw new Error("Expected the field `tco2e` to be a primitive type in the JSON string but got " + data['tco2e']);
        }

        return true;
    }


}

YearTotal.RequiredProperties = ["tco2e", "year"];

/**
 * @member {String} tco2e
 */
YearTotal.prototype['tco2e'] = undefined;

/**
 * @member {Number} year
 */
YearTotal.prototype['year'] = undefined;






export default YearTotal;


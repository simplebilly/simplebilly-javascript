/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReminderLevel from './ReminderLevel';

/**
 * The OpenItem model module.
 * @module model/OpenItem
 * @version 0.1.0
 */
class OpenItem {
    /**
     * Constructs a new <code>OpenItem</code>.
     * @alias module:model/OpenItem
     * @param amountDue {String} 
     * @param amountPaid {String} 
     * @param invoiceId {String} 
     * @param invoiceNumber {String} 
     * @param issueDate {String} 
     * @param openAmount {String} 
     * @param reminderLevel {module:model/ReminderLevel} 
     */
    constructor(amountDue, amountPaid, invoiceId, invoiceNumber, issueDate, openAmount, reminderLevel) { 
        
        OpenItem.initialize(this, amountDue, amountPaid, invoiceId, invoiceNumber, issueDate, openAmount, reminderLevel);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amountDue, amountPaid, invoiceId, invoiceNumber, issueDate, openAmount, reminderLevel) { 
        obj['amount_due'] = amountDue;
        obj['amount_paid'] = amountPaid;
        obj['invoice_id'] = invoiceId;
        obj['invoice_number'] = invoiceNumber;
        obj['issue_date'] = issueDate;
        obj['open_amount'] = openAmount;
        obj['reminder_level'] = reminderLevel;
    }

    /**
     * Constructs a <code>OpenItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OpenItem} obj Optional instance to populate.
     * @return {module:model/OpenItem} The populated <code>OpenItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpenItem();

            if (data.hasOwnProperty('amount_due')) {
                obj['amount_due'] = ApiClient.convertToType(data['amount_due'], 'String');
            }
            if (data.hasOwnProperty('amount_paid')) {
                obj['amount_paid'] = ApiClient.convertToType(data['amount_paid'], 'String');
            }
            if (data.hasOwnProperty('customer_id')) {
                obj['customer_id'] = ApiClient.convertToType(data['customer_id'], 'String');
            }
            if (data.hasOwnProperty('days_overdue')) {
                obj['days_overdue'] = ApiClient.convertToType(data['days_overdue'], 'Number');
            }
            if (data.hasOwnProperty('due_date')) {
                obj['due_date'] = ApiClient.convertToType(data['due_date'], 'String');
            }
            if (data.hasOwnProperty('invoice_id')) {
                obj['invoice_id'] = ApiClient.convertToType(data['invoice_id'], 'String');
            }
            if (data.hasOwnProperty('invoice_number')) {
                obj['invoice_number'] = ApiClient.convertToType(data['invoice_number'], 'String');
            }
            if (data.hasOwnProperty('issue_date')) {
                obj['issue_date'] = ApiClient.convertToType(data['issue_date'], 'String');
            }
            if (data.hasOwnProperty('open_amount')) {
                obj['open_amount'] = ApiClient.convertToType(data['open_amount'], 'String');
            }
            if (data.hasOwnProperty('reminder_level')) {
                obj['reminder_level'] = ReminderLevel.constructFromObject(data['reminder_level']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpenItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpenItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OpenItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount_due'] && !(typeof data['amount_due'] === 'string' || data['amount_due'] instanceof String)) {
            throw new Error("Expected the field `amount_due` to be a primitive type in the JSON string but got " + data['amount_due']);
        }
        // ensure the json data is a string
        if (data['amount_paid'] && !(typeof data['amount_paid'] === 'string' || data['amount_paid'] instanceof String)) {
            throw new Error("Expected the field `amount_paid` to be a primitive type in the JSON string but got " + data['amount_paid']);
        }
        // ensure the json data is a string
        if (data['customer_id'] && !(typeof data['customer_id'] === 'string' || data['customer_id'] instanceof String)) {
            throw new Error("Expected the field `customer_id` to be a primitive type in the JSON string but got " + data['customer_id']);
        }
        // ensure the json data is a string
        if (data['due_date'] && !(typeof data['due_date'] === 'string' || data['due_date'] instanceof String)) {
            throw new Error("Expected the field `due_date` to be a primitive type in the JSON string but got " + data['due_date']);
        }
        // ensure the json data is a string
        if (data['invoice_id'] && !(typeof data['invoice_id'] === 'string' || data['invoice_id'] instanceof String)) {
            throw new Error("Expected the field `invoice_id` to be a primitive type in the JSON string but got " + data['invoice_id']);
        }
        // ensure the json data is a string
        if (data['invoice_number'] && !(typeof data['invoice_number'] === 'string' || data['invoice_number'] instanceof String)) {
            throw new Error("Expected the field `invoice_number` to be a primitive type in the JSON string but got " + data['invoice_number']);
        }
        // ensure the json data is a string
        if (data['issue_date'] && !(typeof data['issue_date'] === 'string' || data['issue_date'] instanceof String)) {
            throw new Error("Expected the field `issue_date` to be a primitive type in the JSON string but got " + data['issue_date']);
        }
        // ensure the json data is a string
        if (data['open_amount'] && !(typeof data['open_amount'] === 'string' || data['open_amount'] instanceof String)) {
            throw new Error("Expected the field `open_amount` to be a primitive type in the JSON string but got " + data['open_amount']);
        }

        return true;
    }


}

OpenItem.RequiredProperties = ["amount_due", "amount_paid", "invoice_id", "invoice_number", "issue_date", "open_amount", "reminder_level"];

/**
 * @member {String} amount_due
 */
OpenItem.prototype['amount_due'] = undefined;

/**
 * @member {String} amount_paid
 */
OpenItem.prototype['amount_paid'] = undefined;

/**
 * @member {String} customer_id
 */
OpenItem.prototype['customer_id'] = undefined;

/**
 * @member {Number} days_overdue
 */
OpenItem.prototype['days_overdue'] = undefined;

/**
 * @member {String} due_date
 */
OpenItem.prototype['due_date'] = undefined;

/**
 * @member {String} invoice_id
 */
OpenItem.prototype['invoice_id'] = undefined;

/**
 * @member {String} invoice_number
 */
OpenItem.prototype['invoice_number'] = undefined;

/**
 * @member {String} issue_date
 */
OpenItem.prototype['issue_date'] = undefined;

/**
 * @member {String} open_amount
 */
OpenItem.prototype['open_amount'] = undefined;

/**
 * @member {module:model/ReminderLevel} reminder_level
 */
OpenItem.prototype['reminder_level'] = undefined;






export default OpenItem;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PnLItem from './PnLItem';

/**
 * The IncomeStatement model module.
 * @module model/IncomeStatement
 * @version 0.1.0
 */
class IncomeStatement {
    /**
     * Constructs a new <code>IncomeStatement</code>.
     * @alias module:model/IncomeStatement
     * @param expenseItems {Array.<module:model/PnLItem>} 
     * @param netIncome {String} 
     * @param revenueItems {Array.<module:model/PnLItem>} 
     * @param totalExpenses {String} 
     * @param totalRevenue {String} 
     */
    constructor(expenseItems, netIncome, revenueItems, totalExpenses, totalRevenue) { 
        
        IncomeStatement.initialize(this, expenseItems, netIncome, revenueItems, totalExpenses, totalRevenue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, expenseItems, netIncome, revenueItems, totalExpenses, totalRevenue) { 
        obj['expense_items'] = expenseItems;
        obj['net_income'] = netIncome;
        obj['revenue_items'] = revenueItems;
        obj['total_expenses'] = totalExpenses;
        obj['total_revenue'] = totalRevenue;
    }

    /**
     * Constructs a <code>IncomeStatement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IncomeStatement} obj Optional instance to populate.
     * @return {module:model/IncomeStatement} The populated <code>IncomeStatement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IncomeStatement();

            if (data.hasOwnProperty('expense_items')) {
                obj['expense_items'] = ApiClient.convertToType(data['expense_items'], [PnLItem]);
            }
            if (data.hasOwnProperty('net_income')) {
                obj['net_income'] = ApiClient.convertToType(data['net_income'], 'String');
            }
            if (data.hasOwnProperty('revenue_items')) {
                obj['revenue_items'] = ApiClient.convertToType(data['revenue_items'], [PnLItem]);
            }
            if (data.hasOwnProperty('total_expenses')) {
                obj['total_expenses'] = ApiClient.convertToType(data['total_expenses'], 'String');
            }
            if (data.hasOwnProperty('total_revenue')) {
                obj['total_revenue'] = ApiClient.convertToType(data['total_revenue'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IncomeStatement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IncomeStatement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IncomeStatement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['expense_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['expense_items'])) {
                throw new Error("Expected the field `expense_items` to be an array in the JSON data but got " + data['expense_items']);
            }
            // validate the optional field `expense_items` (array)
            for (const item of data['expense_items']) {
                PnLItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['net_income'] && !(typeof data['net_income'] === 'string' || data['net_income'] instanceof String)) {
            throw new Error("Expected the field `net_income` to be a primitive type in the JSON string but got " + data['net_income']);
        }
        if (data['revenue_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['revenue_items'])) {
                throw new Error("Expected the field `revenue_items` to be an array in the JSON data but got " + data['revenue_items']);
            }
            // validate the optional field `revenue_items` (array)
            for (const item of data['revenue_items']) {
                PnLItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_expenses'] && !(typeof data['total_expenses'] === 'string' || data['total_expenses'] instanceof String)) {
            throw new Error("Expected the field `total_expenses` to be a primitive type in the JSON string but got " + data['total_expenses']);
        }
        // ensure the json data is a string
        if (data['total_revenue'] && !(typeof data['total_revenue'] === 'string' || data['total_revenue'] instanceof String)) {
            throw new Error("Expected the field `total_revenue` to be a primitive type in the JSON string but got " + data['total_revenue']);
        }

        return true;
    }


}

IncomeStatement.RequiredProperties = ["expense_items", "net_income", "revenue_items", "total_expenses", "total_revenue"];

/**
 * @member {Array.<module:model/PnLItem>} expense_items
 */
IncomeStatement.prototype['expense_items'] = undefined;

/**
 * @member {String} net_income
 */
IncomeStatement.prototype['net_income'] = undefined;

/**
 * @member {Array.<module:model/PnLItem>} revenue_items
 */
IncomeStatement.prototype['revenue_items'] = undefined;

/**
 * @member {String} total_expenses
 */
IncomeStatement.prototype['total_expenses'] = undefined;

/**
 * @member {String} total_revenue
 */
IncomeStatement.prototype['total_revenue'] = undefined;






export default IncomeStatement;


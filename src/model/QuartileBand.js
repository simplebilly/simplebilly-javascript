/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The QuartileBand model module.
 * @module model/QuartileBand
 * @version 0.1.0
 */
class QuartileBand {
    /**
     * Constructs a new <code>QuartileBand</code>.
     * @alias module:model/QuartileBand
     * @param femaleSharePct {Number} 
     * @param hourlyMedian {String} 
     * @param maleSharePct {Number} 
     * @param quartile {String} 
     */
    constructor(femaleSharePct, hourlyMedian, maleSharePct, quartile) { 
        
        QuartileBand.initialize(this, femaleSharePct, hourlyMedian, maleSharePct, quartile);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, femaleSharePct, hourlyMedian, maleSharePct, quartile) { 
        obj['female_share_pct'] = femaleSharePct;
        obj['hourly_median'] = hourlyMedian;
        obj['male_share_pct'] = maleSharePct;
        obj['quartile'] = quartile;
    }

    /**
     * Constructs a <code>QuartileBand</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/QuartileBand} obj Optional instance to populate.
     * @return {module:model/QuartileBand} The populated <code>QuartileBand</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new QuartileBand();

            if (data.hasOwnProperty('female_share_pct')) {
                obj['female_share_pct'] = ApiClient.convertToType(data['female_share_pct'], 'Number');
            }
            if (data.hasOwnProperty('hourly_median')) {
                obj['hourly_median'] = ApiClient.convertToType(data['hourly_median'], 'String');
            }
            if (data.hasOwnProperty('male_share_pct')) {
                obj['male_share_pct'] = ApiClient.convertToType(data['male_share_pct'], 'Number');
            }
            if (data.hasOwnProperty('quartile')) {
                obj['quartile'] = ApiClient.convertToType(data['quartile'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>QuartileBand</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QuartileBand</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of QuartileBand.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['hourly_median'] && !(typeof data['hourly_median'] === 'string' || data['hourly_median'] instanceof String)) {
            throw new Error("Expected the field `hourly_median` to be a primitive type in the JSON string but got " + data['hourly_median']);
        }
        // ensure the json data is a string
        if (data['quartile'] && !(typeof data['quartile'] === 'string' || data['quartile'] instanceof String)) {
            throw new Error("Expected the field `quartile` to be a primitive type in the JSON string but got " + data['quartile']);
        }

        return true;
    }


}

QuartileBand.RequiredProperties = ["female_share_pct", "hourly_median", "male_share_pct", "quartile"];

/**
 * @member {Number} female_share_pct
 */
QuartileBand.prototype['female_share_pct'] = undefined;

/**
 * @member {String} hourly_median
 */
QuartileBand.prototype['hourly_median'] = undefined;

/**
 * @member {Number} male_share_pct
 */
QuartileBand.prototype['male_share_pct'] = undefined;

/**
 * @member {String} quartile
 */
QuartileBand.prototype['quartile'] = undefined;






export default QuartileBand;


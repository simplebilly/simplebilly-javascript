/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ResolvedPriceResponse model module.
 * @module model/ResolvedPriceResponse
 * @version 0.1.0
 */
class ResolvedPriceResponse {
    /**
     * Constructs a new <code>ResolvedPriceResponse</code>.
     * @alias module:model/ResolvedPriceResponse
     * @param isListPrice {Boolean} True when no tier matched and the product list price was used.
     * @param productId {String} 
     * @param quantity {Number} 
     * @param unitPrice {String} 
     */
    constructor(isListPrice, productId, quantity, unitPrice) { 
        
        ResolvedPriceResponse.initialize(this, isListPrice, productId, quantity, unitPrice);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, isListPrice, productId, quantity, unitPrice) { 
        obj['isListPrice'] = isListPrice;
        obj['productId'] = productId;
        obj['quantity'] = quantity;
        obj['unitPrice'] = unitPrice;
    }

    /**
     * Constructs a <code>ResolvedPriceResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ResolvedPriceResponse} obj Optional instance to populate.
     * @return {module:model/ResolvedPriceResponse} The populated <code>ResolvedPriceResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ResolvedPriceResponse();

            if (data.hasOwnProperty('isListPrice')) {
                obj['isListPrice'] = ApiClient.convertToType(data['isListPrice'], 'Boolean');
            }
            if (data.hasOwnProperty('priceTierId')) {
                obj['priceTierId'] = ApiClient.convertToType(data['priceTierId'], 'String');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('unitPrice')) {
                obj['unitPrice'] = ApiClient.convertToType(data['unitPrice'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ResolvedPriceResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ResolvedPriceResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ResolvedPriceResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['priceTierId'] && !(typeof data['priceTierId'] === 'string' || data['priceTierId'] instanceof String)) {
            throw new Error("Expected the field `priceTierId` to be a primitive type in the JSON string but got " + data['priceTierId']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['unitPrice'] && !(typeof data['unitPrice'] === 'string' || data['unitPrice'] instanceof String)) {
            throw new Error("Expected the field `unitPrice` to be a primitive type in the JSON string but got " + data['unitPrice']);
        }

        return true;
    }


}

ResolvedPriceResponse.RequiredProperties = ["isListPrice", "productId", "quantity", "unitPrice"];

/**
 * True when no tier matched and the product list price was used.
 * @member {Boolean} isListPrice
 */
ResolvedPriceResponse.prototype['isListPrice'] = undefined;

/**
 * Applied tier, if any matched.
 * @member {String} priceTierId
 */
ResolvedPriceResponse.prototype['priceTierId'] = undefined;

/**
 * @member {String} productId
 */
ResolvedPriceResponse.prototype['productId'] = undefined;

/**
 * @member {Number} quantity
 */
ResolvedPriceResponse.prototype['quantity'] = undefined;

/**
 * @member {String} unitPrice
 */
ResolvedPriceResponse.prototype['unitPrice'] = undefined;






export default ResolvedPriceResponse;


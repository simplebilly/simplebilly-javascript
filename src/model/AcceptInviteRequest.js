/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AcceptInviteRequest model module.
 * @module model/AcceptInviteRequest
 * @version 0.1.0
 */
class AcceptInviteRequest {
    /**
     * Constructs a new <code>AcceptInviteRequest</code>.
     * @alias module:model/AcceptInviteRequest
     * @param firstName {String} 
     * @param lastName {String} 
     * @param password {String} 
     * @param privacyAccepted {Boolean} GDPR consent — rejected unless true.
     * @param token {String} 
     */
    constructor(firstName, lastName, password, privacyAccepted, token) { 
        
        AcceptInviteRequest.initialize(this, firstName, lastName, password, privacyAccepted, token);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, firstName, lastName, password, privacyAccepted, token) { 
        obj['first_name'] = firstName;
        obj['last_name'] = lastName;
        obj['password'] = password;
        obj['privacy_accepted'] = privacyAccepted;
        obj['token'] = token;
    }

    /**
     * Constructs a <code>AcceptInviteRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AcceptInviteRequest} obj Optional instance to populate.
     * @return {module:model/AcceptInviteRequest} The populated <code>AcceptInviteRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AcceptInviteRequest();

            if (data.hasOwnProperty('first_name')) {
                obj['first_name'] = ApiClient.convertToType(data['first_name'], 'String');
            }
            if (data.hasOwnProperty('last_name')) {
                obj['last_name'] = ApiClient.convertToType(data['last_name'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('privacy_accepted')) {
                obj['privacy_accepted'] = ApiClient.convertToType(data['privacy_accepted'], 'Boolean');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AcceptInviteRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AcceptInviteRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AcceptInviteRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['first_name'] && !(typeof data['first_name'] === 'string' || data['first_name'] instanceof String)) {
            throw new Error("Expected the field `first_name` to be a primitive type in the JSON string but got " + data['first_name']);
        }
        // ensure the json data is a string
        if (data['last_name'] && !(typeof data['last_name'] === 'string' || data['last_name'] instanceof String)) {
            throw new Error("Expected the field `last_name` to be a primitive type in the JSON string but got " + data['last_name']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }

        return true;
    }


}

AcceptInviteRequest.RequiredProperties = ["first_name", "last_name", "password", "privacy_accepted", "token"];

/**
 * @member {String} first_name
 */
AcceptInviteRequest.prototype['first_name'] = undefined;

/**
 * @member {String} last_name
 */
AcceptInviteRequest.prototype['last_name'] = undefined;

/**
 * @member {String} password
 */
AcceptInviteRequest.prototype['password'] = undefined;

/**
 * GDPR consent — rejected unless true.
 * @member {Boolean} privacy_accepted
 */
AcceptInviteRequest.prototype['privacy_accepted'] = undefined;

/**
 * @member {String} token
 */
AcceptInviteRequest.prototype['token'] = undefined;






export default AcceptInviteRequest;


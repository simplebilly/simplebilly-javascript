/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExtraPayment from './ExtraPayment';

/**
 * The PayrollCreatePayload model module.
 * @module model/PayrollCreatePayload
 * @version 0.1.0
 */
class PayrollCreatePayload {
    /**
     * Constructs a new <code>PayrollCreatePayload</code>.
     * @alias module:model/PayrollCreatePayload
     * @param employeeIds {Array.<String>} 
     * @param month {Number} 
     * @param year {Number} 
     */
    constructor(employeeIds, month, year) { 
        
        PayrollCreatePayload.initialize(this, employeeIds, month, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, employeeIds, month, year) { 
        obj['employee_ids'] = employeeIds;
        obj['month'] = month;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>PayrollCreatePayload</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayrollCreatePayload} obj Optional instance to populate.
     * @return {module:model/PayrollCreatePayload} The populated <code>PayrollCreatePayload</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayrollCreatePayload();

            if (data.hasOwnProperty('employee_ids')) {
                obj['employee_ids'] = ApiClient.convertToType(data['employee_ids'], ['String']);
            }
            if (data.hasOwnProperty('extra_payments')) {
                obj['extra_payments'] = ApiClient.convertToType(data['extra_payments'], [ExtraPayment]);
            }
            if (data.hasOwnProperty('month')) {
                obj['month'] = ApiClient.convertToType(data['month'], 'Number');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayrollCreatePayload</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayrollCreatePayload</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayrollCreatePayload.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['employee_ids'])) {
            throw new Error("Expected the field `employee_ids` to be an array in the JSON data but got " + data['employee_ids']);
        }
        if (data['extra_payments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['extra_payments'])) {
                throw new Error("Expected the field `extra_payments` to be an array in the JSON data but got " + data['extra_payments']);
            }
            // validate the optional field `extra_payments` (array)
            for (const item of data['extra_payments']) {
                ExtraPayment.validateJSON(item);
            };
        }

        return true;
    }


}

PayrollCreatePayload.RequiredProperties = ["employee_ids", "month", "year"];

/**
 * @member {Array.<String>} employee_ids
 */
PayrollCreatePayload.prototype['employee_ids'] = undefined;

/**
 * @member {Array.<module:model/ExtraPayment>} extra_payments
 */
PayrollCreatePayload.prototype['extra_payments'] = undefined;

/**
 * @member {Number} month
 */
PayrollCreatePayload.prototype['month'] = undefined;

/**
 * @member {Number} year
 */
PayrollCreatePayload.prototype['year'] = undefined;






export default PayrollCreatePayload;


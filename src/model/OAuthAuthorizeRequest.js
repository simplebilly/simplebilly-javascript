/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OAuthAuthorizeRequest model module.
 * @module model/OAuthAuthorizeRequest
 * @version 0.1.0
 */
class OAuthAuthorizeRequest {
    /**
     * Constructs a new <code>OAuthAuthorizeRequest</code>.
     * @alias module:model/OAuthAuthorizeRequest
     * @param platform {String} 
     * @param redirectUri {String} 
     */
    constructor(platform, redirectUri) { 
        
        OAuthAuthorizeRequest.initialize(this, platform, redirectUri);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, platform, redirectUri) { 
        obj['platform'] = platform;
        obj['redirect_uri'] = redirectUri;
    }

    /**
     * Constructs a <code>OAuthAuthorizeRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OAuthAuthorizeRequest} obj Optional instance to populate.
     * @return {module:model/OAuthAuthorizeRequest} The populated <code>OAuthAuthorizeRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OAuthAuthorizeRequest();

            if (data.hasOwnProperty('config')) {
                obj['config'] = ApiClient.convertToType(data['config'], Object);
            }
            if (data.hasOwnProperty('platform')) {
                obj['platform'] = ApiClient.convertToType(data['platform'], 'String');
            }
            if (data.hasOwnProperty('redirect_uri')) {
                obj['redirect_uri'] = ApiClient.convertToType(data['redirect_uri'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OAuthAuthorizeRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OAuthAuthorizeRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OAuthAuthorizeRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['platform'] && !(typeof data['platform'] === 'string' || data['platform'] instanceof String)) {
            throw new Error("Expected the field `platform` to be a primitive type in the JSON string but got " + data['platform']);
        }
        // ensure the json data is a string
        if (data['redirect_uri'] && !(typeof data['redirect_uri'] === 'string' || data['redirect_uri'] instanceof String)) {
            throw new Error("Expected the field `redirect_uri` to be a primitive type in the JSON string but got " + data['redirect_uri']);
        }

        return true;
    }


}

OAuthAuthorizeRequest.RequiredProperties = ["platform", "redirect_uri"];

/**
 * Optional platform-specific config (e.g. Shopify `shop_domain`, `api_key`, `api_secret`) needed to build the authorization URL.
 * @member {Object} config
 */
OAuthAuthorizeRequest.prototype['config'] = undefined;

/**
 * @member {String} platform
 */
OAuthAuthorizeRequest.prototype['platform'] = undefined;

/**
 * @member {String} redirect_uri
 */
OAuthAuthorizeRequest.prototype['redirect_uri'] = undefined;






export default OAuthAuthorizeRequest;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DeliveryAppointmentStatus from './DeliveryAppointmentStatus';

/**
 * The DeliveryAppointment model module.
 * @module model/DeliveryAppointment
 * @version 0.1.0
 */
class DeliveryAppointment {
    /**
     * Constructs a new <code>DeliveryAppointment</code>.
     * @alias module:model/DeliveryAppointment
     * @param email {String} 
     * @param requestedDate {Date} 
     * @param status {module:model/DeliveryAppointmentStatus} One of: requested | confirmed | arrived | cancelled | completed
     * @param supplierName {String} 
     * @param warehouseId {String} References the warehouse entity.
     */
    constructor(email, requestedDate, status, supplierName, warehouseId) { 
        
        DeliveryAppointment.initialize(this, email, requestedDate, status, supplierName, warehouseId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, requestedDate, status, supplierName, warehouseId) { 
        obj['email'] = email;
        obj['requestedDate'] = requestedDate;
        obj['status'] = status;
        obj['supplierName'] = supplierName;
        obj['warehouseId'] = warehouseId;
    }

    /**
     * Constructs a <code>DeliveryAppointment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeliveryAppointment} obj Optional instance to populate.
     * @return {module:model/DeliveryAppointment} The populated <code>DeliveryAppointment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeliveryAppointment();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
            if (data.hasOwnProperty('requestedDate')) {
                obj['requestedDate'] = ApiClient.convertToType(data['requestedDate'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], DeliveryAppointmentStatus);
            }
            if (data.hasOwnProperty('supplierName')) {
                obj['supplierName'] = ApiClient.convertToType(data['supplierName'], 'String');
            }
            if (data.hasOwnProperty('timeSlot')) {
                obj['timeSlot'] = ApiClient.convertToType(data['timeSlot'], 'String');
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeliveryAppointment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeliveryAppointment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeliveryAppointment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }
        // ensure the json data is a string
        if (data['supplierName'] && !(typeof data['supplierName'] === 'string' || data['supplierName'] instanceof String)) {
            throw new Error("Expected the field `supplierName` to be a primitive type in the JSON string but got " + data['supplierName']);
        }
        // ensure the json data is a string
        if (data['timeSlot'] && !(typeof data['timeSlot'] === 'string' || data['timeSlot'] instanceof String)) {
            throw new Error("Expected the field `timeSlot` to be a primitive type in the JSON string but got " + data['timeSlot']);
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}

DeliveryAppointment.RequiredProperties = ["email", "requestedDate", "status", "supplierName", "warehouseId"];

/**
 * @member {String} email
 */
DeliveryAppointment.prototype['email'] = undefined;

/**
 * @member {String} notes
 */
DeliveryAppointment.prototype['notes'] = undefined;

/**
 * @member {String} phone
 */
DeliveryAppointment.prototype['phone'] = undefined;

/**
 * @member {Date} requestedDate
 */
DeliveryAppointment.prototype['requestedDate'] = undefined;

/**
 * One of: requested | confirmed | arrived | cancelled | completed
 * @member {module:model/DeliveryAppointmentStatus} status
 */
DeliveryAppointment.prototype['status'] = undefined;

/**
 * @member {String} supplierName
 */
DeliveryAppointment.prototype['supplierName'] = undefined;

/**
 * e.g. \"08:00-10:00\"
 * @member {String} timeSlot
 */
DeliveryAppointment.prototype['timeSlot'] = undefined;

/**
 * References the warehouse entity.
 * @member {String} warehouseId
 */
DeliveryAppointment.prototype['warehouseId'] = undefined;






export default DeliveryAppointment;


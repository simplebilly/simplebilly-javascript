/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AiSuggestion model module.
 * @module model/AiSuggestion
 * @version 0.1.0
 */
class AiSuggestion {
    /**
     * Constructs a new <code>AiSuggestion</code>.
     * @alias module:model/AiSuggestion
     * @param confidence {Number} 
     * @param reasoning {String} 
     * @param suggestedReply {String} 
     * @param toolCalls {Array.<String>} 
     */
    constructor(confidence, reasoning, suggestedReply, toolCalls) { 
        
        AiSuggestion.initialize(this, confidence, reasoning, suggestedReply, toolCalls);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, confidence, reasoning, suggestedReply, toolCalls) { 
        obj['confidence'] = confidence;
        obj['reasoning'] = reasoning;
        obj['suggested_reply'] = suggestedReply;
        obj['tool_calls'] = toolCalls;
    }

    /**
     * Constructs a <code>AiSuggestion</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AiSuggestion} obj Optional instance to populate.
     * @return {module:model/AiSuggestion} The populated <code>AiSuggestion</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AiSuggestion();

            if (data.hasOwnProperty('confidence')) {
                obj['confidence'] = ApiClient.convertToType(data['confidence'], 'Number');
            }
            if (data.hasOwnProperty('reasoning')) {
                obj['reasoning'] = ApiClient.convertToType(data['reasoning'], 'String');
            }
            if (data.hasOwnProperty('suggested_priority')) {
                obj['suggested_priority'] = ApiClient.convertToType(data['suggested_priority'], 'String');
            }
            if (data.hasOwnProperty('suggested_reply')) {
                obj['suggested_reply'] = ApiClient.convertToType(data['suggested_reply'], 'String');
            }
            if (data.hasOwnProperty('suggested_status')) {
                obj['suggested_status'] = ApiClient.convertToType(data['suggested_status'], 'String');
            }
            if (data.hasOwnProperty('tool_calls')) {
                obj['tool_calls'] = ApiClient.convertToType(data['tool_calls'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AiSuggestion</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AiSuggestion</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AiSuggestion.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['reasoning'] && !(typeof data['reasoning'] === 'string' || data['reasoning'] instanceof String)) {
            throw new Error("Expected the field `reasoning` to be a primitive type in the JSON string but got " + data['reasoning']);
        }
        // ensure the json data is a string
        if (data['suggested_priority'] && !(typeof data['suggested_priority'] === 'string' || data['suggested_priority'] instanceof String)) {
            throw new Error("Expected the field `suggested_priority` to be a primitive type in the JSON string but got " + data['suggested_priority']);
        }
        // ensure the json data is a string
        if (data['suggested_reply'] && !(typeof data['suggested_reply'] === 'string' || data['suggested_reply'] instanceof String)) {
            throw new Error("Expected the field `suggested_reply` to be a primitive type in the JSON string but got " + data['suggested_reply']);
        }
        // ensure the json data is a string
        if (data['suggested_status'] && !(typeof data['suggested_status'] === 'string' || data['suggested_status'] instanceof String)) {
            throw new Error("Expected the field `suggested_status` to be a primitive type in the JSON string but got " + data['suggested_status']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tool_calls'])) {
            throw new Error("Expected the field `tool_calls` to be an array in the JSON data but got " + data['tool_calls']);
        }

        return true;
    }


}

AiSuggestion.RequiredProperties = ["confidence", "reasoning", "suggested_reply", "tool_calls"];

/**
 * @member {Number} confidence
 */
AiSuggestion.prototype['confidence'] = undefined;

/**
 * @member {String} reasoning
 */
AiSuggestion.prototype['reasoning'] = undefined;

/**
 * @member {String} suggested_priority
 */
AiSuggestion.prototype['suggested_priority'] = undefined;

/**
 * @member {String} suggested_reply
 */
AiSuggestion.prototype['suggested_reply'] = undefined;

/**
 * @member {String} suggested_status
 */
AiSuggestion.prototype['suggested_status'] = undefined;

/**
 * @member {Array.<String>} tool_calls
 */
AiSuggestion.prototype['tool_calls'] = undefined;






export default AiSuggestion;


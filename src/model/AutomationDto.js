/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AutomationDto model module.
 * @module model/AutomationDto
 * @version 0.1.0
 */
class AutomationDto {
    /**
     * Constructs a new <code>AutomationDto</code>.
     * @alias module:model/AutomationDto
     * @param automationKey {String} 
     * @param config {Object} 
     * @param description {String} 
     * @param enabled {Boolean} 
     * @param kind {String} 
     * @param scheduleKind {String} 
     */
    constructor(automationKey, config, description, enabled, kind, scheduleKind) { 
        
        AutomationDto.initialize(this, automationKey, config, description, enabled, kind, scheduleKind);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, automationKey, config, description, enabled, kind, scheduleKind) { 
        obj['automationKey'] = automationKey;
        obj['config'] = config;
        obj['description'] = description;
        obj['enabled'] = enabled;
        obj['kind'] = kind;
        obj['scheduleKind'] = scheduleKind;
    }

    /**
     * Constructs a <code>AutomationDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AutomationDto} obj Optional instance to populate.
     * @return {module:model/AutomationDto} The populated <code>AutomationDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AutomationDto();

            if (data.hasOwnProperty('automationKey')) {
                obj['automationKey'] = ApiClient.convertToType(data['automationKey'], 'String');
            }
            if (data.hasOwnProperty('config')) {
                obj['config'] = ApiClient.convertToType(data['config'], Object);
            }
            if (data.hasOwnProperty('defaultDay')) {
                obj['defaultDay'] = ApiClient.convertToType(data['defaultDay'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('enabled')) {
                obj['enabled'] = ApiClient.convertToType(data['enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('lastRunAt')) {
                obj['lastRunAt'] = ApiClient.convertToType(data['lastRunAt'], 'Date');
            }
            if (data.hasOwnProperty('nextRunAt')) {
                obj['nextRunAt'] = ApiClient.convertToType(data['nextRunAt'], 'Date');
            }
            if (data.hasOwnProperty('scheduleKind')) {
                obj['scheduleKind'] = ApiClient.convertToType(data['scheduleKind'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AutomationDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AutomationDto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AutomationDto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['automationKey'] && !(typeof data['automationKey'] === 'string' || data['automationKey'] instanceof String)) {
            throw new Error("Expected the field `automationKey` to be a primitive type in the JSON string but got " + data['automationKey']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['scheduleKind'] && !(typeof data['scheduleKind'] === 'string' || data['scheduleKind'] instanceof String)) {
            throw new Error("Expected the field `scheduleKind` to be a primitive type in the JSON string but got " + data['scheduleKind']);
        }

        return true;
    }


}

AutomationDto.RequiredProperties = ["automationKey", "config", "description", "enabled", "kind", "scheduleKind"];

/**
 * @member {String} automationKey
 */
AutomationDto.prototype['automationKey'] = undefined;

/**
 * @member {Object} config
 */
AutomationDto.prototype['config'] = undefined;

/**
 * @member {Number} defaultDay
 */
AutomationDto.prototype['defaultDay'] = undefined;

/**
 * @member {String} description
 */
AutomationDto.prototype['description'] = undefined;

/**
 * @member {Boolean} enabled
 */
AutomationDto.prototype['enabled'] = undefined;

/**
 * @member {String} kind
 */
AutomationDto.prototype['kind'] = undefined;

/**
 * @member {Date} lastRunAt
 */
AutomationDto.prototype['lastRunAt'] = undefined;

/**
 * @member {Date} nextRunAt
 */
AutomationDto.prototype['nextRunAt'] = undefined;

/**
 * @member {String} scheduleKind
 */
AutomationDto.prototype['scheduleKind'] = undefined;






export default AutomationDto;


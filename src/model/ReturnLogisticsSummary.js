/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ReturnWarehouseSummary from './ReturnWarehouseSummary';

/**
 * The ReturnLogisticsSummary model module.
 * @module model/ReturnLogisticsSummary
 * @version 0.1.0
 */
class ReturnLogisticsSummary {
    /**
     * Constructs a new <code>ReturnLogisticsSummary</code>.
     * Warehouse-level aggregation for the returns logistics dashboard.
     * @alias module:model/ReturnLogisticsSummary
     * @param byStatus {Object} Number of return orders per status.
     * @param byWarehouse {Array.<module:model/ReturnWarehouseSummary>} Per-warehouse aggregation.
     * @param itemsRestocked {Number} Sum of `restock: true` line-item quantities.
     * @param itemsScrapped {Number} Sum of `restock: false` line-item quantities (scrapped/disposed).
     * @param totalItems {Number} Sum of all line-item quantities across returns.
     * @param totalReturns {Number} Total number of return orders (excluding soft-deleted).
     */
    constructor(byStatus, byWarehouse, itemsRestocked, itemsScrapped, totalItems, totalReturns) { 
        
        ReturnLogisticsSummary.initialize(this, byStatus, byWarehouse, itemsRestocked, itemsScrapped, totalItems, totalReturns);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, byStatus, byWarehouse, itemsRestocked, itemsScrapped, totalItems, totalReturns) { 
        obj['byStatus'] = byStatus;
        obj['byWarehouse'] = byWarehouse;
        obj['itemsRestocked'] = itemsRestocked;
        obj['itemsScrapped'] = itemsScrapped;
        obj['totalItems'] = totalItems;
        obj['totalReturns'] = totalReturns;
    }

    /**
     * Constructs a <code>ReturnLogisticsSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReturnLogisticsSummary} obj Optional instance to populate.
     * @return {module:model/ReturnLogisticsSummary} The populated <code>ReturnLogisticsSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReturnLogisticsSummary();

            if (data.hasOwnProperty('byStatus')) {
                obj['byStatus'] = ApiClient.convertToType(data['byStatus'], Object);
            }
            if (data.hasOwnProperty('byWarehouse')) {
                obj['byWarehouse'] = ApiClient.convertToType(data['byWarehouse'], [ReturnWarehouseSummary]);
            }
            if (data.hasOwnProperty('itemsRestocked')) {
                obj['itemsRestocked'] = ApiClient.convertToType(data['itemsRestocked'], 'Number');
            }
            if (data.hasOwnProperty('itemsScrapped')) {
                obj['itemsScrapped'] = ApiClient.convertToType(data['itemsScrapped'], 'Number');
            }
            if (data.hasOwnProperty('totalItems')) {
                obj['totalItems'] = ApiClient.convertToType(data['totalItems'], 'Number');
            }
            if (data.hasOwnProperty('totalReturns')) {
                obj['totalReturns'] = ApiClient.convertToType(data['totalReturns'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReturnLogisticsSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReturnLogisticsSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReturnLogisticsSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['byWarehouse']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['byWarehouse'])) {
                throw new Error("Expected the field `byWarehouse` to be an array in the JSON data but got " + data['byWarehouse']);
            }
            // validate the optional field `byWarehouse` (array)
            for (const item of data['byWarehouse']) {
                ReturnWarehouseSummary.validateJSON(item);
            };
        }

        return true;
    }


}

ReturnLogisticsSummary.RequiredProperties = ["byStatus", "byWarehouse", "itemsRestocked", "itemsScrapped", "totalItems", "totalReturns"];

/**
 * Number of return orders per status.
 * @member {Object} byStatus
 */
ReturnLogisticsSummary.prototype['byStatus'] = undefined;

/**
 * Per-warehouse aggregation.
 * @member {Array.<module:model/ReturnWarehouseSummary>} byWarehouse
 */
ReturnLogisticsSummary.prototype['byWarehouse'] = undefined;

/**
 * Sum of `restock: true` line-item quantities.
 * @member {Number} itemsRestocked
 */
ReturnLogisticsSummary.prototype['itemsRestocked'] = undefined;

/**
 * Sum of `restock: false` line-item quantities (scrapped/disposed).
 * @member {Number} itemsScrapped
 */
ReturnLogisticsSummary.prototype['itemsScrapped'] = undefined;

/**
 * Sum of all line-item quantities across returns.
 * @member {Number} totalItems
 */
ReturnLogisticsSummary.prototype['totalItems'] = undefined;

/**
 * Total number of return orders (excluding soft-deleted).
 * @member {Number} totalReturns
 */
ReturnLogisticsSummary.prototype['totalReturns'] = undefined;






export default ReturnLogisticsSummary;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TargetProgress model module.
 * @module model/TargetProgress
 * @version 0.1.0
 */
class TargetProgress {
    /**
     * Constructs a new <code>TargetProgress</code>.
     * @alias module:model/TargetProgress
     * @param baseValue {Number} 
     * @param baseYear {Number} 
     * @param description {String} 
     * @param id {String} 
     * @param scope {String} 
     * @param targetValue {Number} 
     * @param targetYear {Number} 
     */
    constructor(baseValue, baseYear, description, id, scope, targetValue, targetYear) { 
        
        TargetProgress.initialize(this, baseValue, baseYear, description, id, scope, targetValue, targetYear);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, baseValue, baseYear, description, id, scope, targetValue, targetYear) { 
        obj['base_value'] = baseValue;
        obj['base_year'] = baseYear;
        obj['description'] = description;
        obj['id'] = id;
        obj['scope'] = scope;
        obj['target_value'] = targetValue;
        obj['target_year'] = targetYear;
    }

    /**
     * Constructs a <code>TargetProgress</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TargetProgress} obj Optional instance to populate.
     * @return {module:model/TargetProgress} The populated <code>TargetProgress</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TargetProgress();

            if (data.hasOwnProperty('base_value')) {
                obj['base_value'] = ApiClient.convertToType(data['base_value'], 'Number');
            }
            if (data.hasOwnProperty('base_year')) {
                obj['base_year'] = ApiClient.convertToType(data['base_year'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('progress_pct')) {
                obj['progress_pct'] = ApiClient.convertToType(data['progress_pct'], 'Number');
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
            if (data.hasOwnProperty('target_value')) {
                obj['target_value'] = ApiClient.convertToType(data['target_value'], 'Number');
            }
            if (data.hasOwnProperty('target_year')) {
                obj['target_year'] = ApiClient.convertToType(data['target_year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TargetProgress</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TargetProgress</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TargetProgress.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }

        return true;
    }


}

TargetProgress.RequiredProperties = ["base_value", "base_year", "description", "id", "scope", "target_value", "target_year"];

/**
 * @member {Number} base_value
 */
TargetProgress.prototype['base_value'] = undefined;

/**
 * @member {Number} base_year
 */
TargetProgress.prototype['base_year'] = undefined;

/**
 * @member {String} description
 */
TargetProgress.prototype['description'] = undefined;

/**
 * @member {String} id
 */
TargetProgress.prototype['id'] = undefined;

/**
 * Current year's emissions for the scope as % of the target. None when no data.
 * @member {Number} progress_pct
 */
TargetProgress.prototype['progress_pct'] = undefined;

/**
 * @member {String} scope
 */
TargetProgress.prototype['scope'] = undefined;

/**
 * @member {Number} target_value
 */
TargetProgress.prototype['target_value'] = undefined;

/**
 * @member {Number} target_year
 */
TargetProgress.prototype['target_year'] = undefined;






export default TargetProgress;


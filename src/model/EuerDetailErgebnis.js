/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EuerZeileDetail from './EuerZeileDetail';

/**
 * The EuerDetailErgebnis model module.
 * @module model/EuerDetailErgebnis
 * @version 0.1.0
 */
class EuerDetailErgebnis {
    /**
     * Constructs a new <code>EuerDetailErgebnis</code>.
     * @alias module:model/EuerDetailErgebnis
     * @param jahr {Number} 
     * @param zeilen {Array.<module:model/EuerZeileDetail>} 
     */
    constructor(jahr, zeilen) { 
        
        EuerDetailErgebnis.initialize(this, jahr, zeilen);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jahr, zeilen) { 
        obj['jahr'] = jahr;
        obj['zeilen'] = zeilen;
    }

    /**
     * Constructs a <code>EuerDetailErgebnis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EuerDetailErgebnis} obj Optional instance to populate.
     * @return {module:model/EuerDetailErgebnis} The populated <code>EuerDetailErgebnis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EuerDetailErgebnis();

            if (data.hasOwnProperty('jahr')) {
                obj['jahr'] = ApiClient.convertToType(data['jahr'], 'Number');
            }
            if (data.hasOwnProperty('zeilen')) {
                obj['zeilen'] = ApiClient.convertToType(data['zeilen'], [EuerZeileDetail]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EuerDetailErgebnis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EuerDetailErgebnis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EuerDetailErgebnis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['zeilen']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['zeilen'])) {
                throw new Error("Expected the field `zeilen` to be an array in the JSON data but got " + data['zeilen']);
            }
            // validate the optional field `zeilen` (array)
            for (const item of data['zeilen']) {
                EuerZeileDetail.validateJSON(item);
            };
        }

        return true;
    }


}

EuerDetailErgebnis.RequiredProperties = ["jahr", "zeilen"];

/**
 * @member {Number} jahr
 */
EuerDetailErgebnis.prototype['jahr'] = undefined;

/**
 * @member {Array.<module:model/EuerZeileDetail>} zeilen
 */
EuerDetailErgebnis.prototype['zeilen'] = undefined;






export default EuerDetailErgebnis;


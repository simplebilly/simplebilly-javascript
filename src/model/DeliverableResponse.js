/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DeliverableResponse model module.
 * @module model/DeliverableResponse
 * @version 0.1.0
 */
class DeliverableResponse {
    /**
     * Constructs a new <code>DeliverableResponse</code>.
     * @alias module:model/DeliverableResponse
     * @param availableStock {Number} 
     * @param deliverableQuantity {Number} 
     * @param productId {String} 
     * @param reservedStock {Number} 
     */
    constructor(availableStock, deliverableQuantity, productId, reservedStock) { 
        
        DeliverableResponse.initialize(this, availableStock, deliverableQuantity, productId, reservedStock);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, availableStock, deliverableQuantity, productId, reservedStock) { 
        obj['availableStock'] = availableStock;
        obj['deliverableQuantity'] = deliverableQuantity;
        obj['productId'] = productId;
        obj['reservedStock'] = reservedStock;
    }

    /**
     * Constructs a <code>DeliverableResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DeliverableResponse} obj Optional instance to populate.
     * @return {module:model/DeliverableResponse} The populated <code>DeliverableResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DeliverableResponse();

            if (data.hasOwnProperty('availableStock')) {
                obj['availableStock'] = ApiClient.convertToType(data['availableStock'], 'Number');
            }
            if (data.hasOwnProperty('deliverableQuantity')) {
                obj['deliverableQuantity'] = ApiClient.convertToType(data['deliverableQuantity'], 'Number');
            }
            if (data.hasOwnProperty('maxSellable')) {
                obj['maxSellable'] = ApiClient.convertToType(data['maxSellable'], 'Number');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('reservedStock')) {
                obj['reservedStock'] = ApiClient.convertToType(data['reservedStock'], 'Number');
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DeliverableResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DeliverableResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DeliverableResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}

DeliverableResponse.RequiredProperties = ["availableStock", "deliverableQuantity", "productId", "reservedStock"];

/**
 * @member {Number} availableStock
 */
DeliverableResponse.prototype['availableStock'] = undefined;

/**
 * @member {Number} deliverableQuantity
 */
DeliverableResponse.prototype['deliverableQuantity'] = undefined;

/**
 * @member {Number} maxSellable
 */
DeliverableResponse.prototype['maxSellable'] = undefined;

/**
 * @member {String} productId
 */
DeliverableResponse.prototype['productId'] = undefined;

/**
 * @member {Number} reservedStock
 */
DeliverableResponse.prototype['reservedStock'] = undefined;

/**
 * @member {String} warehouseId
 */
DeliverableResponse.prototype['warehouseId'] = undefined;






export default DeliverableResponse;


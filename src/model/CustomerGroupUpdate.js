/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CustomerGroupUpdate model module.
 * @module model/CustomerGroupUpdate
 * @version 0.1.0
 */
class CustomerGroupUpdate {
    /**
     * Constructs a new <code>CustomerGroupUpdate</code>.
     * @alias module:model/CustomerGroupUpdate
     */
    constructor() { 
        
        CustomerGroupUpdate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CustomerGroupUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomerGroupUpdate} obj Optional instance to populate.
     * @return {module:model/CustomerGroupUpdate} The populated <code>CustomerGroupUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomerGroupUpdate();

            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('memberIds')) {
                obj['memberIds'] = ApiClient.convertToType(data['memberIds'], ['String']);
            }
            if (data.hasOwnProperty('membershipFilter')) {
                obj['membershipFilter'] = ApiClient.convertToType(data['membershipFilter'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomerGroupUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomerGroupUpdate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['memberIds'])) {
            throw new Error("Expected the field `memberIds` to be an array in the JSON data but got " + data['memberIds']);
        }
        // ensure the json data is a string
        if (data['membershipFilter'] && !(typeof data['membershipFilter'] === 'string' || data['membershipFilter'] instanceof String)) {
            throw new Error("Expected the field `membershipFilter` to be a primitive type in the JSON string but got " + data['membershipFilter']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}



/**
 * @member {String} description
 */
CustomerGroupUpdate.prototype['description'] = undefined;

/**
 * Contact ids that are members of this group.
 * @member {Array.<String>} memberIds
 */
CustomerGroupUpdate.prototype['memberIds'] = undefined;

/**
 * Rule description for membership, e.g. \"orders > 5 last 12 months\".
 * @member {String} membershipFilter
 */
CustomerGroupUpdate.prototype['membershipFilter'] = undefined;

/**
 * Unique group name, e.g. \"VIP\", \"Wholesale\", \"Newsletter\".
 * @member {String} name
 */
CustomerGroupUpdate.prototype['name'] = undefined;






export default CustomerGroupUpdate;


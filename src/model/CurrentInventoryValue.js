/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InventoryValuePoint from './InventoryValuePoint';

/**
 * The CurrentInventoryValue model module.
 * @module model/CurrentInventoryValue
 * @version 0.1.0
 */
class CurrentInventoryValue {
    /**
     * Constructs a new <code>CurrentInventoryValue</code>.
     * @alias module:model/CurrentInventoryValue
     * @param history {Array.<module:model/InventoryValuePoint>} 
     * @param productCount {Number} 
     * @param totalPurchaseValue {String} 
     * @param totalSalesValue {String} 
     */
    constructor(history, productCount, totalPurchaseValue, totalSalesValue) { 
        
        CurrentInventoryValue.initialize(this, history, productCount, totalPurchaseValue, totalSalesValue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, history, productCount, totalPurchaseValue, totalSalesValue) { 
        obj['history'] = history;
        obj['product_count'] = productCount;
        obj['total_purchase_value'] = totalPurchaseValue;
        obj['total_sales_value'] = totalSalesValue;
    }

    /**
     * Constructs a <code>CurrentInventoryValue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurrentInventoryValue} obj Optional instance to populate.
     * @return {module:model/CurrentInventoryValue} The populated <code>CurrentInventoryValue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurrentInventoryValue();

            if (data.hasOwnProperty('history')) {
                obj['history'] = ApiClient.convertToType(data['history'], [InventoryValuePoint]);
            }
            if (data.hasOwnProperty('product_count')) {
                obj['product_count'] = ApiClient.convertToType(data['product_count'], 'Number');
            }
            if (data.hasOwnProperty('total_purchase_value')) {
                obj['total_purchase_value'] = ApiClient.convertToType(data['total_purchase_value'], 'String');
            }
            if (data.hasOwnProperty('total_sales_value')) {
                obj['total_sales_value'] = ApiClient.convertToType(data['total_sales_value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurrentInventoryValue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurrentInventoryValue</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurrentInventoryValue.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['history']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['history'])) {
                throw new Error("Expected the field `history` to be an array in the JSON data but got " + data['history']);
            }
            // validate the optional field `history` (array)
            for (const item of data['history']) {
                InventoryValuePoint.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_purchase_value'] && !(typeof data['total_purchase_value'] === 'string' || data['total_purchase_value'] instanceof String)) {
            throw new Error("Expected the field `total_purchase_value` to be a primitive type in the JSON string but got " + data['total_purchase_value']);
        }
        // ensure the json data is a string
        if (data['total_sales_value'] && !(typeof data['total_sales_value'] === 'string' || data['total_sales_value'] instanceof String)) {
            throw new Error("Expected the field `total_sales_value` to be a primitive type in the JSON string but got " + data['total_sales_value']);
        }

        return true;
    }


}

CurrentInventoryValue.RequiredProperties = ["history", "product_count", "total_purchase_value", "total_sales_value"];

/**
 * @member {Array.<module:model/InventoryValuePoint>} history
 */
CurrentInventoryValue.prototype['history'] = undefined;

/**
 * @member {Number} product_count
 */
CurrentInventoryValue.prototype['product_count'] = undefined;

/**
 * @member {String} total_purchase_value
 */
CurrentInventoryValue.prototype['total_purchase_value'] = undefined;

/**
 * @member {String} total_sales_value
 */
CurrentInventoryValue.prototype['total_sales_value'] = undefined;






export default CurrentInventoryValue;


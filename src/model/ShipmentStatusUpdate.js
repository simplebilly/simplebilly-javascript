/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ShipmentStatusUpdate model module.
 * @module model/ShipmentStatusUpdate
 * @version 0.1.0
 */
class ShipmentStatusUpdate {
    /**
     * Constructs a new <code>ShipmentStatusUpdate</code>.
     * @alias module:model/ShipmentStatusUpdate
     * @param status {String} 
     */
    constructor(status) { 
        
        ShipmentStatusUpdate.initialize(this, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, status) { 
        obj['status'] = status;
    }

    /**
     * Constructs a <code>ShipmentStatusUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ShipmentStatusUpdate} obj Optional instance to populate.
     * @return {module:model/ShipmentStatusUpdate} The populated <code>ShipmentStatusUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ShipmentStatusUpdate();

            if (data.hasOwnProperty('delivered_at')) {
                obj['delivered_at'] = ApiClient.convertToType(data['delivered_at'], 'String');
            }
            if (data.hasOwnProperty('signed_by')) {
                obj['signed_by'] = ApiClient.convertToType(data['signed_by'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('tracking_number')) {
                obj['tracking_number'] = ApiClient.convertToType(data['tracking_number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ShipmentStatusUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ShipmentStatusUpdate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ShipmentStatusUpdate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['delivered_at'] && !(typeof data['delivered_at'] === 'string' || data['delivered_at'] instanceof String)) {
            throw new Error("Expected the field `delivered_at` to be a primitive type in the JSON string but got " + data['delivered_at']);
        }
        // ensure the json data is a string
        if (data['signed_by'] && !(typeof data['signed_by'] === 'string' || data['signed_by'] instanceof String)) {
            throw new Error("Expected the field `signed_by` to be a primitive type in the JSON string but got " + data['signed_by']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['tracking_number'] && !(typeof data['tracking_number'] === 'string' || data['tracking_number'] instanceof String)) {
            throw new Error("Expected the field `tracking_number` to be a primitive type in the JSON string but got " + data['tracking_number']);
        }

        return true;
    }


}

ShipmentStatusUpdate.RequiredProperties = ["status"];

/**
 * @member {String} delivered_at
 */
ShipmentStatusUpdate.prototype['delivered_at'] = undefined;

/**
 * @member {String} signed_by
 */
ShipmentStatusUpdate.prototype['signed_by'] = undefined;

/**
 * @member {String} status
 */
ShipmentStatusUpdate.prototype['status'] = undefined;

/**
 * @member {String} tracking_number
 */
ShipmentStatusUpdate.prototype['tracking_number'] = undefined;






export default ShipmentStatusUpdate;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PostingCategory model module.
 * @module model/PostingCategory
 * @version 0.1.0
 */
class PostingCategory {
    /**
     * Constructs a new <code>PostingCategory</code>.
     * @alias module:model/PostingCategory
     * @param categoryId {String} 
     * @param defaultVatRate {Number} 
     * @param isActive {Boolean} 
     * @param isSystem {Boolean} 
     * @param name {String} 
     * @param skrVersion {String} 
     * @param type {String} 
     */
    constructor(categoryId, defaultVatRate, isActive, isSystem, name, skrVersion, type) { 
        
        PostingCategory.initialize(this, categoryId, defaultVatRate, isActive, isSystem, name, skrVersion, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, categoryId, defaultVatRate, isActive, isSystem, name, skrVersion, type) { 
        obj['category_id'] = categoryId;
        obj['default_vat_rate'] = defaultVatRate;
        obj['is_active'] = isActive;
        obj['is_system'] = isSystem;
        obj['name'] = name;
        obj['skr_version'] = skrVersion;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>PostingCategory</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PostingCategory} obj Optional instance to populate.
     * @return {module:model/PostingCategory} The populated <code>PostingCategory</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PostingCategory();

            if (data.hasOwnProperty('account_number')) {
                obj['account_number'] = ApiClient.convertToType(data['account_number'], 'String');
            }
            if (data.hasOwnProperty('account_number_skr03')) {
                obj['account_number_skr03'] = ApiClient.convertToType(data['account_number_skr03'], 'String');
            }
            if (data.hasOwnProperty('account_number_skr04')) {
                obj['account_number_skr04'] = ApiClient.convertToType(data['account_number_skr04'], 'String');
            }
            if (data.hasOwnProperty('account_number_skr49')) {
                obj['account_number_skr49'] = ApiClient.convertToType(data['account_number_skr49'], 'String');
            }
            if (data.hasOwnProperty('category_id')) {
                obj['category_id'] = ApiClient.convertToType(data['category_id'], 'String');
            }
            if (data.hasOwnProperty('default_vat_rate')) {
                obj['default_vat_rate'] = ApiClient.convertToType(data['default_vat_rate'], 'Number');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('eks_category')) {
                obj['eks_category'] = ApiClient.convertToType(data['eks_category'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('is_system')) {
                obj['is_system'] = ApiClient.convertToType(data['is_system'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('skr_version')) {
                obj['skr_version'] = ApiClient.convertToType(data['skr_version'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PostingCategory</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PostingCategory</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PostingCategory.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_number'] && !(typeof data['account_number'] === 'string' || data['account_number'] instanceof String)) {
            throw new Error("Expected the field `account_number` to be a primitive type in the JSON string but got " + data['account_number']);
        }
        // ensure the json data is a string
        if (data['account_number_skr03'] && !(typeof data['account_number_skr03'] === 'string' || data['account_number_skr03'] instanceof String)) {
            throw new Error("Expected the field `account_number_skr03` to be a primitive type in the JSON string but got " + data['account_number_skr03']);
        }
        // ensure the json data is a string
        if (data['account_number_skr04'] && !(typeof data['account_number_skr04'] === 'string' || data['account_number_skr04'] instanceof String)) {
            throw new Error("Expected the field `account_number_skr04` to be a primitive type in the JSON string but got " + data['account_number_skr04']);
        }
        // ensure the json data is a string
        if (data['account_number_skr49'] && !(typeof data['account_number_skr49'] === 'string' || data['account_number_skr49'] instanceof String)) {
            throw new Error("Expected the field `account_number_skr49` to be a primitive type in the JSON string but got " + data['account_number_skr49']);
        }
        // ensure the json data is a string
        if (data['category_id'] && !(typeof data['category_id'] === 'string' || data['category_id'] instanceof String)) {
            throw new Error("Expected the field `category_id` to be a primitive type in the JSON string but got " + data['category_id']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['eks_category'] && !(typeof data['eks_category'] === 'string' || data['eks_category'] instanceof String)) {
            throw new Error("Expected the field `eks_category` to be a primitive type in the JSON string but got " + data['eks_category']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['skr_version'] && !(typeof data['skr_version'] === 'string' || data['skr_version'] instanceof String)) {
            throw new Error("Expected the field `skr_version` to be a primitive type in the JSON string but got " + data['skr_version']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

PostingCategory.RequiredProperties = ["category_id", "default_vat_rate", "is_active", "is_system", "name", "skr_version", "type"];

/**
 * @member {String} account_number
 */
PostingCategory.prototype['account_number'] = undefined;

/**
 * @member {String} account_number_skr03
 */
PostingCategory.prototype['account_number_skr03'] = undefined;

/**
 * @member {String} account_number_skr04
 */
PostingCategory.prototype['account_number_skr04'] = undefined;

/**
 * @member {String} account_number_skr49
 */
PostingCategory.prototype['account_number_skr49'] = undefined;

/**
 * @member {String} category_id
 */
PostingCategory.prototype['category_id'] = undefined;

/**
 * @member {Number} default_vat_rate
 */
PostingCategory.prototype['default_vat_rate'] = undefined;

/**
 * @member {String} description
 */
PostingCategory.prototype['description'] = undefined;

/**
 * @member {String} eks_category
 */
PostingCategory.prototype['eks_category'] = undefined;

/**
 * @member {Boolean} is_active
 */
PostingCategory.prototype['is_active'] = undefined;

/**
 * @member {Boolean} is_system
 */
PostingCategory.prototype['is_system'] = undefined;

/**
 * @member {String} name
 */
PostingCategory.prototype['name'] = undefined;

/**
 * @member {String} skr_version
 */
PostingCategory.prototype['skr_version'] = undefined;

/**
 * @member {String} type
 */
PostingCategory.prototype['type'] = undefined;






export default PostingCategory;


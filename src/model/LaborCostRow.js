/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The LaborCostRow model module.
 * @module model/LaborCostRow
 * @version 0.1.0
 */
class LaborCostRow {
    /**
     * Constructs a new <code>LaborCostRow</code>.
     * @alias module:model/LaborCostRow
     * @param cost {String} 
     * @param groupKey {String} 
     * @param hours {String} 
     */
    constructor(cost, groupKey, hours) { 
        
        LaborCostRow.initialize(this, cost, groupKey, hours);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, cost, groupKey, hours) { 
        obj['cost'] = cost;
        obj['groupKey'] = groupKey;
        obj['hours'] = hours;
    }

    /**
     * Constructs a <code>LaborCostRow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LaborCostRow} obj Optional instance to populate.
     * @return {module:model/LaborCostRow} The populated <code>LaborCostRow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LaborCostRow();

            if (data.hasOwnProperty('cost')) {
                obj['cost'] = ApiClient.convertToType(data['cost'], 'String');
            }
            if (data.hasOwnProperty('employeeId')) {
                obj['employeeId'] = ApiClient.convertToType(data['employeeId'], 'String');
            }
            if (data.hasOwnProperty('groupKey')) {
                obj['groupKey'] = ApiClient.convertToType(data['groupKey'], 'String');
            }
            if (data.hasOwnProperty('hours')) {
                obj['hours'] = ApiClient.convertToType(data['hours'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LaborCostRow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LaborCostRow</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LaborCostRow.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cost'] && !(typeof data['cost'] === 'string' || data['cost'] instanceof String)) {
            throw new Error("Expected the field `cost` to be a primitive type in the JSON string but got " + data['cost']);
        }
        // ensure the json data is a string
        if (data['employeeId'] && !(typeof data['employeeId'] === 'string' || data['employeeId'] instanceof String)) {
            throw new Error("Expected the field `employeeId` to be a primitive type in the JSON string but got " + data['employeeId']);
        }
        // ensure the json data is a string
        if (data['groupKey'] && !(typeof data['groupKey'] === 'string' || data['groupKey'] instanceof String)) {
            throw new Error("Expected the field `groupKey` to be a primitive type in the JSON string but got " + data['groupKey']);
        }
        // ensure the json data is a string
        if (data['hours'] && !(typeof data['hours'] === 'string' || data['hours'] instanceof String)) {
            throw new Error("Expected the field `hours` to be a primitive type in the JSON string but got " + data['hours']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

LaborCostRow.RequiredProperties = ["cost", "groupKey", "hours"];

/**
 * @member {String} cost
 */
LaborCostRow.prototype['cost'] = undefined;

/**
 * @member {String} employeeId
 */
LaborCostRow.prototype['employeeId'] = undefined;

/**
 * @member {String} groupKey
 */
LaborCostRow.prototype['groupKey'] = undefined;

/**
 * @member {String} hours
 */
LaborCostRow.prototype['hours'] = undefined;

/**
 * @member {String} name
 */
LaborCostRow.prototype['name'] = undefined;






export default LaborCostRow;


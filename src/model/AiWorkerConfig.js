/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AiWorkerConfig model module.
 * @module model/AiWorkerConfig
 * @version 0.1.0
 */
class AiWorkerConfig {
    /**
     * Constructs a new <code>AiWorkerConfig</code>.
     * @alias module:model/AiWorkerConfig
     * @param autoReply {Boolean} 
     * @param createdAt {Date} 
     * @param id {String} 
     * @param isActive {Boolean} 
     * @param maxToolCalls {Number} 
     * @param model {String} 
     * @param name {String} 
     * @param provider {String} 
     * @param systemPrompt {String} 
     * @param tenantId {String} 
     * @param triggerOn {Array.<String>} 
     */
    constructor(autoReply, createdAt, id, isActive, maxToolCalls, model, name, provider, systemPrompt, tenantId, triggerOn) { 
        
        AiWorkerConfig.initialize(this, autoReply, createdAt, id, isActive, maxToolCalls, model, name, provider, systemPrompt, tenantId, triggerOn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, autoReply, createdAt, id, isActive, maxToolCalls, model, name, provider, systemPrompt, tenantId, triggerOn) { 
        obj['auto_reply'] = autoReply;
        obj['created_at'] = createdAt;
        obj['id'] = id;
        obj['is_active'] = isActive;
        obj['max_tool_calls'] = maxToolCalls;
        obj['model'] = model;
        obj['name'] = name;
        obj['provider'] = provider;
        obj['system_prompt'] = systemPrompt;
        obj['tenant_id'] = tenantId;
        obj['trigger_on'] = triggerOn;
    }

    /**
     * Constructs a <code>AiWorkerConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AiWorkerConfig} obj Optional instance to populate.
     * @return {module:model/AiWorkerConfig} The populated <code>AiWorkerConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AiWorkerConfig();

            if (data.hasOwnProperty('auto_reply')) {
                obj['auto_reply'] = ApiClient.convertToType(data['auto_reply'], 'Boolean');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('max_tool_calls')) {
                obj['max_tool_calls'] = ApiClient.convertToType(data['max_tool_calls'], 'Number');
            }
            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('system_prompt')) {
                obj['system_prompt'] = ApiClient.convertToType(data['system_prompt'], 'String');
            }
            if (data.hasOwnProperty('tenant_id')) {
                obj['tenant_id'] = ApiClient.convertToType(data['tenant_id'], 'String');
            }
            if (data.hasOwnProperty('trigger_on')) {
                obj['trigger_on'] = ApiClient.convertToType(data['trigger_on'], ['String']);
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AiWorkerConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AiWorkerConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AiWorkerConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['system_prompt'] && !(typeof data['system_prompt'] === 'string' || data['system_prompt'] instanceof String)) {
            throw new Error("Expected the field `system_prompt` to be a primitive type in the JSON string but got " + data['system_prompt']);
        }
        // ensure the json data is a string
        if (data['tenant_id'] && !(typeof data['tenant_id'] === 'string' || data['tenant_id'] instanceof String)) {
            throw new Error("Expected the field `tenant_id` to be a primitive type in the JSON string but got " + data['tenant_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['trigger_on'])) {
            throw new Error("Expected the field `trigger_on` to be an array in the JSON data but got " + data['trigger_on']);
        }

        return true;
    }


}

AiWorkerConfig.RequiredProperties = ["auto_reply", "created_at", "id", "is_active", "max_tool_calls", "model", "name", "provider", "system_prompt", "tenant_id", "trigger_on"];

/**
 * @member {Boolean} auto_reply
 */
AiWorkerConfig.prototype['auto_reply'] = undefined;

/**
 * @member {Date} created_at
 */
AiWorkerConfig.prototype['created_at'] = undefined;

/**
 * @member {String} id
 */
AiWorkerConfig.prototype['id'] = undefined;

/**
 * @member {Boolean} is_active
 */
AiWorkerConfig.prototype['is_active'] = undefined;

/**
 * @member {Number} max_tool_calls
 */
AiWorkerConfig.prototype['max_tool_calls'] = undefined;

/**
 * @member {String} model
 */
AiWorkerConfig.prototype['model'] = undefined;

/**
 * @member {String} name
 */
AiWorkerConfig.prototype['name'] = undefined;

/**
 * @member {String} provider
 */
AiWorkerConfig.prototype['provider'] = undefined;

/**
 * @member {String} system_prompt
 */
AiWorkerConfig.prototype['system_prompt'] = undefined;

/**
 * @member {String} tenant_id
 */
AiWorkerConfig.prototype['tenant_id'] = undefined;

/**
 * @member {Array.<String>} trigger_on
 */
AiWorkerConfig.prototype['trigger_on'] = undefined;

/**
 * @member {Date} updated_at
 */
AiWorkerConfig.prototype['updated_at'] = undefined;






export default AiWorkerConfig;


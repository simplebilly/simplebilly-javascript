/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ActivityStatus from './ActivityStatus';
import ActivityType from './ActivityType';

/**
 * The ActivityUpdate model module.
 * @module model/ActivityUpdate
 * @version 0.1.0
 */
class ActivityUpdate {
    /**
     * Constructs a new <code>ActivityUpdate</code>.
     * @alias module:model/ActivityUpdate
     */
    constructor() { 
        
        ActivityUpdate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ActivityUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ActivityUpdate} obj Optional instance to populate.
     * @return {module:model/ActivityUpdate} The populated <code>ActivityUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ActivityUpdate();

            if (data.hasOwnProperty('activityType')) {
                obj['activityType'] = ApiClient.convertToType(data['activityType'], ActivityType);
            }
            if (data.hasOwnProperty('assignedTo')) {
                obj['assignedTo'] = ApiClient.convertToType(data['assignedTo'], 'String');
            }
            if (data.hasOwnProperty('contactId')) {
                obj['contactId'] = ApiClient.convertToType(data['contactId'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('dueDate')) {
                obj['dueDate'] = ApiClient.convertToType(data['dueDate'], 'Date');
            }
            if (data.hasOwnProperty('reminderDate')) {
                obj['reminderDate'] = ApiClient.convertToType(data['reminderDate'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], ActivityStatus);
            }
            if (data.hasOwnProperty('subject')) {
                obj['subject'] = ApiClient.convertToType(data['subject'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ActivityUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ActivityUpdate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['assignedTo'] && !(typeof data['assignedTo'] === 'string' || data['assignedTo'] instanceof String)) {
            throw new Error("Expected the field `assignedTo` to be a primitive type in the JSON string but got " + data['assignedTo']);
        }
        // ensure the json data is a string
        if (data['contactId'] && !(typeof data['contactId'] === 'string' || data['contactId'] instanceof String)) {
            throw new Error("Expected the field `contactId` to be a primitive type in the JSON string but got " + data['contactId']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['subject'] && !(typeof data['subject'] === 'string' || data['subject'] instanceof String)) {
            throw new Error("Expected the field `subject` to be a primitive type in the JSON string but got " + data['subject']);
        }

        return true;
    }


}



/**
 * One of: call | email | meeting | task | note
 * @member {module:model/ActivityType} activityType
 */
ActivityUpdate.prototype['activityType'] = undefined;

/**
 * User responsible (`employee.employee_id`).
 * @member {String} assignedTo
 */
ActivityUpdate.prototype['assignedTo'] = undefined;

/**
 * Contact this activity belongs to (`contact.contact_id`). References the contact entity.
 * @member {String} contactId
 */
ActivityUpdate.prototype['contactId'] = undefined;

/**
 * @member {String} description
 */
ActivityUpdate.prototype['description'] = undefined;

/**
 * Follow-up / Wiedervorlage date. Open activities with a due date in the past are overdue.
 * @member {Date} dueDate
 */
ActivityUpdate.prototype['dueDate'] = undefined;

/**
 * When to remind about the follow-up.
 * @member {Date} reminderDate
 */
ActivityUpdate.prototype['reminderDate'] = undefined;

/**
 * One of: open | done | cancelled
 * @member {module:model/ActivityStatus} status
 */
ActivityUpdate.prototype['status'] = undefined;

/**
 * Short subject line.
 * @member {String} subject
 */
ActivityUpdate.prototype['subject'] = undefined;






export default ActivityUpdate;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ElsterStatus model module.
 * @module model/ElsterStatus
 * @version 0.1.0
 */
class ElsterStatus {
    /**
     * Constructs a new <code>ElsterStatus</code>.
     * @alias module:model/ElsterStatus
     * @param certConfigured {Boolean} 
     * @param ericAvailable {Boolean} 
     * @param featureEnabled {Boolean} 
     * @param hint {String} 
     * @param mode {String} 
     * @param vendorIdConfigured {Boolean} 
     */
    constructor(certConfigured, ericAvailable, featureEnabled, hint, mode, vendorIdConfigured) { 
        
        ElsterStatus.initialize(this, certConfigured, ericAvailable, featureEnabled, hint, mode, vendorIdConfigured);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, certConfigured, ericAvailable, featureEnabled, hint, mode, vendorIdConfigured) { 
        obj['cert_configured'] = certConfigured;
        obj['eric_available'] = ericAvailable;
        obj['feature_enabled'] = featureEnabled;
        obj['hint'] = hint;
        obj['mode'] = mode;
        obj['vendor_id_configured'] = vendorIdConfigured;
    }

    /**
     * Constructs a <code>ElsterStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ElsterStatus} obj Optional instance to populate.
     * @return {module:model/ElsterStatus} The populated <code>ElsterStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ElsterStatus();

            if (data.hasOwnProperty('cert_configured')) {
                obj['cert_configured'] = ApiClient.convertToType(data['cert_configured'], 'Boolean');
            }
            if (data.hasOwnProperty('eric_available')) {
                obj['eric_available'] = ApiClient.convertToType(data['eric_available'], 'Boolean');
            }
            if (data.hasOwnProperty('eric_version')) {
                obj['eric_version'] = ApiClient.convertToType(data['eric_version'], 'String');
            }
            if (data.hasOwnProperty('feature_enabled')) {
                obj['feature_enabled'] = ApiClient.convertToType(data['feature_enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('hint')) {
                obj['hint'] = ApiClient.convertToType(data['hint'], 'String');
            }
            if (data.hasOwnProperty('mode')) {
                obj['mode'] = ApiClient.convertToType(data['mode'], 'String');
            }
            if (data.hasOwnProperty('vendor_id_configured')) {
                obj['vendor_id_configured'] = ApiClient.convertToType(data['vendor_id_configured'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ElsterStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ElsterStatus</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ElsterStatus.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['eric_version'] && !(typeof data['eric_version'] === 'string' || data['eric_version'] instanceof String)) {
            throw new Error("Expected the field `eric_version` to be a primitive type in the JSON string but got " + data['eric_version']);
        }
        // ensure the json data is a string
        if (data['hint'] && !(typeof data['hint'] === 'string' || data['hint'] instanceof String)) {
            throw new Error("Expected the field `hint` to be a primitive type in the JSON string but got " + data['hint']);
        }
        // ensure the json data is a string
        if (data['mode'] && !(typeof data['mode'] === 'string' || data['mode'] instanceof String)) {
            throw new Error("Expected the field `mode` to be a primitive type in the JSON string but got " + data['mode']);
        }

        return true;
    }


}

ElsterStatus.RequiredProperties = ["cert_configured", "eric_available", "feature_enabled", "hint", "mode", "vendor_id_configured"];

/**
 * @member {Boolean} cert_configured
 */
ElsterStatus.prototype['cert_configured'] = undefined;

/**
 * @member {Boolean} eric_available
 */
ElsterStatus.prototype['eric_available'] = undefined;

/**
 * @member {String} eric_version
 */
ElsterStatus.prototype['eric_version'] = undefined;

/**
 * @member {Boolean} feature_enabled
 */
ElsterStatus.prototype['feature_enabled'] = undefined;

/**
 * @member {String} hint
 */
ElsterStatus.prototype['hint'] = undefined;

/**
 * @member {String} mode
 */
ElsterStatus.prototype['mode'] = undefined;

/**
 * @member {Boolean} vendor_id_configured
 */
ElsterStatus.prototype['vendor_id_configured'] = undefined;






export default ElsterStatus;


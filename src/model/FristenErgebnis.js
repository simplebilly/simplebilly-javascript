/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FristEintrag from './FristEintrag';

/**
 * The FristenErgebnis model module.
 * @module model/FristenErgebnis
 * @version 0.1.0
 */
class FristenErgebnis {
    /**
     * Constructs a new <code>FristenErgebnis</code>.
     * @alias module:model/FristenErgebnis
     * @param anzahl {Number} 
     * @param fristen {Array.<module:model/FristEintrag>} 
     */
    constructor(anzahl, fristen) { 
        
        FristenErgebnis.initialize(this, anzahl, fristen);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, anzahl, fristen) { 
        obj['anzahl'] = anzahl;
        obj['fristen'] = fristen;
    }

    /**
     * Constructs a <code>FristenErgebnis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FristenErgebnis} obj Optional instance to populate.
     * @return {module:model/FristenErgebnis} The populated <code>FristenErgebnis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FristenErgebnis();

            if (data.hasOwnProperty('anzahl')) {
                obj['anzahl'] = ApiClient.convertToType(data['anzahl'], 'Number');
            }
            if (data.hasOwnProperty('fristen')) {
                obj['fristen'] = ApiClient.convertToType(data['fristen'], [FristEintrag]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FristenErgebnis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FristenErgebnis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FristenErgebnis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['fristen']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['fristen'])) {
                throw new Error("Expected the field `fristen` to be an array in the JSON data but got " + data['fristen']);
            }
            // validate the optional field `fristen` (array)
            for (const item of data['fristen']) {
                FristEintrag.validateJSON(item);
            };
        }

        return true;
    }


}

FristenErgebnis.RequiredProperties = ["anzahl", "fristen"];

/**
 * @member {Number} anzahl
 */
FristenErgebnis.prototype['anzahl'] = undefined;

/**
 * @member {Array.<module:model/FristEintrag>} fristen
 */
FristenErgebnis.prototype['fristen'] = undefined;






export default FristenErgebnis;


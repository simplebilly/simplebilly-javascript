/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PayrollMonth from './PayrollMonth';

/**
 * The PayrollSummary model module.
 * @module model/PayrollSummary
 * @version 0.1.0
 */
class PayrollSummary {
    /**
     * Constructs a new <code>PayrollSummary</code>.
     * @alias module:model/PayrollSummary
     * @param firstName {String} 
     * @param id {String} 
     * @param jobTitle {String} 
     * @param lastName {String} 
     * @param months {Array.<module:model/PayrollMonth>} 
     * @param year {Number} 
     */
    constructor(firstName, id, jobTitle, lastName, months, year) { 
        
        PayrollSummary.initialize(this, firstName, id, jobTitle, lastName, months, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, firstName, id, jobTitle, lastName, months, year) { 
        obj['firstName'] = firstName;
        obj['id'] = id;
        obj['jobTitle'] = jobTitle;
        obj['lastName'] = lastName;
        obj['months'] = months;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>PayrollSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayrollSummary} obj Optional instance to populate.
     * @return {module:model/PayrollSummary} The populated <code>PayrollSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayrollSummary();

            if (data.hasOwnProperty('firstName')) {
                obj['firstName'] = ApiClient.convertToType(data['firstName'], 'String');
            }
            if (data.hasOwnProperty('hourlyGross')) {
                obj['hourlyGross'] = ApiClient.convertToType(data['hourlyGross'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('jobTitle')) {
                obj['jobTitle'] = ApiClient.convertToType(data['jobTitle'], 'String');
            }
            if (data.hasOwnProperty('lastName')) {
                obj['lastName'] = ApiClient.convertToType(data['lastName'], 'String');
            }
            if (data.hasOwnProperty('monthlySalary')) {
                obj['monthlySalary'] = ApiClient.convertToType(data['monthlySalary'], 'String');
            }
            if (data.hasOwnProperty('months')) {
                obj['months'] = ApiClient.convertToType(data['months'], [PayrollMonth]);
            }
            if (data.hasOwnProperty('weeklyHours')) {
                obj['weeklyHours'] = ApiClient.convertToType(data['weeklyHours'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayrollSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayrollSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayrollSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['firstName'] && !(typeof data['firstName'] === 'string' || data['firstName'] instanceof String)) {
            throw new Error("Expected the field `firstName` to be a primitive type in the JSON string but got " + data['firstName']);
        }
        // ensure the json data is a string
        if (data['hourlyGross'] && !(typeof data['hourlyGross'] === 'string' || data['hourlyGross'] instanceof String)) {
            throw new Error("Expected the field `hourlyGross` to be a primitive type in the JSON string but got " + data['hourlyGross']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['jobTitle'] && !(typeof data['jobTitle'] === 'string' || data['jobTitle'] instanceof String)) {
            throw new Error("Expected the field `jobTitle` to be a primitive type in the JSON string but got " + data['jobTitle']);
        }
        // ensure the json data is a string
        if (data['lastName'] && !(typeof data['lastName'] === 'string' || data['lastName'] instanceof String)) {
            throw new Error("Expected the field `lastName` to be a primitive type in the JSON string but got " + data['lastName']);
        }
        // ensure the json data is a string
        if (data['monthlySalary'] && !(typeof data['monthlySalary'] === 'string' || data['monthlySalary'] instanceof String)) {
            throw new Error("Expected the field `monthlySalary` to be a primitive type in the JSON string but got " + data['monthlySalary']);
        }
        if (data['months']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['months'])) {
                throw new Error("Expected the field `months` to be an array in the JSON data but got " + data['months']);
            }
            // validate the optional field `months` (array)
            for (const item of data['months']) {
                PayrollMonth.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['weeklyHours'] && !(typeof data['weeklyHours'] === 'string' || data['weeklyHours'] instanceof String)) {
            throw new Error("Expected the field `weeklyHours` to be a primitive type in the JSON string but got " + data['weeklyHours']);
        }

        return true;
    }


}

PayrollSummary.RequiredProperties = ["firstName", "id", "jobTitle", "lastName", "months", "year"];

/**
 * @member {String} firstName
 */
PayrollSummary.prototype['firstName'] = undefined;

/**
 * @member {String} hourlyGross
 */
PayrollSummary.prototype['hourlyGross'] = undefined;

/**
 * @member {String} id
 */
PayrollSummary.prototype['id'] = undefined;

/**
 * @member {String} jobTitle
 */
PayrollSummary.prototype['jobTitle'] = undefined;

/**
 * @member {String} lastName
 */
PayrollSummary.prototype['lastName'] = undefined;

/**
 * @member {String} monthlySalary
 */
PayrollSummary.prototype['monthlySalary'] = undefined;

/**
 * @member {Array.<module:model/PayrollMonth>} months
 */
PayrollSummary.prototype['months'] = undefined;

/**
 * @member {String} weeklyHours
 */
PayrollSummary.prototype['weeklyHours'] = undefined;

/**
 * @member {Number} year
 */
PayrollSummary.prototype['year'] = undefined;






export default PayrollSummary;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PayrollSummaryItem from './PayrollSummaryItem';

/**
 * The YearlyPayrollSummary model module.
 * @module model/YearlyPayrollSummary
 * @version 0.1.0
 */
class YearlyPayrollSummary {
    /**
     * Constructs a new <code>YearlyPayrollSummary</code>.
     * @alias module:model/YearlyPayrollSummary
     * @param avgEmployeeCount {Number} 
     * @param months {Array.<module:model/PayrollSummaryItem>} 
     * @param year {Number} 
     * @param yearlyEmployerCost {String} 
     * @param yearlyGross {String} 
     * @param yearlyNet {String} 
     */
    constructor(avgEmployeeCount, months, year, yearlyEmployerCost, yearlyGross, yearlyNet) { 
        
        YearlyPayrollSummary.initialize(this, avgEmployeeCount, months, year, yearlyEmployerCost, yearlyGross, yearlyNet);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, avgEmployeeCount, months, year, yearlyEmployerCost, yearlyGross, yearlyNet) { 
        obj['avg_employee_count'] = avgEmployeeCount;
        obj['months'] = months;
        obj['year'] = year;
        obj['yearly_employer_cost'] = yearlyEmployerCost;
        obj['yearly_gross'] = yearlyGross;
        obj['yearly_net'] = yearlyNet;
    }

    /**
     * Constructs a <code>YearlyPayrollSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/YearlyPayrollSummary} obj Optional instance to populate.
     * @return {module:model/YearlyPayrollSummary} The populated <code>YearlyPayrollSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new YearlyPayrollSummary();

            if (data.hasOwnProperty('avg_employee_count')) {
                obj['avg_employee_count'] = ApiClient.convertToType(data['avg_employee_count'], 'Number');
            }
            if (data.hasOwnProperty('months')) {
                obj['months'] = ApiClient.convertToType(data['months'], [PayrollSummaryItem]);
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
            if (data.hasOwnProperty('yearly_employer_cost')) {
                obj['yearly_employer_cost'] = ApiClient.convertToType(data['yearly_employer_cost'], 'String');
            }
            if (data.hasOwnProperty('yearly_gross')) {
                obj['yearly_gross'] = ApiClient.convertToType(data['yearly_gross'], 'String');
            }
            if (data.hasOwnProperty('yearly_net')) {
                obj['yearly_net'] = ApiClient.convertToType(data['yearly_net'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>YearlyPayrollSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>YearlyPayrollSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of YearlyPayrollSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['months']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['months'])) {
                throw new Error("Expected the field `months` to be an array in the JSON data but got " + data['months']);
            }
            // validate the optional field `months` (array)
            for (const item of data['months']) {
                PayrollSummaryItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['yearly_employer_cost'] && !(typeof data['yearly_employer_cost'] === 'string' || data['yearly_employer_cost'] instanceof String)) {
            throw new Error("Expected the field `yearly_employer_cost` to be a primitive type in the JSON string but got " + data['yearly_employer_cost']);
        }
        // ensure the json data is a string
        if (data['yearly_gross'] && !(typeof data['yearly_gross'] === 'string' || data['yearly_gross'] instanceof String)) {
            throw new Error("Expected the field `yearly_gross` to be a primitive type in the JSON string but got " + data['yearly_gross']);
        }
        // ensure the json data is a string
        if (data['yearly_net'] && !(typeof data['yearly_net'] === 'string' || data['yearly_net'] instanceof String)) {
            throw new Error("Expected the field `yearly_net` to be a primitive type in the JSON string but got " + data['yearly_net']);
        }

        return true;
    }


}

YearlyPayrollSummary.RequiredProperties = ["avg_employee_count", "months", "year", "yearly_employer_cost", "yearly_gross", "yearly_net"];

/**
 * @member {Number} avg_employee_count
 */
YearlyPayrollSummary.prototype['avg_employee_count'] = undefined;

/**
 * @member {Array.<module:model/PayrollSummaryItem>} months
 */
YearlyPayrollSummary.prototype['months'] = undefined;

/**
 * @member {Number} year
 */
YearlyPayrollSummary.prototype['year'] = undefined;

/**
 * @member {String} yearly_employer_cost
 */
YearlyPayrollSummary.prototype['yearly_employer_cost'] = undefined;

/**
 * @member {String} yearly_gross
 */
YearlyPayrollSummary.prototype['yearly_gross'] = undefined;

/**
 * @member {String} yearly_net
 */
YearlyPayrollSummary.prototype['yearly_net'] = undefined;






export default YearlyPayrollSummary;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ReturnWarehouseSummary model module.
 * @module model/ReturnWarehouseSummary
 * @version 0.1.0
 */
class ReturnWarehouseSummary {
    /**
     * Constructs a new <code>ReturnWarehouseSummary</code>.
     * Aggregation for a single warehouse.
     * @alias module:model/ReturnWarehouseSummary
     * @param itemsRestocked {Number} 
     * @param itemsScrapped {Number} 
     * @param returns {Number} 
     */
    constructor(itemsRestocked, itemsScrapped, returns) { 
        
        ReturnWarehouseSummary.initialize(this, itemsRestocked, itemsScrapped, returns);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, itemsRestocked, itemsScrapped, returns) { 
        obj['itemsRestocked'] = itemsRestocked;
        obj['itemsScrapped'] = itemsScrapped;
        obj['returns'] = returns;
    }

    /**
     * Constructs a <code>ReturnWarehouseSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ReturnWarehouseSummary} obj Optional instance to populate.
     * @return {module:model/ReturnWarehouseSummary} The populated <code>ReturnWarehouseSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ReturnWarehouseSummary();

            if (data.hasOwnProperty('itemsRestocked')) {
                obj['itemsRestocked'] = ApiClient.convertToType(data['itemsRestocked'], 'Number');
            }
            if (data.hasOwnProperty('itemsScrapped')) {
                obj['itemsScrapped'] = ApiClient.convertToType(data['itemsScrapped'], 'Number');
            }
            if (data.hasOwnProperty('returns')) {
                obj['returns'] = ApiClient.convertToType(data['returns'], 'Number');
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ReturnWarehouseSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ReturnWarehouseSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ReturnWarehouseSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}

ReturnWarehouseSummary.RequiredProperties = ["itemsRestocked", "itemsScrapped", "returns"];

/**
 * @member {Number} itemsRestocked
 */
ReturnWarehouseSummary.prototype['itemsRestocked'] = undefined;

/**
 * @member {Number} itemsScrapped
 */
ReturnWarehouseSummary.prototype['itemsScrapped'] = undefined;

/**
 * @member {Number} returns
 */
ReturnWarehouseSummary.prototype['returns'] = undefined;

/**
 * @member {String} warehouseId
 */
ReturnWarehouseSummary.prototype['warehouseId'] = undefined;






export default ReturnWarehouseSummary;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BilanzItem model module.
 * @module model/BilanzItem
 * @version 0.1.0
 */
class BilanzItem {
    /**
     * Constructs a new <code>BilanzItem</code>.
     * @alias module:model/BilanzItem
     * @param amount {String} 
     * @param konto {String} 
     * @param name {String} 
     */
    constructor(amount, konto, name) { 
        
        BilanzItem.initialize(this, amount, konto, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount, konto, name) { 
        obj['amount'] = amount;
        obj['konto'] = konto;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>BilanzItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BilanzItem} obj Optional instance to populate.
     * @return {module:model/BilanzItem} The populated <code>BilanzItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BilanzItem();

            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
            if (data.hasOwnProperty('konto')) {
                obj['konto'] = ApiClient.convertToType(data['konto'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BilanzItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BilanzItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BilanzItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }
        // ensure the json data is a string
        if (data['konto'] && !(typeof data['konto'] === 'string' || data['konto'] instanceof String)) {
            throw new Error("Expected the field `konto` to be a primitive type in the JSON string but got " + data['konto']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

BilanzItem.RequiredProperties = ["amount", "konto", "name"];

/**
 * @member {String} amount
 */
BilanzItem.prototype['amount'] = undefined;

/**
 * @member {String} konto
 */
BilanzItem.prototype['konto'] = undefined;

/**
 * @member {String} name
 */
BilanzItem.prototype['name'] = undefined;






export default BilanzItem;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EuerZeile from './EuerZeile';

/**
 * The EuerErgebnis model module.
 * @module model/EuerErgebnis
 * @version 0.1.0
 */
class EuerErgebnis {
    /**
     * Constructs a new <code>EuerErgebnis</code>.
     * @alias module:model/EuerErgebnis
     * @param anlageZugaenge {String} 
     * @param gewinnVerlust {String} 
     * @param jahr {Number} 
     * @param summeAusgaben {String} 
     * @param summeEinnahmen {String} 
     * @param zeilen {Array.<module:model/EuerZeile>} 
     */
    constructor(anlageZugaenge, gewinnVerlust, jahr, summeAusgaben, summeEinnahmen, zeilen) { 
        
        EuerErgebnis.initialize(this, anlageZugaenge, gewinnVerlust, jahr, summeAusgaben, summeEinnahmen, zeilen);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, anlageZugaenge, gewinnVerlust, jahr, summeAusgaben, summeEinnahmen, zeilen) { 
        obj['anlage_zugaenge'] = anlageZugaenge;
        obj['gewinn_verlust'] = gewinnVerlust;
        obj['jahr'] = jahr;
        obj['summe_ausgaben'] = summeAusgaben;
        obj['summe_einnahmen'] = summeEinnahmen;
        obj['zeilen'] = zeilen;
    }

    /**
     * Constructs a <code>EuerErgebnis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EuerErgebnis} obj Optional instance to populate.
     * @return {module:model/EuerErgebnis} The populated <code>EuerErgebnis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EuerErgebnis();

            if (data.hasOwnProperty('anlage_zugaenge')) {
                obj['anlage_zugaenge'] = ApiClient.convertToType(data['anlage_zugaenge'], 'String');
            }
            if (data.hasOwnProperty('gewinn_verlust')) {
                obj['gewinn_verlust'] = ApiClient.convertToType(data['gewinn_verlust'], 'String');
            }
            if (data.hasOwnProperty('jahr')) {
                obj['jahr'] = ApiClient.convertToType(data['jahr'], 'Number');
            }
            if (data.hasOwnProperty('summe_ausgaben')) {
                obj['summe_ausgaben'] = ApiClient.convertToType(data['summe_ausgaben'], 'String');
            }
            if (data.hasOwnProperty('summe_einnahmen')) {
                obj['summe_einnahmen'] = ApiClient.convertToType(data['summe_einnahmen'], 'String');
            }
            if (data.hasOwnProperty('zeilen')) {
                obj['zeilen'] = ApiClient.convertToType(data['zeilen'], [EuerZeile]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EuerErgebnis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EuerErgebnis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EuerErgebnis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['anlage_zugaenge'] && !(typeof data['anlage_zugaenge'] === 'string' || data['anlage_zugaenge'] instanceof String)) {
            throw new Error("Expected the field `anlage_zugaenge` to be a primitive type in the JSON string but got " + data['anlage_zugaenge']);
        }
        // ensure the json data is a string
        if (data['gewinn_verlust'] && !(typeof data['gewinn_verlust'] === 'string' || data['gewinn_verlust'] instanceof String)) {
            throw new Error("Expected the field `gewinn_verlust` to be a primitive type in the JSON string but got " + data['gewinn_verlust']);
        }
        // ensure the json data is a string
        if (data['summe_ausgaben'] && !(typeof data['summe_ausgaben'] === 'string' || data['summe_ausgaben'] instanceof String)) {
            throw new Error("Expected the field `summe_ausgaben` to be a primitive type in the JSON string but got " + data['summe_ausgaben']);
        }
        // ensure the json data is a string
        if (data['summe_einnahmen'] && !(typeof data['summe_einnahmen'] === 'string' || data['summe_einnahmen'] instanceof String)) {
            throw new Error("Expected the field `summe_einnahmen` to be a primitive type in the JSON string but got " + data['summe_einnahmen']);
        }
        if (data['zeilen']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['zeilen'])) {
                throw new Error("Expected the field `zeilen` to be an array in the JSON data but got " + data['zeilen']);
            }
            // validate the optional field `zeilen` (array)
            for (const item of data['zeilen']) {
                EuerZeile.validateJSON(item);
            };
        }

        return true;
    }


}

EuerErgebnis.RequiredProperties = ["anlage_zugaenge", "gewinn_verlust", "jahr", "summe_ausgaben", "summe_einnahmen", "zeilen"];

/**
 * @member {String} anlage_zugaenge
 */
EuerErgebnis.prototype['anlage_zugaenge'] = undefined;

/**
 * @member {String} gewinn_verlust
 */
EuerErgebnis.prototype['gewinn_verlust'] = undefined;

/**
 * @member {Number} jahr
 */
EuerErgebnis.prototype['jahr'] = undefined;

/**
 * @member {String} summe_ausgaben
 */
EuerErgebnis.prototype['summe_ausgaben'] = undefined;

/**
 * @member {String} summe_einnahmen
 */
EuerErgebnis.prototype['summe_einnahmen'] = undefined;

/**
 * @member {Array.<module:model/EuerZeile>} zeilen
 */
EuerErgebnis.prototype['zeilen'] = undefined;






export default EuerErgebnis;


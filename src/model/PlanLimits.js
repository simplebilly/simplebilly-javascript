/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PlanLimits model module.
 * @module model/PlanLimits
 * @version 0.1.0
 */
class PlanLimits {
    /**
     * Constructs a new <code>PlanLimits</code>.
     * Per-plan numeric limits. &#x60;-1&#x60; in any field means unlimited.
     * @alias module:model/PlanLimits
     * @param maxConnectors {Number} 
     * @param maxInvoicesPerMonth {Number} 
     * @param maxUsers {Number} 
     * @param paidConnectors {Array.<String>} Connectors that are *not* included in this plan (require a higher tier). Empty = all connectors included on this plan.
     */
    constructor(maxConnectors, maxInvoicesPerMonth, maxUsers, paidConnectors) { 
        
        PlanLimits.initialize(this, maxConnectors, maxInvoicesPerMonth, maxUsers, paidConnectors);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, maxConnectors, maxInvoicesPerMonth, maxUsers, paidConnectors) { 
        obj['maxConnectors'] = maxConnectors;
        obj['maxInvoicesPerMonth'] = maxInvoicesPerMonth;
        obj['maxUsers'] = maxUsers;
        obj['paidConnectors'] = paidConnectors;
    }

    /**
     * Constructs a <code>PlanLimits</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PlanLimits} obj Optional instance to populate.
     * @return {module:model/PlanLimits} The populated <code>PlanLimits</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PlanLimits();

            if (data.hasOwnProperty('maxConnectors')) {
                obj['maxConnectors'] = ApiClient.convertToType(data['maxConnectors'], 'Number');
            }
            if (data.hasOwnProperty('maxInvoicesPerMonth')) {
                obj['maxInvoicesPerMonth'] = ApiClient.convertToType(data['maxInvoicesPerMonth'], 'Number');
            }
            if (data.hasOwnProperty('maxUsers')) {
                obj['maxUsers'] = ApiClient.convertToType(data['maxUsers'], 'Number');
            }
            if (data.hasOwnProperty('metered')) {
                obj['metered'] = ApiClient.convertToType(data['metered'], {'String': 'Number'});
            }
            if (data.hasOwnProperty('paidConnectors')) {
                obj['paidConnectors'] = ApiClient.convertToType(data['paidConnectors'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PlanLimits</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PlanLimits</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PlanLimits.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['paidConnectors'])) {
            throw new Error("Expected the field `paidConnectors` to be an array in the JSON data but got " + data['paidConnectors']);
        }

        return true;
    }


}

PlanLimits.RequiredProperties = ["maxConnectors", "maxInvoicesPerMonth", "maxUsers", "paidConnectors"];

/**
 * @member {Number} maxConnectors
 */
PlanLimits.prototype['maxConnectors'] = undefined;

/**
 * @member {Number} maxInvoicesPerMonth
 */
PlanLimits.prototype['maxInvoicesPerMonth'] = undefined;

/**
 * @member {Number} maxUsers
 */
PlanLimits.prototype['maxUsers'] = undefined;

/**
 * @member {Object.<String, Number>} metered
 */
PlanLimits.prototype['metered'] = undefined;

/**
 * Connectors that are *not* included in this plan (require a higher tier). Empty = all connectors included on this plan.
 * @member {Array.<String>} paidConnectors
 */
PlanLimits.prototype['paidConnectors'] = undefined;






export default PlanLimits;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ApplicationStatus from './ApplicationStatus';

/**
 * The JobApplication model module.
 * @module model/JobApplication
 * @version 0.1.0
 */
class JobApplication {
    /**
     * Constructs a new <code>JobApplication</code>.
     * @alias module:model/JobApplication
     * @param source {String} website | email | board
     * @param status {module:model/ApplicationStatus} new | reviewing | interview | hired | rejected
     */
    constructor(source, status) { 
        
        JobApplication.initialize(this, source, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, source, status) { 
        obj['source'] = source;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>JobApplication</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobApplication} obj Optional instance to populate.
     * @return {module:model/JobApplication} The populated <code>JobApplication</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobApplication();

            if (data.hasOwnProperty('cvFile')) {
                obj['cvFile'] = ApiClient.convertToType(data['cvFile'], 'String');
            }
            if (data.hasOwnProperty('cvText')) {
                obj['cvText'] = ApiClient.convertToType(data['cvText'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('matchReason')) {
                obj['matchReason'] = ApiClient.convertToType(data['matchReason'], 'String');
            }
            if (data.hasOwnProperty('matchScore')) {
                obj['matchScore'] = ApiClient.convertToType(data['matchScore'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('phone')) {
                obj['phone'] = ApiClient.convertToType(data['phone'], 'String');
            }
            if (data.hasOwnProperty('postingId')) {
                obj['postingId'] = ApiClient.convertToType(data['postingId'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], ApplicationStatus);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobApplication</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobApplication</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JobApplication.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['cvFile'] && !(typeof data['cvFile'] === 'string' || data['cvFile'] instanceof String)) {
            throw new Error("Expected the field `cvFile` to be a primitive type in the JSON string but got " + data['cvFile']);
        }
        // ensure the json data is a string
        if (data['cvText'] && !(typeof data['cvText'] === 'string' || data['cvText'] instanceof String)) {
            throw new Error("Expected the field `cvText` to be a primitive type in the JSON string but got " + data['cvText']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['matchReason'] && !(typeof data['matchReason'] === 'string' || data['matchReason'] instanceof String)) {
            throw new Error("Expected the field `matchReason` to be a primitive type in the JSON string but got " + data['matchReason']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['phone'] && !(typeof data['phone'] === 'string' || data['phone'] instanceof String)) {
            throw new Error("Expected the field `phone` to be a primitive type in the JSON string but got " + data['phone']);
        }
        // ensure the json data is a string
        if (data['postingId'] && !(typeof data['postingId'] === 'string' || data['postingId'] instanceof String)) {
            throw new Error("Expected the field `postingId` to be a primitive type in the JSON string but got " + data['postingId']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

JobApplication.RequiredProperties = ["source", "status"];

/**
 * Relative path of the stored CV file under the upload dir.
 * @member {String} cvFile
 */
JobApplication.prototype['cvFile'] = undefined;

/**
 * Extracted CV text, used for match-scoring.
 * @member {String} cvText
 */
JobApplication.prototype['cvText'] = undefined;

/**
 * @member {String} email
 */
JobApplication.prototype['email'] = undefined;

/**
 * @member {String} matchReason
 */
JobApplication.prototype['matchReason'] = undefined;

/**
 * 0-100 LLM match score against the posting's required profile.
 * @member {Number} matchScore
 */
JobApplication.prototype['matchScore'] = undefined;

/**
 * @member {String} name
 */
JobApplication.prototype['name'] = undefined;

/**
 * @member {String} phone
 */
JobApplication.prototype['phone'] = undefined;

/**
 * References the job_posting entity.
 * @member {String} postingId
 */
JobApplication.prototype['postingId'] = undefined;

/**
 * website | email | board
 * @member {String} source
 */
JobApplication.prototype['source'] = undefined;

/**
 * new | reviewing | interview | hired | rejected
 * @member {module:model/ApplicationStatus} status
 */
JobApplication.prototype['status'] = undefined;






export default JobApplication;


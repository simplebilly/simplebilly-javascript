/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ComplianceEntry from './ComplianceEntry';

/**
 * The Verfahrensdokumentation model module.
 * @module model/Verfahrensdokumentation
 * @version 0.1.0
 */
class Verfahrensdokumentation {
    /**
     * Constructs a new <code>Verfahrensdokumentation</code>.
     * @alias module:model/Verfahrensdokumentation
     * @param entries {Array.<module:model/ComplianceEntry>} 
     * @param generatedAt {String} 
     * @param title {String} 
     * @param version {String} 
     */
    constructor(entries, generatedAt, title, version) { 
        
        Verfahrensdokumentation.initialize(this, entries, generatedAt, title, version);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, entries, generatedAt, title, version) { 
        obj['entries'] = entries;
        obj['generated_at'] = generatedAt;
        obj['title'] = title;
        obj['version'] = version;
    }

    /**
     * Constructs a <code>Verfahrensdokumentation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Verfahrensdokumentation} obj Optional instance to populate.
     * @return {module:model/Verfahrensdokumentation} The populated <code>Verfahrensdokumentation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Verfahrensdokumentation();

            if (data.hasOwnProperty('entries')) {
                obj['entries'] = ApiClient.convertToType(data['entries'], [ComplianceEntry]);
            }
            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Verfahrensdokumentation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Verfahrensdokumentation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Verfahrensdokumentation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['entries']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['entries'])) {
                throw new Error("Expected the field `entries` to be an array in the JSON data but got " + data['entries']);
            }
            // validate the optional field `entries` (array)
            for (const item of data['entries']) {
                ComplianceEntry.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }

        return true;
    }


}

Verfahrensdokumentation.RequiredProperties = ["entries", "generated_at", "title", "version"];

/**
 * @member {Array.<module:model/ComplianceEntry>} entries
 */
Verfahrensdokumentation.prototype['entries'] = undefined;

/**
 * @member {String} generated_at
 */
Verfahrensdokumentation.prototype['generated_at'] = undefined;

/**
 * @member {String} title
 */
Verfahrensdokumentation.prototype['title'] = undefined;

/**
 * @member {String} version
 */
Verfahrensdokumentation.prototype['version'] = undefined;






export default Verfahrensdokumentation;


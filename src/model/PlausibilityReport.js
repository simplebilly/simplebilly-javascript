/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PlausibilityCheck from './PlausibilityCheck';
import PlausibilitySummary from './PlausibilitySummary';

/**
 * The PlausibilityReport model module.
 * @module model/PlausibilityReport
 * @version 0.1.0
 */
class PlausibilityReport {
    /**
     * Constructs a new <code>PlausibilityReport</code>.
     * @alias module:model/PlausibilityReport
     * @param checks {Array.<module:model/PlausibilityCheck>} 
     * @param generatedAt {String} 
     * @param summary {module:model/PlausibilitySummary} 
     */
    constructor(checks, generatedAt, summary) { 
        
        PlausibilityReport.initialize(this, checks, generatedAt, summary);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, checks, generatedAt, summary) { 
        obj['checks'] = checks;
        obj['generated_at'] = generatedAt;
        obj['summary'] = summary;
    }

    /**
     * Constructs a <code>PlausibilityReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PlausibilityReport} obj Optional instance to populate.
     * @return {module:model/PlausibilityReport} The populated <code>PlausibilityReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PlausibilityReport();

            if (data.hasOwnProperty('checks')) {
                obj['checks'] = ApiClient.convertToType(data['checks'], [PlausibilityCheck]);
            }
            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('summary')) {
                obj['summary'] = PlausibilitySummary.constructFromObject(data['summary']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PlausibilityReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PlausibilityReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PlausibilityReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['checks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checks'])) {
                throw new Error("Expected the field `checks` to be an array in the JSON data but got " + data['checks']);
            }
            // validate the optional field `checks` (array)
            for (const item of data['checks']) {
                PlausibilityCheck.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        // validate the optional field `summary`
        if (data['summary']) { // data not null
          PlausibilitySummary.validateJSON(data['summary']);
        }

        return true;
    }


}

PlausibilityReport.RequiredProperties = ["checks", "generated_at", "summary"];

/**
 * @member {Array.<module:model/PlausibilityCheck>} checks
 */
PlausibilityReport.prototype['checks'] = undefined;

/**
 * @member {String} generated_at
 */
PlausibilityReport.prototype['generated_at'] = undefined;

/**
 * @member {module:model/PlausibilitySummary} summary
 */
PlausibilityReport.prototype['summary'] = undefined;






export default PlausibilityReport;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PaymentCondition model module.
 * @module model/PaymentCondition
 * @version 0.1.0
 */
class PaymentCondition {
    /**
     * Constructs a new <code>PaymentCondition</code>.
     * @alias module:model/PaymentCondition
     * @param discountDays {Number} 
     * @param discountPercentage {Number} 
     * @param id {String} 
     * @param name {String} 
     * @param paymentTermDays {Number} 
     */
    constructor(discountDays, discountPercentage, id, name, paymentTermDays) { 
        
        PaymentCondition.initialize(this, discountDays, discountPercentage, id, name, paymentTermDays);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, discountDays, discountPercentage, id, name, paymentTermDays) { 
        obj['discount_days'] = discountDays;
        obj['discount_percentage'] = discountPercentage;
        obj['id'] = id;
        obj['name'] = name;
        obj['payment_term_days'] = paymentTermDays;
    }

    /**
     * Constructs a <code>PaymentCondition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaymentCondition} obj Optional instance to populate.
     * @return {module:model/PaymentCondition} The populated <code>PaymentCondition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentCondition();

            if (data.hasOwnProperty('discount_days')) {
                obj['discount_days'] = ApiClient.convertToType(data['discount_days'], 'Number');
            }
            if (data.hasOwnProperty('discount_percentage')) {
                obj['discount_percentage'] = ApiClient.convertToType(data['discount_percentage'], 'Number');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('payment_term_days')) {
                obj['payment_term_days'] = ApiClient.convertToType(data['payment_term_days'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentCondition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentCondition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentCondition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

PaymentCondition.RequiredProperties = ["discount_days", "discount_percentage", "id", "name", "payment_term_days"];

/**
 * @member {Number} discount_days
 */
PaymentCondition.prototype['discount_days'] = undefined;

/**
 * @member {Number} discount_percentage
 */
PaymentCondition.prototype['discount_percentage'] = undefined;

/**
 * @member {String} id
 */
PaymentCondition.prototype['id'] = undefined;

/**
 * @member {String} name
 */
PaymentCondition.prototype['name'] = undefined;

/**
 * @member {Number} payment_term_days
 */
PaymentCondition.prototype['payment_term_days'] = undefined;






export default PaymentCondition;


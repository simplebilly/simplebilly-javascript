/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Model model module.
 * @module model/Model
 * @version 0.1.0
 */
class Model {
    /**
     * Constructs a new <code>Model</code>.
     * @alias module:model/Model
     * @param backupCodes {Array.<String>} 
     * @param createdAt {Date} 
     * @param email {String} 
     * @param emailVerified {Boolean} 
     * @param id {String} 
     * @param isActive {Boolean} 
     * @param isTotpEnabled {Boolean} 
     * @param name {String} 
     * @param passwordHash {String} 
     * @param updatedAt {Date} 
     */
    constructor(backupCodes, createdAt, email, emailVerified, id, isActive, isTotpEnabled, name, passwordHash, updatedAt) { 
        
        Model.initialize(this, backupCodes, createdAt, email, emailVerified, id, isActive, isTotpEnabled, name, passwordHash, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, backupCodes, createdAt, email, emailVerified, id, isActive, isTotpEnabled, name, passwordHash, updatedAt) { 
        obj['backup_codes'] = backupCodes;
        obj['created_at'] = createdAt;
        obj['email'] = email;
        obj['email_verified'] = emailVerified;
        obj['id'] = id;
        obj['is_active'] = isActive;
        obj['is_totp_enabled'] = isTotpEnabled;
        obj['name'] = name;
        obj['password_hash'] = passwordHash;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>Model</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Model} obj Optional instance to populate.
     * @return {module:model/Model} The populated <code>Model</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Model();

            if (data.hasOwnProperty('backup_codes')) {
                obj['backup_codes'] = ApiClient.convertToType(data['backup_codes'], ['String']);
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('deleted_at')) {
                obj['deleted_at'] = ApiClient.convertToType(data['deleted_at'], 'Date');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('email_verified')) {
                obj['email_verified'] = ApiClient.convertToType(data['email_verified'], 'Boolean');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('is_totp_enabled')) {
                obj['is_totp_enabled'] = ApiClient.convertToType(data['is_totp_enabled'], 'Boolean');
            }
            if (data.hasOwnProperty('last_login')) {
                obj['last_login'] = ApiClient.convertToType(data['last_login'], 'Date');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('oauth_id')) {
                obj['oauth_id'] = ApiClient.convertToType(data['oauth_id'], 'String');
            }
            if (data.hasOwnProperty('oauth_provider')) {
                obj['oauth_provider'] = ApiClient.convertToType(data['oauth_provider'], 'String');
            }
            if (data.hasOwnProperty('password_changed_at')) {
                obj['password_changed_at'] = ApiClient.convertToType(data['password_changed_at'], 'Date');
            }
            if (data.hasOwnProperty('password_hash')) {
                obj['password_hash'] = ApiClient.convertToType(data['password_hash'], 'String');
            }
            if (data.hasOwnProperty('picture')) {
                obj['picture'] = ApiClient.convertToType(data['picture'], 'String');
            }
            if (data.hasOwnProperty('privacy_accepted_at')) {
                obj['privacy_accepted_at'] = ApiClient.convertToType(data['privacy_accepted_at'], 'Date');
            }
            if (data.hasOwnProperty('totp_secret')) {
                obj['totp_secret'] = ApiClient.convertToType(data['totp_secret'], 'String');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Model</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Model</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Model.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['backup_codes'])) {
            throw new Error("Expected the field `backup_codes` to be an array in the JSON data but got " + data['backup_codes']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['oauth_id'] && !(typeof data['oauth_id'] === 'string' || data['oauth_id'] instanceof String)) {
            throw new Error("Expected the field `oauth_id` to be a primitive type in the JSON string but got " + data['oauth_id']);
        }
        // ensure the json data is a string
        if (data['oauth_provider'] && !(typeof data['oauth_provider'] === 'string' || data['oauth_provider'] instanceof String)) {
            throw new Error("Expected the field `oauth_provider` to be a primitive type in the JSON string but got " + data['oauth_provider']);
        }
        // ensure the json data is a string
        if (data['password_hash'] && !(typeof data['password_hash'] === 'string' || data['password_hash'] instanceof String)) {
            throw new Error("Expected the field `password_hash` to be a primitive type in the JSON string but got " + data['password_hash']);
        }
        // ensure the json data is a string
        if (data['picture'] && !(typeof data['picture'] === 'string' || data['picture'] instanceof String)) {
            throw new Error("Expected the field `picture` to be a primitive type in the JSON string but got " + data['picture']);
        }
        // ensure the json data is a string
        if (data['totp_secret'] && !(typeof data['totp_secret'] === 'string' || data['totp_secret'] instanceof String)) {
            throw new Error("Expected the field `totp_secret` to be a primitive type in the JSON string but got " + data['totp_secret']);
        }

        return true;
    }


}

Model.RequiredProperties = ["backup_codes", "created_at", "email", "email_verified", "id", "is_active", "is_totp_enabled", "name", "password_hash", "updated_at"];

/**
 * @member {Array.<String>} backup_codes
 */
Model.prototype['backup_codes'] = undefined;

/**
 * @member {Date} created_at
 */
Model.prototype['created_at'] = undefined;

/**
 * @member {Date} deleted_at
 */
Model.prototype['deleted_at'] = undefined;

/**
 * @member {String} email
 */
Model.prototype['email'] = undefined;

/**
 * @member {Boolean} email_verified
 */
Model.prototype['email_verified'] = undefined;

/**
 * @member {String} id
 */
Model.prototype['id'] = undefined;

/**
 * @member {Boolean} is_active
 */
Model.prototype['is_active'] = undefined;

/**
 * @member {Boolean} is_totp_enabled
 */
Model.prototype['is_totp_enabled'] = undefined;

/**
 * @member {Date} last_login
 */
Model.prototype['last_login'] = undefined;

/**
 * @member {String} name
 */
Model.prototype['name'] = undefined;

/**
 * @member {String} oauth_id
 */
Model.prototype['oauth_id'] = undefined;

/**
 * @member {String} oauth_provider
 */
Model.prototype['oauth_provider'] = undefined;

/**
 * Set on password change; auth/refresh tokens issued before this timestamp are rejected by the auth middleware.
 * @member {Date} password_changed_at
 */
Model.prototype['password_changed_at'] = undefined;

/**
 * @member {String} password_hash
 */
Model.prototype['password_hash'] = undefined;

/**
 * @member {String} picture
 */
Model.prototype['picture'] = undefined;

/**
 * When the user accepted the data privacy policy (GDPR consent record).
 * @member {Date} privacy_accepted_at
 */
Model.prototype['privacy_accepted_at'] = undefined;

/**
 * @member {String} totp_secret
 */
Model.prototype['totp_secret'] = undefined;

/**
 * @member {Date} updated_at
 */
Model.prototype['updated_at'] = undefined;






export default Model;


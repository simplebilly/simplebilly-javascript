/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TotpSetupResponse model module.
 * @module model/TotpSetupResponse
 * @version 0.1.0
 */
class TotpSetupResponse {
    /**
     * Constructs a new <code>TotpSetupResponse</code>.
     * @alias module:model/TotpSetupResponse
     * @param backupCodes {Array.<String>} 
     * @param qrCodeUrl {String} 
     * @param secret {String} 
     */
    constructor(backupCodes, qrCodeUrl, secret) { 
        
        TotpSetupResponse.initialize(this, backupCodes, qrCodeUrl, secret);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, backupCodes, qrCodeUrl, secret) { 
        obj['backup_codes'] = backupCodes;
        obj['qr_code_url'] = qrCodeUrl;
        obj['secret'] = secret;
    }

    /**
     * Constructs a <code>TotpSetupResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TotpSetupResponse} obj Optional instance to populate.
     * @return {module:model/TotpSetupResponse} The populated <code>TotpSetupResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TotpSetupResponse();

            if (data.hasOwnProperty('backup_codes')) {
                obj['backup_codes'] = ApiClient.convertToType(data['backup_codes'], ['String']);
            }
            if (data.hasOwnProperty('qr_code_url')) {
                obj['qr_code_url'] = ApiClient.convertToType(data['qr_code_url'], 'String');
            }
            if (data.hasOwnProperty('secret')) {
                obj['secret'] = ApiClient.convertToType(data['secret'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TotpSetupResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TotpSetupResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TotpSetupResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['backup_codes'])) {
            throw new Error("Expected the field `backup_codes` to be an array in the JSON data but got " + data['backup_codes']);
        }
        // ensure the json data is a string
        if (data['qr_code_url'] && !(typeof data['qr_code_url'] === 'string' || data['qr_code_url'] instanceof String)) {
            throw new Error("Expected the field `qr_code_url` to be a primitive type in the JSON string but got " + data['qr_code_url']);
        }
        // ensure the json data is a string
        if (data['secret'] && !(typeof data['secret'] === 'string' || data['secret'] instanceof String)) {
            throw new Error("Expected the field `secret` to be a primitive type in the JSON string but got " + data['secret']);
        }

        return true;
    }


}

TotpSetupResponse.RequiredProperties = ["backup_codes", "qr_code_url", "secret"];

/**
 * @member {Array.<String>} backup_codes
 */
TotpSetupResponse.prototype['backup_codes'] = undefined;

/**
 * @member {String} qr_code_url
 */
TotpSetupResponse.prototype['qr_code_url'] = undefined;

/**
 * @member {String} secret
 */
TotpSetupResponse.prototype['secret'] = undefined;






export default TotpSetupResponse;


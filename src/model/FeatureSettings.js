/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FeatureSettings model module.
 * @module model/FeatureSettings
 * @version 0.1.0
 */
class FeatureSettings {
    /**
     * Constructs a new <code>FeatureSettings</code>.
     * Feature toggles for a tenant.
     * @alias module:model/FeatureSettings
     * @param onlineshop {Boolean} Online shop / storefront module (default: enabled).
     * @param reportBilanz {Boolean} Bilanz (balance sheet) report.
     * @param reportBwa {Boolean} BWA (betriebswirtschaftliche Auswertung).
     * @param reportEuer {Boolean} EÜR (Einnahmen-Überschuss-Rechnung).
     * @param reportGewerbesteuer {Boolean} Gewerbesteuer report.
     * @param reportGuv {Boolean} GuV (profit & loss) report.
     * @param reportKst {Boolean} KSt (Körperschaftsteuer) report.
     * @param reportUstva {Boolean} UStVA (Umsatzsteuervoranmeldung).
     */
    constructor(onlineshop, reportBilanz, reportBwa, reportEuer, reportGewerbesteuer, reportGuv, reportKst, reportUstva) { 
        
        FeatureSettings.initialize(this, onlineshop, reportBilanz, reportBwa, reportEuer, reportGewerbesteuer, reportGuv, reportKst, reportUstva);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, onlineshop, reportBilanz, reportBwa, reportEuer, reportGewerbesteuer, reportGuv, reportKst, reportUstva) { 
        obj['onlineshop'] = onlineshop;
        obj['reportBilanz'] = reportBilanz;
        obj['reportBwa'] = reportBwa;
        obj['reportEuer'] = reportEuer;
        obj['reportGewerbesteuer'] = reportGewerbesteuer;
        obj['reportGuv'] = reportGuv;
        obj['reportKst'] = reportKst;
        obj['reportUstva'] = reportUstva;
    }

    /**
     * Constructs a <code>FeatureSettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FeatureSettings} obj Optional instance to populate.
     * @return {module:model/FeatureSettings} The populated <code>FeatureSettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FeatureSettings();

            if (data.hasOwnProperty('onlineshop')) {
                obj['onlineshop'] = ApiClient.convertToType(data['onlineshop'], 'Boolean');
            }
            if (data.hasOwnProperty('reportBilanz')) {
                obj['reportBilanz'] = ApiClient.convertToType(data['reportBilanz'], 'Boolean');
            }
            if (data.hasOwnProperty('reportBwa')) {
                obj['reportBwa'] = ApiClient.convertToType(data['reportBwa'], 'Boolean');
            }
            if (data.hasOwnProperty('reportEuer')) {
                obj['reportEuer'] = ApiClient.convertToType(data['reportEuer'], 'Boolean');
            }
            if (data.hasOwnProperty('reportGewerbesteuer')) {
                obj['reportGewerbesteuer'] = ApiClient.convertToType(data['reportGewerbesteuer'], 'Boolean');
            }
            if (data.hasOwnProperty('reportGuv')) {
                obj['reportGuv'] = ApiClient.convertToType(data['reportGuv'], 'Boolean');
            }
            if (data.hasOwnProperty('reportKst')) {
                obj['reportKst'] = ApiClient.convertToType(data['reportKst'], 'Boolean');
            }
            if (data.hasOwnProperty('reportUstva')) {
                obj['reportUstva'] = ApiClient.convertToType(data['reportUstva'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FeatureSettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FeatureSettings</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FeatureSettings.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

FeatureSettings.RequiredProperties = ["onlineshop", "reportBilanz", "reportBwa", "reportEuer", "reportGewerbesteuer", "reportGuv", "reportKst", "reportUstva"];

/**
 * Online shop / storefront module (default: enabled).
 * @member {Boolean} onlineshop
 */
FeatureSettings.prototype['onlineshop'] = undefined;

/**
 * Bilanz (balance sheet) report.
 * @member {Boolean} reportBilanz
 */
FeatureSettings.prototype['reportBilanz'] = undefined;

/**
 * BWA (betriebswirtschaftliche Auswertung).
 * @member {Boolean} reportBwa
 */
FeatureSettings.prototype['reportBwa'] = undefined;

/**
 * EÜR (Einnahmen-Überschuss-Rechnung).
 * @member {Boolean} reportEuer
 */
FeatureSettings.prototype['reportEuer'] = undefined;

/**
 * Gewerbesteuer report.
 * @member {Boolean} reportGewerbesteuer
 */
FeatureSettings.prototype['reportGewerbesteuer'] = undefined;

/**
 * GuV (profit & loss) report.
 * @member {Boolean} reportGuv
 */
FeatureSettings.prototype['reportGuv'] = undefined;

/**
 * KSt (Körperschaftsteuer) report.
 * @member {Boolean} reportKst
 */
FeatureSettings.prototype['reportKst'] = undefined;

/**
 * UStVA (Umsatzsteuervoranmeldung).
 * @member {Boolean} reportUstva
 */
FeatureSettings.prototype['reportUstva'] = undefined;






export default FeatureSettings;


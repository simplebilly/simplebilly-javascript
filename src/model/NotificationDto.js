/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NotificationDto model module.
 * @module model/NotificationDto
 * @version 0.1.0
 */
class NotificationDto {
    /**
     * Constructs a new <code>NotificationDto</code>.
     * Wire shape of a notification row (mirrors frontend Notification type).
     * @alias module:model/NotificationDto
     * @param createdAt {Date} 
     * @param id {String} 
     * @param isRead {Boolean} 
     * @param sentViaEmail {Boolean} 
     * @param tenantId {String} 
     * @param title {String} 
     * @param userId {String} 
     */
    constructor(createdAt, id, isRead, sentViaEmail, tenantId, title, userId) { 
        
        NotificationDto.initialize(this, createdAt, id, isRead, sentViaEmail, tenantId, title, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, createdAt, id, isRead, sentViaEmail, tenantId, title, userId) { 
        obj['created_at'] = createdAt;
        obj['id'] = id;
        obj['is_read'] = isRead;
        obj['sent_via_email'] = sentViaEmail;
        obj['tenant_id'] = tenantId;
        obj['title'] = title;
        obj['user_id'] = userId;
    }

    /**
     * Constructs a <code>NotificationDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NotificationDto} obj Optional instance to populate.
     * @return {module:model/NotificationDto} The populated <code>NotificationDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NotificationDto();

            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('is_read')) {
                obj['is_read'] = ApiClient.convertToType(data['is_read'], 'Boolean');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('sent_via_email')) {
                obj['sent_via_email'] = ApiClient.convertToType(data['sent_via_email'], 'Boolean');
            }
            if (data.hasOwnProperty('tenant_id')) {
                obj['tenant_id'] = ApiClient.convertToType(data['tenant_id'], 'String');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NotificationDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NotificationDto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NotificationDto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['tenant_id'] && !(typeof data['tenant_id'] === 'string' || data['tenant_id'] instanceof String)) {
            throw new Error("Expected the field `tenant_id` to be a primitive type in the JSON string but got " + data['tenant_id']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }

        return true;
    }


}

NotificationDto.RequiredProperties = ["created_at", "id", "is_read", "sent_via_email", "tenant_id", "title", "user_id"];

/**
 * @member {Date} created_at
 */
NotificationDto.prototype['created_at'] = undefined;

/**
 * @member {String} id
 */
NotificationDto.prototype['id'] = undefined;

/**
 * @member {Boolean} is_read
 */
NotificationDto.prototype['is_read'] = undefined;

/**
 * @member {String} message
 */
NotificationDto.prototype['message'] = undefined;

/**
 * @member {Boolean} sent_via_email
 */
NotificationDto.prototype['sent_via_email'] = undefined;

/**
 * @member {String} tenant_id
 */
NotificationDto.prototype['tenant_id'] = undefined;

/**
 * @member {String} title
 */
NotificationDto.prototype['title'] = undefined;

/**
 * @member {String} user_id
 */
NotificationDto.prototype['user_id'] = undefined;






export default NotificationDto;


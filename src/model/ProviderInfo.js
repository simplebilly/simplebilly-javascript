/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProviderInfo model module.
 * @module model/ProviderInfo
 * @version 0.1.0
 */
class ProviderInfo {
    /**
     * Constructs a new <code>ProviderInfo</code>.
     * @alias module:model/ProviderInfo
     * @param displayName {String} 
     * @param name {String} 
     * @param requiresApiKey {Boolean} 
     * @param services {Array.<String>} 
     * @param supportsLabelCreation {Boolean} 
     * @param supportsRateEstimation {Boolean} 
     * @param supportsTracking {Boolean} 
     */
    constructor(displayName, name, requiresApiKey, services, supportsLabelCreation, supportsRateEstimation, supportsTracking) { 
        
        ProviderInfo.initialize(this, displayName, name, requiresApiKey, services, supportsLabelCreation, supportsRateEstimation, supportsTracking);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, displayName, name, requiresApiKey, services, supportsLabelCreation, supportsRateEstimation, supportsTracking) { 
        obj['display_name'] = displayName;
        obj['name'] = name;
        obj['requires_api_key'] = requiresApiKey;
        obj['services'] = services;
        obj['supports_label_creation'] = supportsLabelCreation;
        obj['supports_rate_estimation'] = supportsRateEstimation;
        obj['supports_tracking'] = supportsTracking;
    }

    /**
     * Constructs a <code>ProviderInfo</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProviderInfo} obj Optional instance to populate.
     * @return {module:model/ProviderInfo} The populated <code>ProviderInfo</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProviderInfo();

            if (data.hasOwnProperty('display_name')) {
                obj['display_name'] = ApiClient.convertToType(data['display_name'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('requires_api_key')) {
                obj['requires_api_key'] = ApiClient.convertToType(data['requires_api_key'], 'Boolean');
            }
            if (data.hasOwnProperty('services')) {
                obj['services'] = ApiClient.convertToType(data['services'], ['String']);
            }
            if (data.hasOwnProperty('supports_label_creation')) {
                obj['supports_label_creation'] = ApiClient.convertToType(data['supports_label_creation'], 'Boolean');
            }
            if (data.hasOwnProperty('supports_rate_estimation')) {
                obj['supports_rate_estimation'] = ApiClient.convertToType(data['supports_rate_estimation'], 'Boolean');
            }
            if (data.hasOwnProperty('supports_tracking')) {
                obj['supports_tracking'] = ApiClient.convertToType(data['supports_tracking'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProviderInfo</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProviderInfo</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProviderInfo.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['display_name'] && !(typeof data['display_name'] === 'string' || data['display_name'] instanceof String)) {
            throw new Error("Expected the field `display_name` to be a primitive type in the JSON string but got " + data['display_name']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['services'])) {
            throw new Error("Expected the field `services` to be an array in the JSON data but got " + data['services']);
        }

        return true;
    }


}

ProviderInfo.RequiredProperties = ["display_name", "name", "requires_api_key", "services", "supports_label_creation", "supports_rate_estimation", "supports_tracking"];

/**
 * @member {String} display_name
 */
ProviderInfo.prototype['display_name'] = undefined;

/**
 * @member {String} name
 */
ProviderInfo.prototype['name'] = undefined;

/**
 * @member {Boolean} requires_api_key
 */
ProviderInfo.prototype['requires_api_key'] = undefined;

/**
 * @member {Array.<String>} services
 */
ProviderInfo.prototype['services'] = undefined;

/**
 * @member {Boolean} supports_label_creation
 */
ProviderInfo.prototype['supports_label_creation'] = undefined;

/**
 * @member {Boolean} supports_rate_estimation
 */
ProviderInfo.prototype['supports_rate_estimation'] = undefined;

/**
 * @member {Boolean} supports_tracking
 */
ProviderInfo.prototype['supports_tracking'] = undefined;






export default ProviderInfo;


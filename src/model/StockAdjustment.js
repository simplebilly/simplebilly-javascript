/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The StockAdjustment model module.
 * @module model/StockAdjustment
 * @version 0.1.0
 */
class StockAdjustment {
    /**
     * Constructs a new <code>StockAdjustment</code>.
     * @alias module:model/StockAdjustment
     * @param quantity {Number} 
     */
    constructor(quantity) { 
        
        StockAdjustment.initialize(this, quantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, quantity) { 
        obj['quantity'] = quantity;
    }

    /**
     * Constructs a <code>StockAdjustment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StockAdjustment} obj Optional instance to populate.
     * @return {module:model/StockAdjustment} The populated <code>StockAdjustment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StockAdjustment();

            if (data.hasOwnProperty('batchNumber')) {
                obj['batchNumber'] = ApiClient.convertToType(data['batchNumber'], 'String');
            }
            if (data.hasOwnProperty('binLocation')) {
                obj['binLocation'] = ApiClient.convertToType(data['binLocation'], 'String');
            }
            if (data.hasOwnProperty('expiryDate')) {
                obj['expiryDate'] = ApiClient.convertToType(data['expiryDate'], 'Date');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('serialNumbers')) {
                obj['serialNumbers'] = ApiClient.convertToType(data['serialNumbers'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StockAdjustment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StockAdjustment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StockAdjustment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['batchNumber'] && !(typeof data['batchNumber'] === 'string' || data['batchNumber'] instanceof String)) {
            throw new Error("Expected the field `batchNumber` to be a primitive type in the JSON string but got " + data['batchNumber']);
        }
        // ensure the json data is a string
        if (data['binLocation'] && !(typeof data['binLocation'] === 'string' || data['binLocation'] instanceof String)) {
            throw new Error("Expected the field `binLocation` to be a primitive type in the JSON string but got " + data['binLocation']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['serialNumbers'])) {
            throw new Error("Expected the field `serialNumbers` to be an array in the JSON data but got " + data['serialNumbers']);
        }

        return true;
    }


}

StockAdjustment.RequiredProperties = ["quantity"];

/**
 * @member {String} batchNumber
 */
StockAdjustment.prototype['batchNumber'] = undefined;

/**
 * @member {String} binLocation
 */
StockAdjustment.prototype['binLocation'] = undefined;

/**
 * @member {Date} expiryDate
 */
StockAdjustment.prototype['expiryDate'] = undefined;

/**
 * @member {String} productId
 */
StockAdjustment.prototype['productId'] = undefined;

/**
 * @member {Number} quantity
 */
StockAdjustment.prototype['quantity'] = undefined;

/**
 * @member {Array.<String>} serialNumbers
 */
StockAdjustment.prototype['serialNumbers'] = undefined;






export default StockAdjustment;


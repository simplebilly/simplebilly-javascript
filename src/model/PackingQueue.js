/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PackingQueueItem from './PackingQueueItem';

/**
 * The PackingQueue model module.
 * @module model/PackingQueue
 * @version 0.1.0
 */
class PackingQueue {
    /**
     * Constructs a new <code>PackingQueue</code>.
     * @alias module:model/PackingQueue
     * @param items {Array.<module:model/PackingQueueItem>} 
     * @param page {Number} 
     * @param pageSize {Number} 
     * @param totalCount {Number} 
     */
    constructor(items, page, pageSize, totalCount) { 
        
        PackingQueue.initialize(this, items, page, pageSize, totalCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, items, page, pageSize, totalCount) { 
        obj['items'] = items;
        obj['page'] = page;
        obj['page_size'] = pageSize;
        obj['total_count'] = totalCount;
    }

    /**
     * Constructs a <code>PackingQueue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PackingQueue} obj Optional instance to populate.
     * @return {module:model/PackingQueue} The populated <code>PackingQueue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PackingQueue();

            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [PackingQueueItem]);
            }
            if (data.hasOwnProperty('page')) {
                obj['page'] = ApiClient.convertToType(data['page'], 'Number');
            }
            if (data.hasOwnProperty('page_size')) {
                obj['page_size'] = ApiClient.convertToType(data['page_size'], 'Number');
            }
            if (data.hasOwnProperty('total_count')) {
                obj['total_count'] = ApiClient.convertToType(data['total_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PackingQueue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PackingQueue</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PackingQueue.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                PackingQueueItem.validateJSON(item);
            };
        }

        return true;
    }


}

PackingQueue.RequiredProperties = ["items", "page", "page_size", "total_count"];

/**
 * @member {Array.<module:model/PackingQueueItem>} items
 */
PackingQueue.prototype['items'] = undefined;

/**
 * @member {Number} page
 */
PackingQueue.prototype['page'] = undefined;

/**
 * @member {Number} page_size
 */
PackingQueue.prototype['page_size'] = undefined;

/**
 * @member {Number} total_count
 */
PackingQueue.prototype['total_count'] = undefined;






export default PackingQueue;


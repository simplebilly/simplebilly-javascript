/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AiConfigDto model module.
 * @module model/AiConfigDto
 * @version 0.1.0
 */
class AiConfigDto {
    /**
     * Constructs a new <code>AiConfigDto</code>.
     * @alias module:model/AiConfigDto
     * @param model {String} 
     * @param name {String} 
     * @param provider {String} 
     */
    constructor(model, name, provider) { 
        
        AiConfigDto.initialize(this, model, name, provider);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, model, name, provider) { 
        obj['model'] = model;
        obj['name'] = name;
        obj['provider'] = provider;
    }

    /**
     * Constructs a <code>AiConfigDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AiConfigDto} obj Optional instance to populate.
     * @return {module:model/AiConfigDto} The populated <code>AiConfigDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AiConfigDto();

            if (data.hasOwnProperty('auto_reply')) {
                obj['auto_reply'] = ApiClient.convertToType(data['auto_reply'], 'Boolean');
            }
            if (data.hasOwnProperty('max_tool_calls')) {
                obj['max_tool_calls'] = ApiClient.convertToType(data['max_tool_calls'], 'Number');
            }
            if (data.hasOwnProperty('model')) {
                obj['model'] = ApiClient.convertToType(data['model'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('system_prompt')) {
                obj['system_prompt'] = ApiClient.convertToType(data['system_prompt'], 'String');
            }
            if (data.hasOwnProperty('trigger_on')) {
                obj['trigger_on'] = ApiClient.convertToType(data['trigger_on'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AiConfigDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AiConfigDto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AiConfigDto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['model'] && !(typeof data['model'] === 'string' || data['model'] instanceof String)) {
            throw new Error("Expected the field `model` to be a primitive type in the JSON string but got " + data['model']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['system_prompt'] && !(typeof data['system_prompt'] === 'string' || data['system_prompt'] instanceof String)) {
            throw new Error("Expected the field `system_prompt` to be a primitive type in the JSON string but got " + data['system_prompt']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['trigger_on'])) {
            throw new Error("Expected the field `trigger_on` to be an array in the JSON data but got " + data['trigger_on']);
        }

        return true;
    }


}

AiConfigDto.RequiredProperties = ["model", "name", "provider"];

/**
 * @member {Boolean} auto_reply
 */
AiConfigDto.prototype['auto_reply'] = undefined;

/**
 * @member {Number} max_tool_calls
 */
AiConfigDto.prototype['max_tool_calls'] = undefined;

/**
 * @member {String} model
 */
AiConfigDto.prototype['model'] = undefined;

/**
 * @member {String} name
 */
AiConfigDto.prototype['name'] = undefined;

/**
 * @member {String} provider
 */
AiConfigDto.prototype['provider'] = undefined;

/**
 * @member {String} system_prompt
 */
AiConfigDto.prototype['system_prompt'] = undefined;

/**
 * @member {Array.<String>} trigger_on
 */
AiConfigDto.prototype['trigger_on'] = undefined;






export default AiConfigDto;


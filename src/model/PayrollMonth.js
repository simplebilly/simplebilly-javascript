/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PayrollMonth model module.
 * @module model/PayrollMonth
 * @version 0.1.0
 */
class PayrollMonth {
    /**
     * Constructs a new <code>PayrollMonth</code>.
     * @alias module:model/PayrollMonth
     * @param gross {String} 
     * @param month {Number} 
     * @param net {String} 
     */
    constructor(gross, month, net) { 
        
        PayrollMonth.initialize(this, gross, month, net);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, gross, month, net) { 
        obj['gross'] = gross;
        obj['month'] = month;
        obj['net'] = net;
    }

    /**
     * Constructs a <code>PayrollMonth</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayrollMonth} obj Optional instance to populate.
     * @return {module:model/PayrollMonth} The populated <code>PayrollMonth</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayrollMonth();

            if (data.hasOwnProperty('gross')) {
                obj['gross'] = ApiClient.convertToType(data['gross'], 'String');
            }
            if (data.hasOwnProperty('month')) {
                obj['month'] = ApiClient.convertToType(data['month'], 'Number');
            }
            if (data.hasOwnProperty('net')) {
                obj['net'] = ApiClient.convertToType(data['net'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayrollMonth</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayrollMonth</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayrollMonth.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['gross'] && !(typeof data['gross'] === 'string' || data['gross'] instanceof String)) {
            throw new Error("Expected the field `gross` to be a primitive type in the JSON string but got " + data['gross']);
        }
        // ensure the json data is a string
        if (data['net'] && !(typeof data['net'] === 'string' || data['net'] instanceof String)) {
            throw new Error("Expected the field `net` to be a primitive type in the JSON string but got " + data['net']);
        }

        return true;
    }


}

PayrollMonth.RequiredProperties = ["gross", "month", "net"];

/**
 * @member {String} gross
 */
PayrollMonth.prototype['gross'] = undefined;

/**
 * @member {Number} month
 */
PayrollMonth.prototype['month'] = undefined;

/**
 * @member {String} net
 */
PayrollMonth.prototype['net'] = undefined;






export default PayrollMonth;


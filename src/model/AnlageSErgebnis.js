/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AnlageSKfzHinweis from './AnlageSKfzHinweis';

/**
 * The AnlageSErgebnis model module.
 * @module model/AnlageSErgebnis
 * @version 0.1.0
 */
class AnlageSErgebnis {
    /**
     * Constructs a new <code>AnlageSErgebnis</code>.
     * @alias module:model/AnlageSErgebnis
     * @param gewinnVerlust {String} 
     * @param jahr {Number} 
     * @param kfzHinweise {Array.<module:model/AnlageSKfzHinweis>} 
     */
    constructor(gewinnVerlust, jahr, kfzHinweise) { 
        
        AnlageSErgebnis.initialize(this, gewinnVerlust, jahr, kfzHinweise);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, gewinnVerlust, jahr, kfzHinweise) { 
        obj['gewinn_verlust'] = gewinnVerlust;
        obj['jahr'] = jahr;
        obj['kfz_hinweise'] = kfzHinweise;
    }

    /**
     * Constructs a <code>AnlageSErgebnis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AnlageSErgebnis} obj Optional instance to populate.
     * @return {module:model/AnlageSErgebnis} The populated <code>AnlageSErgebnis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AnlageSErgebnis();

            if (data.hasOwnProperty('gewinn_verlust')) {
                obj['gewinn_verlust'] = ApiClient.convertToType(data['gewinn_verlust'], 'String');
            }
            if (data.hasOwnProperty('jahr')) {
                obj['jahr'] = ApiClient.convertToType(data['jahr'], 'Number');
            }
            if (data.hasOwnProperty('kfz_hinweise')) {
                obj['kfz_hinweise'] = ApiClient.convertToType(data['kfz_hinweise'], [AnlageSKfzHinweis]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AnlageSErgebnis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AnlageSErgebnis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AnlageSErgebnis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['gewinn_verlust'] && !(typeof data['gewinn_verlust'] === 'string' || data['gewinn_verlust'] instanceof String)) {
            throw new Error("Expected the field `gewinn_verlust` to be a primitive type in the JSON string but got " + data['gewinn_verlust']);
        }
        if (data['kfz_hinweise']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['kfz_hinweise'])) {
                throw new Error("Expected the field `kfz_hinweise` to be an array in the JSON data but got " + data['kfz_hinweise']);
            }
            // validate the optional field `kfz_hinweise` (array)
            for (const item of data['kfz_hinweise']) {
                AnlageSKfzHinweis.validateJSON(item);
            };
        }

        return true;
    }


}

AnlageSErgebnis.RequiredProperties = ["gewinn_verlust", "jahr", "kfz_hinweise"];

/**
 * @member {String} gewinn_verlust
 */
AnlageSErgebnis.prototype['gewinn_verlust'] = undefined;

/**
 * @member {Number} jahr
 */
AnlageSErgebnis.prototype['jahr'] = undefined;

/**
 * @member {Array.<module:model/AnlageSKfzHinweis>} kfz_hinweise
 */
AnlageSErgebnis.prototype['kfz_hinweise'] = undefined;






export default AnlageSErgebnis;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CommunicationChannel from './CommunicationChannel';
import CommunicationDirection from './CommunicationDirection';

/**
 * The CustomerCommunication model module.
 * @module model/CustomerCommunication
 * @version 0.1.0
 */
class CustomerCommunication {
    /**
     * Constructs a new <code>CustomerCommunication</code>.
     * @alias module:model/CustomerCommunication
     * @param channel {module:model/CommunicationChannel} 
     * @param contactId {String} The contact (customer/supplier) this communication belongs to. References the contact entity.
     * @param direction {module:model/CommunicationDirection} 
     */
    constructor(channel, contactId, direction) { 
        
        CustomerCommunication.initialize(this, channel, contactId, direction);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, channel, contactId, direction) { 
        obj['channel'] = channel;
        obj['contactId'] = contactId;
        obj['direction'] = direction;
    }

    /**
     * Constructs a <code>CustomerCommunication</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomerCommunication} obj Optional instance to populate.
     * @return {module:model/CustomerCommunication} The populated <code>CustomerCommunication</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomerCommunication();

            if (data.hasOwnProperty('body')) {
                obj['body'] = ApiClient.convertToType(data['body'], 'String');
            }
            if (data.hasOwnProperty('channel')) {
                obj['channel'] = CommunicationChannel.constructFromObject(data['channel']);
            }
            if (data.hasOwnProperty('contactId')) {
                obj['contactId'] = ApiClient.convertToType(data['contactId'], 'String');
            }
            if (data.hasOwnProperty('counterparty')) {
                obj['counterparty'] = ApiClient.convertToType(data['counterparty'], 'String');
            }
            if (data.hasOwnProperty('direction')) {
                obj['direction'] = CommunicationDirection.constructFromObject(data['direction']);
            }
            if (data.hasOwnProperty('occurredAt')) {
                obj['occurredAt'] = ApiClient.convertToType(data['occurredAt'], 'Date');
            }
            if (data.hasOwnProperty('subject')) {
                obj['subject'] = ApiClient.convertToType(data['subject'], 'String');
            }
            if (data.hasOwnProperty('tags')) {
                obj['tags'] = ApiClient.convertToType(data['tags'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomerCommunication</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomerCommunication</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CustomerCommunication.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['body'] && !(typeof data['body'] === 'string' || data['body'] instanceof String)) {
            throw new Error("Expected the field `body` to be a primitive type in the JSON string but got " + data['body']);
        }
        // ensure the json data is a string
        if (data['contactId'] && !(typeof data['contactId'] === 'string' || data['contactId'] instanceof String)) {
            throw new Error("Expected the field `contactId` to be a primitive type in the JSON string but got " + data['contactId']);
        }
        // ensure the json data is a string
        if (data['counterparty'] && !(typeof data['counterparty'] === 'string' || data['counterparty'] instanceof String)) {
            throw new Error("Expected the field `counterparty` to be a primitive type in the JSON string but got " + data['counterparty']);
        }
        // ensure the json data is a string
        if (data['subject'] && !(typeof data['subject'] === 'string' || data['subject'] instanceof String)) {
            throw new Error("Expected the field `subject` to be a primitive type in the JSON string but got " + data['subject']);
        }

        return true;
    }


}

CustomerCommunication.RequiredProperties = ["channel", "contactId", "direction"];

/**
 * The message body, call summary or note text.
 * @member {String} body
 */
CustomerCommunication.prototype['body'] = undefined;

/**
 * @member {module:model/CommunicationChannel} channel
 */
CustomerCommunication.prototype['channel'] = undefined;

/**
 * The contact (customer/supplier) this communication belongs to. References the contact entity.
 * @member {String} contactId
 */
CustomerCommunication.prototype['contactId'] = undefined;

/**
 * Email/phone of the counterparty, if applicable.
 * @member {String} counterparty
 */
CustomerCommunication.prototype['counterparty'] = undefined;

/**
 * @member {module:model/CommunicationDirection} direction
 */
CustomerCommunication.prototype['direction'] = undefined;

/**
 * When the communication happened (defaults to now on create).
 * @member {Date} occurredAt
 */
CustomerCommunication.prototype['occurredAt'] = undefined;

/**
 * @member {String} subject
 */
CustomerCommunication.prototype['subject'] = undefined;

/**
 * Free-form tags, e.g. `[\"follow-up-required\"]`.
 * @member {Object} tags
 */
CustomerCommunication.prototype['tags'] = undefined;






export default CustomerCommunication;


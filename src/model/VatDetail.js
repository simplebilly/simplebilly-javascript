/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The VatDetail model module.
 * @module model/VatDetail
 * @version 0.1.0
 */
class VatDetail {
    /**
     * Constructs a new <code>VatDetail</code>.
     * @alias module:model/VatDetail
     * @param count {Number} 
     * @param netAmount {String} 
     * @param taxAmount {String} 
     * @param taxRate {String} 
     */
    constructor(count, netAmount, taxAmount, taxRate) { 
        
        VatDetail.initialize(this, count, netAmount, taxAmount, taxRate);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, netAmount, taxAmount, taxRate) { 
        obj['count'] = count;
        obj['net_amount'] = netAmount;
        obj['tax_amount'] = taxAmount;
        obj['tax_rate'] = taxRate;
    }

    /**
     * Constructs a <code>VatDetail</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VatDetail} obj Optional instance to populate.
     * @return {module:model/VatDetail} The populated <code>VatDetail</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VatDetail();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('net_amount')) {
                obj['net_amount'] = ApiClient.convertToType(data['net_amount'], 'String');
            }
            if (data.hasOwnProperty('tax_amount')) {
                obj['tax_amount'] = ApiClient.convertToType(data['tax_amount'], 'String');
            }
            if (data.hasOwnProperty('tax_rate')) {
                obj['tax_rate'] = ApiClient.convertToType(data['tax_rate'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VatDetail</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VatDetail</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VatDetail.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['net_amount'] && !(typeof data['net_amount'] === 'string' || data['net_amount'] instanceof String)) {
            throw new Error("Expected the field `net_amount` to be a primitive type in the JSON string but got " + data['net_amount']);
        }
        // ensure the json data is a string
        if (data['tax_amount'] && !(typeof data['tax_amount'] === 'string' || data['tax_amount'] instanceof String)) {
            throw new Error("Expected the field `tax_amount` to be a primitive type in the JSON string but got " + data['tax_amount']);
        }
        // ensure the json data is a string
        if (data['tax_rate'] && !(typeof data['tax_rate'] === 'string' || data['tax_rate'] instanceof String)) {
            throw new Error("Expected the field `tax_rate` to be a primitive type in the JSON string but got " + data['tax_rate']);
        }

        return true;
    }


}

VatDetail.RequiredProperties = ["count", "net_amount", "tax_amount", "tax_rate"];

/**
 * @member {Number} count
 */
VatDetail.prototype['count'] = undefined;

/**
 * @member {String} net_amount
 */
VatDetail.prototype['net_amount'] = undefined;

/**
 * @member {String} tax_amount
 */
VatDetail.prototype['tax_amount'] = undefined;

/**
 * @member {String} tax_rate
 */
VatDetail.prototype['tax_rate'] = undefined;






export default VatDetail;


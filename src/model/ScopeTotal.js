/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ScopeTotal model module.
 * @module model/ScopeTotal
 * @version 0.1.0
 */
class ScopeTotal {
    /**
     * Constructs a new <code>ScopeTotal</code>.
     * @alias module:model/ScopeTotal
     * @param scope {String} 
     * @param tco2e {String} 
     */
    constructor(scope, tco2e) { 
        
        ScopeTotal.initialize(this, scope, tco2e);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, scope, tco2e) { 
        obj['scope'] = scope;
        obj['tco2e'] = tco2e;
    }

    /**
     * Constructs a <code>ScopeTotal</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ScopeTotal} obj Optional instance to populate.
     * @return {module:model/ScopeTotal} The populated <code>ScopeTotal</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ScopeTotal();

            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], 'String');
            }
            if (data.hasOwnProperty('tco2e')) {
                obj['tco2e'] = ApiClient.convertToType(data['tco2e'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ScopeTotal</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ScopeTotal</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ScopeTotal.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['scope'] && !(typeof data['scope'] === 'string' || data['scope'] instanceof String)) {
            throw new Error("Expected the field `scope` to be a primitive type in the JSON string but got " + data['scope']);
        }
        // ensure the json data is a string
        if (data['tco2e'] && !(typeof data['tco2e'] === 'string' || data['tco2e'] instanceof String)) {
            throw new Error("Expected the field `tco2e` to be a primitive type in the JSON string but got " + data['tco2e']);
        }

        return true;
    }


}

ScopeTotal.RequiredProperties = ["scope", "tco2e"];

/**
 * @member {String} scope
 */
ScopeTotal.prototype['scope'] = undefined;

/**
 * @member {String} tco2e
 */
ScopeTotal.prototype['tco2e'] = undefined;






export default ScopeTotal;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import VatItem from './VatItem';

/**
 * The VatSummary model module.
 * @module model/VatSummary
 * @version 0.1.0
 */
class VatSummary {
    /**
     * Constructs a new <code>VatSummary</code>.
     * @alias module:model/VatSummary
     * @param inputTaxItems {Array.<module:model/VatItem>} 
     * @param outputTaxItems {Array.<module:model/VatItem>} 
     * @param totalInputTax {String} 
     * @param totalOutputTax {String} 
     * @param vatDue {String} 
     */
    constructor(inputTaxItems, outputTaxItems, totalInputTax, totalOutputTax, vatDue) { 
        
        VatSummary.initialize(this, inputTaxItems, outputTaxItems, totalInputTax, totalOutputTax, vatDue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, inputTaxItems, outputTaxItems, totalInputTax, totalOutputTax, vatDue) { 
        obj['input_tax_items'] = inputTaxItems;
        obj['output_tax_items'] = outputTaxItems;
        obj['total_input_tax'] = totalInputTax;
        obj['total_output_tax'] = totalOutputTax;
        obj['vat_due'] = vatDue;
    }

    /**
     * Constructs a <code>VatSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/VatSummary} obj Optional instance to populate.
     * @return {module:model/VatSummary} The populated <code>VatSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new VatSummary();

            if (data.hasOwnProperty('input_tax_items')) {
                obj['input_tax_items'] = ApiClient.convertToType(data['input_tax_items'], [VatItem]);
            }
            if (data.hasOwnProperty('output_tax_items')) {
                obj['output_tax_items'] = ApiClient.convertToType(data['output_tax_items'], [VatItem]);
            }
            if (data.hasOwnProperty('total_input_tax')) {
                obj['total_input_tax'] = ApiClient.convertToType(data['total_input_tax'], 'String');
            }
            if (data.hasOwnProperty('total_output_tax')) {
                obj['total_output_tax'] = ApiClient.convertToType(data['total_output_tax'], 'String');
            }
            if (data.hasOwnProperty('vat_due')) {
                obj['vat_due'] = ApiClient.convertToType(data['vat_due'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>VatSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>VatSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of VatSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['input_tax_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['input_tax_items'])) {
                throw new Error("Expected the field `input_tax_items` to be an array in the JSON data but got " + data['input_tax_items']);
            }
            // validate the optional field `input_tax_items` (array)
            for (const item of data['input_tax_items']) {
                VatItem.validateJSON(item);
            };
        }
        if (data['output_tax_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['output_tax_items'])) {
                throw new Error("Expected the field `output_tax_items` to be an array in the JSON data but got " + data['output_tax_items']);
            }
            // validate the optional field `output_tax_items` (array)
            for (const item of data['output_tax_items']) {
                VatItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_input_tax'] && !(typeof data['total_input_tax'] === 'string' || data['total_input_tax'] instanceof String)) {
            throw new Error("Expected the field `total_input_tax` to be a primitive type in the JSON string but got " + data['total_input_tax']);
        }
        // ensure the json data is a string
        if (data['total_output_tax'] && !(typeof data['total_output_tax'] === 'string' || data['total_output_tax'] instanceof String)) {
            throw new Error("Expected the field `total_output_tax` to be a primitive type in the JSON string but got " + data['total_output_tax']);
        }
        // ensure the json data is a string
        if (data['vat_due'] && !(typeof data['vat_due'] === 'string' || data['vat_due'] instanceof String)) {
            throw new Error("Expected the field `vat_due` to be a primitive type in the JSON string but got " + data['vat_due']);
        }

        return true;
    }


}

VatSummary.RequiredProperties = ["input_tax_items", "output_tax_items", "total_input_tax", "total_output_tax", "vat_due"];

/**
 * @member {Array.<module:model/VatItem>} input_tax_items
 */
VatSummary.prototype['input_tax_items'] = undefined;

/**
 * @member {Array.<module:model/VatItem>} output_tax_items
 */
VatSummary.prototype['output_tax_items'] = undefined;

/**
 * @member {String} total_input_tax
 */
VatSummary.prototype['total_input_tax'] = undefined;

/**
 * @member {String} total_output_tax
 */
VatSummary.prototype['total_output_tax'] = undefined;

/**
 * @member {String} vat_due
 */
VatSummary.prototype['vat_due'] = undefined;






export default VatSummary;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ProductAttributeCreate model module.
 * @module model/ProductAttributeCreate
 * @version 0.1.0
 */
class ProductAttributeCreate {
    /**
     * Constructs a new <code>ProductAttributeCreate</code>.
     * @alias module:model/ProductAttributeCreate
     * @param name {String} Attribute name, e.g. `Material`, `Farbe`, `Gewicht`.
     * @param productId {String} The product this attribute belongs to. References the product entity.
     * @param value {String} Attribute value, e.g. `Baumwolle`, `Rot`, `180g`.
     */
    constructor(name, productId, value) { 
        
        ProductAttributeCreate.initialize(this, name, productId, value);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, productId, value) { 
        obj['name'] = name;
        obj['productId'] = productId;
        obj['value'] = value;
    }

    /**
     * Constructs a <code>ProductAttributeCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductAttributeCreate} obj Optional instance to populate.
     * @return {module:model/ProductAttributeCreate} The populated <code>ProductAttributeCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductAttributeCreate();

            if (data.hasOwnProperty('isFilterable')) {
                obj['isFilterable'] = ApiClient.convertToType(data['isFilterable'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('position')) {
                obj['position'] = ApiClient.convertToType(data['position'], 'Number');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = ApiClient.convertToType(data['unit'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductAttributeCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductAttributeCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductAttributeCreate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['unit'] && !(typeof data['unit'] === 'string' || data['unit'] instanceof String)) {
            throw new Error("Expected the field `unit` to be a primitive type in the JSON string but got " + data['unit']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }

        return true;
    }


}

ProductAttributeCreate.RequiredProperties = ["name", "productId", "value"];

/**
 * Whether this attribute participates in the shop's faceted filters.
 * @member {Boolean} isFilterable
 */
ProductAttributeCreate.prototype['isFilterable'] = undefined;

/**
 * Attribute name, e.g. `Material`, `Farbe`, `Gewicht`.
 * @member {String} name
 */
ProductAttributeCreate.prototype['name'] = undefined;

/**
 * Ordering position within the product's attribute list.
 * @member {Number} position
 */
ProductAttributeCreate.prototype['position'] = undefined;

/**
 * The product this attribute belongs to. References the product entity.
 * @member {String} productId
 */
ProductAttributeCreate.prototype['productId'] = undefined;

/**
 * Optional unit of measure for numeric attributes, e.g. `g`, `cm`.
 * @member {String} unit
 */
ProductAttributeCreate.prototype['unit'] = undefined;

/**
 * Attribute value, e.g. `Baumwolle`, `Rot`, `180g`.
 * @member {String} value
 */
ProductAttributeCreate.prototype['value'] = undefined;






export default ProductAttributeCreate;


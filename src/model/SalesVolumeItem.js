/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SalesVolumeItem model module.
 * @module model/SalesVolumeItem
 * @version 0.1.0
 */
class SalesVolumeItem {
    /**
     * Constructs a new <code>SalesVolumeItem</code>.
     * @alias module:model/SalesVolumeItem
     * @param contactId {String} 
     * @param contactType {String} 
     * @param name {String} 
     * @param totalInvoices {Number} 
     * @param totalRevenue {String} 
     */
    constructor(contactId, contactType, name, totalInvoices, totalRevenue) { 
        
        SalesVolumeItem.initialize(this, contactId, contactType, name, totalInvoices, totalRevenue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contactId, contactType, name, totalInvoices, totalRevenue) { 
        obj['contact_id'] = contactId;
        obj['contact_type'] = contactType;
        obj['name'] = name;
        obj['total_invoices'] = totalInvoices;
        obj['total_revenue'] = totalRevenue;
    }

    /**
     * Constructs a <code>SalesVolumeItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SalesVolumeItem} obj Optional instance to populate.
     * @return {module:model/SalesVolumeItem} The populated <code>SalesVolumeItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SalesVolumeItem();

            if (data.hasOwnProperty('contact_id')) {
                obj['contact_id'] = ApiClient.convertToType(data['contact_id'], 'String');
            }
            if (data.hasOwnProperty('contact_type')) {
                obj['contact_type'] = ApiClient.convertToType(data['contact_type'], 'String');
            }
            if (data.hasOwnProperty('last_purchase_date')) {
                obj['last_purchase_date'] = ApiClient.convertToType(data['last_purchase_date'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('total_invoices')) {
                obj['total_invoices'] = ApiClient.convertToType(data['total_invoices'], 'Number');
            }
            if (data.hasOwnProperty('total_revenue')) {
                obj['total_revenue'] = ApiClient.convertToType(data['total_revenue'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SalesVolumeItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SalesVolumeItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SalesVolumeItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['contact_id'] && !(typeof data['contact_id'] === 'string' || data['contact_id'] instanceof String)) {
            throw new Error("Expected the field `contact_id` to be a primitive type in the JSON string but got " + data['contact_id']);
        }
        // ensure the json data is a string
        if (data['contact_type'] && !(typeof data['contact_type'] === 'string' || data['contact_type'] instanceof String)) {
            throw new Error("Expected the field `contact_type` to be a primitive type in the JSON string but got " + data['contact_type']);
        }
        // ensure the json data is a string
        if (data['last_purchase_date'] && !(typeof data['last_purchase_date'] === 'string' || data['last_purchase_date'] instanceof String)) {
            throw new Error("Expected the field `last_purchase_date` to be a primitive type in the JSON string but got " + data['last_purchase_date']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['total_revenue'] && !(typeof data['total_revenue'] === 'string' || data['total_revenue'] instanceof String)) {
            throw new Error("Expected the field `total_revenue` to be a primitive type in the JSON string but got " + data['total_revenue']);
        }

        return true;
    }


}

SalesVolumeItem.RequiredProperties = ["contact_id", "contact_type", "name", "total_invoices", "total_revenue"];

/**
 * @member {String} contact_id
 */
SalesVolumeItem.prototype['contact_id'] = undefined;

/**
 * @member {String} contact_type
 */
SalesVolumeItem.prototype['contact_type'] = undefined;

/**
 * @member {String} last_purchase_date
 */
SalesVolumeItem.prototype['last_purchase_date'] = undefined;

/**
 * @member {String} name
 */
SalesVolumeItem.prototype['name'] = undefined;

/**
 * @member {Number} total_invoices
 */
SalesVolumeItem.prototype['total_invoices'] = undefined;

/**
 * @member {String} total_revenue
 */
SalesVolumeItem.prototype['total_revenue'] = undefined;






export default SalesVolumeItem;


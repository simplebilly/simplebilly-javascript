/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OffenlegungItem model module.
 * @module model/OffenlegungItem
 * @version 0.1.0
 */
class OffenlegungItem {
    /**
     * Constructs a new <code>OffenlegungItem</code>.
     * @alias module:model/OffenlegungItem
     * @param exists {Boolean} Ob die zugrunde liegenden Daten im System vorhanden sind.
     * @param name {String} Bezeichnung des Offenlegungsbestandteils (§ 325 Abs. 1 HGB).
     * @param source {String} Woher der Bestandteil stammt bzw. fehlt.
     */
    constructor(exists, name, source) { 
        
        OffenlegungItem.initialize(this, exists, name, source);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, exists, name, source) { 
        obj['exists'] = exists;
        obj['name'] = name;
        obj['source'] = source;
    }

    /**
     * Constructs a <code>OffenlegungItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OffenlegungItem} obj Optional instance to populate.
     * @return {module:model/OffenlegungItem} The populated <code>OffenlegungItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OffenlegungItem();

            if (data.hasOwnProperty('exists')) {
                obj['exists'] = ApiClient.convertToType(data['exists'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OffenlegungItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OffenlegungItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OffenlegungItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }

        return true;
    }


}

OffenlegungItem.RequiredProperties = ["exists", "name", "source"];

/**
 * Ob die zugrunde liegenden Daten im System vorhanden sind.
 * @member {Boolean} exists
 */
OffenlegungItem.prototype['exists'] = undefined;

/**
 * Bezeichnung des Offenlegungsbestandteils (§ 325 Abs. 1 HGB).
 * @member {String} name
 */
OffenlegungItem.prototype['name'] = undefined;

/**
 * Woher der Bestandteil stammt bzw. fehlt.
 * @member {String} source
 */
OffenlegungItem.prototype['source'] = undefined;






export default OffenlegungItem;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import RevenueItem from './RevenueItem';

/**
 * The BWARevenue model module.
 * @module model/BWARevenue
 * @version 0.1.0
 */
class BWARevenue {
    /**
     * Constructs a new <code>BWARevenue</code>.
     * @alias module:model/BWARevenue
     * @param revenueBreakdown {Array.<module:model/RevenueItem>} 
     * @param totalRevenue {String} 
     */
    constructor(revenueBreakdown, totalRevenue) { 
        
        BWARevenue.initialize(this, revenueBreakdown, totalRevenue);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, revenueBreakdown, totalRevenue) { 
        obj['revenue_breakdown'] = revenueBreakdown;
        obj['total_revenue'] = totalRevenue;
    }

    /**
     * Constructs a <code>BWARevenue</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BWARevenue} obj Optional instance to populate.
     * @return {module:model/BWARevenue} The populated <code>BWARevenue</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BWARevenue();

            if (data.hasOwnProperty('revenue_breakdown')) {
                obj['revenue_breakdown'] = ApiClient.convertToType(data['revenue_breakdown'], [RevenueItem]);
            }
            if (data.hasOwnProperty('total_revenue')) {
                obj['total_revenue'] = ApiClient.convertToType(data['total_revenue'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BWARevenue</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BWARevenue</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BWARevenue.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['revenue_breakdown']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['revenue_breakdown'])) {
                throw new Error("Expected the field `revenue_breakdown` to be an array in the JSON data but got " + data['revenue_breakdown']);
            }
            // validate the optional field `revenue_breakdown` (array)
            for (const item of data['revenue_breakdown']) {
                RevenueItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_revenue'] && !(typeof data['total_revenue'] === 'string' || data['total_revenue'] instanceof String)) {
            throw new Error("Expected the field `total_revenue` to be a primitive type in the JSON string but got " + data['total_revenue']);
        }

        return true;
    }


}

BWARevenue.RequiredProperties = ["revenue_breakdown", "total_revenue"];

/**
 * @member {Array.<module:model/RevenueItem>} revenue_breakdown
 */
BWARevenue.prototype['revenue_breakdown'] = undefined;

/**
 * @member {String} total_revenue
 */
BWARevenue.prototype['total_revenue'] = undefined;






export default BWARevenue;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SubmitResultDto model module.
 * @module model/SubmitResultDto
 * @version 0.1.0
 */
class SubmitResultDto {
    /**
     * Constructs a new <code>SubmitResultDto</code>.
     * @alias module:model/SubmitResultDto
     * @param answers {Array.<Number>} Selected answer indices (required for scored builtin trainings).
     * @param score {Number} Score 0–100. Only trusted for plugin trainings without server-side scoring; builtin trainings are always re-scored from `answers`.
     * @param trainingCode {String} 
     */
    constructor(answers, score, trainingCode) { 
        
        SubmitResultDto.initialize(this, answers, score, trainingCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, answers, score, trainingCode) { 
        obj['answers'] = answers;
        obj['score'] = score;
        obj['trainingCode'] = trainingCode;
    }

    /**
     * Constructs a <code>SubmitResultDto</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SubmitResultDto} obj Optional instance to populate.
     * @return {module:model/SubmitResultDto} The populated <code>SubmitResultDto</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SubmitResultDto();

            if (data.hasOwnProperty('answers')) {
                obj['answers'] = ApiClient.convertToType(data['answers'], ['Number']);
            }
            if (data.hasOwnProperty('assignmentId')) {
                obj['assignmentId'] = ApiClient.convertToType(data['assignmentId'], 'String');
            }
            if (data.hasOwnProperty('score')) {
                obj['score'] = ApiClient.convertToType(data['score'], 'Number');
            }
            if (data.hasOwnProperty('trainingCode')) {
                obj['trainingCode'] = ApiClient.convertToType(data['trainingCode'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SubmitResultDto</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SubmitResultDto</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SubmitResultDto.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['answers'])) {
            throw new Error("Expected the field `answers` to be an array in the JSON data but got " + data['answers']);
        }
        // ensure the json data is a string
        if (data['assignmentId'] && !(typeof data['assignmentId'] === 'string' || data['assignmentId'] instanceof String)) {
            throw new Error("Expected the field `assignmentId` to be a primitive type in the JSON string but got " + data['assignmentId']);
        }
        // ensure the json data is a string
        if (data['trainingCode'] && !(typeof data['trainingCode'] === 'string' || data['trainingCode'] instanceof String)) {
            throw new Error("Expected the field `trainingCode` to be a primitive type in the JSON string but got " + data['trainingCode']);
        }

        return true;
    }


}

SubmitResultDto.RequiredProperties = ["answers", "score", "trainingCode"];

/**
 * Selected answer indices (required for scored builtin trainings).
 * @member {Array.<Number>} answers
 */
SubmitResultDto.prototype['answers'] = undefined;

/**
 * @member {String} assignmentId
 */
SubmitResultDto.prototype['assignmentId'] = undefined;

/**
 * Score 0–100. Only trusted for plugin trainings without server-side scoring; builtin trainings are always re-scored from `answers`.
 * @member {Number} score
 */
SubmitResultDto.prototype['score'] = undefined;

/**
 * @member {String} trainingCode
 */
SubmitResultDto.prototype['trainingCode'] = undefined;






export default SubmitResultDto;


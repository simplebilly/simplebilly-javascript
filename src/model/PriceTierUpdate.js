/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PriceTierUpdate model module.
 * @module model/PriceTierUpdate
 * @version 0.1.0
 */
class PriceTierUpdate {
    /**
     * Constructs a new <code>PriceTierUpdate</code>.
     * @alias module:model/PriceTierUpdate
     * @param unitPrice {String} Net unit price once `min_quantity` is reached.
     */
    constructor(unitPrice) { 
        
        PriceTierUpdate.initialize(this, unitPrice);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, unitPrice) { 
        obj['unitPrice'] = unitPrice;
    }

    /**
     * Constructs a <code>PriceTierUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PriceTierUpdate} obj Optional instance to populate.
     * @return {module:model/PriceTierUpdate} The populated <code>PriceTierUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PriceTierUpdate();

            if (data.hasOwnProperty('customerGroupId')) {
                obj['customerGroupId'] = ApiClient.convertToType(data['customerGroupId'], 'String');
            }
            if (data.hasOwnProperty('minQuantity')) {
                obj['minQuantity'] = ApiClient.convertToType(data['minQuantity'], 'Number');
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('unitPrice')) {
                obj['unitPrice'] = ApiClient.convertToType(data['unitPrice'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PriceTierUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PriceTierUpdate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PriceTierUpdate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['customerGroupId'] && !(typeof data['customerGroupId'] === 'string' || data['customerGroupId'] instanceof String)) {
            throw new Error("Expected the field `customerGroupId` to be a primitive type in the JSON string but got " + data['customerGroupId']);
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['unitPrice'] && !(typeof data['unitPrice'] === 'string' || data['unitPrice'] instanceof String)) {
            throw new Error("Expected the field `unitPrice` to be a primitive type in the JSON string but got " + data['unitPrice']);
        }

        return true;
    }


}

PriceTierUpdate.RequiredProperties = ["unitPrice"];

/**
 * None = tier applies to all customers; otherwise a customer group id.
 * @member {String} customerGroupId
 */
PriceTierUpdate.prototype['customerGroupId'] = undefined;

/**
 * Quantity from which this tier applies (inclusive).
 * @member {Number} minQuantity
 */
PriceTierUpdate.prototype['minQuantity'] = undefined;

/**
 * References the product entity.
 * @member {String} productId
 */
PriceTierUpdate.prototype['productId'] = undefined;

/**
 * Net unit price once `min_quantity` is reached.
 * @member {String} unitPrice
 */
PriceTierUpdate.prototype['unitPrice'] = undefined;






export default PriceTierUpdate;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GewinnverwendungsZeile model module.
 * @module model/GewinnverwendungsZeile
 * @version 0.1.0
 */
class GewinnverwendungsZeile {
    /**
     * Constructs a new <code>GewinnverwendungsZeile</code>.
     * @alias module:model/GewinnverwendungsZeile
     * @param betrag {String} Betrag in EUR (2 Nachkommastellen, als String formatiert).
     * @param label {String} Deutsche Bezeichnung der Zeile.
     */
    constructor(betrag, label) { 
        
        GewinnverwendungsZeile.initialize(this, betrag, label);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, betrag, label) { 
        obj['betrag'] = betrag;
        obj['label'] = label;
    }

    /**
     * Constructs a <code>GewinnverwendungsZeile</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GewinnverwendungsZeile} obj Optional instance to populate.
     * @return {module:model/GewinnverwendungsZeile} The populated <code>GewinnverwendungsZeile</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GewinnverwendungsZeile();

            if (data.hasOwnProperty('betrag')) {
                obj['betrag'] = ApiClient.convertToType(data['betrag'], 'String');
            }
            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GewinnverwendungsZeile</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GewinnverwendungsZeile</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GewinnverwendungsZeile.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['betrag'] && !(typeof data['betrag'] === 'string' || data['betrag'] instanceof String)) {
            throw new Error("Expected the field `betrag` to be a primitive type in the JSON string but got " + data['betrag']);
        }
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }

        return true;
    }


}

GewinnverwendungsZeile.RequiredProperties = ["betrag", "label"];

/**
 * Betrag in EUR (2 Nachkommastellen, als String formatiert).
 * @member {String} betrag
 */
GewinnverwendungsZeile.prototype['betrag'] = undefined;

/**
 * Deutsche Bezeichnung der Zeile.
 * @member {String} label
 */
GewinnverwendungsZeile.prototype['label'] = undefined;






export default GewinnverwendungsZeile;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The KstErgebnis model module.
 * @module model/KstErgebnis
 * @version 0.1.0
 */
class KstErgebnis {
    /**
     * Constructs a new <code>KstErgebnis</code>.
     * @alias module:model/KstErgebnis
     * @param gesamt {String} 
     * @param gesamtbelastung {String} 
     * @param gewerbesteuer {String} 
     * @param gewinn {String} 
     * @param istKapitalgesellschaft {Boolean} 
     * @param jahr {Number} 
     * @param koerperschaftsteuer {String} 
     * @param solidaritaetszuschlag {String} 
     */
    constructor(gesamt, gesamtbelastung, gewerbesteuer, gewinn, istKapitalgesellschaft, jahr, koerperschaftsteuer, solidaritaetszuschlag) { 
        
        KstErgebnis.initialize(this, gesamt, gesamtbelastung, gewerbesteuer, gewinn, istKapitalgesellschaft, jahr, koerperschaftsteuer, solidaritaetszuschlag);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, gesamt, gesamtbelastung, gewerbesteuer, gewinn, istKapitalgesellschaft, jahr, koerperschaftsteuer, solidaritaetszuschlag) { 
        obj['gesamt'] = gesamt;
        obj['gesamtbelastung'] = gesamtbelastung;
        obj['gewerbesteuer'] = gewerbesteuer;
        obj['gewinn'] = gewinn;
        obj['ist_kapitalgesellschaft'] = istKapitalgesellschaft;
        obj['jahr'] = jahr;
        obj['koerperschaftsteuer'] = koerperschaftsteuer;
        obj['solidaritaetszuschlag'] = solidaritaetszuschlag;
    }

    /**
     * Constructs a <code>KstErgebnis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KstErgebnis} obj Optional instance to populate.
     * @return {module:model/KstErgebnis} The populated <code>KstErgebnis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KstErgebnis();

            if (data.hasOwnProperty('gesamt')) {
                obj['gesamt'] = ApiClient.convertToType(data['gesamt'], 'String');
            }
            if (data.hasOwnProperty('gesamtbelastung')) {
                obj['gesamtbelastung'] = ApiClient.convertToType(data['gesamtbelastung'], 'String');
            }
            if (data.hasOwnProperty('gewerbesteuer')) {
                obj['gewerbesteuer'] = ApiClient.convertToType(data['gewerbesteuer'], 'String');
            }
            if (data.hasOwnProperty('gewinn')) {
                obj['gewinn'] = ApiClient.convertToType(data['gewinn'], 'String');
            }
            if (data.hasOwnProperty('ist_kapitalgesellschaft')) {
                obj['ist_kapitalgesellschaft'] = ApiClient.convertToType(data['ist_kapitalgesellschaft'], 'Boolean');
            }
            if (data.hasOwnProperty('jahr')) {
                obj['jahr'] = ApiClient.convertToType(data['jahr'], 'Number');
            }
            if (data.hasOwnProperty('koerperschaftsteuer')) {
                obj['koerperschaftsteuer'] = ApiClient.convertToType(data['koerperschaftsteuer'], 'String');
            }
            if (data.hasOwnProperty('solidaritaetszuschlag')) {
                obj['solidaritaetszuschlag'] = ApiClient.convertToType(data['solidaritaetszuschlag'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KstErgebnis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KstErgebnis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KstErgebnis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['gesamt'] && !(typeof data['gesamt'] === 'string' || data['gesamt'] instanceof String)) {
            throw new Error("Expected the field `gesamt` to be a primitive type in the JSON string but got " + data['gesamt']);
        }
        // ensure the json data is a string
        if (data['gesamtbelastung'] && !(typeof data['gesamtbelastung'] === 'string' || data['gesamtbelastung'] instanceof String)) {
            throw new Error("Expected the field `gesamtbelastung` to be a primitive type in the JSON string but got " + data['gesamtbelastung']);
        }
        // ensure the json data is a string
        if (data['gewerbesteuer'] && !(typeof data['gewerbesteuer'] === 'string' || data['gewerbesteuer'] instanceof String)) {
            throw new Error("Expected the field `gewerbesteuer` to be a primitive type in the JSON string but got " + data['gewerbesteuer']);
        }
        // ensure the json data is a string
        if (data['gewinn'] && !(typeof data['gewinn'] === 'string' || data['gewinn'] instanceof String)) {
            throw new Error("Expected the field `gewinn` to be a primitive type in the JSON string but got " + data['gewinn']);
        }
        // ensure the json data is a string
        if (data['koerperschaftsteuer'] && !(typeof data['koerperschaftsteuer'] === 'string' || data['koerperschaftsteuer'] instanceof String)) {
            throw new Error("Expected the field `koerperschaftsteuer` to be a primitive type in the JSON string but got " + data['koerperschaftsteuer']);
        }
        // ensure the json data is a string
        if (data['solidaritaetszuschlag'] && !(typeof data['solidaritaetszuschlag'] === 'string' || data['solidaritaetszuschlag'] instanceof String)) {
            throw new Error("Expected the field `solidaritaetszuschlag` to be a primitive type in the JSON string but got " + data['solidaritaetszuschlag']);
        }

        return true;
    }


}

KstErgebnis.RequiredProperties = ["gesamt", "gesamtbelastung", "gewerbesteuer", "gewinn", "ist_kapitalgesellschaft", "jahr", "koerperschaftsteuer", "solidaritaetszuschlag"];

/**
 * @member {String} gesamt
 */
KstErgebnis.prototype['gesamt'] = undefined;

/**
 * @member {String} gesamtbelastung
 */
KstErgebnis.prototype['gesamtbelastung'] = undefined;

/**
 * @member {String} gewerbesteuer
 */
KstErgebnis.prototype['gewerbesteuer'] = undefined;

/**
 * @member {String} gewinn
 */
KstErgebnis.prototype['gewinn'] = undefined;

/**
 * @member {Boolean} ist_kapitalgesellschaft
 */
KstErgebnis.prototype['ist_kapitalgesellschaft'] = undefined;

/**
 * @member {Number} jahr
 */
KstErgebnis.prototype['jahr'] = undefined;

/**
 * @member {String} koerperschaftsteuer
 */
KstErgebnis.prototype['koerperschaftsteuer'] = undefined;

/**
 * @member {String} solidaritaetszuschlag
 */
KstErgebnis.prototype['solidaritaetszuschlag'] = undefined;






export default KstErgebnis;


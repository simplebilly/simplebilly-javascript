/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PayrollRunStatus from './PayrollRunStatus';

/**
 * The PayrollSummaryItem model module.
 * @module model/PayrollSummaryItem
 * @version 0.1.0
 */
class PayrollSummaryItem {
    /**
     * Constructs a new <code>PayrollSummaryItem</code>.
     * @alias module:model/PayrollSummaryItem
     * @param employeeCount {Number} 
     * @param month {String} 
     * @param status {module:model/PayrollRunStatus} 
     * @param totalEmployerCost {String} 
     * @param totalGross {String} 
     * @param totalNet {String} 
     * @param year {Number} 
     */
    constructor(employeeCount, month, status, totalEmployerCost, totalGross, totalNet, year) { 
        
        PayrollSummaryItem.initialize(this, employeeCount, month, status, totalEmployerCost, totalGross, totalNet, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, employeeCount, month, status, totalEmployerCost, totalGross, totalNet, year) { 
        obj['employee_count'] = employeeCount;
        obj['month'] = month;
        obj['status'] = status;
        obj['total_employer_cost'] = totalEmployerCost;
        obj['total_gross'] = totalGross;
        obj['total_net'] = totalNet;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>PayrollSummaryItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PayrollSummaryItem} obj Optional instance to populate.
     * @return {module:model/PayrollSummaryItem} The populated <code>PayrollSummaryItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PayrollSummaryItem();

            if (data.hasOwnProperty('employee_count')) {
                obj['employee_count'] = ApiClient.convertToType(data['employee_count'], 'Number');
            }
            if (data.hasOwnProperty('month')) {
                obj['month'] = ApiClient.convertToType(data['month'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = PayrollRunStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('total_employer_cost')) {
                obj['total_employer_cost'] = ApiClient.convertToType(data['total_employer_cost'], 'String');
            }
            if (data.hasOwnProperty('total_gross')) {
                obj['total_gross'] = ApiClient.convertToType(data['total_gross'], 'String');
            }
            if (data.hasOwnProperty('total_net')) {
                obj['total_net'] = ApiClient.convertToType(data['total_net'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PayrollSummaryItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PayrollSummaryItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PayrollSummaryItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['month'] && !(typeof data['month'] === 'string' || data['month'] instanceof String)) {
            throw new Error("Expected the field `month` to be a primitive type in the JSON string but got " + data['month']);
        }
        // ensure the json data is a string
        if (data['total_employer_cost'] && !(typeof data['total_employer_cost'] === 'string' || data['total_employer_cost'] instanceof String)) {
            throw new Error("Expected the field `total_employer_cost` to be a primitive type in the JSON string but got " + data['total_employer_cost']);
        }
        // ensure the json data is a string
        if (data['total_gross'] && !(typeof data['total_gross'] === 'string' || data['total_gross'] instanceof String)) {
            throw new Error("Expected the field `total_gross` to be a primitive type in the JSON string but got " + data['total_gross']);
        }
        // ensure the json data is a string
        if (data['total_net'] && !(typeof data['total_net'] === 'string' || data['total_net'] instanceof String)) {
            throw new Error("Expected the field `total_net` to be a primitive type in the JSON string but got " + data['total_net']);
        }

        return true;
    }


}

PayrollSummaryItem.RequiredProperties = ["employee_count", "month", "status", "total_employer_cost", "total_gross", "total_net", "year"];

/**
 * @member {Number} employee_count
 */
PayrollSummaryItem.prototype['employee_count'] = undefined;

/**
 * @member {String} month
 */
PayrollSummaryItem.prototype['month'] = undefined;

/**
 * @member {module:model/PayrollRunStatus} status
 */
PayrollSummaryItem.prototype['status'] = undefined;

/**
 * @member {String} total_employer_cost
 */
PayrollSummaryItem.prototype['total_employer_cost'] = undefined;

/**
 * @member {String} total_gross
 */
PayrollSummaryItem.prototype['total_gross'] = undefined;

/**
 * @member {String} total_net
 */
PayrollSummaryItem.prototype['total_net'] = undefined;

/**
 * @member {Number} year
 */
PayrollSummaryItem.prototype['year'] = undefined;






export default PayrollSummaryItem;


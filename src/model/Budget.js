/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Budget model module.
 * @module model/Budget
 * @version 0.1.0
 */
class Budget {
    /**
     * Constructs a new <code>Budget</code>.
     * @alias module:model/Budget
     * @param category {String} Posting category key (matches `category` on journal entries).
     * @param monthlyGoal {String} Monthly goal amount (gross). 0 means \"no goal set\".
     * @param year {Number} Budget year the goal applies to.
     */
    constructor(category, monthlyGoal, year) { 
        
        Budget.initialize(this, category, monthlyGoal, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, category, monthlyGoal, year) { 
        obj['category'] = category;
        obj['monthlyGoal'] = monthlyGoal;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>Budget</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Budget} obj Optional instance to populate.
     * @return {module:model/Budget} The populated <code>Budget</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Budget();

            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('monthlyGoal')) {
                obj['monthlyGoal'] = ApiClient.convertToType(data['monthlyGoal'], 'String');
            }
            if (data.hasOwnProperty('updatedAt')) {
                obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Budget</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Budget</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Budget.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }
        // ensure the json data is a string
        if (data['monthlyGoal'] && !(typeof data['monthlyGoal'] === 'string' || data['monthlyGoal'] instanceof String)) {
            throw new Error("Expected the field `monthlyGoal` to be a primitive type in the JSON string but got " + data['monthlyGoal']);
        }

        return true;
    }


}

Budget.RequiredProperties = ["category", "monthlyGoal", "year"];

/**
 * Posting category key (matches `category` on journal entries).
 * @member {String} category
 */
Budget.prototype['category'] = undefined;

/**
 * Monthly goal amount (gross). 0 means \"no goal set\".
 * @member {String} monthlyGoal
 */
Budget.prototype['monthlyGoal'] = undefined;

/**
 * @member {Date} updatedAt
 */
Budget.prototype['updatedAt'] = undefined;

/**
 * Budget year the goal applies to.
 * @member {Number} year
 */
Budget.prototype['year'] = undefined;






export default Budget;


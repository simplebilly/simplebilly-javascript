/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TrackedShipment from './TrackedShipment';

/**
 * The TrackOrderResponse model module.
 * @module model/TrackOrderResponse
 * @version 0.1.0
 */
class TrackOrderResponse {
    /**
     * Constructs a new <code>TrackOrderResponse</code>.
     * @alias module:model/TrackOrderResponse
     * @param orderNumber {String} 
     * @param orderStatus {String} 
     * @param shipments {Array.<module:model/TrackedShipment>} 
     */
    constructor(orderNumber, orderStatus, shipments) { 
        
        TrackOrderResponse.initialize(this, orderNumber, orderStatus, shipments);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderNumber, orderStatus, shipments) { 
        obj['order_number'] = orderNumber;
        obj['order_status'] = orderStatus;
        obj['shipments'] = shipments;
    }

    /**
     * Constructs a <code>TrackOrderResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrackOrderResponse} obj Optional instance to populate.
     * @return {module:model/TrackOrderResponse} The populated <code>TrackOrderResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrackOrderResponse();

            if (data.hasOwnProperty('order_number')) {
                obj['order_number'] = ApiClient.convertToType(data['order_number'], 'String');
            }
            if (data.hasOwnProperty('order_status')) {
                obj['order_status'] = ApiClient.convertToType(data['order_status'], 'String');
            }
            if (data.hasOwnProperty('shipments')) {
                obj['shipments'] = ApiClient.convertToType(data['shipments'], [TrackedShipment]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrackOrderResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrackOrderResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrackOrderResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['order_number'] && !(typeof data['order_number'] === 'string' || data['order_number'] instanceof String)) {
            throw new Error("Expected the field `order_number` to be a primitive type in the JSON string but got " + data['order_number']);
        }
        // ensure the json data is a string
        if (data['order_status'] && !(typeof data['order_status'] === 'string' || data['order_status'] instanceof String)) {
            throw new Error("Expected the field `order_status` to be a primitive type in the JSON string but got " + data['order_status']);
        }
        if (data['shipments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['shipments'])) {
                throw new Error("Expected the field `shipments` to be an array in the JSON data but got " + data['shipments']);
            }
            // validate the optional field `shipments` (array)
            for (const item of data['shipments']) {
                TrackedShipment.validateJSON(item);
            };
        }

        return true;
    }


}

TrackOrderResponse.RequiredProperties = ["order_number", "order_status", "shipments"];

/**
 * @member {String} order_number
 */
TrackOrderResponse.prototype['order_number'] = undefined;

/**
 * @member {String} order_status
 */
TrackOrderResponse.prototype['order_status'] = undefined;

/**
 * @member {Array.<module:model/TrackedShipment>} shipments
 */
TrackOrderResponse.prototype['shipments'] = undefined;






export default TrackOrderResponse;


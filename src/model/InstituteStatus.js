/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import InstituteCheckItem from './InstituteCheckItem';
import InstituteDeadlines from './InstituteDeadlines';

/**
 * The InstituteStatus model module.
 * @module model/InstituteStatus
 * @version 0.1.0
 */
class InstituteStatus {
    /**
     * Constructs a new <code>InstituteStatus</code>.
     * @alias module:model/InstituteStatus
     * @param checklist {Array.<module:model/InstituteCheckItem>} 
     * @param deadlines {module:model/InstituteDeadlines} 
     * @param instituteType {String} 
     * @param kapitalmarktorientiert {Boolean} 
     */
    constructor(checklist, deadlines, instituteType, kapitalmarktorientiert) { 
        
        InstituteStatus.initialize(this, checklist, deadlines, instituteType, kapitalmarktorientiert);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, checklist, deadlines, instituteType, kapitalmarktorientiert) { 
        obj['checklist'] = checklist;
        obj['deadlines'] = deadlines;
        obj['instituteType'] = instituteType;
        obj['kapitalmarktorientiert'] = kapitalmarktorientiert;
    }

    /**
     * Constructs a <code>InstituteStatus</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/InstituteStatus} obj Optional instance to populate.
     * @return {module:model/InstituteStatus} The populated <code>InstituteStatus</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new InstituteStatus();

            if (data.hasOwnProperty('checklist')) {
                obj['checklist'] = ApiClient.convertToType(data['checklist'], [InstituteCheckItem]);
            }
            if (data.hasOwnProperty('deadlines')) {
                obj['deadlines'] = InstituteDeadlines.constructFromObject(data['deadlines']);
            }
            if (data.hasOwnProperty('instituteType')) {
                obj['instituteType'] = ApiClient.convertToType(data['instituteType'], 'String');
            }
            if (data.hasOwnProperty('kapitalmarktorientiert')) {
                obj['kapitalmarktorientiert'] = ApiClient.convertToType(data['kapitalmarktorientiert'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>InstituteStatus</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>InstituteStatus</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of InstituteStatus.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['checklist']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['checklist'])) {
                throw new Error("Expected the field `checklist` to be an array in the JSON data but got " + data['checklist']);
            }
            // validate the optional field `checklist` (array)
            for (const item of data['checklist']) {
                InstituteCheckItem.validateJSON(item);
            };
        }
        // validate the optional field `deadlines`
        if (data['deadlines']) { // data not null
          InstituteDeadlines.validateJSON(data['deadlines']);
        }
        // ensure the json data is a string
        if (data['instituteType'] && !(typeof data['instituteType'] === 'string' || data['instituteType'] instanceof String)) {
            throw new Error("Expected the field `instituteType` to be a primitive type in the JSON string but got " + data['instituteType']);
        }

        return true;
    }


}

InstituteStatus.RequiredProperties = ["checklist", "deadlines", "instituteType", "kapitalmarktorientiert"];

/**
 * @member {Array.<module:model/InstituteCheckItem>} checklist
 */
InstituteStatus.prototype['checklist'] = undefined;

/**
 * @member {module:model/InstituteDeadlines} deadlines
 */
InstituteStatus.prototype['deadlines'] = undefined;

/**
 * @member {String} instituteType
 */
InstituteStatus.prototype['instituteType'] = undefined;

/**
 * @member {Boolean} kapitalmarktorientiert
 */
InstituteStatus.prototype['kapitalmarktorientiert'] = undefined;






export default InstituteStatus;


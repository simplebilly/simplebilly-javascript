/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import KostenEintrag from './KostenEintrag';

/**
 * The KostenVorschau model module.
 * @module model/KostenVorschau
 * @version 0.1.0
 */
class KostenVorschau {
    /**
     * Constructs a new <code>KostenVorschau</code>.
     * @alias module:model/KostenVorschau
     * @param eintraege {Array.<module:model/KostenEintrag>} 
     * @param gesamt {String} 
     */
    constructor(eintraege, gesamt) { 
        
        KostenVorschau.initialize(this, eintraege, gesamt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, eintraege, gesamt) { 
        obj['eintraege'] = eintraege;
        obj['gesamt'] = gesamt;
    }

    /**
     * Constructs a <code>KostenVorschau</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KostenVorschau} obj Optional instance to populate.
     * @return {module:model/KostenVorschau} The populated <code>KostenVorschau</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KostenVorschau();

            if (data.hasOwnProperty('eintraege')) {
                obj['eintraege'] = ApiClient.convertToType(data['eintraege'], [KostenEintrag]);
            }
            if (data.hasOwnProperty('gesamt')) {
                obj['gesamt'] = ApiClient.convertToType(data['gesamt'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KostenVorschau</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KostenVorschau</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KostenVorschau.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['eintraege']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['eintraege'])) {
                throw new Error("Expected the field `eintraege` to be an array in the JSON data but got " + data['eintraege']);
            }
            // validate the optional field `eintraege` (array)
            for (const item of data['eintraege']) {
                KostenEintrag.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['gesamt'] && !(typeof data['gesamt'] === 'string' || data['gesamt'] instanceof String)) {
            throw new Error("Expected the field `gesamt` to be a primitive type in the JSON string but got " + data['gesamt']);
        }

        return true;
    }


}

KostenVorschau.RequiredProperties = ["eintraege", "gesamt"];

/**
 * @member {Array.<module:model/KostenEintrag>} eintraege
 */
KostenVorschau.prototype['eintraege'] = undefined;

/**
 * @member {String} gesamt
 */
KostenVorschau.prototype['gesamt'] = undefined;






export default KostenVorschau;


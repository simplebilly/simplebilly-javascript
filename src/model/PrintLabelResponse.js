/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PrintLabelResponse model module.
 * @module model/PrintLabelResponse
 * @version 0.1.0
 */
class PrintLabelResponse {
    /**
     * Constructs a new <code>PrintLabelResponse</code>.
     * @alias module:model/PrintLabelResponse
     * @param message {String} 
     * @param success {Boolean} 
     */
    constructor(message, success) { 
        
        PrintLabelResponse.initialize(this, message, success);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, message, success) { 
        obj['message'] = message;
        obj['success'] = success;
    }

    /**
     * Constructs a <code>PrintLabelResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PrintLabelResponse} obj Optional instance to populate.
     * @return {module:model/PrintLabelResponse} The populated <code>PrintLabelResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PrintLabelResponse();

            if (data.hasOwnProperty('label_url')) {
                obj['label_url'] = ApiClient.convertToType(data['label_url'], 'String');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('sscc')) {
                obj['sscc'] = ApiClient.convertToType(data['sscc'], 'String');
            }
            if (data.hasOwnProperty('success')) {
                obj['success'] = ApiClient.convertToType(data['success'], 'Boolean');
            }
            if (data.hasOwnProperty('tracking_number')) {
                obj['tracking_number'] = ApiClient.convertToType(data['tracking_number'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PrintLabelResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PrintLabelResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PrintLabelResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['label_url'] && !(typeof data['label_url'] === 'string' || data['label_url'] instanceof String)) {
            throw new Error("Expected the field `label_url` to be a primitive type in the JSON string but got " + data['label_url']);
        }
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        // ensure the json data is a string
        if (data['sscc'] && !(typeof data['sscc'] === 'string' || data['sscc'] instanceof String)) {
            throw new Error("Expected the field `sscc` to be a primitive type in the JSON string but got " + data['sscc']);
        }
        // ensure the json data is a string
        if (data['tracking_number'] && !(typeof data['tracking_number'] === 'string' || data['tracking_number'] instanceof String)) {
            throw new Error("Expected the field `tracking_number` to be a primitive type in the JSON string but got " + data['tracking_number']);
        }

        return true;
    }


}

PrintLabelResponse.RequiredProperties = ["message", "success"];

/**
 * @member {String} label_url
 */
PrintLabelResponse.prototype['label_url'] = undefined;

/**
 * @member {String} message
 */
PrintLabelResponse.prototype['message'] = undefined;

/**
 * @member {String} sscc
 */
PrintLabelResponse.prototype['sscc'] = undefined;

/**
 * @member {Boolean} success
 */
PrintLabelResponse.prototype['success'] = undefined;

/**
 * @member {String} tracking_number
 */
PrintLabelResponse.prototype['tracking_number'] = undefined;






export default PrintLabelResponse;


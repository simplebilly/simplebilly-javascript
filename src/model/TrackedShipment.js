/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TrackingEvent from './TrackingEvent';

/**
 * The TrackedShipment model module.
 * @module model/TrackedShipment
 * @version 0.1.0
 */
class TrackedShipment {
    /**
     * Constructs a new <code>TrackedShipment</code>.
     * @alias module:model/TrackedShipment
     * @param carrier {String} 
     * @param events {Array.<module:model/TrackingEvent>} 
     * @param status {String} 
     */
    constructor(carrier, events, status) { 
        
        TrackedShipment.initialize(this, carrier, events, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, carrier, events, status) { 
        obj['carrier'] = carrier;
        obj['events'] = events;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>TrackedShipment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrackedShipment} obj Optional instance to populate.
     * @return {module:model/TrackedShipment} The populated <code>TrackedShipment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrackedShipment();

            if (data.hasOwnProperty('carrier')) {
                obj['carrier'] = ApiClient.convertToType(data['carrier'], 'String');
            }
            if (data.hasOwnProperty('events')) {
                obj['events'] = ApiClient.convertToType(data['events'], [TrackingEvent]);
            }
            if (data.hasOwnProperty('label_url')) {
                obj['label_url'] = ApiClient.convertToType(data['label_url'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('tracking_number')) {
                obj['tracking_number'] = ApiClient.convertToType(data['tracking_number'], 'String');
            }
            if (data.hasOwnProperty('tracking_url')) {
                obj['tracking_url'] = ApiClient.convertToType(data['tracking_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrackedShipment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrackedShipment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrackedShipment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['carrier'] && !(typeof data['carrier'] === 'string' || data['carrier'] instanceof String)) {
            throw new Error("Expected the field `carrier` to be a primitive type in the JSON string but got " + data['carrier']);
        }
        if (data['events']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['events'])) {
                throw new Error("Expected the field `events` to be an array in the JSON data but got " + data['events']);
            }
            // validate the optional field `events` (array)
            for (const item of data['events']) {
                TrackingEvent.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['label_url'] && !(typeof data['label_url'] === 'string' || data['label_url'] instanceof String)) {
            throw new Error("Expected the field `label_url` to be a primitive type in the JSON string but got " + data['label_url']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['tracking_number'] && !(typeof data['tracking_number'] === 'string' || data['tracking_number'] instanceof String)) {
            throw new Error("Expected the field `tracking_number` to be a primitive type in the JSON string but got " + data['tracking_number']);
        }
        // ensure the json data is a string
        if (data['tracking_url'] && !(typeof data['tracking_url'] === 'string' || data['tracking_url'] instanceof String)) {
            throw new Error("Expected the field `tracking_url` to be a primitive type in the JSON string but got " + data['tracking_url']);
        }

        return true;
    }


}

TrackedShipment.RequiredProperties = ["carrier", "events", "status"];

/**
 * @member {String} carrier
 */
TrackedShipment.prototype['carrier'] = undefined;

/**
 * @member {Array.<module:model/TrackingEvent>} events
 */
TrackedShipment.prototype['events'] = undefined;

/**
 * @member {String} label_url
 */
TrackedShipment.prototype['label_url'] = undefined;

/**
 * @member {String} status
 */
TrackedShipment.prototype['status'] = undefined;

/**
 * @member {String} tracking_number
 */
TrackedShipment.prototype['tracking_number'] = undefined;

/**
 * @member {String} tracking_url
 */
TrackedShipment.prototype['tracking_url'] = undefined;






export default TrackedShipment;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ShareholderUpdate model module.
 * @module model/ShareholderUpdate
 * @version 0.1.0
 */
class ShareholderUpdate {
    /**
     * Constructs a new <code>ShareholderUpdate</code>.
     * @alias module:model/ShareholderUpdate
     */
    constructor() { 
        
        ShareholderUpdate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ShareholderUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ShareholderUpdate} obj Optional instance to populate.
     * @return {module:model/ShareholderUpdate} The populated <code>ShareholderUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ShareholderUpdate();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('birthDate')) {
                obj['birthDate'] = ApiClient.convertToType(data['birthDate'], 'Date');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('firstName')) {
                obj['firstName'] = ApiClient.convertToType(data['firstName'], 'String');
            }
            if (data.hasOwnProperty('lastName')) {
                obj['lastName'] = ApiClient.convertToType(data['lastName'], 'String');
            }
            if (data.hasOwnProperty('shareNumber')) {
                obj['shareNumber'] = ApiClient.convertToType(data['shareNumber'], 'String');
            }
            if (data.hasOwnProperty('shares')) {
                obj['shares'] = ApiClient.convertToType(data['shares'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ShareholderUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ShareholderUpdate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['firstName'] && !(typeof data['firstName'] === 'string' || data['firstName'] instanceof String)) {
            throw new Error("Expected the field `firstName` to be a primitive type in the JSON string but got " + data['firstName']);
        }
        // ensure the json data is a string
        if (data['lastName'] && !(typeof data['lastName'] === 'string' || data['lastName'] instanceof String)) {
            throw new Error("Expected the field `lastName` to be a primitive type in the JSON string but got " + data['lastName']);
        }
        // ensure the json data is a string
        if (data['shareNumber'] && !(typeof data['shareNumber'] === 'string' || data['shareNumber'] instanceof String)) {
            throw new Error("Expected the field `shareNumber` to be a primitive type in the JSON string but got " + data['shareNumber']);
        }
        // ensure the json data is a string
        if (data['shares'] && !(typeof data['shares'] === 'string' || data['shares'] instanceof String)) {
            throw new Error("Expected the field `shares` to be a primitive type in the JSON string but got " + data['shares']);
        }

        return true;
    }


}



/**
 * Anschrift des Aktionärs (§ 67 Abs. 1 AktG).
 * @member {String} address
 */
ShareholderUpdate.prototype['address'] = undefined;

/**
 * Geburtsdatum des Aktionärs (§ 67 Abs. 1 AktG).
 * @member {Date} birthDate
 */
ShareholderUpdate.prototype['birthDate'] = undefined;

/**
 * Elektronische Adresse (E-Mail) für die Kommunikation der Gesellschaft.
 * @member {String} email
 */
ShareholderUpdate.prototype['email'] = undefined;

/**
 * Vorname des Aktionärs (§ 67 Abs. 1 AktG).
 * @member {String} firstName
 */
ShareholderUpdate.prototype['firstName'] = undefined;

/**
 * Nachname des Aktionärs (§ 67 Abs. 1 AktG).
 * @member {String} lastName
 */
ShareholderUpdate.prototype['lastName'] = undefined;

/**
 * Aktiennummer bzw. Sammelurkunde (bei Nennbetragsaktien).
 * @member {String} shareNumber
 */
ShareholderUpdate.prototype['shareNumber'] = undefined;

/**
 * Stückzahl der gehaltenen Stückaktien (§ 67 Abs. 1 AktG).
 * @member {String} shares
 */
ShareholderUpdate.prototype['shares'] = undefined;






export default ShareholderUpdate;


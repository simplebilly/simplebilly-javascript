/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ContactInfo from './ContactInfo';
import QuizQuestion from './QuizQuestion';
import Section from './Section';

/**
 * The TrainingContent model module.
 * @module model/TrainingContent
 * @version 0.1.0
 */
class TrainingContent {
    /**
     * Constructs a new <code>TrainingContent</code>.
     * @alias module:model/TrainingContent
     * @param code {String} 
     * @param contact {module:model/ContactInfo} 
     * @param passScore {Number} 
     * @param quiz {Array.<module:model/QuizQuestion>} 
     * @param sections {Array.<module:model/Section>} 
     * @param title {String} 
     * @param titleEn {String} 
     */
    constructor(code, contact, passScore, quiz, sections, title, titleEn) { 
        
        TrainingContent.initialize(this, code, contact, passScore, quiz, sections, title, titleEn);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, code, contact, passScore, quiz, sections, title, titleEn) { 
        obj['code'] = code;
        obj['contact'] = contact;
        obj['passScore'] = passScore;
        obj['quiz'] = quiz;
        obj['sections'] = sections;
        obj['title'] = title;
        obj['titleEn'] = titleEn;
    }

    /**
     * Constructs a <code>TrainingContent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TrainingContent} obj Optional instance to populate.
     * @return {module:model/TrainingContent} The populated <code>TrainingContent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TrainingContent();

            if (data.hasOwnProperty('code')) {
                obj['code'] = ApiClient.convertToType(data['code'], 'String');
            }
            if (data.hasOwnProperty('contact')) {
                obj['contact'] = ContactInfo.constructFromObject(data['contact']);
            }
            if (data.hasOwnProperty('passScore')) {
                obj['passScore'] = ApiClient.convertToType(data['passScore'], 'Number');
            }
            if (data.hasOwnProperty('quiz')) {
                obj['quiz'] = ApiClient.convertToType(data['quiz'], [QuizQuestion]);
            }
            if (data.hasOwnProperty('sections')) {
                obj['sections'] = ApiClient.convertToType(data['sections'], [Section]);
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('titleEn')) {
                obj['titleEn'] = ApiClient.convertToType(data['titleEn'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TrainingContent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TrainingContent</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TrainingContent.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['code'] && !(typeof data['code'] === 'string' || data['code'] instanceof String)) {
            throw new Error("Expected the field `code` to be a primitive type in the JSON string but got " + data['code']);
        }
        // validate the optional field `contact`
        if (data['contact']) { // data not null
          ContactInfo.validateJSON(data['contact']);
        }
        if (data['quiz']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['quiz'])) {
                throw new Error("Expected the field `quiz` to be an array in the JSON data but got " + data['quiz']);
            }
            // validate the optional field `quiz` (array)
            for (const item of data['quiz']) {
                QuizQuestion.validateJSON(item);
            };
        }
        if (data['sections']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['sections'])) {
                throw new Error("Expected the field `sections` to be an array in the JSON data but got " + data['sections']);
            }
            // validate the optional field `sections` (array)
            for (const item of data['sections']) {
                Section.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['titleEn'] && !(typeof data['titleEn'] === 'string' || data['titleEn'] instanceof String)) {
            throw new Error("Expected the field `titleEn` to be a primitive type in the JSON string but got " + data['titleEn']);
        }

        return true;
    }


}

TrainingContent.RequiredProperties = ["code", "contact", "passScore", "quiz", "sections", "title", "titleEn"];

/**
 * @member {String} code
 */
TrainingContent.prototype['code'] = undefined;

/**
 * @member {module:model/ContactInfo} contact
 */
TrainingContent.prototype['contact'] = undefined;

/**
 * @member {Number} passScore
 */
TrainingContent.prototype['passScore'] = undefined;

/**
 * @member {Array.<module:model/QuizQuestion>} quiz
 */
TrainingContent.prototype['quiz'] = undefined;

/**
 * @member {Array.<module:model/Section>} sections
 */
TrainingContent.prototype['sections'] = undefined;

/**
 * @member {String} title
 */
TrainingContent.prototype['title'] = undefined;

/**
 * @member {String} titleEn
 */
TrainingContent.prototype['titleEn'] = undefined;






export default TrainingContent;


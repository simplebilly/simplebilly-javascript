/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExpenseItem from './ExpenseItem';

/**
 * The BWAExpenses model module.
 * @module model/BWAExpenses
 * @version 0.1.0
 */
class BWAExpenses {
    /**
     * Constructs a new <code>BWAExpenses</code>.
     * @alias module:model/BWAExpenses
     * @param expenseBreakdown {Array.<module:model/ExpenseItem>} 
     * @param totalExpenses {String} 
     */
    constructor(expenseBreakdown, totalExpenses) { 
        
        BWAExpenses.initialize(this, expenseBreakdown, totalExpenses);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, expenseBreakdown, totalExpenses) { 
        obj['expense_breakdown'] = expenseBreakdown;
        obj['total_expenses'] = totalExpenses;
    }

    /**
     * Constructs a <code>BWAExpenses</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BWAExpenses} obj Optional instance to populate.
     * @return {module:model/BWAExpenses} The populated <code>BWAExpenses</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BWAExpenses();

            if (data.hasOwnProperty('expense_breakdown')) {
                obj['expense_breakdown'] = ApiClient.convertToType(data['expense_breakdown'], [ExpenseItem]);
            }
            if (data.hasOwnProperty('total_expenses')) {
                obj['total_expenses'] = ApiClient.convertToType(data['total_expenses'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BWAExpenses</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BWAExpenses</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BWAExpenses.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['expense_breakdown']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['expense_breakdown'])) {
                throw new Error("Expected the field `expense_breakdown` to be an array in the JSON data but got " + data['expense_breakdown']);
            }
            // validate the optional field `expense_breakdown` (array)
            for (const item of data['expense_breakdown']) {
                ExpenseItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['total_expenses'] && !(typeof data['total_expenses'] === 'string' || data['total_expenses'] instanceof String)) {
            throw new Error("Expected the field `total_expenses` to be a primitive type in the JSON string but got " + data['total_expenses']);
        }

        return true;
    }


}

BWAExpenses.RequiredProperties = ["expense_breakdown", "total_expenses"];

/**
 * @member {Array.<module:model/ExpenseItem>} expense_breakdown
 */
BWAExpenses.prototype['expense_breakdown'] = undefined;

/**
 * @member {String} total_expenses
 */
BWAExpenses.prototype['total_expenses'] = undefined;






export default BWAExpenses;


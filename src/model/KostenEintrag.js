/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The KostenEintrag model module.
 * @module model/KostenEintrag
 * @version 0.1.0
 */
class KostenEintrag {
    /**
     * Constructs a new <code>KostenEintrag</code>.
     * @alias module:model/KostenEintrag
     * @param beschreibung {String} 
     * @param betrag {String} 
     * @param datum {String} 
     * @param typ {String} 
     */
    constructor(beschreibung, betrag, datum, typ) { 
        
        KostenEintrag.initialize(this, beschreibung, betrag, datum, typ);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, beschreibung, betrag, datum, typ) { 
        obj['beschreibung'] = beschreibung;
        obj['betrag'] = betrag;
        obj['datum'] = datum;
        obj['typ'] = typ;
    }

    /**
     * Constructs a <code>KostenEintrag</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KostenEintrag} obj Optional instance to populate.
     * @return {module:model/KostenEintrag} The populated <code>KostenEintrag</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KostenEintrag();

            if (data.hasOwnProperty('beschreibung')) {
                obj['beschreibung'] = ApiClient.convertToType(data['beschreibung'], 'String');
            }
            if (data.hasOwnProperty('betrag')) {
                obj['betrag'] = ApiClient.convertToType(data['betrag'], 'String');
            }
            if (data.hasOwnProperty('datum')) {
                obj['datum'] = ApiClient.convertToType(data['datum'], 'String');
            }
            if (data.hasOwnProperty('typ')) {
                obj['typ'] = ApiClient.convertToType(data['typ'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KostenEintrag</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KostenEintrag</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KostenEintrag.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['beschreibung'] && !(typeof data['beschreibung'] === 'string' || data['beschreibung'] instanceof String)) {
            throw new Error("Expected the field `beschreibung` to be a primitive type in the JSON string but got " + data['beschreibung']);
        }
        // ensure the json data is a string
        if (data['betrag'] && !(typeof data['betrag'] === 'string' || data['betrag'] instanceof String)) {
            throw new Error("Expected the field `betrag` to be a primitive type in the JSON string but got " + data['betrag']);
        }
        // ensure the json data is a string
        if (data['datum'] && !(typeof data['datum'] === 'string' || data['datum'] instanceof String)) {
            throw new Error("Expected the field `datum` to be a primitive type in the JSON string but got " + data['datum']);
        }
        // ensure the json data is a string
        if (data['typ'] && !(typeof data['typ'] === 'string' || data['typ'] instanceof String)) {
            throw new Error("Expected the field `typ` to be a primitive type in the JSON string but got " + data['typ']);
        }

        return true;
    }


}

KostenEintrag.RequiredProperties = ["beschreibung", "betrag", "datum", "typ"];

/**
 * @member {String} beschreibung
 */
KostenEintrag.prototype['beschreibung'] = undefined;

/**
 * @member {String} betrag
 */
KostenEintrag.prototype['betrag'] = undefined;

/**
 * @member {String} datum
 */
KostenEintrag.prototype['datum'] = undefined;

/**
 * @member {String} typ
 */
KostenEintrag.prototype['typ'] = undefined;






export default KostenEintrag;


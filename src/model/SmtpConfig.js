/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import SmtpEncryption from './SmtpEncryption';

/**
 * The SmtpConfig model module.
 * @module model/SmtpConfig
 * @version 0.1.0
 */
class SmtpConfig {
    /**
     * Constructs a new <code>SmtpConfig</code>.
     * @alias module:model/SmtpConfig
     * @param encryption {module:model/SmtpEncryption} 
     * @param fromAddress {String} 
     * @param host {String} 
     * @param password {String} 
     * @param port {Number} 
     * @param username {String} 
     */
    constructor(encryption, fromAddress, host, password, port, username) { 
        
        SmtpConfig.initialize(this, encryption, fromAddress, host, password, port, username);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, encryption, fromAddress, host, password, port, username) { 
        obj['encryption'] = encryption;
        obj['from_address'] = fromAddress;
        obj['host'] = host;
        obj['password'] = password;
        obj['port'] = port;
        obj['username'] = username;
    }

    /**
     * Constructs a <code>SmtpConfig</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SmtpConfig} obj Optional instance to populate.
     * @return {module:model/SmtpConfig} The populated <code>SmtpConfig</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SmtpConfig();

            if (data.hasOwnProperty('encryption')) {
                obj['encryption'] = SmtpEncryption.constructFromObject(data['encryption']);
            }
            if (data.hasOwnProperty('from_address')) {
                obj['from_address'] = ApiClient.convertToType(data['from_address'], 'String');
            }
            if (data.hasOwnProperty('from_name')) {
                obj['from_name'] = ApiClient.convertToType(data['from_name'], 'String');
            }
            if (data.hasOwnProperty('host')) {
                obj['host'] = ApiClient.convertToType(data['host'], 'String');
            }
            if (data.hasOwnProperty('password')) {
                obj['password'] = ApiClient.convertToType(data['password'], 'String');
            }
            if (data.hasOwnProperty('port')) {
                obj['port'] = ApiClient.convertToType(data['port'], 'Number');
            }
            if (data.hasOwnProperty('timeout_seconds')) {
                obj['timeout_seconds'] = ApiClient.convertToType(data['timeout_seconds'], 'Number');
            }
            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SmtpConfig</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SmtpConfig</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SmtpConfig.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['from_address'] && !(typeof data['from_address'] === 'string' || data['from_address'] instanceof String)) {
            throw new Error("Expected the field `from_address` to be a primitive type in the JSON string but got " + data['from_address']);
        }
        // ensure the json data is a string
        if (data['from_name'] && !(typeof data['from_name'] === 'string' || data['from_name'] instanceof String)) {
            throw new Error("Expected the field `from_name` to be a primitive type in the JSON string but got " + data['from_name']);
        }
        // ensure the json data is a string
        if (data['host'] && !(typeof data['host'] === 'string' || data['host'] instanceof String)) {
            throw new Error("Expected the field `host` to be a primitive type in the JSON string but got " + data['host']);
        }
        // ensure the json data is a string
        if (data['password'] && !(typeof data['password'] === 'string' || data['password'] instanceof String)) {
            throw new Error("Expected the field `password` to be a primitive type in the JSON string but got " + data['password']);
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }

        return true;
    }


}

SmtpConfig.RequiredProperties = ["encryption", "from_address", "host", "password", "port", "username"];

/**
 * @member {module:model/SmtpEncryption} encryption
 */
SmtpConfig.prototype['encryption'] = undefined;

/**
 * @member {String} from_address
 */
SmtpConfig.prototype['from_address'] = undefined;

/**
 * @member {String} from_name
 */
SmtpConfig.prototype['from_name'] = undefined;

/**
 * @member {String} host
 */
SmtpConfig.prototype['host'] = undefined;

/**
 * @member {String} password
 */
SmtpConfig.prototype['password'] = undefined;

/**
 * @member {Number} port
 */
SmtpConfig.prototype['port'] = undefined;

/**
 * @member {Number} timeout_seconds
 */
SmtpConfig.prototype['timeout_seconds'] = undefined;

/**
 * @member {String} username
 */
SmtpConfig.prototype['username'] = undefined;






export default SmtpConfig;


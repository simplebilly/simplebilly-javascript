/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EmissionMethod from './EmissionMethod';
import GhgScope from './GhgScope';

/**
 * The EmissionEntry model module.
 * @module model/EmissionEntry
 * @version 0.1.0
 */
class EmissionEntry {
    /**
     * Constructs a new <code>EmissionEntry</code>.
     * @alias module:model/EmissionEntry
     * @param activityValue {String} Activity amount in `unit` (kWh, l, km, t, tkm, EUR).
     * @param categoryId {String} GHG-Protocol category key, e.g. \"purchased_goods\", \"business_travel\".
     * @param description {String} 
     * @param efSource {String} Emission-factor source, e.g. \"UBA-2024\", \"DEFRA-2024\".
     * @param efVersion {String} 
     * @param method {module:model/EmissionMethod} \"activity\" | \"spend\" | \"supplier\".
     * @param scope {module:model/GhgScope} GHG scope: \"1\" | \"2\" | \"3\".
     * @param tco2e {String} Computed server-side: activity * factor / 1000, rounded to 4 dp.
     * @param unit {String} Unit of the activity value.
     * @param year {Number} Reporting year.
     */
    constructor(activityValue, categoryId, description, efSource, efVersion, method, scope, tco2e, unit, year) { 
        
        EmissionEntry.initialize(this, activityValue, categoryId, description, efSource, efVersion, method, scope, tco2e, unit, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, activityValue, categoryId, description, efSource, efVersion, method, scope, tco2e, unit, year) { 
        obj['activityValue'] = activityValue;
        obj['categoryId'] = categoryId;
        obj['description'] = description;
        obj['efSource'] = efSource;
        obj['efVersion'] = efVersion;
        obj['method'] = method;
        obj['scope'] = scope;
        obj['tco2e'] = tco2e;
        obj['unit'] = unit;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>EmissionEntry</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EmissionEntry} obj Optional instance to populate.
     * @return {module:model/EmissionEntry} The populated <code>EmissionEntry</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmissionEntry();

            if (data.hasOwnProperty('activityValue')) {
                obj['activityValue'] = ApiClient.convertToType(data['activityValue'], 'String');
            }
            if (data.hasOwnProperty('categoryId')) {
                obj['categoryId'] = ApiClient.convertToType(data['categoryId'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('efSource')) {
                obj['efSource'] = ApiClient.convertToType(data['efSource'], 'String');
            }
            if (data.hasOwnProperty('efVersion')) {
                obj['efVersion'] = ApiClient.convertToType(data['efVersion'], 'String');
            }
            if (data.hasOwnProperty('method')) {
                obj['method'] = ApiClient.convertToType(data['method'], EmissionMethod);
            }
            if (data.hasOwnProperty('scope')) {
                obj['scope'] = ApiClient.convertToType(data['scope'], GhgScope);
            }
            if (data.hasOwnProperty('tco2e')) {
                obj['tco2e'] = ApiClient.convertToType(data['tco2e'], 'String');
            }
            if (data.hasOwnProperty('unit')) {
                obj['unit'] = ApiClient.convertToType(data['unit'], 'String');
            }
            if (data.hasOwnProperty('updatedAt')) {
                obj['updatedAt'] = ApiClient.convertToType(data['updatedAt'], 'Date');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmissionEntry</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmissionEntry</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmissionEntry.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['activityValue'] && !(typeof data['activityValue'] === 'string' || data['activityValue'] instanceof String)) {
            throw new Error("Expected the field `activityValue` to be a primitive type in the JSON string but got " + data['activityValue']);
        }
        // ensure the json data is a string
        if (data['categoryId'] && !(typeof data['categoryId'] === 'string' || data['categoryId'] instanceof String)) {
            throw new Error("Expected the field `categoryId` to be a primitive type in the JSON string but got " + data['categoryId']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['efSource'] && !(typeof data['efSource'] === 'string' || data['efSource'] instanceof String)) {
            throw new Error("Expected the field `efSource` to be a primitive type in the JSON string but got " + data['efSource']);
        }
        // ensure the json data is a string
        if (data['efVersion'] && !(typeof data['efVersion'] === 'string' || data['efVersion'] instanceof String)) {
            throw new Error("Expected the field `efVersion` to be a primitive type in the JSON string but got " + data['efVersion']);
        }
        // ensure the json data is a string
        if (data['tco2e'] && !(typeof data['tco2e'] === 'string' || data['tco2e'] instanceof String)) {
            throw new Error("Expected the field `tco2e` to be a primitive type in the JSON string but got " + data['tco2e']);
        }
        // ensure the json data is a string
        if (data['unit'] && !(typeof data['unit'] === 'string' || data['unit'] instanceof String)) {
            throw new Error("Expected the field `unit` to be a primitive type in the JSON string but got " + data['unit']);
        }

        return true;
    }


}

EmissionEntry.RequiredProperties = ["activityValue", "categoryId", "description", "efSource", "efVersion", "method", "scope", "tco2e", "unit", "year"];

/**
 * Activity amount in `unit` (kWh, l, km, t, tkm, EUR).
 * @member {String} activityValue
 */
EmissionEntry.prototype['activityValue'] = undefined;

/**
 * GHG-Protocol category key, e.g. \"purchased_goods\", \"business_travel\".
 * @member {String} categoryId
 */
EmissionEntry.prototype['categoryId'] = undefined;

/**
 * @member {String} description
 */
EmissionEntry.prototype['description'] = undefined;

/**
 * Emission-factor source, e.g. \"UBA-2024\", \"DEFRA-2024\".
 * @member {String} efSource
 */
EmissionEntry.prototype['efSource'] = undefined;

/**
 * @member {String} efVersion
 */
EmissionEntry.prototype['efVersion'] = undefined;

/**
 * \"activity\" | \"spend\" | \"supplier\".
 * @member {module:model/EmissionMethod} method
 */
EmissionEntry.prototype['method'] = undefined;

/**
 * GHG scope: \"1\" | \"2\" | \"3\".
 * @member {module:model/GhgScope} scope
 */
EmissionEntry.prototype['scope'] = undefined;

/**
 * Computed server-side: activity * factor / 1000, rounded to 4 dp.
 * @member {String} tco2e
 */
EmissionEntry.prototype['tco2e'] = undefined;

/**
 * Unit of the activity value.
 * @member {String} unit
 */
EmissionEntry.prototype['unit'] = undefined;

/**
 * @member {Date} updatedAt
 */
EmissionEntry.prototype['updatedAt'] = undefined;

/**
 * Reporting year.
 * @member {Number} year
 */
EmissionEntry.prototype['year'] = undefined;






export default EmissionEntry;


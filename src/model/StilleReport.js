/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StillePartnerZeile from './StillePartnerZeile';

/**
 * The StilleReport model module.
 * @module model/StilleReport
 * @version 0.1.0
 */
class StilleReport {
    /**
     * Constructs a new <code>StilleReport</code>.
     * @alias module:model/StilleReport
     * @param jahresueberschuss {String} 
     * @param partners {Array.<module:model/StillePartnerZeile>} 
     * @param year {Number} 
     */
    constructor(jahresueberschuss, partners, year) { 
        
        StilleReport.initialize(this, jahresueberschuss, partners, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jahresueberschuss, partners, year) { 
        obj['jahresueberschuss'] = jahresueberschuss;
        obj['partners'] = partners;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>StilleReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StilleReport} obj Optional instance to populate.
     * @return {module:model/StilleReport} The populated <code>StilleReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StilleReport();

            if (data.hasOwnProperty('jahresueberschuss')) {
                obj['jahresueberschuss'] = ApiClient.convertToType(data['jahresueberschuss'], 'String');
            }
            if (data.hasOwnProperty('partners')) {
                obj['partners'] = ApiClient.convertToType(data['partners'], [StillePartnerZeile]);
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StilleReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StilleReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StilleReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['jahresueberschuss'] && !(typeof data['jahresueberschuss'] === 'string' || data['jahresueberschuss'] instanceof String)) {
            throw new Error("Expected the field `jahresueberschuss` to be a primitive type in the JSON string but got " + data['jahresueberschuss']);
        }
        if (data['partners']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['partners'])) {
                throw new Error("Expected the field `partners` to be an array in the JSON data but got " + data['partners']);
            }
            // validate the optional field `partners` (array)
            for (const item of data['partners']) {
                StillePartnerZeile.validateJSON(item);
            };
        }

        return true;
    }


}

StilleReport.RequiredProperties = ["jahresueberschuss", "partners", "year"];

/**
 * @member {String} jahresueberschuss
 */
StilleReport.prototype['jahresueberschuss'] = undefined;

/**
 * @member {Array.<module:model/StillePartnerZeile>} partners
 */
StilleReport.prototype['partners'] = undefined;

/**
 * @member {Number} year
 */
StilleReport.prototype['year'] = undefined;






export default StilleReport;


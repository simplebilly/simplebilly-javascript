/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EksMonatsWert model module.
 * @module model/EksMonatsWert
 * @version 0.1.0
 */
class EksMonatsWert {
    /**
     * Constructs a new <code>EksMonatsWert</code>.
     * @alias module:model/EksMonatsWert
     * @param ausgaben {String} 
     * @param einnahmen {String} 
     * @param ergebnis {String} 
     * @param monat {String} 
     */
    constructor(ausgaben, einnahmen, ergebnis, monat) { 
        
        EksMonatsWert.initialize(this, ausgaben, einnahmen, ergebnis, monat);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ausgaben, einnahmen, ergebnis, monat) { 
        obj['ausgaben'] = ausgaben;
        obj['einnahmen'] = einnahmen;
        obj['ergebnis'] = ergebnis;
        obj['monat'] = monat;
    }

    /**
     * Constructs a <code>EksMonatsWert</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EksMonatsWert} obj Optional instance to populate.
     * @return {module:model/EksMonatsWert} The populated <code>EksMonatsWert</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EksMonatsWert();

            if (data.hasOwnProperty('ausgaben')) {
                obj['ausgaben'] = ApiClient.convertToType(data['ausgaben'], 'String');
            }
            if (data.hasOwnProperty('einnahmen')) {
                obj['einnahmen'] = ApiClient.convertToType(data['einnahmen'], 'String');
            }
            if (data.hasOwnProperty('ergebnis')) {
                obj['ergebnis'] = ApiClient.convertToType(data['ergebnis'], 'String');
            }
            if (data.hasOwnProperty('monat')) {
                obj['monat'] = ApiClient.convertToType(data['monat'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EksMonatsWert</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EksMonatsWert</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EksMonatsWert.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ausgaben'] && !(typeof data['ausgaben'] === 'string' || data['ausgaben'] instanceof String)) {
            throw new Error("Expected the field `ausgaben` to be a primitive type in the JSON string but got " + data['ausgaben']);
        }
        // ensure the json data is a string
        if (data['einnahmen'] && !(typeof data['einnahmen'] === 'string' || data['einnahmen'] instanceof String)) {
            throw new Error("Expected the field `einnahmen` to be a primitive type in the JSON string but got " + data['einnahmen']);
        }
        // ensure the json data is a string
        if (data['ergebnis'] && !(typeof data['ergebnis'] === 'string' || data['ergebnis'] instanceof String)) {
            throw new Error("Expected the field `ergebnis` to be a primitive type in the JSON string but got " + data['ergebnis']);
        }
        // ensure the json data is a string
        if (data['monat'] && !(typeof data['monat'] === 'string' || data['monat'] instanceof String)) {
            throw new Error("Expected the field `monat` to be a primitive type in the JSON string but got " + data['monat']);
        }

        return true;
    }


}

EksMonatsWert.RequiredProperties = ["ausgaben", "einnahmen", "ergebnis", "monat"];

/**
 * @member {String} ausgaben
 */
EksMonatsWert.prototype['ausgaben'] = undefined;

/**
 * @member {String} einnahmen
 */
EksMonatsWert.prototype['einnahmen'] = undefined;

/**
 * @member {String} ergebnis
 */
EksMonatsWert.prototype['ergebnis'] = undefined;

/**
 * @member {String} monat
 */
EksMonatsWert.prototype['monat'] = undefined;






export default EksMonatsWert;


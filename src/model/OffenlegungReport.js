/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OffenlegungItem from './OffenlegungItem';

/**
 * The OffenlegungReport model module.
 * @module model/OffenlegungReport
 * @version 0.1.0
 */
class OffenlegungReport {
    /**
     * Constructs a new <code>OffenlegungReport</code>.
     * @alias module:model/OffenlegungReport
     * @param deadline {Date} Fristende (Abschlussstichtag + Frist).
     * @param deadlineMonths {Number} Offenlegungsfrist in Monaten (§ 325 Abs. 4 HGB).
     * @param items {Array.<module:model/OffenlegungItem>} 
     * @param kapitalmarktorientiert {Boolean} Annahme über die Kapitalmarktorientierung.
     * @param note {String} 
     * @param year {Number} Berichtsjahr (laufendes Kalenderjahr).
     */
    constructor(deadline, deadlineMonths, items, kapitalmarktorientiert, note, year) { 
        
        OffenlegungReport.initialize(this, deadline, deadlineMonths, items, kapitalmarktorientiert, note, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, deadline, deadlineMonths, items, kapitalmarktorientiert, note, year) { 
        obj['deadline'] = deadline;
        obj['deadline_months'] = deadlineMonths;
        obj['items'] = items;
        obj['kapitalmarktorientiert'] = kapitalmarktorientiert;
        obj['note'] = note;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>OffenlegungReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OffenlegungReport} obj Optional instance to populate.
     * @return {module:model/OffenlegungReport} The populated <code>OffenlegungReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OffenlegungReport();

            if (data.hasOwnProperty('deadline')) {
                obj['deadline'] = ApiClient.convertToType(data['deadline'], 'Date');
            }
            if (data.hasOwnProperty('deadline_months')) {
                obj['deadline_months'] = ApiClient.convertToType(data['deadline_months'], 'Number');
            }
            if (data.hasOwnProperty('items')) {
                obj['items'] = ApiClient.convertToType(data['items'], [OffenlegungItem]);
            }
            if (data.hasOwnProperty('kapitalmarktorientiert')) {
                obj['kapitalmarktorientiert'] = ApiClient.convertToType(data['kapitalmarktorientiert'], 'Boolean');
            }
            if (data.hasOwnProperty('note')) {
                obj['note'] = ApiClient.convertToType(data['note'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OffenlegungReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OffenlegungReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OffenlegungReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['items'])) {
                throw new Error("Expected the field `items` to be an array in the JSON data but got " + data['items']);
            }
            // validate the optional field `items` (array)
            for (const item of data['items']) {
                OffenlegungItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['note'] && !(typeof data['note'] === 'string' || data['note'] instanceof String)) {
            throw new Error("Expected the field `note` to be a primitive type in the JSON string but got " + data['note']);
        }

        return true;
    }


}

OffenlegungReport.RequiredProperties = ["deadline", "deadline_months", "items", "kapitalmarktorientiert", "note", "year"];

/**
 * Fristende (Abschlussstichtag + Frist).
 * @member {Date} deadline
 */
OffenlegungReport.prototype['deadline'] = undefined;

/**
 * Offenlegungsfrist in Monaten (§ 325 Abs. 4 HGB).
 * @member {Number} deadline_months
 */
OffenlegungReport.prototype['deadline_months'] = undefined;

/**
 * @member {Array.<module:model/OffenlegungItem>} items
 */
OffenlegungReport.prototype['items'] = undefined;

/**
 * Annahme über die Kapitalmarktorientierung.
 * @member {Boolean} kapitalmarktorientiert
 */
OffenlegungReport.prototype['kapitalmarktorientiert'] = undefined;

/**
 * @member {String} note
 */
OffenlegungReport.prototype['note'] = undefined;

/**
 * Berichtsjahr (laufendes Kalenderjahr).
 * @member {Number} year
 */
OffenlegungReport.prototype['year'] = undefined;






export default OffenlegungReport;


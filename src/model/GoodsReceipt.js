/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GoodsReceipt model module.
 * @module model/GoodsReceipt
 * @version 0.1.0
 */
class GoodsReceipt {
    /**
     * Constructs a new <code>GoodsReceipt</code>.
     * @alias module:model/GoodsReceipt
     * @param grNumber {String} 
     * @param lineItems {Object} JSON array of `{product_id, name, quantity, batch_number?, expiry_date?, bin_location?}`.
     * @param receiptDate {Date} 
     * @param warehouseId {String} References the warehouse entity.
     */
    constructor(grNumber, lineItems, receiptDate, warehouseId) { 
        
        GoodsReceipt.initialize(this, grNumber, lineItems, receiptDate, warehouseId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, grNumber, lineItems, receiptDate, warehouseId) { 
        obj['grNumber'] = grNumber;
        obj['lineItems'] = lineItems;
        obj['receiptDate'] = receiptDate;
        obj['warehouseId'] = warehouseId;
    }

    /**
     * Constructs a <code>GoodsReceipt</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GoodsReceipt} obj Optional instance to populate.
     * @return {module:model/GoodsReceipt} The populated <code>GoodsReceipt</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoodsReceipt();

            if (data.hasOwnProperty('grNumber')) {
                obj['grNumber'] = ApiClient.convertToType(data['grNumber'], 'String');
            }
            if (data.hasOwnProperty('lineItems')) {
                obj['lineItems'] = ApiClient.convertToType(data['lineItems'], Object);
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('purchaseOrderId')) {
                obj['purchaseOrderId'] = ApiClient.convertToType(data['purchaseOrderId'], 'String');
            }
            if (data.hasOwnProperty('receiptDate')) {
                obj['receiptDate'] = ApiClient.convertToType(data['receiptDate'], 'Date');
            }
            if (data.hasOwnProperty('supplierContactId')) {
                obj['supplierContactId'] = ApiClient.convertToType(data['supplierContactId'], 'String');
            }
            if (data.hasOwnProperty('supplierName')) {
                obj['supplierName'] = ApiClient.convertToType(data['supplierName'], 'String');
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoodsReceipt</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoodsReceipt</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GoodsReceipt.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['grNumber'] && !(typeof data['grNumber'] === 'string' || data['grNumber'] instanceof String)) {
            throw new Error("Expected the field `grNumber` to be a primitive type in the JSON string but got " + data['grNumber']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['purchaseOrderId'] && !(typeof data['purchaseOrderId'] === 'string' || data['purchaseOrderId'] instanceof String)) {
            throw new Error("Expected the field `purchaseOrderId` to be a primitive type in the JSON string but got " + data['purchaseOrderId']);
        }
        // ensure the json data is a string
        if (data['supplierContactId'] && !(typeof data['supplierContactId'] === 'string' || data['supplierContactId'] instanceof String)) {
            throw new Error("Expected the field `supplierContactId` to be a primitive type in the JSON string but got " + data['supplierContactId']);
        }
        // ensure the json data is a string
        if (data['supplierName'] && !(typeof data['supplierName'] === 'string' || data['supplierName'] instanceof String)) {
            throw new Error("Expected the field `supplierName` to be a primitive type in the JSON string but got " + data['supplierName']);
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}

GoodsReceipt.RequiredProperties = ["grNumber", "lineItems", "receiptDate", "warehouseId"];

/**
 * @member {String} grNumber
 */
GoodsReceipt.prototype['grNumber'] = undefined;

/**
 * JSON array of `{product_id, name, quantity, batch_number?, expiry_date?, bin_location?}`.
 * @member {Object} lineItems
 */
GoodsReceipt.prototype['lineItems'] = undefined;

/**
 * @member {String} notes
 */
GoodsReceipt.prototype['notes'] = undefined;

/**
 * References the purchase order entity.
 * @member {String} purchaseOrderId
 */
GoodsReceipt.prototype['purchaseOrderId'] = undefined;

/**
 * @member {Date} receiptDate
 */
GoodsReceipt.prototype['receiptDate'] = undefined;

/**
 * References the supplier entity.
 * @member {String} supplierContactId
 */
GoodsReceipt.prototype['supplierContactId'] = undefined;

/**
 * @member {String} supplierName
 */
GoodsReceipt.prototype['supplierName'] = undefined;

/**
 * References the warehouse entity.
 * @member {String} warehouseId
 */
GoodsReceipt.prototype['warehouseId'] = undefined;






export default GoodsReceipt;


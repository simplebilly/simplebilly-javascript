/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PackingCompleteRequest model module.
 * @module model/PackingCompleteRequest
 * @version 0.1.0
 */
class PackingCompleteRequest {
    /**
     * Constructs a new <code>PackingCompleteRequest</code>.
     * @alias module:model/PackingCompleteRequest
     * @param orderNumber {String} 
     */
    constructor(orderNumber) { 
        
        PackingCompleteRequest.initialize(this, orderNumber);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderNumber) { 
        obj['order_number'] = orderNumber;
    }

    /**
     * Constructs a <code>PackingCompleteRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PackingCompleteRequest} obj Optional instance to populate.
     * @return {module:model/PackingCompleteRequest} The populated <code>PackingCompleteRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PackingCompleteRequest();

            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('order_number')) {
                obj['order_number'] = ApiClient.convertToType(data['order_number'], 'String');
            }
            if (data.hasOwnProperty('shipment_id')) {
                obj['shipment_id'] = ApiClient.convertToType(data['shipment_id'], 'String');
            }
            if (data.hasOwnProperty('video_url')) {
                obj['video_url'] = ApiClient.convertToType(data['video_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PackingCompleteRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PackingCompleteRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PackingCompleteRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['order_number'] && !(typeof data['order_number'] === 'string' || data['order_number'] instanceof String)) {
            throw new Error("Expected the field `order_number` to be a primitive type in the JSON string but got " + data['order_number']);
        }
        // ensure the json data is a string
        if (data['shipment_id'] && !(typeof data['shipment_id'] === 'string' || data['shipment_id'] instanceof String)) {
            throw new Error("Expected the field `shipment_id` to be a primitive type in the JSON string but got " + data['shipment_id']);
        }
        // ensure the json data is a string
        if (data['video_url'] && !(typeof data['video_url'] === 'string' || data['video_url'] instanceof String)) {
            throw new Error("Expected the field `video_url` to be a primitive type in the JSON string but got " + data['video_url']);
        }

        return true;
    }


}

PackingCompleteRequest.RequiredProperties = ["order_number"];

/**
 * @member {String} notes
 */
PackingCompleteRequest.prototype['notes'] = undefined;

/**
 * @member {String} order_number
 */
PackingCompleteRequest.prototype['order_number'] = undefined;

/**
 * @member {String} shipment_id
 */
PackingCompleteRequest.prototype['shipment_id'] = undefined;

/**
 * @member {String} video_url
 */
PackingCompleteRequest.prototype['video_url'] = undefined;






export default PackingCompleteRequest;


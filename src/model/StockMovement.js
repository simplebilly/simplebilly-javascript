/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import MovementType from './MovementType';
import ReferenceType from './ReferenceType';

/**
 * The StockMovement model module.
 * @module model/StockMovement
 * @version 0.1.0
 */
class StockMovement {
    /**
     * Constructs a new <code>StockMovement</code>.
     * @alias module:model/StockMovement
     * @param delta {Number} Signed movement: positive = into stock, negative = out of stock.
     * @param movementType {module:model/MovementType} One of the `MOVEMENT_*` constants.
     * @param productId {String} References the product entity.
     * @param quantity {Number} Absolute quantity moved (always >= 0).
     * @param warehouseId {String} References the warehouse entity.
     */
    constructor(delta, movementType, productId, quantity, warehouseId) { 
        
        StockMovement.initialize(this, delta, movementType, productId, quantity, warehouseId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, delta, movementType, productId, quantity, warehouseId) { 
        obj['delta'] = delta;
        obj['movementType'] = movementType;
        obj['productId'] = productId;
        obj['quantity'] = quantity;
        obj['warehouseId'] = warehouseId;
    }

    /**
     * Constructs a <code>StockMovement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StockMovement} obj Optional instance to populate.
     * @return {module:model/StockMovement} The populated <code>StockMovement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StockMovement();

            if (data.hasOwnProperty('delta')) {
                obj['delta'] = ApiClient.convertToType(data['delta'], 'Number');
            }
            if (data.hasOwnProperty('movementType')) {
                obj['movementType'] = ApiClient.convertToType(data['movementType'], MovementType);
            }
            if (data.hasOwnProperty('productId')) {
                obj['productId'] = ApiClient.convertToType(data['productId'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('referenceId')) {
                obj['referenceId'] = ApiClient.convertToType(data['referenceId'], 'String');
            }
            if (data.hasOwnProperty('referenceType')) {
                obj['referenceType'] = ApiClient.convertToType(data['referenceType'], ReferenceType);
            }
            if (data.hasOwnProperty('warehouseId')) {
                obj['warehouseId'] = ApiClient.convertToType(data['warehouseId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StockMovement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StockMovement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StockMovement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['productId'] && !(typeof data['productId'] === 'string' || data['productId'] instanceof String)) {
            throw new Error("Expected the field `productId` to be a primitive type in the JSON string but got " + data['productId']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['referenceId'] && !(typeof data['referenceId'] === 'string' || data['referenceId'] instanceof String)) {
            throw new Error("Expected the field `referenceId` to be a primitive type in the JSON string but got " + data['referenceId']);
        }
        // ensure the json data is a string
        if (data['warehouseId'] && !(typeof data['warehouseId'] === 'string' || data['warehouseId'] instanceof String)) {
            throw new Error("Expected the field `warehouseId` to be a primitive type in the JSON string but got " + data['warehouseId']);
        }

        return true;
    }


}

StockMovement.RequiredProperties = ["delta", "movementType", "productId", "quantity", "warehouseId"];

/**
 * Signed movement: positive = into stock, negative = out of stock.
 * @member {Number} delta
 */
StockMovement.prototype['delta'] = undefined;

/**
 * One of the `MOVEMENT_*` constants.
 * @member {module:model/MovementType} movementType
 */
StockMovement.prototype['movementType'] = undefined;

/**
 * References the product entity.
 * @member {String} productId
 */
StockMovement.prototype['productId'] = undefined;

/**
 * Absolute quantity moved (always >= 0).
 * @member {Number} quantity
 */
StockMovement.prototype['quantity'] = undefined;

/**
 * @member {String} reason
 */
StockMovement.prototype['reason'] = undefined;

/**
 * Primary-key of the referencing entity.
 * @member {String} referenceId
 */
StockMovement.prototype['referenceId'] = undefined;

/**
 * Entity that caused the movement, e.g. `goods_receipt`, `stock_transfer`.
 * @member {module:model/ReferenceType} referenceType
 */
StockMovement.prototype['referenceType'] = undefined;

/**
 * References the warehouse entity.
 * @member {String} warehouseId
 */
StockMovement.prototype['warehouseId'] = undefined;






export default StockMovement;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AnlageGKfzHinweis model module.
 * @module model/AnlageGKfzHinweis
 * @version 0.1.0
 */
class AnlageGKfzHinweis {
    /**
     * Constructs a new <code>AnlageGKfzHinweis</code>.
     * @alias module:model/AnlageGKfzHinweis
     * @param bezeichnung {String} 
     * @param kennzeichen {String} 
     * @param privatAnteilProzent {String} 
     */
    constructor(bezeichnung, kennzeichen, privatAnteilProzent) { 
        
        AnlageGKfzHinweis.initialize(this, bezeichnung, kennzeichen, privatAnteilProzent);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bezeichnung, kennzeichen, privatAnteilProzent) { 
        obj['bezeichnung'] = bezeichnung;
        obj['kennzeichen'] = kennzeichen;
        obj['privat_anteil_prozent'] = privatAnteilProzent;
    }

    /**
     * Constructs a <code>AnlageGKfzHinweis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AnlageGKfzHinweis} obj Optional instance to populate.
     * @return {module:model/AnlageGKfzHinweis} The populated <code>AnlageGKfzHinweis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AnlageGKfzHinweis();

            if (data.hasOwnProperty('bezeichnung')) {
                obj['bezeichnung'] = ApiClient.convertToType(data['bezeichnung'], 'String');
            }
            if (data.hasOwnProperty('kennzeichen')) {
                obj['kennzeichen'] = ApiClient.convertToType(data['kennzeichen'], 'String');
            }
            if (data.hasOwnProperty('privat_anteil_prozent')) {
                obj['privat_anteil_prozent'] = ApiClient.convertToType(data['privat_anteil_prozent'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AnlageGKfzHinweis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AnlageGKfzHinweis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AnlageGKfzHinweis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['bezeichnung'] && !(typeof data['bezeichnung'] === 'string' || data['bezeichnung'] instanceof String)) {
            throw new Error("Expected the field `bezeichnung` to be a primitive type in the JSON string but got " + data['bezeichnung']);
        }
        // ensure the json data is a string
        if (data['kennzeichen'] && !(typeof data['kennzeichen'] === 'string' || data['kennzeichen'] instanceof String)) {
            throw new Error("Expected the field `kennzeichen` to be a primitive type in the JSON string but got " + data['kennzeichen']);
        }
        // ensure the json data is a string
        if (data['privat_anteil_prozent'] && !(typeof data['privat_anteil_prozent'] === 'string' || data['privat_anteil_prozent'] instanceof String)) {
            throw new Error("Expected the field `privat_anteil_prozent` to be a primitive type in the JSON string but got " + data['privat_anteil_prozent']);
        }

        return true;
    }


}

AnlageGKfzHinweis.RequiredProperties = ["bezeichnung", "kennzeichen", "privat_anteil_prozent"];

/**
 * @member {String} bezeichnung
 */
AnlageGKfzHinweis.prototype['bezeichnung'] = undefined;

/**
 * @member {String} kennzeichen
 */
AnlageGKfzHinweis.prototype['kennzeichen'] = undefined;

/**
 * @member {String} privat_anteil_prozent
 */
AnlageGKfzHinweis.prototype['privat_anteil_prozent'] = undefined;






export default AnlageGKfzHinweis;


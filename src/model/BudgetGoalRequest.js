/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BudgetGoalRequest model module.
 * @module model/BudgetGoalRequest
 * @version 0.1.0
 */
class BudgetGoalRequest {
    /**
     * Constructs a new <code>BudgetGoalRequest</code>.
     * @alias module:model/BudgetGoalRequest
     * @param monthlyGoal {String} Monthly goal amount (gross). 0 means \"no goal\" (fallback to default).
     * @param year {Number} Budget year the goal applies to.
     */
    constructor(monthlyGoal, year) { 
        
        BudgetGoalRequest.initialize(this, monthlyGoal, year);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, monthlyGoal, year) { 
        obj['monthly_goal'] = monthlyGoal;
        obj['year'] = year;
    }

    /**
     * Constructs a <code>BudgetGoalRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BudgetGoalRequest} obj Optional instance to populate.
     * @return {module:model/BudgetGoalRequest} The populated <code>BudgetGoalRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BudgetGoalRequest();

            if (data.hasOwnProperty('monthly_goal')) {
                obj['monthly_goal'] = ApiClient.convertToType(data['monthly_goal'], 'String');
            }
            if (data.hasOwnProperty('year')) {
                obj['year'] = ApiClient.convertToType(data['year'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BudgetGoalRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BudgetGoalRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BudgetGoalRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['monthly_goal'] && !(typeof data['monthly_goal'] === 'string' || data['monthly_goal'] instanceof String)) {
            throw new Error("Expected the field `monthly_goal` to be a primitive type in the JSON string but got " + data['monthly_goal']);
        }

        return true;
    }


}

BudgetGoalRequest.RequiredProperties = ["monthly_goal", "year"];

/**
 * Monthly goal amount (gross). 0 means \"no goal\" (fallback to default).
 * @member {String} monthly_goal
 */
BudgetGoalRequest.prototype['monthly_goal'] = undefined;

/**
 * Budget year the goal applies to.
 * @member {Number} year
 */
BudgetGoalRequest.prototype['year'] = undefined;






export default BudgetGoalRequest;


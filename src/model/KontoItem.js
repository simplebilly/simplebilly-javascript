/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The KontoItem model module.
 * @module model/KontoItem
 * @version 0.1.0
 */
class KontoItem {
    /**
     * Constructs a new <code>KontoItem</code>.
     * @alias module:model/KontoItem
     * @param anfangsbestand {String} 
     * @param habenUmsatz {String} 
     * @param konto {String} 
     * @param name {String} 
     * @param saldo {String} 
     * @param sollUmsatz {String} 
     */
    constructor(anfangsbestand, habenUmsatz, konto, name, saldo, sollUmsatz) { 
        
        KontoItem.initialize(this, anfangsbestand, habenUmsatz, konto, name, saldo, sollUmsatz);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, anfangsbestand, habenUmsatz, konto, name, saldo, sollUmsatz) { 
        obj['anfangsbestand'] = anfangsbestand;
        obj['haben_umsatz'] = habenUmsatz;
        obj['konto'] = konto;
        obj['name'] = name;
        obj['saldo'] = saldo;
        obj['soll_umsatz'] = sollUmsatz;
    }

    /**
     * Constructs a <code>KontoItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/KontoItem} obj Optional instance to populate.
     * @return {module:model/KontoItem} The populated <code>KontoItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new KontoItem();

            if (data.hasOwnProperty('anfangsbestand')) {
                obj['anfangsbestand'] = ApiClient.convertToType(data['anfangsbestand'], 'String');
            }
            if (data.hasOwnProperty('haben_umsatz')) {
                obj['haben_umsatz'] = ApiClient.convertToType(data['haben_umsatz'], 'String');
            }
            if (data.hasOwnProperty('konto')) {
                obj['konto'] = ApiClient.convertToType(data['konto'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('saldo')) {
                obj['saldo'] = ApiClient.convertToType(data['saldo'], 'String');
            }
            if (data.hasOwnProperty('soll_umsatz')) {
                obj['soll_umsatz'] = ApiClient.convertToType(data['soll_umsatz'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>KontoItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>KontoItem</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of KontoItem.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['anfangsbestand'] && !(typeof data['anfangsbestand'] === 'string' || data['anfangsbestand'] instanceof String)) {
            throw new Error("Expected the field `anfangsbestand` to be a primitive type in the JSON string but got " + data['anfangsbestand']);
        }
        // ensure the json data is a string
        if (data['haben_umsatz'] && !(typeof data['haben_umsatz'] === 'string' || data['haben_umsatz'] instanceof String)) {
            throw new Error("Expected the field `haben_umsatz` to be a primitive type in the JSON string but got " + data['haben_umsatz']);
        }
        // ensure the json data is a string
        if (data['konto'] && !(typeof data['konto'] === 'string' || data['konto'] instanceof String)) {
            throw new Error("Expected the field `konto` to be a primitive type in the JSON string but got " + data['konto']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['saldo'] && !(typeof data['saldo'] === 'string' || data['saldo'] instanceof String)) {
            throw new Error("Expected the field `saldo` to be a primitive type in the JSON string but got " + data['saldo']);
        }
        // ensure the json data is a string
        if (data['soll_umsatz'] && !(typeof data['soll_umsatz'] === 'string' || data['soll_umsatz'] instanceof String)) {
            throw new Error("Expected the field `soll_umsatz` to be a primitive type in the JSON string but got " + data['soll_umsatz']);
        }

        return true;
    }


}

KontoItem.RequiredProperties = ["anfangsbestand", "haben_umsatz", "konto", "name", "saldo", "soll_umsatz"];

/**
 * @member {String} anfangsbestand
 */
KontoItem.prototype['anfangsbestand'] = undefined;

/**
 * @member {String} haben_umsatz
 */
KontoItem.prototype['haben_umsatz'] = undefined;

/**
 * @member {String} konto
 */
KontoItem.prototype['konto'] = undefined;

/**
 * @member {String} name
 */
KontoItem.prototype['name'] = undefined;

/**
 * @member {String} saldo
 */
KontoItem.prototype['saldo'] = undefined;

/**
 * @member {String} soll_umsatz
 */
KontoItem.prototype['soll_umsatz'] = undefined;






export default KontoItem;


/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import EuerKatSumme from './EuerKatSumme';

/**
 * The EuerZeileDetail model module.
 * @module model/EuerZeileDetail
 * @version 0.1.0
 */
class EuerZeileDetail {
    /**
     * Constructs a new <code>EuerZeileDetail</code>.
     * @alias module:model/EuerZeileDetail
     * @param abschnitt {String} 
     * @param betragGesamt {String} 
     * @param bezeichnung {String} 
     * @param kategorien {Array.<module:model/EuerKatSumme>} 
     * @param zeile {Number} 
     */
    constructor(abschnitt, betragGesamt, bezeichnung, kategorien, zeile) { 
        
        EuerZeileDetail.initialize(this, abschnitt, betragGesamt, bezeichnung, kategorien, zeile);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, abschnitt, betragGesamt, bezeichnung, kategorien, zeile) { 
        obj['abschnitt'] = abschnitt;
        obj['betrag_gesamt'] = betragGesamt;
        obj['bezeichnung'] = bezeichnung;
        obj['kategorien'] = kategorien;
        obj['zeile'] = zeile;
    }

    /**
     * Constructs a <code>EuerZeileDetail</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EuerZeileDetail} obj Optional instance to populate.
     * @return {module:model/EuerZeileDetail} The populated <code>EuerZeileDetail</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EuerZeileDetail();

            if (data.hasOwnProperty('abschnitt')) {
                obj['abschnitt'] = ApiClient.convertToType(data['abschnitt'], 'String');
            }
            if (data.hasOwnProperty('betrag_gesamt')) {
                obj['betrag_gesamt'] = ApiClient.convertToType(data['betrag_gesamt'], 'String');
            }
            if (data.hasOwnProperty('bezeichnung')) {
                obj['bezeichnung'] = ApiClient.convertToType(data['bezeichnung'], 'String');
            }
            if (data.hasOwnProperty('kategorien')) {
                obj['kategorien'] = ApiClient.convertToType(data['kategorien'], [EuerKatSumme]);
            }
            if (data.hasOwnProperty('zeile')) {
                obj['zeile'] = ApiClient.convertToType(data['zeile'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EuerZeileDetail</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EuerZeileDetail</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EuerZeileDetail.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['abschnitt'] && !(typeof data['abschnitt'] === 'string' || data['abschnitt'] instanceof String)) {
            throw new Error("Expected the field `abschnitt` to be a primitive type in the JSON string but got " + data['abschnitt']);
        }
        // ensure the json data is a string
        if (data['betrag_gesamt'] && !(typeof data['betrag_gesamt'] === 'string' || data['betrag_gesamt'] instanceof String)) {
            throw new Error("Expected the field `betrag_gesamt` to be a primitive type in the JSON string but got " + data['betrag_gesamt']);
        }
        // ensure the json data is a string
        if (data['bezeichnung'] && !(typeof data['bezeichnung'] === 'string' || data['bezeichnung'] instanceof String)) {
            throw new Error("Expected the field `bezeichnung` to be a primitive type in the JSON string but got " + data['bezeichnung']);
        }
        if (data['kategorien']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['kategorien'])) {
                throw new Error("Expected the field `kategorien` to be an array in the JSON data but got " + data['kategorien']);
            }
            // validate the optional field `kategorien` (array)
            for (const item of data['kategorien']) {
                EuerKatSumme.validateJSON(item);
            };
        }

        return true;
    }


}

EuerZeileDetail.RequiredProperties = ["abschnitt", "betrag_gesamt", "bezeichnung", "kategorien", "zeile"];

/**
 * @member {String} abschnitt
 */
EuerZeileDetail.prototype['abschnitt'] = undefined;

/**
 * @member {String} betrag_gesamt
 */
EuerZeileDetail.prototype['betrag_gesamt'] = undefined;

/**
 * @member {String} bezeichnung
 */
EuerZeileDetail.prototype['bezeichnung'] = undefined;

/**
 * @member {Array.<module:model/EuerKatSumme>} kategorien
 */
EuerZeileDetail.prototype['kategorien'] = undefined;

/**
 * @member {Number} zeile
 */
EuerZeileDetail.prototype['zeile'] = undefined;






export default EuerZeileDetail;


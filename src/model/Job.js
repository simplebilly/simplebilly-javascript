/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import JobStatus from './JobStatus';

/**
 * The Job model module.
 * @module model/Job
 * @version 0.1.0
 */
class Job {
    /**
     * Constructs a new <code>Job</code>.
     * @alias module:model/Job
     * @param jobType {String} Discriminator the worker dispatches on (e.g. \"webhook.deliver\").
     * @param maxAttempts {Number} 
     * @param status {module:model/JobStatus} pending | running | done | failed
     */
    constructor(jobType, maxAttempts, status) { 
        
        Job.initialize(this, jobType, maxAttempts, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jobType, maxAttempts, status) { 
        obj['jobType'] = jobType;
        obj['maxAttempts'] = maxAttempts;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>Job</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Job} obj Optional instance to populate.
     * @return {module:model/Job} The populated <code>Job</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Job();

            if (data.hasOwnProperty('attempts')) {
                obj['attempts'] = ApiClient.convertToType(data['attempts'], 'Number');
            }
            if (data.hasOwnProperty('jobType')) {
                obj['jobType'] = ApiClient.convertToType(data['jobType'], 'String');
            }
            if (data.hasOwnProperty('maxAttempts')) {
                obj['maxAttempts'] = ApiClient.convertToType(data['maxAttempts'], 'Number');
            }
            if (data.hasOwnProperty('payload')) {
                obj['payload'] = ApiClient.convertToType(data['payload'], Object);
            }
            if (data.hasOwnProperty('runAt')) {
                obj['runAt'] = ApiClient.convertToType(data['runAt'], 'Date');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], JobStatus);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Job</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Job</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Job.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['jobType'] && !(typeof data['jobType'] === 'string' || data['jobType'] instanceof String)) {
            throw new Error("Expected the field `jobType` to be a primitive type in the JSON string but got " + data['jobType']);
        }

        return true;
    }


}

Job.RequiredProperties = ["jobType", "maxAttempts", "status"];

/**
 * @member {Number} attempts
 */
Job.prototype['attempts'] = undefined;

/**
 * Discriminator the worker dispatches on (e.g. \"webhook.deliver\").
 * @member {String} jobType
 */
Job.prototype['jobType'] = undefined;

/**
 * @member {Number} maxAttempts
 */
Job.prototype['maxAttempts'] = undefined;

/**
 * @member {Object} payload
 */
Job.prototype['payload'] = undefined;

/**
 * Earliest execution time; None = run now.
 * @member {Date} runAt
 */
Job.prototype['runAt'] = undefined;

/**
 * pending | running | done | failed
 * @member {module:model/JobStatus} status
 */
Job.prototype['status'] = undefined;






export default Job;


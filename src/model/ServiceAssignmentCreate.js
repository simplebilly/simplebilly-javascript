/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ServiceAssignmentStatus from './ServiceAssignmentStatus';

/**
 * The ServiceAssignmentCreate model module.
 * @module model/ServiceAssignmentCreate
 * @version 0.1.0
 */
class ServiceAssignmentCreate {
    /**
     * Constructs a new <code>ServiceAssignmentCreate</code>.
     * @alias module:model/ServiceAssignmentCreate
     */
    constructor() { 
        
        ServiceAssignmentCreate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ServiceAssignmentCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServiceAssignmentCreate} obj Optional instance to populate.
     * @return {module:model/ServiceAssignmentCreate} The populated <code>ServiceAssignmentCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServiceAssignmentCreate();

            if (data.hasOwnProperty('employeeId')) {
                obj['employeeId'] = ApiClient.convertToType(data['employeeId'], 'String');
            }
            if (data.hasOwnProperty('jobId')) {
                obj['jobId'] = ApiClient.convertToType(data['jobId'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('scheduledDate')) {
                obj['scheduledDate'] = ApiClient.convertToType(data['scheduledDate'], 'Date');
            }
            if (data.hasOwnProperty('scheduledEnd')) {
                obj['scheduledEnd'] = ApiClient.convertToType(data['scheduledEnd'], 'String');
            }
            if (data.hasOwnProperty('scheduledStart')) {
                obj['scheduledStart'] = ApiClient.convertToType(data['scheduledStart'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], ServiceAssignmentStatus);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServiceAssignmentCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServiceAssignmentCreate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['employeeId'] && !(typeof data['employeeId'] === 'string' || data['employeeId'] instanceof String)) {
            throw new Error("Expected the field `employeeId` to be a primitive type in the JSON string but got " + data['employeeId']);
        }
        // ensure the json data is a string
        if (data['jobId'] && !(typeof data['jobId'] === 'string' || data['jobId'] instanceof String)) {
            throw new Error("Expected the field `jobId` to be a primitive type in the JSON string but got " + data['jobId']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['scheduledEnd'] && !(typeof data['scheduledEnd'] === 'string' || data['scheduledEnd'] instanceof String)) {
            throw new Error("Expected the field `scheduledEnd` to be a primitive type in the JSON string but got " + data['scheduledEnd']);
        }
        // ensure the json data is a string
        if (data['scheduledStart'] && !(typeof data['scheduledStart'] === 'string' || data['scheduledStart'] instanceof String)) {
            throw new Error("Expected the field `scheduledStart` to be a primitive type in the JSON string but got " + data['scheduledStart']);
        }

        return true;
    }


}



/**
 * References the employees entity.
 * @member {String} employeeId
 */
ServiceAssignmentCreate.prototype['employeeId'] = undefined;

/**
 * References the service_jobs entity.
 * @member {String} jobId
 */
ServiceAssignmentCreate.prototype['jobId'] = undefined;

/**
 * @member {String} notes
 */
ServiceAssignmentCreate.prototype['notes'] = undefined;

/**
 * Work day the assignment is scheduled for.
 * @member {Date} scheduledDate
 */
ServiceAssignmentCreate.prototype['scheduledDate'] = undefined;

/**
 * Planned end time of the assignment.
 * @member {String} scheduledEnd
 */
ServiceAssignmentCreate.prototype['scheduledEnd'] = undefined;

/**
 * Planned start time of the assignment.
 * @member {String} scheduledStart
 */
ServiceAssignmentCreate.prototype['scheduledStart'] = undefined;

/**
 * Assignment lifecycle status: \"planned\", \"confirmed\", \"en_route\", \"in_progress\", \"completed\" or \"cancelled\".
 * @member {module:model/ServiceAssignmentStatus} status
 */
ServiceAssignmentCreate.prototype['status'] = undefined;






export default ServiceAssignmentCreate;


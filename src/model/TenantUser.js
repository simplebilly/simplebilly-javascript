/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TenantUser model module.
 * @module model/TenantUser
 * @version 0.1.0
 */
class TenantUser {
    /**
     * Constructs a new <code>TenantUser</code>.
     * @alias module:model/TenantUser
     * @param email {String} 
     * @param emailVerified {Boolean} 
     * @param isActive {Boolean} 
     * @param joinedAt {Date} 
     * @param name {String} 
     * @param permissions {Array.<String>} 
     * @param role {String} 
     * @param userId {String} 
     */
    constructor(email, emailVerified, isActive, joinedAt, name, permissions, role, userId) { 
        
        TenantUser.initialize(this, email, emailVerified, isActive, joinedAt, name, permissions, role, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, email, emailVerified, isActive, joinedAt, name, permissions, role, userId) { 
        obj['email'] = email;
        obj['email_verified'] = emailVerified;
        obj['is_active'] = isActive;
        obj['joined_at'] = joinedAt;
        obj['name'] = name;
        obj['permissions'] = permissions;
        obj['role'] = role;
        obj['user_id'] = userId;
    }

    /**
     * Constructs a <code>TenantUser</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TenantUser} obj Optional instance to populate.
     * @return {module:model/TenantUser} The populated <code>TenantUser</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TenantUser();

            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('email_verified')) {
                obj['email_verified'] = ApiClient.convertToType(data['email_verified'], 'Boolean');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('joined_at')) {
                obj['joined_at'] = ApiClient.convertToType(data['joined_at'], 'Date');
            }
            if (data.hasOwnProperty('last_login')) {
                obj['last_login'] = ApiClient.convertToType(data['last_login'], 'Date');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('permissions')) {
                obj['permissions'] = ApiClient.convertToType(data['permissions'], ['String']);
            }
            if (data.hasOwnProperty('role')) {
                obj['role'] = ApiClient.convertToType(data['role'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TenantUser</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TenantUser</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TenantUser.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['permissions'])) {
            throw new Error("Expected the field `permissions` to be an array in the JSON data but got " + data['permissions']);
        }
        // ensure the json data is a string
        if (data['role'] && !(typeof data['role'] === 'string' || data['role'] instanceof String)) {
            throw new Error("Expected the field `role` to be a primitive type in the JSON string but got " + data['role']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }

        return true;
    }


}

TenantUser.RequiredProperties = ["email", "email_verified", "is_active", "joined_at", "name", "permissions", "role", "user_id"];

/**
 * @member {String} email
 */
TenantUser.prototype['email'] = undefined;

/**
 * @member {Boolean} email_verified
 */
TenantUser.prototype['email_verified'] = undefined;

/**
 * @member {Boolean} is_active
 */
TenantUser.prototype['is_active'] = undefined;

/**
 * @member {Date} joined_at
 */
TenantUser.prototype['joined_at'] = undefined;

/**
 * @member {Date} last_login
 */
TenantUser.prototype['last_login'] = undefined;

/**
 * @member {String} name
 */
TenantUser.prototype['name'] = undefined;

/**
 * @member {Array.<String>} permissions
 */
TenantUser.prototype['permissions'] = undefined;

/**
 * @member {String} role
 */
TenantUser.prototype['role'] = undefined;

/**
 * @member {String} user_id
 */
TenantUser.prototype['user_id'] = undefined;






export default TenantUser;


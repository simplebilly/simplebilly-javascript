/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The GroupFigureCreate model module.
 * @module model/GroupFigureCreate
 * @version 0.1.0
 */
class GroupFigureCreate {
    /**
     * Constructs a new <code>GroupFigureCreate</code>.
     * @alias module:model/GroupFigureCreate
     */
    constructor() { 
        
        GroupFigureCreate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GroupFigureCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GroupFigureCreate} obj Optional instance to populate.
     * @return {module:model/GroupFigureCreate} The populated <code>GroupFigureCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GroupFigureCreate();

            if (data.hasOwnProperty('bilanzsumme')) {
                obj['bilanzsumme'] = ApiClient.convertToType(data['bilanzsumme'], 'String');
            }
            if (data.hasOwnProperty('exemptionClaimed')) {
                obj['exemptionClaimed'] = ApiClient.convertToType(data['exemptionClaimed'], 'Boolean');
            }
            if (data.hasOwnProperty('mitarbeiter')) {
                obj['mitarbeiter'] = ApiClient.convertToType(data['mitarbeiter'], 'Number');
            }
            if (data.hasOwnProperty('nettoUmsatz')) {
                obj['nettoUmsatz'] = ApiClient.convertToType(data['nettoUmsatz'], 'String');
            }
            if (data.hasOwnProperty('parentName')) {
                obj['parentName'] = ApiClient.convertToType(data['parentName'], 'String');
            }
            if (data.hasOwnProperty('parentSitus')) {
                obj['parentSitus'] = ApiClient.convertToType(data['parentSitus'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GroupFigureCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GroupFigureCreate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['bilanzsumme'] && !(typeof data['bilanzsumme'] === 'string' || data['bilanzsumme'] instanceof String)) {
            throw new Error("Expected the field `bilanzsumme` to be a primitive type in the JSON string but got " + data['bilanzsumme']);
        }
        // ensure the json data is a string
        if (data['nettoUmsatz'] && !(typeof data['nettoUmsatz'] === 'string' || data['nettoUmsatz'] instanceof String)) {
            throw new Error("Expected the field `nettoUmsatz` to be a primitive type in the JSON string but got " + data['nettoUmsatz']);
        }
        // ensure the json data is a string
        if (data['parentName'] && !(typeof data['parentName'] === 'string' || data['parentName'] instanceof String)) {
            throw new Error("Expected the field `parentName` to be a primitive type in the JSON string but got " + data['parentName']);
        }
        // ensure the json data is a string
        if (data['parentSitus'] && !(typeof data['parentSitus'] === 'string' || data['parentSitus'] instanceof String)) {
            throw new Error("Expected the field `parentSitus` to be a primitive type in the JSON string but got " + data['parentSitus']);
        }

        return true;
    }


}



/**
 * Bilanzsumme in EUR (§ 293 Abs. 1 Nr. 1 HGB).
 * @member {String} bilanzsumme
 */
GroupFigureCreate.prototype['bilanzsumme'] = undefined;

/**
 * § 291-Befreiung in Anspruch genommen.
 * @member {Boolean} exemptionClaimed
 */
GroupFigureCreate.prototype['exemptionClaimed'] = undefined;

/**
 * Durchschnittliche Arbeitnehmerzahl (§ 293 Abs. 1 Nr. 3 HGB).
 * @member {Number} mitarbeiter
 */
GroupFigureCreate.prototype['mitarbeiter'] = undefined;

/**
 * Netto-Umsatzerlöse in EUR (§ 293 Abs. 1 Nr. 2 HGB).
 * @member {String} nettoUmsatz
 */
GroupFigureCreate.prototype['nettoUmsatz'] = undefined;

/**
 * Name des Mutterunternehmens (§ 291 HGB, Zwischenholding).
 * @member {String} parentName
 */
GroupFigureCreate.prototype['parentName'] = undefined;

/**
 * Sitz des Mutterunternehmens, z. B. \"EU/EWR\" (§ 291 HGB).
 * @member {String} parentSitus
 */
GroupFigureCreate.prototype['parentSitus'] = undefined;






export default GroupFigureCreate;


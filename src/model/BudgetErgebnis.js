/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BudgetKategorie from './BudgetKategorie';

/**
 * The BudgetErgebnis model module.
 * @module model/BudgetErgebnis
 * @version 0.1.0
 */
class BudgetErgebnis {
    /**
     * Constructs a new <code>BudgetErgebnis</code>.
     * @alias module:model/BudgetErgebnis
     * @param jahr {Number} 
     * @param monat {Number} 
     * @param monatsBudget {Array.<module:model/BudgetKategorie>} 
     * @param prognoseRestjahr {Array.<module:model/BudgetKategorie>} 
     */
    constructor(jahr, monat, monatsBudget, prognoseRestjahr) { 
        
        BudgetErgebnis.initialize(this, jahr, monat, monatsBudget, prognoseRestjahr);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, jahr, monat, monatsBudget, prognoseRestjahr) { 
        obj['jahr'] = jahr;
        obj['monat'] = monat;
        obj['monats_budget'] = monatsBudget;
        obj['prognose_restjahr'] = prognoseRestjahr;
    }

    /**
     * Constructs a <code>BudgetErgebnis</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BudgetErgebnis} obj Optional instance to populate.
     * @return {module:model/BudgetErgebnis} The populated <code>BudgetErgebnis</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BudgetErgebnis();

            if (data.hasOwnProperty('jahr')) {
                obj['jahr'] = ApiClient.convertToType(data['jahr'], 'Number');
            }
            if (data.hasOwnProperty('monat')) {
                obj['monat'] = ApiClient.convertToType(data['monat'], 'Number');
            }
            if (data.hasOwnProperty('monats_budget')) {
                obj['monats_budget'] = ApiClient.convertToType(data['monats_budget'], [BudgetKategorie]);
            }
            if (data.hasOwnProperty('prognose_restjahr')) {
                obj['prognose_restjahr'] = ApiClient.convertToType(data['prognose_restjahr'], [BudgetKategorie]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BudgetErgebnis</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BudgetErgebnis</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BudgetErgebnis.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['monats_budget']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['monats_budget'])) {
                throw new Error("Expected the field `monats_budget` to be an array in the JSON data but got " + data['monats_budget']);
            }
            // validate the optional field `monats_budget` (array)
            for (const item of data['monats_budget']) {
                BudgetKategorie.validateJSON(item);
            };
        }
        if (data['prognose_restjahr']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['prognose_restjahr'])) {
                throw new Error("Expected the field `prognose_restjahr` to be an array in the JSON data but got " + data['prognose_restjahr']);
            }
            // validate the optional field `prognose_restjahr` (array)
            for (const item of data['prognose_restjahr']) {
                BudgetKategorie.validateJSON(item);
            };
        }

        return true;
    }


}

BudgetErgebnis.RequiredProperties = ["jahr", "monat", "monats_budget", "prognose_restjahr"];

/**
 * @member {Number} jahr
 */
BudgetErgebnis.prototype['jahr'] = undefined;

/**
 * @member {Number} monat
 */
BudgetErgebnis.prototype['monat'] = undefined;

/**
 * @member {Array.<module:model/BudgetKategorie>} monats_budget
 */
BudgetErgebnis.prototype['monats_budget'] = undefined;

/**
 * @member {Array.<module:model/BudgetKategorie>} prognose_restjahr
 */
BudgetErgebnis.prototype['prognose_restjahr'] = undefined;






export default BudgetErgebnis;


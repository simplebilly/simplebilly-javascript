/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BilanzItem from './BilanzItem';

/**
 * The BilanzReport model module.
 * @module model/BilanzReport
 * @version 0.1.0
 */
class BilanzReport {
    /**
     * Constructs a new <code>BilanzReport</code>.
     * @alias module:model/BilanzReport
     * @param aktiva {Array.<module:model/BilanzItem>} 
     * @param balanced {Boolean} 
     * @param generatedAt {String} 
     * @param passiva {Array.<module:model/BilanzItem>} 
     * @param period {String} 
     * @param totalAktiva {String} 
     * @param totalPassiva {String} 
     */
    constructor(aktiva, balanced, generatedAt, passiva, period, totalAktiva, totalPassiva) { 
        
        BilanzReport.initialize(this, aktiva, balanced, generatedAt, passiva, period, totalAktiva, totalPassiva);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aktiva, balanced, generatedAt, passiva, period, totalAktiva, totalPassiva) { 
        obj['aktiva'] = aktiva;
        obj['balanced'] = balanced;
        obj['generated_at'] = generatedAt;
        obj['passiva'] = passiva;
        obj['period'] = period;
        obj['total_aktiva'] = totalAktiva;
        obj['total_passiva'] = totalPassiva;
    }

    /**
     * Constructs a <code>BilanzReport</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BilanzReport} obj Optional instance to populate.
     * @return {module:model/BilanzReport} The populated <code>BilanzReport</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BilanzReport();

            if (data.hasOwnProperty('aktiva')) {
                obj['aktiva'] = ApiClient.convertToType(data['aktiva'], [BilanzItem]);
            }
            if (data.hasOwnProperty('balanced')) {
                obj['balanced'] = ApiClient.convertToType(data['balanced'], 'Boolean');
            }
            if (data.hasOwnProperty('generated_at')) {
                obj['generated_at'] = ApiClient.convertToType(data['generated_at'], 'String');
            }
            if (data.hasOwnProperty('passiva')) {
                obj['passiva'] = ApiClient.convertToType(data['passiva'], [BilanzItem]);
            }
            if (data.hasOwnProperty('period')) {
                obj['period'] = ApiClient.convertToType(data['period'], 'String');
            }
            if (data.hasOwnProperty('total_aktiva')) {
                obj['total_aktiva'] = ApiClient.convertToType(data['total_aktiva'], 'String');
            }
            if (data.hasOwnProperty('total_passiva')) {
                obj['total_passiva'] = ApiClient.convertToType(data['total_passiva'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BilanzReport</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BilanzReport</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BilanzReport.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['aktiva']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['aktiva'])) {
                throw new Error("Expected the field `aktiva` to be an array in the JSON data but got " + data['aktiva']);
            }
            // validate the optional field `aktiva` (array)
            for (const item of data['aktiva']) {
                BilanzItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['generated_at'] && !(typeof data['generated_at'] === 'string' || data['generated_at'] instanceof String)) {
            throw new Error("Expected the field `generated_at` to be a primitive type in the JSON string but got " + data['generated_at']);
        }
        if (data['passiva']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['passiva'])) {
                throw new Error("Expected the field `passiva` to be an array in the JSON data but got " + data['passiva']);
            }
            // validate the optional field `passiva` (array)
            for (const item of data['passiva']) {
                BilanzItem.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['period'] && !(typeof data['period'] === 'string' || data['period'] instanceof String)) {
            throw new Error("Expected the field `period` to be a primitive type in the JSON string but got " + data['period']);
        }
        // ensure the json data is a string
        if (data['total_aktiva'] && !(typeof data['total_aktiva'] === 'string' || data['total_aktiva'] instanceof String)) {
            throw new Error("Expected the field `total_aktiva` to be a primitive type in the JSON string but got " + data['total_aktiva']);
        }
        // ensure the json data is a string
        if (data['total_passiva'] && !(typeof data['total_passiva'] === 'string' || data['total_passiva'] instanceof String)) {
            throw new Error("Expected the field `total_passiva` to be a primitive type in the JSON string but got " + data['total_passiva']);
        }

        return true;
    }


}

BilanzReport.RequiredProperties = ["aktiva", "balanced", "generated_at", "passiva", "period", "total_aktiva", "total_passiva"];

/**
 * @member {Array.<module:model/BilanzItem>} aktiva
 */
BilanzReport.prototype['aktiva'] = undefined;

/**
 * @member {Boolean} balanced
 */
BilanzReport.prototype['balanced'] = undefined;

/**
 * @member {String} generated_at
 */
BilanzReport.prototype['generated_at'] = undefined;

/**
 * @member {Array.<module:model/BilanzItem>} passiva
 */
BilanzReport.prototype['passiva'] = undefined;

/**
 * @member {String} period
 */
BilanzReport.prototype['period'] = undefined;

/**
 * @member {String} total_aktiva
 */
BilanzReport.prototype['total_aktiva'] = undefined;

/**
 * @member {String} total_passiva
 */
BilanzReport.prototype['total_passiva'] = undefined;






export default BilanzReport;

